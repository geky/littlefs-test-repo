# Test block eviction
#
# This may seem niche, but eviction is used internally for a surprising
# number of features.
#
# It's also one of the more annoying things to test, as block eviction
# can touch pretty much every part of the filesystem
after = ['test_trvs', 'test_gc']
ifdef = 'LFS3_EVICT'

# Test both with and without the gbmap if available
defines.GBMAP = [false, true]
if = 'LFS3_IFYES_GBMAP(GBMAP, true, !GBMAP)'



# test evicting traversals

# test traversals with mdir eviction

[cases.test_evict_t_mdir_mroot]
defines.SIZE = 'FCACHE_SIZE/2'
defines.SIBLING = [0, 1]
# force early relocations, this will cause the mroot anchor to extend
defines.BLOCK_RECYCLES = 0
code = '''
    lfs3_t lfs3;
    lfs3_format(&lfs3,
            LFS3_F_RDWR
                | ((GBMAP) ? LFS3_IFDEF_GBMAP(LFS3_F_GBMAP, -1) : 0),
            CFG) => 0;
    lfs3_mount(&lfs3, LFS3_M_RDWR, CFG) => 0;

    uint32_t prng = 42;

    // write to our mroot until it's not the mrootanchor
    //
    // we can't evict the mrootanchor
    lfs3_file_t file;
    lfs3_file_open(&lfs3, &file, "jellyfish",
            LFS3_O_RDWR | LFS3_O_CREAT | LFS3_O_EXCL) => 0;
    uint8_t wbuf[SIZE];
    while (file.h.mdir.r.blocks[0] == 0
            || file.h.mdir.r.blocks[0] == 1) {
        lfs3_file_rewind(&lfs3, &file) => 0;
        for (lfs3_size_t j = 0; j < SIZE; j++) {
            wbuf[j] = 'a' + (TEST_PRNG(&prng) % 26);
        }
        lfs3_file_write(&lfs3, &file, wbuf, SIZE) => SIZE;
        lfs3_file_sync(&lfs3, &file) => 0;
    }

    // get a block to evict
    lfs3_block_t victim = file.h.mdir.r.blocks[SIBLING];

    // try evicting
    lfs3_fs_mkbad(&lfs3, victim, LFS3_MKBAD_EVICT) => 0;

    // evicted block should no longer be in use
    assert(file.h.mdir.r.blocks[0] != victim);
    assert(file.h.mdir.r.blocks[1] != victim);

    // evicted block should no longer be in use, anywhere in the
    // filesystem
    lfs3_trv_t trv;
    lfs3_trv_open(&lfs3, &trv, LFS3_T_RDONLY) => 0;
    for (lfs3_block_t i = 0;; i++) {
        assert(i < 2*BLOCK_COUNT);

        struct lfs3_tinfo tinfo;
        int err = lfs3_trv_read(&lfs3, &trv, &tinfo);
        assert(!err || err == LFS3_ERR_NOENT);
        if (err == LFS3_ERR_NOENT) {
            break;
        }

        assert(tinfo.block != victim);
    }
    lfs3_trv_close(&lfs3, &trv) => 0;

    // check we can still read the file
    for (int remount = 0; remount < 2; remount++) {
        // remount?
        if (remount) {
            lfs3_file_close(&lfs3, &file) => 0;
            lfs3_unmount(&lfs3) => 0;
            lfs3_mount(&lfs3, LFS3_M_RDWR, CFG) => 0;
            lfs3_file_open(&lfs3, &file, "jellyfish", LFS3_O_RDONLY) => 0;
        }

        lfs3_file_rewind(&lfs3, &file) => 0;
        uint8_t rbuf[SIZE];
        lfs3_file_read(&lfs3, &file, rbuf, SIZE) => SIZE;
        assert(memcmp(rbuf, wbuf, SIZE) == 0);
    }

    lfs3_file_close(&lfs3, &file) => 0;
    lfs3_unmount(&lfs3) => 0;
'''

[cases.test_evict_t_mrootanchor]
defines.SIZE = 'FCACHE_SIZE/2'
defines.SIBLING = [0, 1]
code = '''
    lfs3_t lfs3;
    lfs3_format(&lfs3,
            LFS3_F_RDWR
                | ((GBMAP) ? LFS3_IFDEF_GBMAP(LFS3_F_GBMAP, -1) : 0),
            CFG) => 0;
    lfs3_mount(&lfs3, LFS3_M_RDWR, CFG) => 0;

    uint32_t prng = 42;

    // write to our mroot
    lfs3_file_t file;
    lfs3_file_open(&lfs3, &file, "jellyfish",
            LFS3_O_RDWR | LFS3_O_CREAT | LFS3_O_EXCL) => 0;
    uint8_t wbuf[SIZE];
    for (lfs3_size_t j = 0; j < SIZE; j++) {
        wbuf[j] = 'a' + (TEST_PRNG(&prng) % 26);
    }
    lfs3_file_write(&lfs3, &file, wbuf, SIZE) => SIZE;
    lfs3_file_sync(&lfs3, &file) => 0;

    // get a block to evict
    lfs3_block_t victim = SIBLING;

    // try evicting
    //
    // we can't evict the mrootanchor, so this should error
    int err = lfs3_fs_mkbad(&lfs3, victim, LFS3_MKBAD_EVICT);
    assert(err == LFS3_ERR_BUSY);

    // check we can still read the file
    for (int remount = 0; remount < 2; remount++) {
        // remount?
        if (remount) {
            lfs3_file_close(&lfs3, &file) => 0;
            lfs3_unmount(&lfs3) => 0;
            lfs3_mount(&lfs3, LFS3_M_RDWR, CFG) => 0;
            lfs3_file_open(&lfs3, &file, "jellyfish", LFS3_O_RDONLY) => 0;
        }

        lfs3_file_rewind(&lfs3, &file) => 0;
        uint8_t rbuf[SIZE];
        lfs3_file_read(&lfs3, &file, rbuf, SIZE) => SIZE;
        assert(memcmp(rbuf, wbuf, SIZE) == 0);
    }

    lfs3_file_close(&lfs3, &file) => 0;
    lfs3_unmount(&lfs3) => 0;
'''

[cases.test_evict_t_oob]
defines.SIZE = 'FCACHE_SIZE/2'
defines.SIBLING = [0, 1, 2, 3, 4, 5]
code = '''
    lfs3_t lfs3;
    lfs3_format(&lfs3,
            LFS3_F_RDWR
                | ((GBMAP) ? LFS3_IFDEF_GBMAP(LFS3_F_GBMAP, -1) : 0),
            CFG) => 0;
    lfs3_mount(&lfs3, LFS3_M_RDWR, CFG) => 0;

    uint32_t prng = 42;

    // write to our mroot
    lfs3_file_t file;
    lfs3_file_open(&lfs3, &file, "jellyfish",
            LFS3_O_RDWR | LFS3_O_CREAT | LFS3_O_EXCL) => 0;
    uint8_t wbuf[SIZE];
    for (lfs3_size_t j = 0; j < SIZE; j++) {
        wbuf[j] = 'a' + (TEST_PRNG(&prng) % 26);
    }
    lfs3_file_write(&lfs3, &file, wbuf, SIZE) => SIZE;
    lfs3_file_sync(&lfs3, &file) => 0;

    // get a block to evict
    lfs3_block_t victim = BLOCK_COUNT + SIBLING;

    // try evicting
    //
    // we can't evict out-of-bound blocks, so this should error
    int err = lfs3_fs_mkbad(&lfs3, victim, LFS3_MKBAD_EVICT);
    assert(err == LFS3_ERR_NOENT);

    // check we can still read the file
    for (int remount = 0; remount < 2; remount++) {
        // remount?
        if (remount) {
            lfs3_file_close(&lfs3, &file) => 0;
            lfs3_unmount(&lfs3) => 0;
            lfs3_mount(&lfs3, LFS3_M_RDWR, CFG) => 0;
            lfs3_file_open(&lfs3, &file, "jellyfish", LFS3_O_RDONLY) => 0;
        }

        lfs3_file_rewind(&lfs3, &file) => 0;
        uint8_t rbuf[SIZE];
        lfs3_file_read(&lfs3, &file, rbuf, SIZE) => SIZE;
        assert(memcmp(rbuf, wbuf, SIZE) == 0);
    }

    lfs3_file_close(&lfs3, &file) => 0;
    lfs3_unmount(&lfs3) => 0;
'''

[cases.test_evict_t_mdir_mroot_extend]
defines.SIZE = 'FCACHE_SIZE/2'
defines.SIBLING = [0, 1]
# force early relocations
defines.BLOCK_RECYCLES = 0
code = '''
    lfs3_t lfs3;
    lfs3_format(&lfs3,
            LFS3_F_RDWR
                | ((GBMAP) ? LFS3_IFDEF_GBMAP(LFS3_F_GBMAP, -1) : 0),
            CFG) => 0;
    lfs3_mount(&lfs3, LFS3_M_RDWR, CFG) => 0;

    uint32_t prng = 42;

    // write to our mroot until it's not the mrootanchor
    //
    // we can't evict the mrootanchor
    lfs3_file_t file;
    lfs3_file_open(&lfs3, &file, "jellyfish",
            LFS3_O_RDWR | LFS3_O_CREAT | LFS3_O_EXCL) => 0;
    uint8_t wbuf[SIZE];
    while (file.h.mdir.r.blocks[0] == 0
            || file.h.mdir.r.blocks[0] == 1) {
        lfs3_file_rewind(&lfs3, &file) => 0;
        for (lfs3_size_t j = 0; j < SIZE; j++) {
            wbuf[j] = 'a' + (TEST_PRNG(&prng) % 26);
        }
        lfs3_file_write(&lfs3, &file, wbuf, SIZE) => SIZE;
        lfs3_file_sync(&lfs3, &file) => 0;
    }

    // I'm too lazy to do this correctly, so just spam evictions until
    // we extend again
    lfs3_block_t victim = 0;
    for (lfs3_block_t i = 0;; i++) {
        // a bit hacky, but this catches infinite loops
        assert(i < 2*BLOCK_COUNT);

        lfs3_sblock_t count = lfs3_fs_usage(&lfs3);
        assert(count >= 0);
        if (count >= 6) {
            assert(count == 6 || count == 7); // maybe gbmap
            break;
        }

        // get a block to evict
        victim = file.h.mdir.r.blocks[SIBLING];

        // try evicting
        lfs3_fs_mkbad(&lfs3, victim, LFS3_MKBAD_EVICT) => 0;

        // evicted block should no longer be in use
        assert(file.h.mdir.r.blocks[0] != victim);
        assert(file.h.mdir.r.blocks[1] != victim);
    }

    // evicted block should no longer be in use, anywhere in the
    // filesystem
    lfs3_trv_t trv;
    lfs3_trv_open(&lfs3, &trv, LFS3_T_RDONLY) => 0;
    for (lfs3_block_t i = 0;; i++) {
        assert(i < 2*BLOCK_COUNT);

        struct lfs3_tinfo tinfo;
        int err = lfs3_trv_read(&lfs3, &trv, &tinfo);
        assert(!err || err == LFS3_ERR_NOENT);
        if (err == LFS3_ERR_NOENT) {
            break;
        }

        assert(tinfo.block != victim);
    }
    lfs3_trv_close(&lfs3, &trv) => 0;

    // check we can still read the file
    for (int remount = 0; remount < 2; remount++) {
        // remount?
        if (remount) {
            lfs3_file_close(&lfs3, &file) => 0;
            lfs3_unmount(&lfs3) => 0;
            lfs3_mount(&lfs3, LFS3_M_RDWR, CFG) => 0;
            lfs3_file_open(&lfs3, &file, "jellyfish", LFS3_O_RDONLY) => 0;
        }

        lfs3_file_rewind(&lfs3, &file) => 0;
        uint8_t rbuf[SIZE];
        lfs3_file_read(&lfs3, &file, rbuf, SIZE) => SIZE;
        assert(memcmp(rbuf, wbuf, SIZE) == 0);
    }

    lfs3_file_close(&lfs3, &file) => 0;
    lfs3_unmount(&lfs3) => 0;
'''

[cases.test_evict_t_mdir_mroot_chain]
defines.SIZE = 'FCACHE_SIZE/2'
defines.SIBLING = [0, 1]
# force early relocations
defines.BLOCK_RECYCLES = 0
code = '''
    lfs3_t lfs3;
    lfs3_format(&lfs3,
            LFS3_F_RDWR
                | ((GBMAP) ? LFS3_IFDEF_GBMAP(LFS3_F_GBMAP, -1) : 0),
            CFG) => 0;
    lfs3_mount(&lfs3, LFS3_M_RDWR, CFG) => 0;

    uint32_t prng = 42;

    // write to our mroot until it's not the mrootanchor
    //
    // we can't evict the mrootanchor
    lfs3_file_t file;
    lfs3_file_open(&lfs3, &file, "jellyfish",
            LFS3_O_RDWR | LFS3_O_CREAT | LFS3_O_EXCL) => 0;
    uint8_t wbuf[SIZE];
    while (file.h.mdir.r.blocks[0] == 0
            || file.h.mdir.r.blocks[0] == 1) {
        lfs3_file_rewind(&lfs3, &file) => 0;
        for (lfs3_size_t j = 0; j < SIZE; j++) {
            wbuf[j] = 'a' + (TEST_PRNG(&prng) % 26);
        }
        lfs3_file_write(&lfs3, &file, wbuf, SIZE) => SIZE;
        lfs3_file_sync(&lfs3, &file) => 0;
    }

    // I'm too lazy to do this correctly, so just spam evictions until
    // we extend again
    lfs3_block_t victim = 0;
    for (lfs3_block_t i = 0;; i++) {
        // a bit hacky, but this catches infinite loops
        assert(i < 2*BLOCK_COUNT);

        lfs3_sblock_t count = lfs3_fs_usage(&lfs3);
        assert(count >= 0);
        if (count >= 6) {
            assert(count == 6 || count == 7); // maybe gbmap
            break;
        }

        // get a block to evict
        victim = file.h.mdir.r.blocks[SIBLING];

        // try evicting
        lfs3_fs_mkbad(&lfs3, victim, LFS3_MKBAD_EVICT) => 0;

        // evicted block should no longer be in use
        assert(file.h.mdir.r.blocks[0] != victim);
        assert(file.h.mdir.r.blocks[1] != victim);
    }

    // now we want to evict the mdir in our mroot chain, that is neither
    // the active mroot, nor the mrootanchor
    lfs3_trv_t trv;
    lfs3_trv_open(&lfs3, &trv, LFS3_T_RDONLY) => 0;
    for (lfs3_block_t i = 0;; i++) {
        assert(i < 2*BLOCK_COUNT);

        struct lfs3_tinfo tinfo;
        int err = lfs3_trv_read(&lfs3, &trv, &tinfo);
        assert(!err || err == LFS3_ERR_NOENT);
        if (err == LFS3_ERR_NOENT) {
            break;
        }

        if (i == 2 + SIBLING) {
            victim = tinfo.block;
            break;
        }
    }
    lfs3_trv_close(&lfs3, &trv) => 0;
    
    // try evicting
    lfs3_fs_mkbad(&lfs3, victim, LFS3_MKBAD_EVICT) => 0;

    // evicted block should no longer be in use, anywhere in the
    // filesystem
    lfs3_trv_open(&lfs3, &trv, LFS3_T_RDONLY) => 0;
    for (lfs3_block_t i = 0;; i++) {
        assert(i < 2*BLOCK_COUNT);

        struct lfs3_tinfo tinfo;
        int err = lfs3_trv_read(&lfs3, &trv, &tinfo);
        assert(!err || err == LFS3_ERR_NOENT);
        if (err == LFS3_ERR_NOENT) {
            break;
        }

        assert(tinfo.block != victim);
    }
    lfs3_trv_close(&lfs3, &trv) => 0;

    // check we can still read the file
    for (int remount = 0; remount < 2; remount++) {
        // remount?
        if (remount) {
            lfs3_file_close(&lfs3, &file) => 0;
            lfs3_unmount(&lfs3) => 0;
            lfs3_mount(&lfs3, LFS3_M_RDWR, CFG) => 0;
            lfs3_file_open(&lfs3, &file, "jellyfish", LFS3_O_RDONLY) => 0;
        }

        lfs3_file_rewind(&lfs3, &file) => 0;
        uint8_t rbuf[SIZE];
        lfs3_file_read(&lfs3, &file, rbuf, SIZE) => SIZE;
        assert(memcmp(rbuf, wbuf, SIZE) == 0);
    }

    lfs3_file_close(&lfs3, &file) => 0;
    lfs3_unmount(&lfs3) => 0;
'''

[cases.test_evict_t_mdir_mroot_split]
defines.SIZE = 'FCACHE_SIZE/2'
defines.SIBLING = [0, 1, 2, 3]
# force early relocations
defines.BLOCK_RECYCLES = 0
in = 'lfs3.c'
code = '''
    lfs3_t lfs3;
    lfs3_format(&lfs3,
            LFS3_F_RDWR
                | ((GBMAP) ? LFS3_IFDEF_GBMAP(LFS3_F_GBMAP, -1) : 0),
            CFG) => 0;
    lfs3_mount(&lfs3, LFS3_M_RDWR, CFG) => 0;

    uint32_t prng = 42;

    // create two files
    lfs3_file_t file1;
    lfs3_file_open(&lfs3, &file1, "jellyfish",
            LFS3_O_RDWR | LFS3_O_CREAT | LFS3_O_EXCL) => 0;
    uint8_t wbuf1[SIZE];
    for (lfs3_size_t j = 0; j < SIZE; j++) {
        wbuf1[j] = 'a' + (TEST_PRNG(&prng) % 26);
    }
    lfs3_file_write(&lfs3, &file1, wbuf1, SIZE) => SIZE;
    lfs3_file_sync(&lfs3, &file1) => 0;

    lfs3_file_t file2;
    lfs3_file_open(&lfs3, &file2, "octopus",
            LFS3_O_RDWR | LFS3_O_CREAT | LFS3_O_EXCL) => 0;
    uint8_t wbuf2[SIZE];
    for (lfs3_size_t j = 0; j < SIZE; j++) {
        wbuf2[j] = 'a' + (TEST_PRNG(&prng) % 26);
    }
    lfs3_file_write(&lfs3, &file2, wbuf2, SIZE) => SIZE;
    lfs3_file_sync(&lfs3, &file2) => 0;

    // write to our mroot until it's not the mrootanchor
    //
    // we can't evict the mrootanchor
    while (file1.h.mdir.r.blocks[0] == 0
            || file1.h.mdir.r.blocks[0] == 1) {
        lfs3_file_rewind(&lfs3, &file1) => 0;
        for (lfs3_size_t j = 0; j < SIZE; j++) {
            wbuf1[j] = 'a' + (TEST_PRNG(&prng) % 26);
        }
        lfs3_file_write(&lfs3, &file1, wbuf1, SIZE) => SIZE;
        lfs3_file_sync(&lfs3, &file1) => 0;
    }

    // create enough files to both compact and split
    lfs3_size_t i = 0;
    while (true) {
        // we should not have split yet
        assert(lfs3.mtree.weight == 0);
        // we need internals to check this
        lfs3_ssize_t estimate = lfs3_mdir_estimate__(&lfs3,
                &file1.h.mdir, -1, -1,
                NULL);
        assert(estimate >= 0);
        if ((file1.h.mdir.r.eoff & 0x7fffffff) > GC_COMPACTMETA_THRESH
                && estimate > BLOCK_SIZE/2) {
            break;
        }

        char name[256];
        sprintf(name, "medusaaaaaaaaaaaaaaaa%03x", i);
        lfs3_file_t file;
        lfs3_file_open(&lfs3, &file, name,
                LFS3_O_WRONLY | LFS3_O_CREAT | LFS3_O_EXCL) => 0;
        lfs3_file_close(&lfs3, &file) => 0;
        i += 1;
    }

    // get a block to evict
    lfs3_block_t victim = (SIBLING < 2)
            ? file1.h.mdir.r.blocks[SIBLING % 2]
            : file2.h.mdir.r.blocks[SIBLING % 2];

    // try evicting
    lfs3_fs_mkbad(&lfs3, victim, LFS3_MKBAD_EVICT) => 0;

    // evicted block should no longer be in use
    assert(file1.h.mdir.r.blocks[0] != victim);
    assert(file1.h.mdir.r.blocks[1] != victim);
    assert(file2.h.mdir.r.blocks[0] != victim);
    assert(file2.h.mdir.r.blocks[1] != victim);
    
    // evicted block should no longer be in use, anywhere in the
    // filesystem
    lfs3_trv_t trv;
    lfs3_trv_open(&lfs3, &trv, LFS3_T_RDONLY) => 0;
    for (lfs3_block_t i = 0;; i++) {
        assert(i < 2*BLOCK_COUNT);

        struct lfs3_tinfo tinfo;
        int err = lfs3_trv_read(&lfs3, &trv, &tinfo);
        assert(!err || err == LFS3_ERR_NOENT);
        if (err == LFS3_ERR_NOENT) {
            break;
        }

        assert(tinfo.block != victim);
    }
    lfs3_trv_close(&lfs3, &trv) => 0;

    // check we can still read the files
    for (int remount = 0; remount < 2; remount++) {
        // remount?
        if (remount) {
            lfs3_file_close(&lfs3, &file1) => 0;
            lfs3_file_close(&lfs3, &file2) => 0;
            lfs3_unmount(&lfs3) => 0;
            lfs3_mount(&lfs3, LFS3_M_RDWR, CFG) => 0;
            lfs3_file_open(&lfs3, &file1, "jellyfish", LFS3_O_RDONLY) => 0;
            lfs3_file_open(&lfs3, &file2, "octopus", LFS3_O_RDONLY) => 0;
        }

        lfs3_file_rewind(&lfs3, &file1) => 0;
        uint8_t rbuf[SIZE];
        lfs3_file_read(&lfs3, &file1, rbuf, SIZE) => SIZE;
        assert(memcmp(rbuf, wbuf1, SIZE) == 0);

        lfs3_file_rewind(&lfs3, &file2) => 0;
        lfs3_file_read(&lfs3, &file2, rbuf, SIZE) => SIZE;
        assert(memcmp(rbuf, wbuf2, SIZE) == 0);
    }

    lfs3_file_close(&lfs3, &file1) => 0;
    lfs3_file_close(&lfs3, &file2) => 0;
    lfs3_unmount(&lfs3) => 0;
'''

[cases.test_evict_t_mdir_mtree]
defines.SIZE = 'FCACHE_SIZE/2'
defines.SIBLING = [0, 1, 2, 3, 4, 5]
code = '''
    lfs3_t lfs3;
    lfs3_format(&lfs3,
            LFS3_F_RDWR
                | ((GBMAP) ? LFS3_IFDEF_GBMAP(LFS3_F_GBMAP, -1) : 0),
            CFG) => 0;
    lfs3_mount(&lfs3, LFS3_M_RDWR, CFG) => 0;

    uint32_t prng = 42;

    // create three files
    lfs3_file_t file1;
    lfs3_file_open(&lfs3, &file1, "cuttlefish",
            LFS3_O_RDWR | LFS3_O_CREAT | LFS3_O_EXCL) => 0;
    uint8_t wbuf1[SIZE];
    for (lfs3_size_t j = 0; j < SIZE; j++) {
        wbuf1[j] = 'a' + (TEST_PRNG(&prng) % 26);
    }
    lfs3_file_write(&lfs3, &file1, wbuf1, SIZE) => SIZE;
    lfs3_file_sync(&lfs3, &file1) => 0;

    lfs3_file_t file2;
    lfs3_file_open(&lfs3, &file2, "jellyfish",
            LFS3_O_RDWR | LFS3_O_CREAT | LFS3_O_EXCL) => 0;
    uint8_t wbuf2[SIZE];
    for (lfs3_size_t j = 0; j < SIZE; j++) {
        wbuf2[j] = 'a' + (TEST_PRNG(&prng) % 26);
    }
    lfs3_file_write(&lfs3, &file2, wbuf2, SIZE) => SIZE;
    lfs3_file_sync(&lfs3, &file2) => 0;

    lfs3_file_t file3;
    lfs3_file_open(&lfs3, &file3, "octopus",
            LFS3_O_RDWR | LFS3_O_CREAT | LFS3_O_EXCL) => 0;
    uint8_t wbuf3[SIZE];
    for (lfs3_size_t j = 0; j < SIZE; j++) {
        wbuf3[j] = 'a' + (TEST_PRNG(&prng) % 26);
    }
    lfs3_file_write(&lfs3, &file3, wbuf3, SIZE) => SIZE;
    lfs3_file_sync(&lfs3, &file3) => 0;

    // create enough files for mroot to split twice
    lfs3_size_t i = 0;
    while (lfs3.mtree.weight == 0) {
        char name[256];
        sprintf(name, "hydroid%03x", i);
        lfs3_file_t file;
        lfs3_file_open(&lfs3, &file, name,
                LFS3_O_WRONLY | LFS3_O_CREAT | LFS3_O_EXCL) => 0;
        lfs3_file_close(&lfs3, &file) => 0;
        i += 1;
    }

    i = 0;
    lfs3_size_t orig = lfs3.mtree.weight;
    while (lfs3.mtree.weight == orig) {
        char name[256];
        sprintf(name, "medusa%03x", i);
        lfs3_file_t file;
        lfs3_file_open(&lfs3, &file, name,
                LFS3_O_WRONLY | LFS3_O_CREAT | LFS3_O_EXCL) => 0;
        lfs3_file_close(&lfs3, &file) => 0;
        i += 1;
    }

    // get a block to evict
    lfs3_block_t victim
            = (SIBLING < 2)
                ? file1.h.mdir.r.blocks[SIBLING % 2]
            : (SIBLING < 4)
                ? file2.h.mdir.r.blocks[SIBLING % 2]
                : file3.h.mdir.r.blocks[SIBLING % 2];
    
    // try evicting
    lfs3_fs_mkbad(&lfs3, victim, LFS3_MKBAD_EVICT) => 0;

    // evicted block should no longer be in use
    assert(file1.h.mdir.r.blocks[0] != victim);
    assert(file1.h.mdir.r.blocks[1] != victim);
    assert(file2.h.mdir.r.blocks[0] != victim);
    assert(file2.h.mdir.r.blocks[1] != victim);
    assert(file3.h.mdir.r.blocks[0] != victim);
    assert(file3.h.mdir.r.blocks[1] != victim);

    // evicted block should no longer be in use, anywhere in the
    // filesystem
    lfs3_trv_t trv;
    lfs3_trv_open(&lfs3, &trv, LFS3_T_RDONLY) => 0;
    for (lfs3_block_t i = 0;; i++) {
        assert(i < 2*BLOCK_COUNT);

        struct lfs3_tinfo tinfo;
        int err = lfs3_trv_read(&lfs3, &trv, &tinfo);
        assert(!err || err == LFS3_ERR_NOENT);
        if (err == LFS3_ERR_NOENT) {
            break;
        }

        assert(tinfo.block != victim);
    }
    lfs3_trv_close(&lfs3, &trv) => 0;

    // check we can still read the files
    for (int remount = 0; remount < 2; remount++) {
        // remount?
        if (remount) {
            lfs3_file_close(&lfs3, &file1) => 0;
            lfs3_file_close(&lfs3, &file2) => 0;
            lfs3_file_close(&lfs3, &file3) => 0;
            lfs3_unmount(&lfs3) => 0;
            lfs3_mount(&lfs3, LFS3_M_RDWR, CFG) => 0;
            lfs3_file_open(&lfs3, &file1, "cuttlefish", LFS3_O_RDONLY) => 0;
            lfs3_file_open(&lfs3, &file2, "jellyfish", LFS3_O_RDONLY) => 0;
            lfs3_file_open(&lfs3, &file3, "octopus", LFS3_O_RDONLY) => 0;
        }

        lfs3_file_rewind(&lfs3, &file1) => 0;
        uint8_t rbuf[SIZE];
        lfs3_file_read(&lfs3, &file1, rbuf, SIZE) => SIZE;
        assert(memcmp(rbuf, wbuf1, SIZE) == 0);

        lfs3_file_rewind(&lfs3, &file2) => 0;
        lfs3_file_read(&lfs3, &file2, rbuf, SIZE) => SIZE;
        assert(memcmp(rbuf, wbuf2, SIZE) == 0);

        lfs3_file_rewind(&lfs3, &file3) => 0;
        lfs3_file_read(&lfs3, &file3, rbuf, SIZE) => SIZE;
        assert(memcmp(rbuf, wbuf3, SIZE) == 0);
    }

    lfs3_file_close(&lfs3, &file1) => 0;
    lfs3_file_close(&lfs3, &file2) => 0;
    lfs3_file_close(&lfs3, &file3) => 0;
    lfs3_unmount(&lfs3) => 0;
'''

[cases.test_evict_t_mdir_mtree_split]
defines.SIZE = 'FCACHE_SIZE/2'
defines.SIBLING = [0, 1, 2, 3, 4, 5, 6, 7]
in = 'lfs3.c'
code = '''
    lfs3_t lfs3;
    lfs3_format(&lfs3,
            LFS3_F_RDWR
                | ((GBMAP) ? LFS3_IFDEF_GBMAP(LFS3_F_GBMAP, -1) : 0),
            CFG) => 0;
    lfs3_mount(&lfs3, LFS3_M_RDWR, CFG) => 0;

    uint32_t prng = 42;

    // create four files
    lfs3_file_t file1;
    lfs3_file_open(&lfs3, &file1, "cuttlefish",
            LFS3_O_RDWR | LFS3_O_CREAT | LFS3_O_EXCL) => 0;
    uint8_t wbuf1[SIZE];
    for (lfs3_size_t j = 0; j < SIZE; j++) {
        wbuf1[j] = 'a' + (TEST_PRNG(&prng) % 26);
    }
    lfs3_file_write(&lfs3, &file1, wbuf1, SIZE) => SIZE;
    lfs3_file_sync(&lfs3, &file1) => 0;

    lfs3_file_t file2;
    lfs3_file_open(&lfs3, &file2, "jellyfish",
            LFS3_O_RDWR | LFS3_O_CREAT | LFS3_O_EXCL) => 0;
    uint8_t wbuf2[SIZE];
    for (lfs3_size_t j = 0; j < SIZE; j++) {
        wbuf2[j] = 'a' + (TEST_PRNG(&prng) % 26);
    }
    lfs3_file_write(&lfs3, &file2, wbuf2, SIZE) => SIZE;
    lfs3_file_sync(&lfs3, &file2) => 0;

    lfs3_file_t file3;
    lfs3_file_open(&lfs3, &file3, "octopus",
            LFS3_O_RDWR | LFS3_O_CREAT | LFS3_O_EXCL) => 0;
    uint8_t wbuf3[SIZE];
    for (lfs3_size_t j = 0; j < SIZE; j++) {
        wbuf3[j] = 'a' + (TEST_PRNG(&prng) % 26);
    }
    lfs3_file_write(&lfs3, &file3, wbuf3, SIZE) => SIZE;
    lfs3_file_sync(&lfs3, &file3) => 0;

    lfs3_file_t file4;
    lfs3_file_open(&lfs3, &file4, "squid",
            LFS3_O_RDWR | LFS3_O_CREAT | LFS3_O_EXCL) => 0;
    uint8_t wbuf4[SIZE];
    for (lfs3_size_t j = 0; j < SIZE; j++) {
        wbuf4[j] = 'a' + (TEST_PRNG(&prng) % 26);
    }
    lfs3_file_write(&lfs3, &file4, wbuf4, SIZE) => SIZE;
    lfs3_file_sync(&lfs3, &file4) => 0;

    // create enough files for mroot to split twice
    lfs3_size_t i = 0;
    while (lfs3.mtree.weight == 0) {
        char name[256];
        sprintf(name, "hydroid%03x", i);
        lfs3_file_t file;
        lfs3_file_open(&lfs3, &file, name,
                LFS3_O_WRONLY | LFS3_O_CREAT | LFS3_O_EXCL) => 0;
        lfs3_file_close(&lfs3, &file) => 0;
        i += 1;
    }

    i = 0;
    lfs3_size_t orig = lfs3.mtree.weight;
    while (lfs3.mtree.weight == orig) {
        char name[256];
        sprintf(name, "polyp%03x", i);
        lfs3_file_t file;
        lfs3_file_open(&lfs3, &file, name,
                LFS3_O_WRONLY | LFS3_O_CREAT | LFS3_O_EXCL) => 0;
        lfs3_file_close(&lfs3, &file) => 0;
        i += 1;
    }

    // create enough files to both compact and split
    i = 0;
    orig = lfs3.mtree.weight;
    while (true) {
        // we should not have split yet
        assert(lfs3.mtree.weight == orig);
        // we need internals to check this
        lfs3_ssize_t estimate = lfs3_mdir_estimate__(&lfs3,
                &file2.h.mdir, -1, -1,
                NULL);
        assert(estimate >= 0);
        if ((file2.h.mdir.r.eoff & 0x7fffffff) > GC_COMPACTMETA_THRESH
                && estimate > BLOCK_SIZE/2) {
            break;
        }

        char name[256];
        sprintf(name, "medusaaaaaaaaaaaaaaaa%03x", i);
        lfs3_file_t file;
        lfs3_file_open(&lfs3, &file, name,
                LFS3_O_WRONLY | LFS3_O_CREAT | LFS3_O_EXCL) => 0;
        lfs3_file_close(&lfs3, &file) => 0;
        i += 1;
    }

    // get a block to evict
    lfs3_block_t victim
            = (SIBLING < 2)
                ? file1.h.mdir.r.blocks[SIBLING % 2]
            : (SIBLING < 4)
                ? file2.h.mdir.r.blocks[SIBLING % 2]
            : (SIBLING < 6)
                ? file3.h.mdir.r.blocks[SIBLING % 2]
                : file4.h.mdir.r.blocks[SIBLING % 2];

    // try evicting
    lfs3_fs_mkbad(&lfs3, victim, LFS3_MKBAD_EVICT) => 0;

    // evicted block should no longer be in use
    assert(file1.h.mdir.r.blocks[0] != victim);
    assert(file1.h.mdir.r.blocks[1] != victim);
    assert(file2.h.mdir.r.blocks[0] != victim);
    assert(file2.h.mdir.r.blocks[1] != victim);
    assert(file3.h.mdir.r.blocks[0] != victim);
    assert(file3.h.mdir.r.blocks[1] != victim);
    assert(file4.h.mdir.r.blocks[0] != victim);
    assert(file4.h.mdir.r.blocks[1] != victim);

    // check we can still read the files
    for (int remount = 0; remount < 2; remount++) {
        // remount?
        if (remount) {
            lfs3_file_close(&lfs3, &file1) => 0;
            lfs3_file_close(&lfs3, &file2) => 0;
            lfs3_file_close(&lfs3, &file3) => 0;
            lfs3_file_close(&lfs3, &file4) => 0;
            lfs3_unmount(&lfs3) => 0;
            lfs3_mount(&lfs3, LFS3_M_RDWR, CFG) => 0;
            lfs3_file_open(&lfs3, &file1, "cuttlefish", LFS3_O_RDONLY) => 0;
            lfs3_file_open(&lfs3, &file2, "jellyfish", LFS3_O_RDONLY) => 0;
            lfs3_file_open(&lfs3, &file3, "octopus", LFS3_O_RDONLY) => 0;
            lfs3_file_open(&lfs3, &file4, "squid", LFS3_O_RDONLY) => 0;
        }

        lfs3_file_rewind(&lfs3, &file1) => 0;
        uint8_t rbuf[SIZE];
        lfs3_file_read(&lfs3, &file1, rbuf, SIZE) => SIZE;
        assert(memcmp(rbuf, wbuf1, SIZE) == 0);

        lfs3_file_rewind(&lfs3, &file2) => 0;
        lfs3_file_read(&lfs3, &file2, rbuf, SIZE) => SIZE;
        assert(memcmp(rbuf, wbuf2, SIZE) == 0);

        lfs3_file_rewind(&lfs3, &file3) => 0;
        lfs3_file_read(&lfs3, &file3, rbuf, SIZE) => SIZE;
        assert(memcmp(rbuf, wbuf3, SIZE) == 0);

        lfs3_file_rewind(&lfs3, &file4) => 0;
        lfs3_file_read(&lfs3, &file4, rbuf, SIZE) => SIZE;
        assert(memcmp(rbuf, wbuf4, SIZE) == 0);
    }

    lfs3_file_close(&lfs3, &file1) => 0;
    lfs3_file_close(&lfs3, &file2) => 0;
    lfs3_file_close(&lfs3, &file3) => 0;
    lfs3_file_close(&lfs3, &file4) => 0;
    lfs3_unmount(&lfs3) => 0;
'''


# test traversals with btree eviction

[cases.test_evict_t_btree_mtree]
defines.SIZE = 'FCACHE_SIZE/2'
# force early relocations
defines.BLOCK_RECYCLES = 0
code = '''
    lfs3_t lfs3;
    lfs3_format(&lfs3,
            LFS3_F_RDWR
                | ((GBMAP) ? LFS3_IFDEF_GBMAP(LFS3_F_GBMAP, -1) : 0),
            CFG) => 0;
    lfs3_mount(&lfs3, LFS3_M_RDWR, CFG) => 0;

    uint32_t prng = 42;

    // create two files
    lfs3_file_t file1;
    lfs3_file_open(&lfs3, &file1, "jellyfish",
            LFS3_O_RDWR | LFS3_O_CREAT | LFS3_O_EXCL) => 0;
    uint8_t wbuf1[SIZE];
    for (lfs3_size_t j = 0; j < SIZE; j++) {
        wbuf1[j] = 'a' + (TEST_PRNG(&prng) % 26);
    }
    lfs3_file_write(&lfs3, &file1, wbuf1, SIZE) => SIZE;
    lfs3_file_sync(&lfs3, &file1) => 0;

    lfs3_file_t file2;
    lfs3_file_open(&lfs3, &file2, "octopus",
            LFS3_O_RDWR | LFS3_O_CREAT | LFS3_O_EXCL) => 0;
    uint8_t wbuf2[SIZE];
    for (lfs3_size_t j = 0; j < SIZE; j++) {
        wbuf2[j] = 'a' + (TEST_PRNG(&prng) % 26);
    }
    lfs3_file_write(&lfs3, &file2, wbuf2, SIZE) => SIZE;
    lfs3_file_sync(&lfs3, &file2) => 0;

    // create more files until we have an mtree
    lfs3_size_t i = 0;
    // hack, don't use the internals like this
    while (lfs3.mtree.weight == 0) {
        char name[256];
        sprintf(name, "medusaaaaaaaaaaaaaaaa%03x", i);
        lfs3_file_t file;
        lfs3_file_open(&lfs3, &file, name,
                LFS3_O_WRONLY | LFS3_O_CREAT | LFS3_O_EXCL) => 0;
        // let's speed this up a bit
        file.h.mdir.r.eoff = -1;
        lfs3_file_close(&lfs3, &file) => 0;
        i += 1;
    }

    // get a block to evict
    lfs3_block_t victim = lfs3.mtree.blocks[0];

    // try evicting
    lfs3_fs_mkbad(&lfs3, victim, LFS3_MKBAD_EVICT) => 0;

    // evicted block should no longer be in use
    assert(lfs3.mtree.blocks[0] != victim);

    // evicted block should no longer be in use, anywhere in the
    // filesystem
    lfs3_trv_t trv;
    lfs3_trv_open(&lfs3, &trv, LFS3_T_RDONLY) => 0;
    for (lfs3_block_t i = 0;; i++) {
        assert(i < 2*BLOCK_COUNT);

        struct lfs3_tinfo tinfo;
        int err = lfs3_trv_read(&lfs3, &trv, &tinfo);
        assert(!err || err == LFS3_ERR_NOENT);
        if (err == LFS3_ERR_NOENT) {
            break;
        }

        assert(tinfo.block != victim);
    }
    lfs3_trv_close(&lfs3, &trv) => 0;

    // check we can still read the files
    for (int remount = 0; remount < 2; remount++) {
        // remount?
        if (remount) {
            lfs3_file_close(&lfs3, &file1) => 0;
            lfs3_file_close(&lfs3, &file2) => 0;
            lfs3_unmount(&lfs3) => 0;
            lfs3_mount(&lfs3, LFS3_M_RDWR, CFG) => 0;
            lfs3_file_open(&lfs3, &file1, "jellyfish", LFS3_O_RDONLY) => 0;
            lfs3_file_open(&lfs3, &file2, "octopus", LFS3_O_RDONLY) => 0;
        }

        lfs3_file_rewind(&lfs3, &file1) => 0;
        uint8_t rbuf[SIZE];
        lfs3_file_read(&lfs3, &file1, rbuf, SIZE) => SIZE;
        assert(memcmp(rbuf, wbuf1, SIZE) == 0);

        lfs3_file_rewind(&lfs3, &file2) => 0;
        lfs3_file_read(&lfs3, &file2, rbuf, SIZE) => SIZE;
        assert(memcmp(rbuf, wbuf2, SIZE) == 0);
    }

    lfs3_file_close(&lfs3, &file1) => 0;
    lfs3_file_close(&lfs3, &file2) => 0;
    lfs3_unmount(&lfs3) => 0;
'''

[cases.test_evict_t_btree_mtree_mtree]
defines.SIZE = 'FCACHE_SIZE/2'
defines.SMACK = 20
defines.SIBLING = [0, 1, 2]
# force early relocations
defines.BLOCK_RECYCLES = 0
code = '''
    lfs3_t lfs3;
    lfs3_format(&lfs3,
            LFS3_F_RDWR
                | ((GBMAP) ? LFS3_IFDEF_GBMAP(LFS3_F_GBMAP, -1) : 0),
            CFG) => 0;
    lfs3_mount(&lfs3, LFS3_M_RDWR, CFG) => 0;

    uint32_t prng = 42;

    // create two files
    lfs3_file_t file1;
    lfs3_file_open(&lfs3, &file1, "jellyfish",
            LFS3_O_RDWR | LFS3_O_CREAT | LFS3_O_EXCL) => 0;
    uint8_t wbuf1[SIZE];
    for (lfs3_size_t j = 0; j < SIZE; j++) {
        wbuf1[j] = 'a' + (TEST_PRNG(&prng) % 26);
    }
    lfs3_file_write(&lfs3, &file1, wbuf1, SIZE) => SIZE;
    lfs3_file_sync(&lfs3, &file1) => 0;

    lfs3_file_t file2;
    lfs3_file_open(&lfs3, &file2, "octopus",
            LFS3_O_RDWR | LFS3_O_CREAT | LFS3_O_EXCL) => 0;
    uint8_t wbuf2[SIZE];
    for (lfs3_size_t j = 0; j < SIZE; j++) {
        wbuf2[j] = 'a' + (TEST_PRNG(&prng) % 26);
    }
    lfs3_file_write(&lfs3, &file2, wbuf2, SIZE) => SIZE;
    lfs3_file_sync(&lfs3, &file2) => 0;

    // create more files until we have at least 3 btree nodes
    lfs3_size_t i = 0;
    while (true) {
        lfs3_size_t count = 0;
        lfs3_trv_t trv;
        lfs3_trv_open(&lfs3, &trv, LFS3_T_RDONLY) => 0;
        for (lfs3_block_t i = 0;; i++) {
            assert(i < 2*BLOCK_COUNT);

            struct lfs3_tinfo tinfo;
            int err = lfs3_trv_read(&lfs3, &trv, &tinfo);
            assert(!err || err == LFS3_ERR_NOENT);
            if (err == LFS3_ERR_NOENT) {
                break;
            }

            if (tinfo.btype == LFS3_BTYPE_BTREE) {
                count += 1;
            }
        }
        lfs3_trv_close(&lfs3, &trv) => 0;
        if (count >= 3 + ((GBMAP) ? 1 : 0)) {
            break;
        }

        for (lfs3_size_t k = 0; k < SMACK; k++) {
            char name[256];
            sprintf(name, "medusaaaaaaaaaaaaaaaa%03x", i);
            lfs3_file_t file;
            lfs3_file_open(&lfs3, &file, name,
                    LFS3_O_WRONLY | LFS3_O_CREAT | LFS3_O_EXCL) => 0;
            lfs3_file_close(&lfs3, &file) => 0;
            i += 1;
        }
    }

    // get a block to evict
    lfs3_block_t victim = 0;
    lfs3_size_t count = 0;
    lfs3_trv_t trv;
    lfs3_trv_open(&lfs3, &trv, LFS3_T_RDONLY) => 0;
    for (lfs3_block_t i = 0;; i++) {
        assert(i < 2*BLOCK_COUNT);

        struct lfs3_tinfo tinfo;
        int err = lfs3_trv_read(&lfs3, &trv, &tinfo);
        assert(!err);

        if (tinfo.btype == LFS3_BTYPE_BTREE) {
            if (count == SIBLING) {
                victim = tinfo.block;
                break;
            }

            count += 1;
        }
    }
    lfs3_trv_close(&lfs3, &trv) => 0;

    // try evicting
    lfs3_fs_mkbad(&lfs3, victim, LFS3_MKBAD_EVICT) => 0;

    // evicted block should no longer be in use, anywhere in the
    // filesystem
    lfs3_trv_open(&lfs3, &trv, LFS3_T_RDONLY) => 0;
    for (lfs3_block_t i = 0;; i++) {
        assert(i < 2*BLOCK_COUNT);

        struct lfs3_tinfo tinfo;
        int err = lfs3_trv_read(&lfs3, &trv, &tinfo);
        assert(!err || err == LFS3_ERR_NOENT);
        if (err == LFS3_ERR_NOENT) {
            break;
        }

        assert(tinfo.block != victim);
    }
    lfs3_trv_close(&lfs3, &trv) => 0;

    // check we can still read the files
    for (int remount = 0; remount < 2; remount++) {
        // remount?
        if (remount) {
            lfs3_file_close(&lfs3, &file1) => 0;
            lfs3_file_close(&lfs3, &file2) => 0;
            lfs3_unmount(&lfs3) => 0;
            lfs3_mount(&lfs3, LFS3_M_RDWR, CFG) => 0;
            lfs3_file_open(&lfs3, &file1, "jellyfish", LFS3_O_RDONLY) => 0;
            lfs3_file_open(&lfs3, &file2, "octopus", LFS3_O_RDONLY) => 0;
        }

        lfs3_file_rewind(&lfs3, &file1) => 0;
        uint8_t rbuf[SIZE];
        lfs3_file_read(&lfs3, &file1, rbuf, SIZE) => SIZE;
        assert(memcmp(rbuf, wbuf1, SIZE) == 0);

        lfs3_file_rewind(&lfs3, &file2) => 0;
        lfs3_file_read(&lfs3, &file2, rbuf, SIZE) => SIZE;
        assert(memcmp(rbuf, wbuf2, SIZE) == 0);
    }

    lfs3_file_close(&lfs3, &file1) => 0;
    lfs3_file_close(&lfs3, &file2) => 0;
    lfs3_unmount(&lfs3) => 0;
'''

[cases.test_evict_t_btree_btree]
defines.CHUNK = 'BLOCK_SIZE/8'
defines.SYNC = [false, true]
defines.CLOSE = [false, true]
defines.SIBLING = [0]
# prevent block crystallization
defines.CRYSTAL_THRESH = -1
code = '''
    lfs3_t lfs3;
    lfs3_format(&lfs3,
            LFS3_F_RDWR
                | ((GBMAP) ? LFS3_IFDEF_GBMAP(LFS3_F_GBMAP, -1) : 0),
            CFG) => 0;
    lfs3_mount(&lfs3, LFS3_M_RDWR, CFG) => 0;

    uint32_t prng = 42;

    // create a file
    lfs3_file_t file;
    lfs3_file_open(&lfs3, &file, "jellyfish",
            LFS3_O_RDWR | LFS3_O_CREAT | LFS3_O_EXCL) => 0;

    // write to our file until we have a btree
    uint32_t wprng = prng;
    lfs3_off_t wsize = 0;
    // hack, don't use the internals like this
    while ((file.bshrub.trunk & 0x80000000)
            || file.bshrub.weight == 0) {
        uint8_t wbuf[CHUNK];
        for (lfs3_size_t j = 0; j < CHUNK; j++) {
            wbuf[j] = 'a' + (TEST_PRNG(&prng) % 26);
        }
        lfs3_file_write(&lfs3, &file, wbuf, CHUNK) => CHUNK;
        if (SYNC) {
            lfs3_file_sync(&lfs3, &file) => 0;
        }
        wsize += CHUNK;
    }

    // close during eviction? this makes sure eviction can find the
    // btree on disk
    if (CLOSE) {
        lfs3_file_close(&lfs3, &file) => 0;
    }

    // get a block to evict
    lfs3_block_t victim = file.bshrub.blocks[0];

    // try evicting
    lfs3_fs_mkbad(&lfs3, victim, LFS3_MKBAD_EVICT) => 0;

    // reopen if necessary
    if (CLOSE) {
        lfs3_file_open(&lfs3, &file, "jellyfish", LFS3_O_RDWR) => 0;
    }

    // evicted block should no longer be in use
    assert(file.bshrub.blocks[0] != victim);

    // evicted block should no longer be in use, anywhere in the
    // filesystem
    lfs3_trv_t trv;
    lfs3_trv_open(&lfs3, &trv, LFS3_T_RDONLY) => 0;
    for (lfs3_block_t i = 0;; i++) {
        assert(i < 2*BLOCK_COUNT);

        struct lfs3_tinfo tinfo;
        int err = lfs3_trv_read(&lfs3, &trv, &tinfo);
        assert(!err || err == LFS3_ERR_NOENT);
        if (err == LFS3_ERR_NOENT) {
            break;
        }

        assert(tinfo.block != victim);
    }
    lfs3_trv_close(&lfs3, &trv) => 0;

    // check we can still read the file
    for (int remount = 0; remount < 2; remount++) {
        // remount?
        if (remount) {
            lfs3_file_close(&lfs3, &file) => 0;
            lfs3_unmount(&lfs3) => 0;
            lfs3_mount(&lfs3, LFS3_M_RDWR, CFG) => 0;
            lfs3_file_open(&lfs3, &file, "jellyfish", LFS3_O_RDONLY) => 0;
        }

        lfs3_file_rewind(&lfs3, &file) => 0;
        prng = wprng;
        for (lfs3_off_t i = 0; i < wsize; i += CHUNK) {
            uint8_t wbuf[CHUNK];
            for (lfs3_size_t j = 0; j < CHUNK; j++) {
                wbuf[j] = 'a' + (TEST_PRNG(&prng) % 26);
            }
            uint8_t rbuf[CHUNK];
            lfs3_file_read(&lfs3, &file, rbuf, CHUNK) => CHUNK;
            assert(memcmp(rbuf, wbuf, CHUNK) == 0);
        }
    }

    lfs3_file_close(&lfs3, &file) => 0;
    lfs3_unmount(&lfs3) => 0;
'''

[cases.test_evict_t_btree_btree_btree]
defines.CHUNK = 'BLOCK_SIZE/8'
defines.SYNC = [false, true]
defines.CLOSE = [false, true]
defines.SIBLING = [0, 1, 2]
# prevent block crystallization
defines.CRYSTAL_THRESH = -1
code = '''
    lfs3_t lfs3;
    lfs3_format(&lfs3,
            LFS3_F_RDWR
                | ((GBMAP) ? LFS3_IFDEF_GBMAP(LFS3_F_GBMAP, -1) : 0),
            CFG) => 0;
    lfs3_mount(&lfs3, LFS3_M_RDWR, CFG) => 0;

    uint32_t prng = 42;

    // create a file
    lfs3_file_t file;
    lfs3_file_open(&lfs3, &file, "jellyfish",
            LFS3_O_RDWR | LFS3_O_CREAT | LFS3_O_EXCL) => 0;

    // write to our file until we are a btree and have at least
    // 3 btree nodes
    uint32_t wprng = prng;
    lfs3_off_t wsize = 0;
    while (true) {
        if (!(file.bshrub.trunk & 0x80000000)) {
            lfs3_size_t count = 0;
            lfs3_trv_t trv;
            lfs3_trv_open(&lfs3, &trv, LFS3_T_RDONLY) => 0;
            for (lfs3_block_t i = 0;; i++) {
                assert(i < 2*BLOCK_COUNT);

                struct lfs3_tinfo tinfo;
                int err = lfs3_trv_read(&lfs3, &trv, &tinfo);
                assert(!err || err == LFS3_ERR_NOENT);
                if (err == LFS3_ERR_NOENT) {
                    break;
                }

                if (tinfo.btype == LFS3_BTYPE_BTREE) {
                    count += 1;
                }
            }
            lfs3_trv_close(&lfs3, &trv) => 0;
            if (count >= 3 + ((GBMAP) ? 1 : 0)) {
                break;
            }
        }

        uint8_t wbuf[CHUNK];
        for (lfs3_size_t j = 0; j < CHUNK; j++) {
            wbuf[j] = 'a' + (TEST_PRNG(&prng) % 26);
        }
        lfs3_file_write(&lfs3, &file, wbuf, CHUNK) => CHUNK;
        if (SYNC) {
            lfs3_file_sync(&lfs3, &file) => 0;
        }
        wsize += CHUNK;
    }

    // close during gc? this makes sure gc can find the btree on disk
    if (CLOSE) {
        lfs3_file_close(&lfs3, &file) => 0;
    }

    // get a block to evict
    lfs3_block_t victim = 0;
    lfs3_size_t count = 0;
    lfs3_trv_t trv;
    lfs3_trv_open(&lfs3, &trv, LFS3_T_RDONLY) => 0;
    for (lfs3_block_t i = 0;; i++) {
        assert(i < 2*BLOCK_COUNT);

        struct lfs3_tinfo tinfo;
        int err = lfs3_trv_read(&lfs3, &trv, &tinfo);
        assert(!err);

        if (tinfo.btype == LFS3_BTYPE_BTREE) {
            if (count == SIBLING) {
                victim = tinfo.block;
                break;
            }

            count += 1;
        }
    }
    lfs3_trv_close(&lfs3, &trv) => 0;

    // try evicting
    lfs3_fs_mkbad(&lfs3, victim, LFS3_MKBAD_EVICT) => 0;

    // reopen if necessary
    if (CLOSE) {
        lfs3_file_open(&lfs3, &file, "jellyfish", LFS3_O_RDWR) => 0;
    }

    // evicted block should no longer be in use, anywhere in the
    // filesystem
    lfs3_trv_open(&lfs3, &trv, LFS3_T_RDONLY) => 0;
    for (lfs3_block_t i = 0;; i++) {
        assert(i < 2*BLOCK_COUNT);

        struct lfs3_tinfo tinfo;
        int err = lfs3_trv_read(&lfs3, &trv, &tinfo);
        assert(!err || err == LFS3_ERR_NOENT);
        if (err == LFS3_ERR_NOENT) {
            break;
        }

        assert(tinfo.block != victim);
    }
    lfs3_trv_close(&lfs3, &trv) => 0;

    // check we can still read the file
    for (int remount = 0; remount < 2; remount++) {
        // remount?
        if (remount) {
            lfs3_file_close(&lfs3, &file) => 0;
            lfs3_unmount(&lfs3) => 0;
            lfs3_mount(&lfs3, LFS3_M_RDWR, CFG) => 0;
            lfs3_file_open(&lfs3, &file, "jellyfish", LFS3_O_RDONLY) => 0;
        }

        lfs3_file_rewind(&lfs3, &file) => 0;
        prng = wprng;
        for (lfs3_off_t i = 0; i < wsize; i += CHUNK) {
            uint8_t wbuf[CHUNK];
            for (lfs3_size_t j = 0; j < CHUNK; j++) {
                wbuf[j] = 'a' + (TEST_PRNG(&prng) % 26);
            }
            uint8_t rbuf[CHUNK];
            lfs3_file_read(&lfs3, &file, rbuf, CHUNK) => CHUNK;
            assert(memcmp(rbuf, wbuf, CHUNK) == 0);
        }
    }

    lfs3_file_close(&lfs3, &file) => 0;
    lfs3_unmount(&lfs3) => 0;
'''

[cases.test_evict_t_btree_bshrub]
defines.CHUNK = 'BLOCK_SIZE/8'
defines.SYNC = [false, true]
defines.CLOSE = [false, true]
defines.SIBLING = [0, 1, 2]
# prevent block crystallization
defines.CRYSTAL_THRESH = -1
code = '''
    lfs3_t lfs3;
    lfs3_format(&lfs3,
            LFS3_F_RDWR
                | ((GBMAP) ? LFS3_IFDEF_GBMAP(LFS3_F_GBMAP, -1) : 0),
            CFG) => 0;
    lfs3_mount(&lfs3, LFS3_M_RDWR, CFG) => 0;

    uint32_t prng = 42;

    // create a file
    lfs3_file_t file;
    lfs3_file_open(&lfs3, &file, "jellyfish",
            LFS3_O_RDWR | LFS3_O_CREAT | LFS3_O_EXCL) => 0;

    // write to our file until we are a bshrub and have at least
    // 3 btree node
    uint32_t wprng = prng;
    lfs3_off_t wsize = 0;
    while (true) {
        if (file.bshrub.trunk & 0x80000000) {
            lfs3_size_t count = 0;
            lfs3_trv_t trv;
            lfs3_trv_open(&lfs3, &trv, LFS3_T_RDONLY) => 0;
            for (lfs3_block_t i = 0;; i++) {
                assert(i < 2*BLOCK_COUNT);

                struct lfs3_tinfo tinfo;
                int err = lfs3_trv_read(&lfs3, &trv, &tinfo);
                assert(!err || err == LFS3_ERR_NOENT);
                if (err == LFS3_ERR_NOENT) {
                    break;
                }

                if (tinfo.btype == LFS3_BTYPE_BTREE) {
                    count += 1;
                }
            }
            lfs3_trv_close(&lfs3, &trv) => 0;
            if (count >= 3 + ((GBMAP) ? 1 : 0)) {
                break;
            }
        }

        uint8_t wbuf[CHUNK];
        for (lfs3_size_t j = 0; j < CHUNK; j++) {
            wbuf[j] = 'a' + (TEST_PRNG(&prng) % 26);
        }
        lfs3_file_write(&lfs3, &file, wbuf, CHUNK) => CHUNK;
        if (SYNC) {
            lfs3_file_sync(&lfs3, &file) => 0;
        }
        wsize += CHUNK;
    }

    // close during gc? this makes sure gc can find the btree on disk
    if (CLOSE) {
        lfs3_file_close(&lfs3, &file) => 0;
    }

    // get a block to evict
    lfs3_block_t victim = 0;
    lfs3_size_t count = 0;
    lfs3_trv_t trv;
    lfs3_trv_open(&lfs3, &trv, LFS3_T_RDONLY) => 0;
    for (lfs3_block_t i = 0;; i++) {
        assert(i < 2*BLOCK_COUNT);

        struct lfs3_tinfo tinfo;
        int err = lfs3_trv_read(&lfs3, &trv, &tinfo);
        assert(!err);

        if (tinfo.btype == LFS3_BTYPE_BTREE) {
            if (count == SIBLING) {
                victim = tinfo.block;
                break;
            }

            count += 1;
        }
    }
    lfs3_trv_close(&lfs3, &trv) => 0;

    // try evicting
    lfs3_fs_mkbad(&lfs3, victim, LFS3_MKBAD_EVICT) => 0;

    // reopen if necessary
    if (CLOSE) {
        lfs3_file_open(&lfs3, &file, "jellyfish", LFS3_O_RDWR) => 0;
    }

    // evicted block should no longer be in use, anywhere in the
    // filesystem
    lfs3_trv_open(&lfs3, &trv, LFS3_T_RDONLY) => 0;
    for (lfs3_block_t i = 0;; i++) {
        assert(i < 2*BLOCK_COUNT);

        struct lfs3_tinfo tinfo;
        int err = lfs3_trv_read(&lfs3, &trv, &tinfo);
        assert(!err || err == LFS3_ERR_NOENT);
        if (err == LFS3_ERR_NOENT) {
            break;
        }

        assert(tinfo.block != victim);
    }
    lfs3_trv_close(&lfs3, &trv) => 0;

    // check we can still read the file
    for (int remount = 0; remount < 2; remount++) {
        // remount?
        if (remount) {
            lfs3_file_close(&lfs3, &file) => 0;
            lfs3_unmount(&lfs3) => 0;
            lfs3_mount(&lfs3, LFS3_M_RDWR, CFG) => 0;
            lfs3_file_open(&lfs3, &file, "jellyfish", LFS3_O_RDONLY) => 0;
        }

        lfs3_file_rewind(&lfs3, &file) => 0;
        prng = wprng;
        for (lfs3_off_t i = 0; i < wsize; i += CHUNK) {
            uint8_t wbuf[CHUNK];
            for (lfs3_size_t j = 0; j < CHUNK; j++) {
                wbuf[j] = 'a' + (TEST_PRNG(&prng) % 26);
            }
            uint8_t rbuf[CHUNK];
            lfs3_file_read(&lfs3, &file, rbuf, CHUNK) => CHUNK;
            assert(memcmp(rbuf, wbuf, CHUNK) == 0);
        }
    }

    lfs3_file_close(&lfs3, &file) => 0;
    lfs3_unmount(&lfs3) => 0;
'''

[cases.test_evict_t_btree_btree_broadcast]
defines.CHUNK = 'BLOCK_SIZE/8'
defines.HANDLESET = 'range(0x8)'
defines.SIBLING = [0]
# prevent block crystallization
defines.CRYSTAL_THRESH = -1
code = '''
    lfs3_t lfs3;
    lfs3_format(&lfs3,
            LFS3_F_RDWR
                | ((GBMAP) ? LFS3_IFDEF_GBMAP(LFS3_F_GBMAP, -1) : 0),
            CFG) => 0;
    lfs3_mount(&lfs3, LFS3_M_RDWR, CFG) => 0;

    uint32_t prng = 42;

    // create a file
    lfs3_file_t file;
    lfs3_file_open(&lfs3, &file, "jellyfish",
            LFS3_O_RDWR | LFS3_O_CREAT | LFS3_O_EXCL) => 0;

    // write to our file until we have a btree
    uint32_t wprng = prng;
    lfs3_off_t wsize = 0;
    // hack, don't use the internals like this
    while ((file.bshrub.trunk & 0x80000000)
            || file.bshrub.weight == 0) {
        uint8_t wbuf[CHUNK];
        for (lfs3_size_t j = 0; j < CHUNK; j++) {
            wbuf[j] = 'a' + (TEST_PRNG(&prng) % 26);
        }
        lfs3_file_write(&lfs3, &file, wbuf, CHUNK) => CHUNK;
        lfs3_file_sync(&lfs3, &file) => 0;
        wsize += CHUNK;
    }

    // open a couple other handles in different states
    lfs3_file_t file_mutated;
    if (HANDLESET & 0x1) {
        lfs3_file_open(&lfs3, &file_mutated, "jellyfish", LFS3_O_RDWR) => 0;
        lfs3_file_write(&lfs3, &file_mutated, "!", 1) => 1;
        lfs3_file_flush(&lfs3, &file_mutated) => 0;
    }

    lfs3_file_t file_desync;
    if (HANDLESET & 0x2) {
        lfs3_file_open(&lfs3, &file_desync, "jellyfish",
                LFS3_O_RDWR | LFS3_O_DESYNC) => 0;
    }

    lfs3_file_t file_desync_mutated;
    if (HANDLESET & 0x4) {
        lfs3_file_open(&lfs3, &file_desync_mutated, "jellyfish",
                LFS3_O_RDWR | LFS3_O_DESYNC) => 0;
        lfs3_file_write(&lfs3, &file_desync_mutated, "!", 1) => 1;
        lfs3_file_flush(&lfs3, &file_desync_mutated) => 0;
    }

    // get a block to evict
    lfs3_block_t victim = file.bshrub.blocks[0];

    // try evicting
    lfs3_fs_mkbad(&lfs3, victim, LFS3_MKBAD_EVICT) => 0;

    // evicted block should no longer be in use
    assert(file.bshrub.blocks[0] != victim);

    // evicted block should no longer be in use, anywhere in the
    // filesystem
    lfs3_trv_t trv;
    lfs3_trv_open(&lfs3, &trv, LFS3_T_RDONLY) => 0;
    for (lfs3_block_t i = 0;; i++) {
        assert(i < 2*BLOCK_COUNT);

        struct lfs3_tinfo tinfo;
        int err = lfs3_trv_read(&lfs3, &trv, &tinfo);
        assert(!err || err == LFS3_ERR_NOENT);
        if (err == LFS3_ERR_NOENT) {
            break;
        }

        assert(tinfo.block != victim);
    }
    lfs3_trv_close(&lfs3, &trv) => 0;

    if (HANDLESET & 0x1) {
        // file_mutated should still be a different bshrub
        assert(file_mutated.bshrub.blocks[0]
                    != file.bshrub.blocks[0]
                || (file_mutated.bshrub.trunk & 0x7fffffff)
                    != (file.bshrub.trunk & 0x7fffffff));
    }

    if (HANDLESET & 0x2) {
        // file_desync should be the same bshrub, yes we ignore desync
        // here!
        assert(file_desync.bshrub.blocks[0]
                    == file.bshrub.blocks[0]
                && (file_desync.bshrub.trunk & 0x7fffffff)
                    == (file.bshrub.trunk & 0x7fffffff));
    }

    if (HANDLESET & 0x4) {
        // file_desync_mutated should still be a different bshrub
        assert(file_desync_mutated.bshrub.blocks[0]
                    != file.bshrub.blocks[0]
                || (file_desync_mutated.bshrub.trunk & 0x7fffffff)
                    != (file.bshrub.trunk & 0x7fffffff));
    }

    // check we can still read the files
    lfs3_file_rewind(&lfs3, &file) => 0;
    prng = wprng;
    for (lfs3_off_t i = 0; i < wsize; i += CHUNK) {
        uint8_t wbuf[CHUNK];
        for (lfs3_size_t j = 0; j < CHUNK; j++) {
            wbuf[j] = 'a' + (TEST_PRNG(&prng) % 26);
        }
        uint8_t rbuf[CHUNK];
        lfs3_file_read(&lfs3, &file, rbuf, CHUNK) => CHUNK;
        assert(memcmp(rbuf, wbuf, CHUNK) == 0);
    }

    if (HANDLESET & 0x1) {
        lfs3_file_rewind(&lfs3, &file_mutated) => 0;
        prng = wprng;
        for (lfs3_off_t i = 0; i < wsize; i += CHUNK) {
            uint8_t wbuf[CHUNK];
            for (lfs3_size_t j = 0; j < CHUNK; j++) {
                wbuf[j] = 'a' + (TEST_PRNG(&prng) % 26);
            }
            if (i == 0) {
                wbuf[0] = '!';
            }
            uint8_t rbuf[CHUNK];
            lfs3_file_read(&lfs3, &file_mutated, rbuf, CHUNK) => CHUNK;
            assert(memcmp(rbuf, wbuf, CHUNK) == 0);
        }
    }

    if (HANDLESET & 0x2) {
        lfs3_file_rewind(&lfs3, &file_desync) => 0;
        prng = wprng;
        for (lfs3_off_t i = 0; i < wsize; i += CHUNK) {
            uint8_t wbuf[CHUNK];
            for (lfs3_size_t j = 0; j < CHUNK; j++) {
                wbuf[j] = 'a' + (TEST_PRNG(&prng) % 26);
            }
            uint8_t rbuf[CHUNK];
            lfs3_file_read(&lfs3, &file_desync, rbuf, CHUNK) => CHUNK;
            assert(memcmp(rbuf, wbuf, CHUNK) == 0);
        }
    }

    if (HANDLESET & 0x4) {
        lfs3_file_rewind(&lfs3, &file_desync_mutated) => 0;
        prng = wprng;
        for (lfs3_off_t i = 0; i < wsize; i += CHUNK) {
            uint8_t wbuf[CHUNK];
            for (lfs3_size_t j = 0; j < CHUNK; j++) {
                wbuf[j] = 'a' + (TEST_PRNG(&prng) % 26);
            }
            if (i == 0) {
                wbuf[0] = '!';
            }
            uint8_t rbuf[CHUNK];
            lfs3_file_read(&lfs3, &file_desync_mutated, rbuf, CHUNK) => CHUNK;
            assert(memcmp(rbuf, wbuf, CHUNK) == 0);
        }
    }

    lfs3_file_close(&lfs3, &file) => 0;
    if (HANDLESET & 0x1) {
        lfs3_file_desync(&lfs3, &file_mutated) => 0;
        lfs3_file_close(&lfs3, &file_mutated) => 0;
    }
    if (HANDLESET & 0x2) {
        lfs3_file_close(&lfs3, &file_desync) => 0;
    }
    if (HANDLESET & 0x4) {
        lfs3_file_close(&lfs3, &file_desync_mutated) => 0;
    }
    lfs3_unmount(&lfs3) => 0;
'''

[cases.test_evict_t_btree_btree_btree_broadcast]
defines.CHUNK = 'BLOCK_SIZE/8'
defines.HANDLESET = 'range(0x8)'
defines.SIBLING = [0, 1, 2]
# prevent block crystallization
defines.CRYSTAL_THRESH = -1
code = '''
    lfs3_t lfs3;
    lfs3_format(&lfs3,
            LFS3_F_RDWR
                | ((GBMAP) ? LFS3_IFDEF_GBMAP(LFS3_F_GBMAP, -1) : 0),
            CFG) => 0;
    lfs3_mount(&lfs3, LFS3_M_RDWR, CFG) => 0;

    uint32_t prng = 42;

    // create a file
    lfs3_file_t file;
    lfs3_file_open(&lfs3, &file, "jellyfish",
            LFS3_O_RDWR | LFS3_O_CREAT | LFS3_O_EXCL) => 0;

    // write to our file until we are a btree and have at least
    // 3 btree nodes
    uint32_t wprng = prng;
    lfs3_off_t wsize = 0;
    while (true) {
        if (!(file.bshrub.trunk & 0x80000000)) {
            lfs3_size_t count = 0;
            lfs3_trv_t trv;
            lfs3_trv_open(&lfs3, &trv, LFS3_T_RDONLY) => 0;
            for (lfs3_block_t i = 0;; i++) {
                assert(i < 2*BLOCK_COUNT);

                struct lfs3_tinfo tinfo;
                int err = lfs3_trv_read(&lfs3, &trv, &tinfo);
                assert(!err || err == LFS3_ERR_NOENT);
                if (err == LFS3_ERR_NOENT) {
                    break;
                }

                if (tinfo.btype == LFS3_BTYPE_BTREE) {
                    count += 1;
                }
            }
            lfs3_trv_close(&lfs3, &trv) => 0;
            if (count >= 3 + ((GBMAP) ? 1 : 0)) {
                break;
            }
        }

        uint8_t wbuf[CHUNK];
        for (lfs3_size_t j = 0; j < CHUNK; j++) {
            wbuf[j] = 'a' + (TEST_PRNG(&prng) % 26);
        }
        lfs3_file_write(&lfs3, &file, wbuf, CHUNK) => CHUNK;
        lfs3_file_sync(&lfs3, &file) => 0;
        wsize += CHUNK;
    }

    // open a couple other handles in different states
    lfs3_file_t file_mutated;
    if (HANDLESET & 0x1) {
        lfs3_file_open(&lfs3, &file_mutated, "jellyfish", LFS3_O_RDWR) => 0;
        lfs3_file_write(&lfs3, &file_mutated, "!", 1) => 1;
        lfs3_file_flush(&lfs3, &file_mutated) => 0;
    }

    lfs3_file_t file_desync;
    if (HANDLESET & 0x2) {
        lfs3_file_open(&lfs3, &file_desync, "jellyfish",
                LFS3_O_RDWR | LFS3_O_DESYNC) => 0;
    }

    lfs3_file_t file_desync_mutated;
    if (HANDLESET & 0x4) {
        lfs3_file_open(&lfs3, &file_desync_mutated, "jellyfish",
                LFS3_O_RDWR | LFS3_O_DESYNC) => 0;
        lfs3_file_write(&lfs3, &file_desync_mutated, "!", 1) => 1;
        lfs3_file_flush(&lfs3, &file_desync_mutated) => 0;
    }

    // get a block to evict
    lfs3_block_t victim = 0;
    lfs3_size_t count = 0;
    lfs3_trv_t trv;
    lfs3_trv_open(&lfs3, &trv, LFS3_T_RDONLY) => 0;
    for (lfs3_block_t i = 0;; i++) {
        assert(i < 2*BLOCK_COUNT);

        struct lfs3_tinfo tinfo;
        int err = lfs3_trv_read(&lfs3, &trv, &tinfo);
        assert(!err);

        if (tinfo.btype == LFS3_BTYPE_BTREE) {
            if (count == SIBLING) {
                victim = tinfo.block;
                break;
            }

            count += 1;
        }
    }
    lfs3_trv_close(&lfs3, &trv) => 0;

    // try evicting
    lfs3_fs_mkbad(&lfs3, victim, LFS3_MKBAD_EVICT) => 0;

    // evicted block should no longer be in use, anywhere in the
    // filesystem
    lfs3_trv_open(&lfs3, &trv, LFS3_T_RDONLY) => 0;
    for (lfs3_block_t i = 0;; i++) {
        assert(i < 2*BLOCK_COUNT);

        struct lfs3_tinfo tinfo;
        int err = lfs3_trv_read(&lfs3, &trv, &tinfo);
        assert(!err || err == LFS3_ERR_NOENT);
        if (err == LFS3_ERR_NOENT) {
            break;
        }

        assert(tinfo.block != victim);
    }
    lfs3_trv_close(&lfs3, &trv) => 0;

    if (HANDLESET & 0x1) {
        // file_mutated should still be a different bshrub
        assert(file_mutated.bshrub.blocks[0]
                    != file.bshrub.blocks[0]
                || (file_mutated.bshrub.trunk & 0x7fffffff)
                    != (file.bshrub.trunk & 0x7fffffff));
    }

    if (HANDLESET & 0x2) {
        // file_desync should be the same bshrub, yes we ignore desync
        // here!
        assert(file_desync.bshrub.blocks[0]
                    == file.bshrub.blocks[0]
                && (file_desync.bshrub.trunk & 0x7fffffff)
                    == (file.bshrub.trunk & 0x7fffffff));
    }

    if (HANDLESET & 0x4) {
        // file_desync_mutated should still be a different bshrub
        assert(file_desync_mutated.bshrub.blocks[0]
                    != file.bshrub.blocks[0]
                || (file_desync_mutated.bshrub.trunk & 0x7fffffff)
                    != (file.bshrub.trunk & 0x7fffffff));
    }

    // check we can still read the files
    lfs3_file_rewind(&lfs3, &file) => 0;
    prng = wprng;
    for (lfs3_off_t i = 0; i < wsize; i += CHUNK) {
        uint8_t wbuf[CHUNK];
        for (lfs3_size_t j = 0; j < CHUNK; j++) {
            wbuf[j] = 'a' + (TEST_PRNG(&prng) % 26);
        }
        uint8_t rbuf[CHUNK];
        lfs3_file_read(&lfs3, &file, rbuf, CHUNK) => CHUNK;
        assert(memcmp(rbuf, wbuf, CHUNK) == 0);
    }

    if (HANDLESET & 0x1) {
        lfs3_file_rewind(&lfs3, &file_mutated) => 0;
        prng = wprng;
        for (lfs3_off_t i = 0; i < wsize; i += CHUNK) {
            uint8_t wbuf[CHUNK];
            for (lfs3_size_t j = 0; j < CHUNK; j++) {
                wbuf[j] = 'a' + (TEST_PRNG(&prng) % 26);
            }
            if (i == 0) {
                wbuf[0] = '!';
            }
            uint8_t rbuf[CHUNK];
            lfs3_file_read(&lfs3, &file_mutated, rbuf, CHUNK) => CHUNK;
            assert(memcmp(rbuf, wbuf, CHUNK) == 0);
        }
    }

    if (HANDLESET & 0x2) {
        lfs3_file_rewind(&lfs3, &file_desync) => 0;
        prng = wprng;
        for (lfs3_off_t i = 0; i < wsize; i += CHUNK) {
            uint8_t wbuf[CHUNK];
            for (lfs3_size_t j = 0; j < CHUNK; j++) {
                wbuf[j] = 'a' + (TEST_PRNG(&prng) % 26);
            }
            uint8_t rbuf[CHUNK];
            lfs3_file_read(&lfs3, &file_desync, rbuf, CHUNK) => CHUNK;
            assert(memcmp(rbuf, wbuf, CHUNK) == 0);
        }
    }

    if (HANDLESET & 0x4) {
        lfs3_file_rewind(&lfs3, &file_desync_mutated) => 0;
        prng = wprng;
        for (lfs3_off_t i = 0; i < wsize; i += CHUNK) {
            uint8_t wbuf[CHUNK];
            for (lfs3_size_t j = 0; j < CHUNK; j++) {
                wbuf[j] = 'a' + (TEST_PRNG(&prng) % 26);
            }
            if (i == 0) {
                wbuf[0] = '!';
            }
            uint8_t rbuf[CHUNK];
            lfs3_file_read(&lfs3, &file_desync_mutated, rbuf, CHUNK) => CHUNK;
            assert(memcmp(rbuf, wbuf, CHUNK) == 0);
        }
    }

    lfs3_file_close(&lfs3, &file) => 0;
    if (HANDLESET & 0x1) {
        lfs3_file_desync(&lfs3, &file_mutated) => 0;
        lfs3_file_close(&lfs3, &file_mutated) => 0;
    }
    if (HANDLESET & 0x2) {
        lfs3_file_close(&lfs3, &file_desync) => 0;
    }
    if (HANDLESET & 0x4) {
        lfs3_file_close(&lfs3, &file_desync_mutated) => 0;
    }
    lfs3_unmount(&lfs3) => 0;
'''

[cases.test_evict_t_btree_bshrub_broadcast]
defines.CHUNK = 'BLOCK_SIZE/8'
defines.HANDLESET = 'range(0x8)'
defines.SIBLING = [0, 1, 2]
# prevent block crystallization
defines.CRYSTAL_THRESH = -1
code = '''
    lfs3_t lfs3;
    lfs3_format(&lfs3,
            LFS3_F_RDWR
                | ((GBMAP) ? LFS3_IFDEF_GBMAP(LFS3_F_GBMAP, -1) : 0),
            CFG) => 0;
    lfs3_mount(&lfs3, LFS3_M_RDWR, CFG) => 0;

    uint32_t prng = 42;

    // create a file
    lfs3_file_t file;
    lfs3_file_open(&lfs3, &file, "jellyfish",
            LFS3_O_RDWR | LFS3_O_CREAT | LFS3_O_EXCL) => 0;

    // write to our file until we are a bshrub and have at least
    // 3 btree node
    uint32_t wprng = prng;
    lfs3_off_t wsize = 0;
    while (true) {
        if (file.bshrub.trunk & 0x80000000) {
            lfs3_size_t count = 0;
            lfs3_trv_t trv;
            lfs3_trv_open(&lfs3, &trv, LFS3_T_RDONLY) => 0;
            for (lfs3_block_t i = 0;; i++) {
                assert(i < 2*BLOCK_COUNT);

                struct lfs3_tinfo tinfo;
                int err = lfs3_trv_read(&lfs3, &trv, &tinfo);
                assert(!err || err == LFS3_ERR_NOENT);
                if (err == LFS3_ERR_NOENT) {
                    break;
                }

                if (tinfo.btype == LFS3_BTYPE_BTREE) {
                    count += 1;
                }
            }
            lfs3_trv_close(&lfs3, &trv) => 0;
            if (count >= 3 + ((GBMAP) ? 1 : 0)) {
                break;
            }
        }

        uint8_t wbuf[CHUNK];
        for (lfs3_size_t j = 0; j < CHUNK; j++) {
            wbuf[j] = 'a' + (TEST_PRNG(&prng) % 26);
        }
        lfs3_file_write(&lfs3, &file, wbuf, CHUNK) => CHUNK;
        lfs3_file_sync(&lfs3, &file) => 0;
        wsize += CHUNK;
    }

    // open a couple other handles in different states
    lfs3_file_t file_mutated;
    if (HANDLESET & 0x1) {
        lfs3_file_open(&lfs3, &file_mutated, "jellyfish", LFS3_O_RDWR) => 0;
        lfs3_file_write(&lfs3, &file_mutated, "!", 1) => 1;
        lfs3_file_flush(&lfs3, &file_mutated) => 0;
    }

    lfs3_file_t file_desync;
    if (HANDLESET & 0x2) {
        lfs3_file_open(&lfs3, &file_desync, "jellyfish",
                LFS3_O_RDWR | LFS3_O_DESYNC) => 0;
    }

    lfs3_file_t file_desync_mutated;
    if (HANDLESET & 0x4) {
        lfs3_file_open(&lfs3, &file_desync_mutated, "jellyfish",
                LFS3_O_RDWR | LFS3_O_DESYNC) => 0;
        lfs3_file_write(&lfs3, &file_desync_mutated, "!", 1) => 1;
        lfs3_file_flush(&lfs3, &file_desync_mutated) => 0;
    }

    // get a block to evict
    lfs3_block_t victim = 0;
    lfs3_size_t count = 0;
    lfs3_trv_t trv;
    lfs3_trv_open(&lfs3, &trv, LFS3_T_RDONLY) => 0;
    for (lfs3_block_t i = 0;; i++) {
        assert(i < 2*BLOCK_COUNT);

        struct lfs3_tinfo tinfo;
        int err = lfs3_trv_read(&lfs3, &trv, &tinfo);
        assert(!err);

        if (tinfo.btype == LFS3_BTYPE_BTREE) {
            if (count == SIBLING) {
                victim = tinfo.block;
                break;
            }

            count += 1;
        }
    }
    lfs3_trv_close(&lfs3, &trv) => 0;

    // try evicting
    lfs3_fs_mkbad(&lfs3, victim, LFS3_MKBAD_EVICT) => 0;

    // evicted block should no longer be in use, anywhere in the
    // filesystem
    lfs3_trv_open(&lfs3, &trv, LFS3_T_RDONLY) => 0;
    for (lfs3_block_t i = 0;; i++) {
        assert(i < 2*BLOCK_COUNT);

        struct lfs3_tinfo tinfo;
        int err = lfs3_trv_read(&lfs3, &trv, &tinfo);
        assert(!err || err == LFS3_ERR_NOENT);
        if (err == LFS3_ERR_NOENT) {
            break;
        }

        assert(tinfo.block != victim);
    }
    lfs3_trv_close(&lfs3, &trv) => 0;

    if (HANDLESET & 0x1) {
        // file_mutated should still be a different bshrub
        assert(file_mutated.bshrub.blocks[0]
                    != file.bshrub.blocks[0]
                || (file_mutated.bshrub.trunk & 0x7fffffff)
                    != (file.bshrub.trunk & 0x7fffffff));
    }

    if (HANDLESET & 0x2) {
        // file_desync should be the same bshrub, yes we ignore desync
        // here!
        assert(file_desync.bshrub.blocks[0]
                    == file.bshrub.blocks[0]
                && (file_desync.bshrub.trunk & 0x7fffffff)
                    == (file.bshrub.trunk & 0x7fffffff));
    }

    if (HANDLESET & 0x4) {
        // file_desync_mutated should still be a different bshrub
        assert(file_desync_mutated.bshrub.blocks[0]
                    != file.bshrub.blocks[0]
                || (file_desync_mutated.bshrub.trunk & 0x7fffffff)
                    != (file.bshrub.trunk & 0x7fffffff));
    }

    // check we can still read the files
    lfs3_file_rewind(&lfs3, &file) => 0;
    prng = wprng;
    for (lfs3_off_t i = 0; i < wsize; i += CHUNK) {
        uint8_t wbuf[CHUNK];
        for (lfs3_size_t j = 0; j < CHUNK; j++) {
            wbuf[j] = 'a' + (TEST_PRNG(&prng) % 26);
        }
        uint8_t rbuf[CHUNK];
        lfs3_file_read(&lfs3, &file, rbuf, CHUNK) => CHUNK;
        assert(memcmp(rbuf, wbuf, CHUNK) == 0);
    }

    if (HANDLESET & 0x1) {
        lfs3_file_rewind(&lfs3, &file_mutated) => 0;
        prng = wprng;
        for (lfs3_off_t i = 0; i < wsize; i += CHUNK) {
            uint8_t wbuf[CHUNK];
            for (lfs3_size_t j = 0; j < CHUNK; j++) {
                wbuf[j] = 'a' + (TEST_PRNG(&prng) % 26);
            }
            if (i == 0) {
                wbuf[0] = '!';
            }
            uint8_t rbuf[CHUNK];
            lfs3_file_read(&lfs3, &file_mutated, rbuf, CHUNK) => CHUNK;
            assert(memcmp(rbuf, wbuf, CHUNK) == 0);
        }
    }

    if (HANDLESET & 0x2) {
        lfs3_file_rewind(&lfs3, &file_desync) => 0;
        prng = wprng;
        for (lfs3_off_t i = 0; i < wsize; i += CHUNK) {
            uint8_t wbuf[CHUNK];
            for (lfs3_size_t j = 0; j < CHUNK; j++) {
                wbuf[j] = 'a' + (TEST_PRNG(&prng) % 26);
            }
            uint8_t rbuf[CHUNK];
            lfs3_file_read(&lfs3, &file_desync, rbuf, CHUNK) => CHUNK;
            assert(memcmp(rbuf, wbuf, CHUNK) == 0);
        }
    }

    if (HANDLESET & 0x4) {
        lfs3_file_rewind(&lfs3, &file_desync_mutated) => 0;
        prng = wprng;
        for (lfs3_off_t i = 0; i < wsize; i += CHUNK) {
            uint8_t wbuf[CHUNK];
            for (lfs3_size_t j = 0; j < CHUNK; j++) {
                wbuf[j] = 'a' + (TEST_PRNG(&prng) % 26);
            }
            if (i == 0) {
                wbuf[0] = '!';
            }
            uint8_t rbuf[CHUNK];
            lfs3_file_read(&lfs3, &file_desync_mutated, rbuf, CHUNK) => CHUNK;
            assert(memcmp(rbuf, wbuf, CHUNK) == 0);
        }
    }

    lfs3_file_close(&lfs3, &file) => 0;
    if (HANDLESET & 0x1) {
        lfs3_file_desync(&lfs3, &file_mutated) => 0;
        lfs3_file_close(&lfs3, &file_mutated) => 0;
    }
    if (HANDLESET & 0x2) {
        lfs3_file_close(&lfs3, &file_desync) => 0;
    }
    if (HANDLESET & 0x4) {
        lfs3_file_close(&lfs3, &file_desync_mutated) => 0;
    }
    lfs3_unmount(&lfs3) => 0;
'''

[cases.test_evict_t_btree_gbmap]
defines.CHUNK = 'BLOCK_SIZE'
defines.SIZE = ['1*CHUNK', '3*CHUNK', '20*CHUNK']
defines.SYNC = [false, true]
if = 'GBMAP'
ifdef = 'LFS3_GBMAP'
code = '''
    lfs3_t lfs3;
    lfs3_format(&lfs3, LFS3_F_RDWR | LFS3_F_GBMAP, CFG) => 0;
    lfs3_mount(&lfs3, LFS3_M_RDWR, CFG) => 0;

    uint32_t prng = 42;

    // create a file
    lfs3_file_t file;
    lfs3_file_open(&lfs3, &file, "jellyfish",
            LFS3_O_RDWR | LFS3_O_CREAT | LFS3_O_EXCL) => 0;

    // write to our file
    //
    // we use a really annoying write pattern to try to fragment the gbmap
    uint32_t wprng = prng;
    lfs3_off_t wsize = 0;
    while (wsize < SIZE) {
        uint8_t wbuf[CHUNK];
        for (lfs3_size_t j = 0; j < CHUNK; j++) {
            wbuf[j] = 'a' + (TEST_PRNG(&prng) % 26);
        }
        lfs3_file_write(&lfs3, &file, wbuf, CHUNK) => CHUNK;
        lfs3_soff_t off = lfs3_file_seek(&lfs3, &file, -CHUNK, LFS3_SEEK_END);
        assert(off >= 0);
        for (lfs3_size_t j = 0; j < CHUNK; j++) {
            wbuf[j] = 'a' + (TEST_PRNG(&prng) % 26);
        }
        lfs3_file_write(&lfs3, &file, wbuf, CHUNK) => CHUNK;
        if (SYNC) {
            lfs3_file_sync(&lfs3, &file) => 0;
        }
        wsize += CHUNK;
    }

    // get a block to evict
    lfs3_block_t victim = lfs3.gbmap.b.blocks[0];

    // try evicting
    lfs3_fs_mkbad(&lfs3, victim, LFS3_MKBAD_EVICT) => 0;

    // evicted block should no longer be in use
    assert(lfs3.gbmap.b.blocks[0] != victim);

    // evicted block should no longer be in use, anywhere in the
    // filesystem
    lfs3_trv_t trv;
    lfs3_trv_open(&lfs3, &trv, LFS3_T_RDONLY) => 0;
    for (lfs3_block_t i = 0;; i++) {
        assert(i < 2*BLOCK_COUNT);

        struct lfs3_tinfo tinfo;
        int err = lfs3_trv_read(&lfs3, &trv, &tinfo);
        assert(!err || err == LFS3_ERR_NOENT);
        if (err == LFS3_ERR_NOENT) {
            break;
        }

        assert(tinfo.block != victim);
    }
    lfs3_trv_close(&lfs3, &trv) => 0;

    // check we can still read the file
    lfs3_file_rewind(&lfs3, &file) => 0;
    prng = wprng;
    for (lfs3_off_t i = 0; i < wsize; i += CHUNK) {
        uint8_t wbuf[CHUNK];
        for (lfs3_size_t j = 0; j < CHUNK; j++) {
            TEST_PRNG(&prng);
        }
        for (lfs3_size_t j = 0; j < CHUNK; j++) {
            wbuf[j] = 'a' + (TEST_PRNG(&prng) % 26);
        }
        uint8_t rbuf[CHUNK];
        lfs3_file_read(&lfs3, &file, rbuf, CHUNK) => CHUNK;
        assert(memcmp(rbuf, wbuf, CHUNK) == 0);
    }

    // try creating a second file to make sure the gbmap wasn't
    // corrupted
    lfs3_file_t file2;
    lfs3_file_open(&lfs3, &file2, "octopus",
            LFS3_O_RDWR | LFS3_O_CREAT | LFS3_O_EXCL) => 0;
    for (lfs3_off_t i = 0; i < wsize/2; i += CHUNK) {
        uint8_t wbuf[CHUNK];
        for (lfs3_size_t j = 0; j < CHUNK; j++) {
            wbuf[j] = 'a' + (TEST_PRNG(&prng) % 26);
        }
        lfs3_file_write(&lfs3, &file2, wbuf, CHUNK) => CHUNK;
    }
    lfs3_file_sync(&lfs3, &file2) => 0;

    // check we can still read the files
    for (int remount = 0; remount < 2; remount++) {
        // remount?
        if (remount) {
            lfs3_file_close(&lfs3, &file) => 0;
            lfs3_file_close(&lfs3, &file2) => 0;
            lfs3_unmount(&lfs3) => 0;
            lfs3_mount(&lfs3, LFS3_M_RDWR, CFG) => 0;
            lfs3_file_open(&lfs3, &file, "jellyfish", LFS3_O_RDONLY) => 0;
            lfs3_file_open(&lfs3, &file2, "octopus", LFS3_O_RDONLY) => 0;
        }

        lfs3_file_rewind(&lfs3, &file) => 0;
        prng = wprng;
        for (lfs3_off_t i = 0; i < wsize; i += CHUNK) {
            uint8_t wbuf[CHUNK];
            for (lfs3_size_t j = 0; j < CHUNK; j++) {
                TEST_PRNG(&prng);
            }
            for (lfs3_size_t j = 0; j < CHUNK; j++) {
                wbuf[j] = 'a' + (TEST_PRNG(&prng) % 26);
            }
            uint8_t rbuf[CHUNK];
            lfs3_file_read(&lfs3, &file, rbuf, CHUNK) => CHUNK;
            assert(memcmp(rbuf, wbuf, CHUNK) == 0);
        }

        lfs3_file_rewind(&lfs3, &file2) => 0;
        for (lfs3_off_t i = 0; i < wsize/2; i += CHUNK) {
            uint8_t wbuf[CHUNK];
            for (lfs3_size_t j = 0; j < CHUNK; j++) {
                wbuf[j] = 'a' + (TEST_PRNG(&prng) % 26);
            }
            uint8_t rbuf[CHUNK];
            lfs3_file_read(&lfs3, &file2, rbuf, CHUNK) => CHUNK;
            assert(memcmp(rbuf, wbuf, CHUNK) == 0);
        }
    }

    lfs3_file_close(&lfs3, &file) => 0;
    lfs3_file_close(&lfs3, &file2) => 0;
    lfs3_unmount(&lfs3) => 0;
'''

[cases.test_evict_t_btree_gbmap_gbmap]
defines.CHUNK = 'BLOCK_SIZE'
defines.SYNC = [false, true]
defines.SIBLING = [0, 1, 2]
if = 'GBMAP'
ifdef = 'LFS3_GBMAP'
in = 'lfs3.c'
code = '''
    lfs3_t lfs3;
    lfs3_format(&lfs3, LFS3_F_RDWR | LFS3_F_GBMAP, CFG) => 0;
    lfs3_mount(&lfs3, LFS3_M_RDWR, CFG) => 0;

    uint32_t prng = 42;

    // create a file
    lfs3_file_t file;
    lfs3_file_open(&lfs3, &file, "jellyfish",
            LFS3_O_RDWR | LFS3_O_CREAT | LFS3_O_EXCL) => 0;

    // write to our file until we have at least 3 btree nodes
    //
    // we use a really annoying write pattern to try to fragment the gbmap
    uint32_t wprng = prng;
    lfs3_off_t wsize = 0;
    while (true) {
        lfs3_size_t count = 0;
        lfs3_trv_t trv;
        lfs3_trv_open(&lfs3, &trv, LFS3_T_RDONLY) => 0;
        for (lfs3_block_t i = 0;; i++) {
            assert(i < 2*BLOCK_COUNT);

            struct lfs3_tinfo tinfo;
            int err = lfs3_trv_read(&lfs3, &trv, &tinfo);
            assert(!err || err == LFS3_ERR_NOENT);
            if (err == LFS3_ERR_NOENT) {
                break;
            }

            if (tinfo.btype == LFS3_BTYPE_BTREE
                    // bit of a hack
                    && trv.t.h.mdir.mid == LFS3_MID_GBMAP) {
                count += 1;
            }
        }
        lfs3_trv_close(&lfs3, &trv) => 0;
        if (count >= 3) {
            break;
        }

        uint8_t wbuf[CHUNK];
        for (lfs3_size_t j = 0; j < CHUNK; j++) {
            wbuf[j] = 'a' + (TEST_PRNG(&prng) % 26);
        }
        lfs3_file_write(&lfs3, &file, wbuf, CHUNK) => CHUNK;
        lfs3_soff_t off = lfs3_file_seek(&lfs3, &file, -CHUNK, LFS3_SEEK_END);
        assert(off >= 0);
        for (lfs3_size_t j = 0; j < CHUNK; j++) {
            wbuf[j] = 'a' + (TEST_PRNG(&prng) % 26);
        }
        lfs3_file_write(&lfs3, &file, wbuf, CHUNK) => CHUNK;
        if (SYNC) {
            lfs3_file_sync(&lfs3, &file) => 0;
        }
        wsize += CHUNK;
    }

    // get a block to evict
    lfs3_block_t victim = 0;
    lfs3_size_t count = 0;
    lfs3_trv_t trv;
    lfs3_trv_open(&lfs3, &trv, LFS3_T_RDONLY) => 0;
    for (lfs3_block_t i = 0;; i++) {
        assert(i < 2*BLOCK_COUNT);

        struct lfs3_tinfo tinfo;
        int err = lfs3_trv_read(&lfs3, &trv, &tinfo);
        assert(!err);

        if (tinfo.btype == LFS3_BTYPE_BTREE
                // bit of a hack
                && trv.t.h.mdir.mid == LFS3_MID_GBMAP) {
            if (count == SIBLING) {
                victim = tinfo.block;
                break;
            }

            count += 1;
        }
    }
    lfs3_trv_close(&lfs3, &trv) => 0;

    // try evicting
    lfs3_fs_mkbad(&lfs3, victim, LFS3_MKBAD_EVICT) => 0;

    // evicted block should no longer be in use, anywhere in the
    // filesystem
    lfs3_trv_open(&lfs3, &trv, LFS3_T_RDONLY) => 0;
    for (lfs3_block_t i = 0;; i++) {
        assert(i < 2*BLOCK_COUNT);

        struct lfs3_tinfo tinfo;
        int err = lfs3_trv_read(&lfs3, &trv, &tinfo);
        assert(!err || err == LFS3_ERR_NOENT);
        if (err == LFS3_ERR_NOENT) {
            break;
        }

        assert(tinfo.block != victim);
    }
    lfs3_trv_close(&lfs3, &trv) => 0;

    // check we can still read the file
    lfs3_file_rewind(&lfs3, &file) => 0;
    prng = wprng;
    for (lfs3_off_t i = 0; i < wsize; i += CHUNK) {
        uint8_t wbuf[CHUNK];
        for (lfs3_size_t j = 0; j < CHUNK; j++) {
            TEST_PRNG(&prng);
        }
        for (lfs3_size_t j = 0; j < CHUNK; j++) {
            wbuf[j] = 'a' + (TEST_PRNG(&prng) % 26);
        }
        uint8_t rbuf[CHUNK];
        lfs3_file_read(&lfs3, &file, rbuf, CHUNK) => CHUNK;
        assert(memcmp(rbuf, wbuf, CHUNK) == 0);
    }

    // try creating a second file to make sure the gbmap wasn't
    // corrupted
    lfs3_file_t file2;
    lfs3_file_open(&lfs3, &file2, "octopus",
            LFS3_O_RDWR | LFS3_O_CREAT | LFS3_O_EXCL) => 0;
    for (lfs3_off_t i = 0; i < wsize/2; i += CHUNK) {
        uint8_t wbuf[CHUNK];
        for (lfs3_size_t j = 0; j < CHUNK; j++) {
            wbuf[j] = 'a' + (TEST_PRNG(&prng) % 26);
        }
        lfs3_file_write(&lfs3, &file2, wbuf, CHUNK) => CHUNK;
    }
    lfs3_file_sync(&lfs3, &file2) => 0;

    // check we can still read the files
    for (int remount = 0; remount < 2; remount++) {
        // remount?
        if (remount) {
            lfs3_file_close(&lfs3, &file) => 0;
            lfs3_file_close(&lfs3, &file2) => 0;
            lfs3_unmount(&lfs3) => 0;
            lfs3_mount(&lfs3, LFS3_M_RDWR, CFG) => 0;
            lfs3_file_open(&lfs3, &file, "jellyfish", LFS3_O_RDONLY) => 0;
            lfs3_file_open(&lfs3, &file2, "octopus", LFS3_O_RDONLY) => 0;
        }

        lfs3_file_rewind(&lfs3, &file) => 0;
        prng = wprng;
        for (lfs3_off_t i = 0; i < wsize; i += CHUNK) {
            uint8_t wbuf[CHUNK];
            for (lfs3_size_t j = 0; j < CHUNK; j++) {
                TEST_PRNG(&prng);
            }
            for (lfs3_size_t j = 0; j < CHUNK; j++) {
                wbuf[j] = 'a' + (TEST_PRNG(&prng) % 26);
            }
            uint8_t rbuf[CHUNK];
            lfs3_file_read(&lfs3, &file, rbuf, CHUNK) => CHUNK;
            assert(memcmp(rbuf, wbuf, CHUNK) == 0);
        }

        lfs3_file_rewind(&lfs3, &file2) => 0;
        for (lfs3_off_t i = 0; i < wsize/2; i += CHUNK) {
            uint8_t wbuf[CHUNK];
            for (lfs3_size_t j = 0; j < CHUNK; j++) {
                wbuf[j] = 'a' + (TEST_PRNG(&prng) % 26);
            }
            uint8_t rbuf[CHUNK];
            lfs3_file_read(&lfs3, &file2, rbuf, CHUNK) => CHUNK;
            assert(memcmp(rbuf, wbuf, CHUNK) == 0);
        }
    }

    lfs3_file_close(&lfs3, &file) => 0;
    lfs3_file_close(&lfs3, &file2) => 0;
    lfs3_unmount(&lfs3) => 0;
'''

[cases.test_evict_t_btree_gbmap_p]
defines.CHUNK = 'BLOCK_SIZE'
defines.SIZE = ['1*CHUNK', '3*CHUNK', '20*CHUNK']
# need to disable shrubs to avoid accidental gbmap syncs
defines.SHRUB_SIZE = 0
if = 'GBMAP'
ifdef = 'LFS3_GBMAP'
code = '''
    lfs3_t lfs3;
    lfs3_format(&lfs3, LFS3_F_RDWR | LFS3_F_GBMAP, CFG) => 0;
    lfs3_mount(&lfs3, LFS3_M_RDWR, CFG) => 0;

    uint32_t prng = 42;

    // create two files
    lfs3_file_t file;
    lfs3_file_open(&lfs3, &file, "jellyfish",
            LFS3_O_RDWR | LFS3_O_CREAT | LFS3_O_EXCL) => 0;

    // write to our file
    //
    // we use a really annoying write pattern to try to fragment the gbmap
    uint32_t wprng = prng;
    lfs3_off_t wsize = 0;
    while (wsize < SIZE) {
        uint8_t wbuf[CHUNK];
        for (lfs3_size_t j = 0; j < CHUNK; j++) {
            wbuf[j] = 'a' + (TEST_PRNG(&prng) % 26);
        }
        lfs3_file_write(&lfs3, &file, wbuf, CHUNK) => CHUNK;
        lfs3_soff_t off = lfs3_file_seek(&lfs3, &file, -CHUNK, LFS3_SEEK_END);
        assert(off >= 0);
        for (lfs3_size_t j = 0; j < CHUNK; j++) {
            wbuf[j] = 'a' + (TEST_PRNG(&prng) % 26);
        }
        lfs3_file_write(&lfs3, &file, wbuf, CHUNK) => CHUNK;
        if (SYNC) {
            lfs3_file_sync(&lfs3, &file) => 0;
        }
        wsize += CHUNK;
    }

    // create an unrelated file, as a side effect this commits the gbmap
    // to disk
    lfs3_file_t file3;
    lfs3_file_open(&lfs3, &file3, "cuttlefish",
            LFS3_O_RDWR | LFS3_O_CREAT | LFS3_O_EXCL) => 0;
    lfs3_file_close(&lfs3, &file3) => 0;
    assert(lfs3.gbmap.b.blocks[0] == lfs3.gbmap.b_p.blocks[0]);

    // keep track of gbmap_p
    lfs3_btree_t b_p = lfs3.gbmap.b_p;

    // truncate our file to free up space and get rid of all the
    // fragmented gbmap ranges
    lfs3_file_truncate(&lfs3, &file, 0) => 0;
    lfs3_file_seek(&lfs3, &file, 0, LFS3_SEEK_SET) => 0;
    wprng = prng;
    wsize = 0;

    // force a lookgbmap scan, _after_ we truncate
    lfs3.gbmap.known = 0;
    lfs3.flags |= LFS3_I_LOOKAHEAD;

    // rewrite our file to force a lookgbmap scan and hopefully compact
    // the gbmap
    //
    // note _write_ triggers lookgbmap scans, but we don't commit the
    // gbmap until _sync_
    //
    // we also need to avoid any mdir commits (open+creat, shrubs, etc)!
    // littlefs is very opportunistic!
    while (lfs3.gbmap.b.blocks[0] == lfs3.gbmap.b_p.blocks[0]) {
        uint8_t wbuf[CHUNK];
        for (lfs3_size_t j = 0; j < CHUNK; j++) {
            wbuf[j] = 'a' + (TEST_PRNG(&prng) % 26);
        }
        lfs3_file_write(&lfs3, &file, wbuf, CHUNK) => CHUNK;
        wsize += CHUNK;
        // don't sync!
    }

    // at this point, gbmap and gbmap_p should differ, with gbmap
    // compacted and gbmap_p uncompacted
    assert(lfs3.gbmap.b.blocks[0] != lfs3.gbmap.b_p.blocks[0]);
    assert(lfs3.gbmap.b_p.blocks[0] == b_p.blocks[0]);

    // get a block to evict
    lfs3_block_t victim = lfs3.gbmap.b_p.blocks[0];

    // try evicting
    lfs3_fs_mkbad(&lfs3, victim, LFS3_MKBAD_EVICT) => 0;

    // evicted block should no longer be in use
    assert(lfs3.gbmap.b_p.blocks[0] != victim);

    // evicted block should no longer be in use, anywhere in the
    // filesystem
    lfs3_trv_t trv;
    lfs3_trv_open(&lfs3, &trv, LFS3_T_RDONLY) => 0;
    for (lfs3_block_t i = 0;; i++) {
        assert(i < 2*BLOCK_COUNT);

        struct lfs3_tinfo tinfo;
        int err = lfs3_trv_read(&lfs3, &trv, &tinfo);
        assert(!err || err == LFS3_ERR_NOENT);
        if (err == LFS3_ERR_NOENT) {
            break;
        }

        assert(tinfo.block != victim);
    }
    lfs3_trv_close(&lfs3, &trv) => 0;

    // gbmap_p should have been discarded, so these should be the same
    assert(lfs3.gbmap.b_p.blocks[0] == lfs3.gbmap.b.blocks[0]);

    // check we can still read the file
    lfs3_file_rewind(&lfs3, &file) => 0;
    prng = wprng;
    for (lfs3_off_t i = 0; i < wsize; i += CHUNK) {
        uint8_t wbuf[CHUNK];
        for (lfs3_size_t j = 0; j < CHUNK; j++) {
            wbuf[j] = 'a' + (TEST_PRNG(&prng) % 26);
        }
        uint8_t rbuf[CHUNK];
        lfs3_file_read(&lfs3, &file, rbuf, CHUNK) => CHUNK;
        assert(memcmp(rbuf, wbuf, CHUNK) == 0);
    }

    // try creating a second file to make sure the gbmap wasn't
    // corrupted
    lfs3_file_t file2;
    lfs3_file_open(&lfs3, &file2, "octopus",
            LFS3_O_RDWR | LFS3_O_CREAT | LFS3_O_EXCL) => 0;
    for (lfs3_off_t i = 0; i < wsize/4; i += CHUNK) {
        uint8_t wbuf[CHUNK];
        for (lfs3_size_t j = 0; j < CHUNK; j++) {
            wbuf[j] = 'a' + (TEST_PRNG(&prng) % 26);
        }
        lfs3_file_write(&lfs3, &file2, wbuf, CHUNK) => CHUNK;
    }
    lfs3_file_sync(&lfs3, &file2) => 0;

    // check we can still read the files
    for (int remount = 0; remount < 2; remount++) {
        // remount?
        if (remount) {
            lfs3_file_close(&lfs3, &file) => 0;
            lfs3_file_close(&lfs3, &file2) => 0;
            lfs3_unmount(&lfs3) => 0;
            lfs3_mount(&lfs3, LFS3_M_RDWR, CFG) => 0;
            lfs3_file_open(&lfs3, &file, "jellyfish", LFS3_O_RDONLY) => 0;
            lfs3_file_open(&lfs3, &file2, "octopus", LFS3_O_RDONLY) => 0;
        }

        lfs3_file_rewind(&lfs3, &file) => 0;
        prng = wprng;
        for (lfs3_off_t i = 0; i < wsize; i += CHUNK) {
            uint8_t wbuf[CHUNK];
            for (lfs3_size_t j = 0; j < CHUNK; j++) {
                wbuf[j] = 'a' + (TEST_PRNG(&prng) % 26);
            }
            uint8_t rbuf[CHUNK];
            lfs3_file_read(&lfs3, &file, rbuf, CHUNK) => CHUNK;
            assert(memcmp(rbuf, wbuf, CHUNK) == 0);
        }

        lfs3_file_rewind(&lfs3, &file2) => 0;
        for (lfs3_off_t i = 0; i < wsize/4; i += CHUNK) {
            uint8_t wbuf[CHUNK];
            for (lfs3_size_t j = 0; j < CHUNK; j++) {
                wbuf[j] = 'a' + (TEST_PRNG(&prng) % 26);
            }
            uint8_t rbuf[CHUNK];
            lfs3_file_read(&lfs3, &file2, rbuf, CHUNK) => CHUNK;
            assert(memcmp(rbuf, wbuf, CHUNK) == 0);
        }
    }

    lfs3_file_close(&lfs3, &file) => 0;
    lfs3_file_close(&lfs3, &file2) => 0;
    lfs3_unmount(&lfs3) => 0;
'''

[cases.test_evict_t_btree_gbmap_p_gbmap_p]
defines.CHUNK = 'BLOCK_SIZE'
defines.SIBLING = [0, 1, 2]
# need to disable shrubs to avoid accidental gbmap syncs
defines.SHRUB_SIZE = 0
if = 'GBMAP'
ifdef = 'LFS3_GBMAP'
in = 'lfs3.c'
code = '''
    lfs3_t lfs3;
    lfs3_format(&lfs3, LFS3_F_RDWR | LFS3_F_GBMAP, CFG) => 0;
    lfs3_mount(&lfs3, LFS3_M_RDWR, CFG) => 0;

    uint32_t prng = 42;

    // create two files
    lfs3_file_t file;
    lfs3_file_open(&lfs3, &file, "jellyfish",
            LFS3_O_RDWR | LFS3_O_CREAT | LFS3_O_EXCL) => 0;

    // write to our file until we have at least 3 btree nodes
    //
    // we use a really annoying write pattern to try to fragment the gbmap
    uint32_t wprng = prng;
    lfs3_off_t wsize = 0;
    while (true) {
        lfs3_size_t count = 0;
        lfs3_trv_t trv;
        lfs3_trv_open(&lfs3, &trv, LFS3_T_RDONLY) => 0;
        for (lfs3_block_t i = 0;; i++) {
            assert(i < 2*BLOCK_COUNT);

            struct lfs3_tinfo tinfo;
            int err = lfs3_trv_read(&lfs3, &trv, &tinfo);
            assert(!err || err == LFS3_ERR_NOENT);
            if (err == LFS3_ERR_NOENT) {
                break;
            }

            if (tinfo.btype == LFS3_BTYPE_BTREE
                    // bit of a hack
                    && trv.t.h.mdir.mid == LFS3_MID_GBMAP) {
                count += 1;
            }
        }
        lfs3_trv_close(&lfs3, &trv) => 0;
        if (count >= 3) {
            break;
        }

        uint8_t wbuf[CHUNK];
        for (lfs3_size_t j = 0; j < CHUNK; j++) {
            wbuf[j] = 'a' + (TEST_PRNG(&prng) % 26);
        }
        lfs3_file_write(&lfs3, &file, wbuf, CHUNK) => CHUNK;
        lfs3_soff_t off = lfs3_file_seek(&lfs3, &file, -CHUNK, LFS3_SEEK_END);
        assert(off >= 0);
        for (lfs3_size_t j = 0; j < CHUNK; j++) {
            wbuf[j] = 'a' + (TEST_PRNG(&prng) % 26);
        }
        lfs3_file_write(&lfs3, &file, wbuf, CHUNK) => CHUNK;
        if (SYNC) {
            lfs3_file_sync(&lfs3, &file) => 0;
        }
        wsize += CHUNK;
    }

    // create an unrelated file, as a side effect this commits the gbmap
    // to disk
    lfs3_file_t file3;
    lfs3_file_open(&lfs3, &file3, "cuttlefish",
            LFS3_O_RDWR | LFS3_O_CREAT | LFS3_O_EXCL) => 0;
    lfs3_file_close(&lfs3, &file3) => 0;
    assert(lfs3.gbmap.b.blocks[0] == lfs3.gbmap.b_p.blocks[0]);

    // keep track of gbmap_p
    lfs3_btree_t b_p = lfs3.gbmap.b_p;

    // truncate our file to free up space and get rid of all the
    // fragmented gbmap ranges
    lfs3_file_truncate(&lfs3, &file, 0) => 0;
    lfs3_file_seek(&lfs3, &file, 0, LFS3_SEEK_SET) => 0;
    wprng = prng;
    wsize = 0;

    // force a lookgbmap scan, _after_ we truncate
    lfs3.gbmap.known = 0;
    lfs3.flags |= LFS3_I_LOOKAHEAD;

    // rewrite our file to force a lookgbmap scan and hopefully compact
    // the gbmap
    //
    // note _write_ triggers lookgbmap scans, but we don't commit the
    // gbmap until _sync_
    //
    // we also need to avoid any mdir commits (open+creat, shrubs, etc)!
    // littlefs is very opportunistic!
    while (lfs3.gbmap.b.blocks[0] == lfs3.gbmap.b_p.blocks[0]) {
        uint8_t wbuf[CHUNK];
        for (lfs3_size_t j = 0; j < CHUNK; j++) {
            wbuf[j] = 'a' + (TEST_PRNG(&prng) % 26);
        }
        lfs3_file_write(&lfs3, &file, wbuf, CHUNK) => CHUNK;
        wsize += CHUNK;
        // don't sync!
    }

    // at this point, gbmap and gbmap_p should differ, with gbmap
    // compacted and gbmap_p uncompacted
    assert(lfs3.gbmap.b.blocks[0] != lfs3.gbmap.b_p.blocks[0]);
    assert(lfs3.gbmap.b_p.blocks[0] == b_p.blocks[0]);

    // get a block to evict
    lfs3_block_t victim = 0;
    lfs3_size_t count = 0;
    lfs3_trv_t trv;
    lfs3_trv_open(&lfs3, &trv, LFS3_T_RDONLY) => 0;
    for (lfs3_block_t i = 0;; i++) {
        assert(i < 2*BLOCK_COUNT);

        struct lfs3_tinfo tinfo;
        int err = lfs3_trv_read(&lfs3, &trv, &tinfo);
        assert(!err);

        if (tinfo.btype == LFS3_BTYPE_BTREE
                // bit of a hack
                && trv.t.h.mdir.mid == LFS3_MID_GBMAP_P) {
            if (count == SIBLING) {
                victim = tinfo.block;
                break;
            }

            count += 1;
        }
    }
    lfs3_trv_close(&lfs3, &trv) => 0;

    // try evicting
    lfs3_fs_mkbad(&lfs3, victim, LFS3_MKBAD_EVICT) => 0;

    // evicted block should no longer be in use, anywhere in the
    // filesystem
    lfs3_trv_open(&lfs3, &trv, LFS3_T_RDONLY) => 0;
    for (lfs3_block_t i = 0;; i++) {
        assert(i < 2*BLOCK_COUNT);

        struct lfs3_tinfo tinfo;
        int err = lfs3_trv_read(&lfs3, &trv, &tinfo);
        assert(!err || err == LFS3_ERR_NOENT);
        if (err == LFS3_ERR_NOENT) {
            break;
        }

        assert(tinfo.block != victim);
    }
    lfs3_trv_close(&lfs3, &trv) => 0;

    // gbmap_p should have been discarded, so these should be the same
    assert(lfs3.gbmap.b_p.blocks[0] == lfs3.gbmap.b.blocks[0]);

    // check we can still read the file
    lfs3_file_rewind(&lfs3, &file) => 0;
    prng = wprng;
    for (lfs3_off_t i = 0; i < wsize; i += CHUNK) {
        uint8_t wbuf[CHUNK];
        for (lfs3_size_t j = 0; j < CHUNK; j++) {
            wbuf[j] = 'a' + (TEST_PRNG(&prng) % 26);
        }
        uint8_t rbuf[CHUNK];
        lfs3_file_read(&lfs3, &file, rbuf, CHUNK) => CHUNK;
        assert(memcmp(rbuf, wbuf, CHUNK) == 0);
    }

    // try creating a second file to make sure the gbmap wasn't
    // corrupted
    lfs3_file_t file2;
    lfs3_file_open(&lfs3, &file2, "octopus",
            LFS3_O_RDWR | LFS3_O_CREAT | LFS3_O_EXCL) => 0;
    for (lfs3_off_t i = 0; i < wsize/4; i += CHUNK) {
        uint8_t wbuf[CHUNK];
        for (lfs3_size_t j = 0; j < CHUNK; j++) {
            wbuf[j] = 'a' + (TEST_PRNG(&prng) % 26);
        }
        lfs3_file_write(&lfs3, &file2, wbuf, CHUNK) => CHUNK;
    }
    lfs3_file_sync(&lfs3, &file2) => 0;

    // check we can still read the files
    for (int remount = 0; remount < 2; remount++) {
        // remount?
        if (remount) {
            lfs3_file_close(&lfs3, &file) => 0;
            lfs3_file_close(&lfs3, &file2) => 0;
            lfs3_unmount(&lfs3) => 0;
            lfs3_mount(&lfs3, LFS3_M_RDWR, CFG) => 0;
            lfs3_file_open(&lfs3, &file, "jellyfish", LFS3_O_RDONLY) => 0;
            lfs3_file_open(&lfs3, &file2, "octopus", LFS3_O_RDONLY) => 0;
        }

        lfs3_file_rewind(&lfs3, &file) => 0;
        prng = wprng;
        for (lfs3_off_t i = 0; i < wsize; i += CHUNK) {
            uint8_t wbuf[CHUNK];
            for (lfs3_size_t j = 0; j < CHUNK; j++) {
                wbuf[j] = 'a' + (TEST_PRNG(&prng) % 26);
            }
            uint8_t rbuf[CHUNK];
            lfs3_file_read(&lfs3, &file, rbuf, CHUNK) => CHUNK;
            assert(memcmp(rbuf, wbuf, CHUNK) == 0);
        }

        lfs3_file_rewind(&lfs3, &file2) => 0;
        for (lfs3_off_t i = 0; i < wsize/4; i += CHUNK) {
            uint8_t wbuf[CHUNK];
            for (lfs3_size_t j = 0; j < CHUNK; j++) {
                wbuf[j] = 'a' + (TEST_PRNG(&prng) % 26);
            }
            uint8_t rbuf[CHUNK];
            lfs3_file_read(&lfs3, &file2, rbuf, CHUNK) => CHUNK;
            assert(memcmp(rbuf, wbuf, CHUNK) == 0);
        }
    }

    lfs3_file_close(&lfs3, &file) => 0;
    lfs3_file_close(&lfs3, &file2) => 0;
    lfs3_unmount(&lfs3) => 0;
'''


# test traversals with bptr eviction

[cases.test_evict_t_bptr]
defines.SYNC = [false, true]
defines.CLOSE = [false, true]
code = '''
    lfs3_t lfs3;
    lfs3_format(&lfs3,
            LFS3_F_RDWR
                | ((GBMAP) ? LFS3_IFDEF_GBMAP(LFS3_F_GBMAP, -1) : 0),
            CFG) => 0;
    lfs3_mount(&lfs3, LFS3_M_RDWR, CFG) => 0;

    uint32_t prng = 42;

    // create a file
    lfs3_file_t file;
    lfs3_file_open(&lfs3, &file, "jellyfish",
            LFS3_O_RDWR | LFS3_O_CREAT | LFS3_O_EXCL) => 0;

    // write a block
    uint32_t wprng = prng;
    lfs3_off_t wsize = 0;
    uint8_t wbuf[BLOCK_SIZE];
    for (lfs3_size_t j = 0; j < BLOCK_SIZE; j++) {
        wbuf[j] = 'a' + (TEST_PRNG(&prng) % 26);
    }
    lfs3_file_write(&lfs3, &file, wbuf, BLOCK_SIZE) => BLOCK_SIZE;
    if (SYNC) {
        lfs3_file_sync(&lfs3, &file) => 0;
    }

    // close during eviction? this makes sure eviction can find the
    // btree on disk
    if (CLOSE) {
        lfs3_file_close(&lfs3, &file) => 0;
    }

    // get a block to evict
    lfs3_block_t victim = file.leaf.bptr.d.u.disk.block;

    // this block should be in use
    lfs3_trv_t trv;
    lfs3_trv_open(&lfs3, &trv, LFS3_T_RDONLY) => 0;
    for (lfs3_block_t i = 0;; i++) {
        assert(i < 2*BLOCK_COUNT);

        struct lfs3_tinfo tinfo;
        int err = lfs3_trv_read(&lfs3, &trv, &tinfo);
        assert(!err);

        if (tinfo.block == victim) {
            assert(tinfo.btype == LFS3_BTYPE_DATA);
            break;
        }
    }
    lfs3_trv_close(&lfs3, &trv) => 0;

    // try evicting
    lfs3_fs_mkbad(&lfs3, victim, LFS3_MKBAD_EVICT) => 0;

    // reopen if necessary
    if (CLOSE) {
        lfs3_file_open(&lfs3, &file, "jellyfish", LFS3_O_RDWR) => 0;
    }

    // evicted block should no longer be in use
    assert(file.leaf.bptr.d.u.disk.block != victim);

    // evicted block should no longer be in use, anywhere in the
    // filesystem
    lfs3_trv_open(&lfs3, &trv, LFS3_T_RDONLY) => 0;
    for (lfs3_block_t i = 0;; i++) {
        assert(i < 2*BLOCK_COUNT);

        struct lfs3_tinfo tinfo;
        int err = lfs3_trv_read(&lfs3, &trv, &tinfo);
        assert(!err || err == LFS3_ERR_NOENT);
        if (err == LFS3_ERR_NOENT) {
            break;
        }

        assert(tinfo.block != victim);
    }
    lfs3_trv_close(&lfs3, &trv) => 0;

    // check we can still read the file
    for (int remount = 0; remount < 2; remount++) {
        // remount?
        if (remount) {
            lfs3_file_close(&lfs3, &file) => 0;
            lfs3_unmount(&lfs3) => 0;
            lfs3_mount(&lfs3, LFS3_M_RDWR, CFG) => 0;
            lfs3_file_open(&lfs3, &file, "jellyfish", LFS3_O_RDONLY) => 0;
        }

        lfs3_file_rewind(&lfs3, &file) => 0;
        prng = wprng;
        for (lfs3_off_t i = 0; i < wsize; i += BLOCK_SIZE) {
            uint8_t wbuf[BLOCK_SIZE];
            for (lfs3_size_t j = 0; j < BLOCK_SIZE; j++) {\
                wbuf[j] = 'a' + (TEST_PRNG(&prng) % 26);
            }
            uint8_t rbuf[BLOCK_SIZE];
            lfs3_file_read(&lfs3, &file, rbuf, BLOCK_SIZE) => BLOCK_SIZE;
            assert(memcmp(rbuf, wbuf, BLOCK_SIZE) == 0);
        }
    }

    lfs3_file_close(&lfs3, &file) => 0;
    lfs3_unmount(&lfs3) => 0;
'''

[cases.test_evict_t_bptr_btree]
defines.SYNC = [false, true]
defines.CLOSE = [false, true]
# encourage a btree
defines.SHRUB_SIZE = 0
code = '''
    lfs3_t lfs3;
    lfs3_format(&lfs3,
            LFS3_F_RDWR
                | ((GBMAP) ? LFS3_IFDEF_GBMAP(LFS3_F_GBMAP, -1) : 0),
            CFG) => 0;
    lfs3_mount(&lfs3, LFS3_M_RDWR, CFG) => 0;

    uint32_t prng = 42;

    // create a file
    lfs3_file_t file;
    lfs3_file_open(&lfs3, &file, "jellyfish",
            LFS3_O_RDWR | LFS3_O_CREAT | LFS3_O_EXCL) => 0;

    // write to our file until we are a btree and have at least
    // 3 btree node
    uint32_t wprng = prng;
    lfs3_off_t wsize = 0;
    while (true) {
        if (!(file.bshrub.trunk & 0x80000000)) {
            lfs3_size_t count = 0;
            lfs3_trv_t trv;
            lfs3_trv_open(&lfs3, &trv, LFS3_T_RDONLY) => 0;
            for (lfs3_block_t i = 0;; i++) {
                assert(i < 2*BLOCK_COUNT);

                struct lfs3_tinfo tinfo;
                int err = lfs3_trv_read(&lfs3, &trv, &tinfo);
                assert(!err || err == LFS3_ERR_NOENT);
                if (err == LFS3_ERR_NOENT) {
                    break;
                }

                if (tinfo.btype == LFS3_BTYPE_BTREE) {
                    count += 1;
                }
            }
            lfs3_trv_close(&lfs3, &trv) => 0;
            if (count >= 3 + ((GBMAP) ? 1 : 0)) {
                break;
            }
        }

        uint8_t wbuf[BLOCK_SIZE];
        for (lfs3_size_t j = 0; j < BLOCK_SIZE; j++) {
            wbuf[j] = 'a' + (TEST_PRNG(&prng) % 26);
        }
        lfs3_file_write(&lfs3, &file, wbuf, BLOCK_SIZE) => BLOCK_SIZE;
        if (SYNC) {
            lfs3_file_sync(&lfs3, &file) => 0;
        }
        wsize += BLOCK_SIZE;
    }

    // close during eviction? this makes sure eviction can find the
    // btree on disk
    if (CLOSE) {
        lfs3_file_close(&lfs3, &file) => 0;
    }

    // get a block to evict
    lfs3_block_t victim = file.leaf.bptr.d.u.disk.block;

    // this block should be in use
    lfs3_trv_t trv;
    lfs3_trv_open(&lfs3, &trv, LFS3_T_RDONLY) => 0;
    for (lfs3_block_t i = 0;; i++) {
        assert(i < 2*BLOCK_COUNT);

        struct lfs3_tinfo tinfo;
        int err = lfs3_trv_read(&lfs3, &trv, &tinfo);
        assert(!err);

        if (tinfo.block == victim) {
            assert(tinfo.btype == LFS3_BTYPE_DATA);
            break;
        }
    }
    lfs3_trv_close(&lfs3, &trv) => 0;

    // try evicting
    lfs3_fs_mkbad(&lfs3, victim, LFS3_MKBAD_EVICT) => 0;

    // reopen if necessary
    if (CLOSE) {
        lfs3_file_open(&lfs3, &file, "jellyfish", LFS3_O_RDWR) => 0;
    }

    // evicted block should no longer be in use
    assert(file.leaf.bptr.d.u.disk.block != victim);

    // evicted block should no longer be in use, anywhere in the
    // filesystem
    lfs3_trv_open(&lfs3, &trv, LFS3_T_RDONLY) => 0;
    for (lfs3_block_t i = 0;; i++) {
        assert(i < 2*BLOCK_COUNT);

        struct lfs3_tinfo tinfo;
        int err = lfs3_trv_read(&lfs3, &trv, &tinfo);
        assert(!err || err == LFS3_ERR_NOENT);
        if (err == LFS3_ERR_NOENT) {
            break;
        }

        assert(tinfo.block != victim);
    }
    lfs3_trv_close(&lfs3, &trv) => 0;

    // check we can still read the file
    for (int remount = 0; remount < 2; remount++) {
        // remount?
        if (remount) {
            lfs3_file_close(&lfs3, &file) => 0;
            lfs3_unmount(&lfs3) => 0;
            lfs3_mount(&lfs3, LFS3_M_RDWR, CFG) => 0;
            lfs3_file_open(&lfs3, &file, "jellyfish", LFS3_O_RDONLY) => 0;
        }

        lfs3_file_rewind(&lfs3, &file) => 0;
        prng = wprng;
        for (lfs3_off_t i = 0; i < wsize; i += BLOCK_SIZE) {
            uint8_t wbuf[BLOCK_SIZE];
            for (lfs3_size_t j = 0; j < BLOCK_SIZE; j++) {\
                wbuf[j] = 'a' + (TEST_PRNG(&prng) % 26);
            }
            uint8_t rbuf[BLOCK_SIZE];
            lfs3_file_read(&lfs3, &file, rbuf, BLOCK_SIZE) => BLOCK_SIZE;
            assert(memcmp(rbuf, wbuf, BLOCK_SIZE) == 0);
        }
    }

    lfs3_file_close(&lfs3, &file) => 0;
    lfs3_unmount(&lfs3) => 0;
'''

[cases.test_evict_t_bptr_bshrub]
defines.SYNC = [false, true]
defines.CLOSE = [false, true]
code = '''
    lfs3_t lfs3;
    lfs3_format(&lfs3,
            LFS3_F_RDWR
                | ((GBMAP) ? LFS3_IFDEF_GBMAP(LFS3_F_GBMAP, -1) : 0),
            CFG) => 0;
    lfs3_mount(&lfs3, LFS3_M_RDWR, CFG) => 0;

    uint32_t prng = 42;

    // create a file
    lfs3_file_t file;
    lfs3_file_open(&lfs3, &file, "jellyfish",
            LFS3_O_RDWR | LFS3_O_CREAT | LFS3_O_EXCL) => 0;

    // write to our file until we are a bshrub and have at least
    // 3 btree node
    uint32_t wprng = prng;
    lfs3_off_t wsize = 0;
    while (true) {
        if (file.bshrub.trunk & 0x80000000) {
            lfs3_size_t count = 0;
            lfs3_trv_t trv;
            lfs3_trv_open(&lfs3, &trv, LFS3_T_RDONLY) => 0;
            for (lfs3_block_t i = 0;; i++) {
                assert(i < 2*BLOCK_COUNT);

                struct lfs3_tinfo tinfo;
                int err = lfs3_trv_read(&lfs3, &trv, &tinfo);
                assert(!err || err == LFS3_ERR_NOENT);
                if (err == LFS3_ERR_NOENT) {
                    break;
                }

                if (tinfo.btype == LFS3_BTYPE_BTREE) {
                    count += 1;
                }
            }
            lfs3_trv_close(&lfs3, &trv) => 0;
            if (count >= 3 + ((GBMAP) ? 1 : 0)) {
                break;
            }
        }

        uint8_t wbuf[BLOCK_SIZE];
        for (lfs3_size_t j = 0; j < BLOCK_SIZE; j++) {
            wbuf[j] = 'a' + (TEST_PRNG(&prng) % 26);
        }
        lfs3_file_write(&lfs3, &file, wbuf, BLOCK_SIZE) => BLOCK_SIZE;
        if (SYNC) {
            lfs3_file_sync(&lfs3, &file) => 0;
        }
        wsize += BLOCK_SIZE;
    }

    // close during eviction? this makes sure eviction can find the
    // btree on disk
    if (CLOSE) {
        lfs3_file_close(&lfs3, &file) => 0;
    }

    // get a block to evict
    lfs3_block_t victim = file.leaf.bptr.d.u.disk.block;

    // this block should be in use
    lfs3_trv_t trv;
    lfs3_trv_open(&lfs3, &trv, LFS3_T_RDONLY) => 0;
    for (lfs3_block_t i = 0;; i++) {
        assert(i < 2*BLOCK_COUNT);

        struct lfs3_tinfo tinfo;
        int err = lfs3_trv_read(&lfs3, &trv, &tinfo);
        assert(!err);

        if (tinfo.block == victim) {
            assert(tinfo.btype == LFS3_BTYPE_DATA);
            break;
        }
    }
    lfs3_trv_close(&lfs3, &trv) => 0;

    // try evicting
    lfs3_fs_mkbad(&lfs3, victim, LFS3_MKBAD_EVICT) => 0;

    // reopen if necessary
    if (CLOSE) {
        lfs3_file_open(&lfs3, &file, "jellyfish", LFS3_O_RDWR) => 0;
    }

    // evicted block should no longer be in use
    assert(file.leaf.bptr.d.u.disk.block != victim);

    // evicted block should no longer be in use, anywhere in the
    // filesystem
    lfs3_trv_open(&lfs3, &trv, LFS3_T_RDONLY) => 0;
    for (lfs3_block_t i = 0;; i++) {
        assert(i < 2*BLOCK_COUNT);

        struct lfs3_tinfo tinfo;
        int err = lfs3_trv_read(&lfs3, &trv, &tinfo);
        assert(!err || err == LFS3_ERR_NOENT);
        if (err == LFS3_ERR_NOENT) {
            break;
        }

        assert(tinfo.block != victim);
    }
    lfs3_trv_close(&lfs3, &trv) => 0;

    // check we can still read the file
    for (int remount = 0; remount < 2; remount++) {
        // remount?
        if (remount) {
            lfs3_file_close(&lfs3, &file) => 0;
            lfs3_unmount(&lfs3) => 0;
            lfs3_mount(&lfs3, LFS3_M_RDWR, CFG) => 0;
            lfs3_file_open(&lfs3, &file, "jellyfish", LFS3_O_RDONLY) => 0;
        }

        lfs3_file_rewind(&lfs3, &file) => 0;
        prng = wprng;
        for (lfs3_off_t i = 0; i < wsize; i += BLOCK_SIZE) {
            uint8_t wbuf[BLOCK_SIZE];
            for (lfs3_size_t j = 0; j < BLOCK_SIZE; j++) {\
                wbuf[j] = 'a' + (TEST_PRNG(&prng) % 26);
            }
            uint8_t rbuf[BLOCK_SIZE];
            lfs3_file_read(&lfs3, &file, rbuf, BLOCK_SIZE) => BLOCK_SIZE;
            assert(memcmp(rbuf, wbuf, BLOCK_SIZE) == 0);
        }
    }

    lfs3_file_close(&lfs3, &file) => 0;
    lfs3_unmount(&lfs3) => 0;
'''

# test evicting a dag block
[cases.test_evict_t_bptr_dag]
defines.SYNC = [false, true]
defines.CLOSE = [false, true]
code = '''
    lfs3_t lfs3;
    lfs3_format(&lfs3,
            LFS3_F_RDWR
                | ((GBMAP) ? LFS3_IFDEF_GBMAP(LFS3_F_GBMAP, -1) : 0),
            CFG) => 0;
    lfs3_mount(&lfs3, LFS3_M_RDWR, CFG) => 0;

    uint32_t prng = 42;

    // create a file
    lfs3_file_t file;
    lfs3_file_open(&lfs3, &file, "jellyfish",
            LFS3_O_RDWR | LFS3_O_CREAT | LFS3_O_EXCL) => 0;

    // write a block
    uint8_t wbuf[BLOCK_SIZE];
    for (lfs3_size_t j = 0; j < BLOCK_SIZE; j++) {
        wbuf[j] = 'a' + (TEST_PRNG(&prng) % 26);
    }
    lfs3_file_write(&lfs3, &file, wbuf, BLOCK_SIZE) => BLOCK_SIZE;
    lfs3_file_flush(&lfs3, &file) => 0;
    // write a single byte in the middle of the block, forcing a dag
    wbuf[BLOCK_SIZE/2] = '!';
    lfs3_file_seek(&lfs3, &file, BLOCK_SIZE/2, LFS3_SEEK_SET)
            => BLOCK_SIZE/2;
    lfs3_file_write(&lfs3, &file, &wbuf[BLOCK_SIZE/2], 1) => 1;
    lfs3_file_flush(&lfs3, &file) => 0;
    // sync?
    if (SYNC) {
        lfs3_file_sync(&lfs3, &file) => 0;
    }

    // read to get the bptr back in our leaf
    uint8_t rbuf[BLOCK_SIZE];
    lfs3_file_seek(&lfs3, &file, -1, LFS3_SEEK_END) => BLOCK_SIZE-1;
    lfs3_file_read(&lfs3, &file, rbuf, 1) => 1;

    // close during eviction? this makes sure eviction can find the
    // btree on disk
    if (CLOSE) {
        lfs3_file_close(&lfs3, &file) => 0;
    }

    // get a block to evict
    lfs3_block_t victim = file.leaf.bptr.d.u.disk.block;

    // this block should be in use, twice! because it's a dag
    lfs3_trv_t trv;
    lfs3_size_t count = 0;
    lfs3_trv_open(&lfs3, &trv, LFS3_T_RDONLY) => 0;
    for (lfs3_block_t i = 0;; i++) {
        assert(i < 2*BLOCK_COUNT);

        struct lfs3_tinfo tinfo;
        int err = lfs3_trv_read(&lfs3, &trv, &tinfo);
        assert(!err || err == LFS3_ERR_NOENT);
        if (err == LFS3_ERR_NOENT) {
            break;
        }

        if (tinfo.btype == LFS3_BTYPE_DATA) {
            assert(tinfo.block == victim);
            count += 1;
        }
    }
    lfs3_trv_close(&lfs3, &trv) => 0;
    assert(count == 2);

    // try evicting
    lfs3_fs_mkbad(&lfs3, victim, LFS3_MKBAD_EVICT) => 0;

    // reopen if necessary
    if (CLOSE) {
        lfs3_file_open(&lfs3, &file, "jellyfish", LFS3_O_RDWR) => 0;
    }

    // evicted block should no longer be in use
    assert(file.leaf.bptr.d.u.disk.block != victim);

    // evicted block should no longer be in use, anywhere in the
    // filesystem
    lfs3_trv_open(&lfs3, &trv, LFS3_T_RDONLY) => 0;
    for (lfs3_block_t i = 0;; i++) {
        assert(i < 2*BLOCK_COUNT);

        struct lfs3_tinfo tinfo;
        int err = lfs3_trv_read(&lfs3, &trv, &tinfo);
        assert(!err || err == LFS3_ERR_NOENT);
        if (err == LFS3_ERR_NOENT) {
            break;
        }

        assert(tinfo.block != victim);
    }
    lfs3_trv_close(&lfs3, &trv) => 0;

    // but we should still only have one block in use, twice, because
    // it's a dag
    lfs3_block_t block = 0;
    count = 0;
    lfs3_trv_open(&lfs3, &trv, LFS3_T_RDONLY) => 0;
    for (lfs3_block_t i = 0;; i++) {
        assert(i < 2*BLOCK_COUNT);

        struct lfs3_tinfo tinfo;
        int err = lfs3_trv_read(&lfs3, &trv, &tinfo);
        assert(!err || err == LFS3_ERR_NOENT);
        if (err == LFS3_ERR_NOENT) {
            break;
        }

        if (tinfo.btype == LFS3_BTYPE_DATA) {
            assert(!block || tinfo.block == block);
            count += 1;
        }
    }
    lfs3_trv_close(&lfs3, &trv) => 0;
    assert(count == 2);

    // check we can still read the file
    for (int remount = 0; remount < 2; remount++) {
        // remount?
        if (remount) {
            lfs3_file_close(&lfs3, &file) => 0;
            lfs3_unmount(&lfs3) => 0;
            lfs3_mount(&lfs3, LFS3_M_RDWR, CFG) => 0;
            lfs3_file_open(&lfs3, &file, "jellyfish", LFS3_O_RDONLY) => 0;
        }

        lfs3_file_rewind(&lfs3, &file) => 0;
        lfs3_file_read(&lfs3, &file, rbuf, BLOCK_SIZE) => BLOCK_SIZE;
        assert(memcmp(rbuf, wbuf, BLOCK_SIZE) == 0);
    }

    lfs3_file_close(&lfs3, &file) => 0;
    lfs3_unmount(&lfs3) => 0;
'''

# test evicting a partially erased block
#
# Well, we can't use erased state in evicted blocks, but we shouldn't
# explode or anything
#
[cases.test_evict_t_bptr_erased]
defines.SYNC = [false, true]
# is half a block prog aligned?
if = '(BLOCK_SIZE/2) % PROG_SIZE == 0'
in = 'lfs3.c'
code = '''
    lfs3_t lfs3;
    lfs3_format(&lfs3,
            LFS3_F_RDWR
                | ((GBMAP) ? LFS3_IFDEF_GBMAP(LFS3_F_GBMAP, -1) : 0),
            CFG) => 0;
    lfs3_mount(&lfs3, LFS3_M_RDWR, CFG) => 0;

    uint32_t prng = 42;

    // create a file
    lfs3_file_t file;
    lfs3_file_open(&lfs3, &file, "jellyfish",
            LFS3_O_RDWR | LFS3_O_CREAT | LFS3_O_EXCL) => 0;

    // write half a block
    uint8_t wbuf[BLOCK_SIZE];
    for (lfs3_size_t j = 0; j < BLOCK_SIZE/2; j++) {
        wbuf[j] = 'a' + (TEST_PRNG(&prng) % 26);
    }
    lfs3_file_write(&lfs3, &file, wbuf, BLOCK_SIZE/2) => BLOCK_SIZE/2;
    lfs3_file_flush(&lfs3, &file) => 0;
    // sync?
    if (SYNC) {
        lfs3_file_sync(&lfs3, &file) => 0;
    }

    // get a block to evict
    lfs3_block_t victim = file.leaf.bptr.d.u.disk.block;

    // this block should be in use
    lfs3_trv_t trv;
    lfs3_trv_open(&lfs3, &trv, LFS3_T_RDONLY) => 0;
    for (lfs3_block_t i = 0;; i++) {
        assert(i < 2*BLOCK_COUNT);

        struct lfs3_tinfo tinfo;
        int err = lfs3_trv_read(&lfs3, &trv, &tinfo);
        assert(!err);

        if (tinfo.block == victim) {
            assert(tinfo.btype == LFS3_BTYPE_DATA);
            break;
        }
    }
    lfs3_trv_close(&lfs3, &trv) => 0;

    // the block should be marked as erased
    assert(lfs3_bptr_iserased(&file.leaf.bptr));

    // try evicting
    lfs3_fs_mkbad(&lfs3, victim, LFS3_MKBAD_EVICT) => 0;

    // evicted block should no longer be in use
    assert(file.leaf.bptr.d.u.disk.block != victim);

    // evicted block should no longer be in use, anywhere in the
    // filesystem
    lfs3_trv_open(&lfs3, &trv, LFS3_T_RDONLY) => 0;
    for (lfs3_block_t i = 0;; i++) {
        assert(i < 2*BLOCK_COUNT);

        struct lfs3_tinfo tinfo;
        int err = lfs3_trv_read(&lfs3, &trv, &tinfo);
        assert(!err || err == LFS3_ERR_NOENT);
        if (err == LFS3_ERR_NOENT) {
            break;
        }

        assert(tinfo.block != victim);
    }
    lfs3_trv_close(&lfs3, &trv) => 0;

    // we should no longer be marked as erased
    assert(file.leaf.bptr.d.u.disk.block == 0
            || !lfs3_bptr_iserased(&file.leaf.bptr));

    // check we can still read the file
    uint8_t rbuf[BLOCK_SIZE];
    lfs3_file_rewind(&lfs3, &file) => 0;
    lfs3_file_read(&lfs3, &file, rbuf, BLOCK_SIZE) => BLOCK_SIZE/2;
    assert(memcmp(rbuf, wbuf, BLOCK_SIZE/2) == 0);

    // finish writing the block to try to make sure nothing is funky
    for (lfs3_size_t j = 0; j < BLOCK_SIZE/2; j++) {
        wbuf[(BLOCK_SIZE/2)+j] = 'a' + (TEST_PRNG(&prng) % 26);
    }
    lfs3_file_write(&lfs3, &file,
            &wbuf[BLOCK_SIZE/2], BLOCK_SIZE/2) => BLOCK_SIZE/2;
    lfs3_file_flush(&lfs3, &file) => 0;
    // sync?
    if (SYNC) {
        lfs3_file_sync(&lfs3, &file) => 0;
    }

    // check we can still read the file
    for (int remount = 0; remount < 2; remount++) {
        // remount?
        if (remount) {
            lfs3_file_close(&lfs3, &file) => 0;
            lfs3_unmount(&lfs3) => 0;
            lfs3_mount(&lfs3, LFS3_M_RDWR, CFG) => 0;
            lfs3_file_open(&lfs3, &file, "jellyfish", LFS3_O_RDONLY) => 0;
        }

        lfs3_file_rewind(&lfs3, &file) => 0;
        lfs3_file_read(&lfs3, &file, rbuf, BLOCK_SIZE) => BLOCK_SIZE;
        assert(memcmp(rbuf, wbuf, BLOCK_SIZE) == 0);
    }

    lfs3_file_close(&lfs3, &file) => 0;
    lfs3_unmount(&lfs3) => 0;
'''

# test bshrubs with evicted bptrs are broadcast correctly
[cases.test_evict_t_bptr_broadcast]
defines.SIZE = [
    '1*BLOCK_SIZE', '2*BLOCK_SIZE', '3*BLOCK_SIZE', '4*BLOCK_SIZE'
]
defines.HANDLESET = 'range(0x8)'
defines.SIBLING = [0, 1, 2, 3]
if = 'SIBLING < SIZE/BLOCK_SIZE'
code = '''
    lfs3_t lfs3;
    lfs3_format(&lfs3,
            LFS3_F_RDWR
                | ((GBMAP) ? LFS3_IFDEF_GBMAP(LFS3_F_GBMAP, -1) : 0),
            CFG) => 0;
    lfs3_mount(&lfs3, LFS3_M_RDWR, CFG) => 0;

    uint32_t prng = 42;

    // create a file
    lfs3_file_t file;
    lfs3_file_open(&lfs3, &file, "jellyfish",
            LFS3_O_RDWR | LFS3_O_CREAT | LFS3_O_EXCL) => 0;

    // write n blocks
    uint32_t wprng = prng;
    lfs3_off_t wsize = 0;
    while (wsize < SIZE) {
        uint8_t wbuf[BLOCK_SIZE];
        for (lfs3_size_t j = 0; j < BLOCK_SIZE; j++) {
            wbuf[j] = 'a' + (TEST_PRNG(&prng) % 26);
        }
        lfs3_file_write(&lfs3, &file, wbuf, BLOCK_SIZE) => BLOCK_SIZE;
        lfs3_file_sync(&lfs3, &file) => 0;
        wsize += BLOCK_SIZE;
    }

    // open a couple other handles in different states
    lfs3_file_t file_mutated;
    if (HANDLESET & 0x1) {
        lfs3_file_open(&lfs3, &file_mutated, "jellyfish", LFS3_O_RDWR) => 0;
        lfs3_file_write(&lfs3, &file_mutated, "!", 1) => 1;
        lfs3_file_flush(&lfs3, &file_mutated) => 0;
    }

    lfs3_file_t file_desync;
    if (HANDLESET & 0x2) {
        lfs3_file_open(&lfs3, &file_desync, "jellyfish",
                LFS3_O_RDWR | LFS3_O_DESYNC) => 0;
    }

    lfs3_file_t file_desync_mutated;
    if (HANDLESET & 0x4) {
        lfs3_file_open(&lfs3, &file_desync_mutated, "jellyfish",
                LFS3_O_RDWR | LFS3_O_DESYNC) => 0;
        lfs3_file_write(&lfs3, &file_desync_mutated, "!", 1) => 1;
        lfs3_file_flush(&lfs3, &file_desync_mutated) => 0;
    }

    // get a block to evict
    lfs3_block_t victim = 0;
    lfs3_size_t count = 0;
    lfs3_trv_t trv;
    lfs3_trv_open(&lfs3, &trv, LFS3_T_RDONLY) => 0;
    for (lfs3_block_t i = 0;; i++) {
        assert(i < 2*BLOCK_COUNT);

        struct lfs3_tinfo tinfo;
        int err = lfs3_trv_read(&lfs3, &trv, &tinfo);
        assert(!err);

        if (tinfo.btype == LFS3_BTYPE_DATA) {
            if (count == SIBLING) {
                victim = tinfo.block;
                break;
            }

            count += 1;
        }
    }
    lfs3_trv_close(&lfs3, &trv) => 0;

    // try evicting
    lfs3_fs_mkbad(&lfs3, victim, LFS3_MKBAD_EVICT) => 0;

    // evicted block should no longer be in use, anywhere in the
    // filesystem
    lfs3_trv_open(&lfs3, &trv, LFS3_T_RDONLY) => 0;
    for (lfs3_block_t i = 0;; i++) {
        assert(i < 2*BLOCK_COUNT);

        struct lfs3_tinfo tinfo;
        int err = lfs3_trv_read(&lfs3, &trv, &tinfo);
        assert(!err || err == LFS3_ERR_NOENT);
        if (err == LFS3_ERR_NOENT) {
            break;
        }

        assert(tinfo.block != victim);
    }
    lfs3_trv_close(&lfs3, &trv) => 0;

    if (HANDLESET & 0x1) {
        // file_mutated should still be a different bshrub
        assert(file_mutated.bshrub.blocks[0]
                    != file.bshrub.blocks[0]
                || (file_mutated.bshrub.trunk & 0x7fffffff)
                    != (file.bshrub.trunk & 0x7fffffff));
    }

    if (HANDLESET & 0x2) {
        // file_desync should be the same bshrub, yes we ignore desync
        // here!
        assert(file_desync.bshrub.blocks[0]
                    == file.bshrub.blocks[0]
                && (file_desync.bshrub.trunk & 0x7fffffff)
                    == (file.bshrub.trunk & 0x7fffffff));
    }

    if (HANDLESET & 0x4) {
        // file_desync_mutated should still be a different bshrub
        assert(file_desync_mutated.bshrub.blocks[0]
                    != file.bshrub.blocks[0]
                || (file_desync_mutated.bshrub.trunk & 0x7fffffff)
                    != (file.bshrub.trunk & 0x7fffffff));
    }

    // check we can still read the files
    lfs3_file_rewind(&lfs3, &file) => 0;
    prng = wprng;
    for (lfs3_off_t i = 0; i < wsize; i += BLOCK_SIZE) {
        uint8_t wbuf[BLOCK_SIZE];
        for (lfs3_size_t j = 0; j < BLOCK_SIZE; j++) {
            wbuf[j] = 'a' + (TEST_PRNG(&prng) % 26);
        }
        uint8_t rbuf[BLOCK_SIZE];
        lfs3_file_read(&lfs3, &file,
                rbuf, BLOCK_SIZE) => BLOCK_SIZE;
        assert(memcmp(rbuf, wbuf, BLOCK_SIZE) == 0);
    }

    if (HANDLESET & 0x1) {
        lfs3_file_rewind(&lfs3, &file_mutated) => 0;
        prng = wprng;
        for (lfs3_off_t i = 0; i < wsize; i += BLOCK_SIZE) {
            uint8_t wbuf[BLOCK_SIZE];
            for (lfs3_size_t j = 0; j < BLOCK_SIZE; j++) {
                wbuf[j] = 'a' + (TEST_PRNG(&prng) % 26);
            }
            if (i == 0) {
                wbuf[0] = '!';
            }
            uint8_t rbuf[BLOCK_SIZE];
            lfs3_file_read(&lfs3, &file_mutated,
                    rbuf, BLOCK_SIZE) => BLOCK_SIZE;
            assert(memcmp(rbuf, wbuf, BLOCK_SIZE) == 0);
        }
    }

    if (HANDLESET & 0x2) {
        lfs3_file_rewind(&lfs3, &file_desync) => 0;
        prng = wprng;
        for (lfs3_off_t i = 0; i < wsize; i += BLOCK_SIZE) {
            uint8_t wbuf[BLOCK_SIZE];
            for (lfs3_size_t j = 0; j < BLOCK_SIZE; j++) {
                wbuf[j] = 'a' + (TEST_PRNG(&prng) % 26);
            }
            uint8_t rbuf[BLOCK_SIZE];
            lfs3_file_read(&lfs3, &file_desync,
                    rbuf, BLOCK_SIZE) => BLOCK_SIZE;
            assert(memcmp(rbuf, wbuf, BLOCK_SIZE) == 0);
        }
    }

    if (HANDLESET & 0x4) {
        lfs3_file_rewind(&lfs3, &file_desync_mutated) => 0;
        prng = wprng;
        for (lfs3_off_t i = 0; i < wsize; i += BLOCK_SIZE) {
            uint8_t wbuf[BLOCK_SIZE];
            for (lfs3_size_t j = 0; j < BLOCK_SIZE; j++) {
                wbuf[j] = 'a' + (TEST_PRNG(&prng) % 26);
            }
            if (i == 0) {
                wbuf[0] = '!';
            }
            uint8_t rbuf[BLOCK_SIZE];
            lfs3_file_read(&lfs3, &file_desync_mutated,
                    rbuf, BLOCK_SIZE) => BLOCK_SIZE;
            assert(memcmp(rbuf, wbuf, BLOCK_SIZE) == 0);
        }
    }

    lfs3_file_close(&lfs3, &file) => 0;
    if (HANDLESET & 0x1) {
        lfs3_file_desync(&lfs3, &file_mutated) => 0;
        lfs3_file_close(&lfs3, &file_mutated) => 0;
    }
    if (HANDLESET & 0x2) {
        lfs3_file_close(&lfs3, &file_desync) => 0;
    }
    if (HANDLESET & 0x4) {
        lfs3_file_close(&lfs3, &file_desync_mutated) => 0;
    }
    lfs3_unmount(&lfs3) => 0;
'''

# test bptrs are updated correctly, even when referenced by unrelated
# btrees
[cases.test_evict_t_bptr_shared]
defines.SIZE = [
    '1*BLOCK_SIZE', '2*BLOCK_SIZE', '3*BLOCK_SIZE', '4*BLOCK_SIZE'
]
defines.N = 4
defines.DELTA = [
    '+2*BLOCK_SIZE', '+1*BLOCK_SIZE', '-1*BLOCK_SIZE', '-2*BLOCK_SIZE'
]
defines.DESYNC = [false, true]
defines.SIBLING = [0, 1, 2, 3]
if = 'SIBLING < SIZE/BLOCK_SIZE'
code = '''
    lfs3_t lfs3;
    lfs3_format(&lfs3,
            LFS3_F_RDWR
                | ((GBMAP) ? LFS3_IFDEF_GBMAP(LFS3_F_GBMAP, -1) : 0),
            CFG) => 0;
    lfs3_mount(&lfs3, LFS3_M_RDWR, CFG) => 0;

    uint32_t prng = 42;

    // create a file
    lfs3_file_t files[N];
    lfs3_file_open(&lfs3, &files[0], "jellyfish",
            LFS3_O_RDWR | LFS3_O_CREAT | LFS3_O_EXCL) => 0;

    // write n blocks
    uint32_t wprng = prng;
    lfs3_off_t wsize = 0;
    while (wsize < SIZE) {
        uint8_t wbuf[BLOCK_SIZE];
        for (lfs3_size_t j = 0; j < BLOCK_SIZE; j++) {
            wbuf[j] = 'a' + (TEST_PRNG(&prng) % 26);
        }
        lfs3_file_write(&lfs3, &files[0], wbuf, BLOCK_SIZE) => BLOCK_SIZE;
        lfs3_file_sync(&lfs3, &files[0]) => 0;
        wsize += BLOCK_SIZE;
    }

    // open N-1 other handles
    for (lfs3_size_t k = 1; k < N; k++) {
        lfs3_file_open(&lfs3, &files[k], "jellyfish", LFS3_O_RDWR) => 0;

        // use DELTA to derive a somewhat chaotic size for each file,
        // limited to at least BLOCK_SIZE
        lfs3_off_t wsize_ = BLOCK_SIZE + DELTA*k;
        while ((lfs3_soff_t)wsize_ < (lfs3_soff_t)BLOCK_SIZE) {
            wsize_ += BLOCK_SIZE;
        }
        // either truncate, or continue writing, note this should keep
        // any overlapping blocks
        if (wsize_ < wsize) {
            lfs3_file_truncate(&lfs3, &files[k], wsize_) => 0;
        } else {
            prng = wprng;
            for (lfs3_size_t i = 0; i < wsize; i += 1) {
                TEST_PRNG(&prng);
            }
            lfs3_file_seek(&lfs3, &files[k], 0, LFS3_SEEK_END) => wsize;
            for (lfs3_size_t i = wsize; i < wsize_; i += BLOCK_SIZE) {
                uint8_t wbuf[BLOCK_SIZE];
                for (lfs3_size_t j = 0; j < BLOCK_SIZE; j++) {
                    wbuf[j] = 'a' + (TEST_PRNG(&prng) % 26);
                }
                lfs3_file_write(&lfs3, &files[k],
                        wbuf, BLOCK_SIZE) => BLOCK_SIZE;
            }
            lfs3_file_flush(&lfs3, &files[k]) => 0;
            // block evictions ignore desync, which risks complicating
            // things
            if (DESYNC) {
                lfs3_file_desync(&lfs3, &files[k]) => 0;
            }
        }
    }

    // get a block to evict
    lfs3_block_t victim = 0;
    lfs3_size_t count = 0;
    lfs3_trv_t trv;
    lfs3_trv_open(&lfs3, &trv, LFS3_T_RDONLY) => 0;
    for (lfs3_block_t i = 0;; i++) {
        assert(i < 2*BLOCK_COUNT);

        struct lfs3_tinfo tinfo;
        int err = lfs3_trv_read(&lfs3, &trv, &tinfo);
        assert(!err);

        if (tinfo.btype == LFS3_BTYPE_DATA) {
            if (count == SIBLING) {
                victim = tinfo.block;
                break;
            }

            count += 1;
        }
    }
    lfs3_trv_close(&lfs3, &trv) => 0;

    // try evicting
    lfs3_fs_mkbad(&lfs3, victim, LFS3_MKBAD_EVICT) => 0;

    // evicted block should no longer be in use, anywhere in the
    // filesystem
    lfs3_trv_open(&lfs3, &trv, LFS3_T_RDONLY) => 0;
    for (lfs3_block_t i = 0;; i++) {
        assert(i < 2*BLOCK_COUNT);

        struct lfs3_tinfo tinfo;
        int err = lfs3_trv_read(&lfs3, &trv, &tinfo);
        assert(!err || err == LFS3_ERR_NOENT);
        if (err == LFS3_ERR_NOENT) {
            break;
        }

        assert(tinfo.block != victim);
    }
    lfs3_trv_close(&lfs3, &trv) => 0;

    // check we can still read the files
    lfs3_file_rewind(&lfs3, &files[0]) => 0;
    prng = wprng;
    for (lfs3_off_t i = 0; i < wsize; i += BLOCK_SIZE) {
        uint8_t wbuf[BLOCK_SIZE];
        for (lfs3_size_t j = 0; j < BLOCK_SIZE; j++) {
            wbuf[j] = 'a' + (TEST_PRNG(&prng) % 26);
        }
        uint8_t rbuf[BLOCK_SIZE];
        lfs3_file_read(&lfs3, &files[0],
                rbuf, BLOCK_SIZE) => BLOCK_SIZE;
        assert(memcmp(rbuf, wbuf, BLOCK_SIZE) == 0);
    }

    for (lfs3_size_t k = 1; k < N; k++) {
        lfs3_off_t wsize_ = BLOCK_SIZE + DELTA*k;
        while ((lfs3_soff_t)wsize_ < (lfs3_soff_t)BLOCK_SIZE) {
            wsize_ += BLOCK_SIZE;
        }
        lfs3_file_rewind(&lfs3, &files[k]) => 0;
        prng = wprng;
        for (lfs3_off_t i = 0; i < wsize_; i += BLOCK_SIZE) {
            uint8_t wbuf[BLOCK_SIZE];
            for (lfs3_size_t j = 0; j < BLOCK_SIZE; j++) {
                wbuf[j] = 'a' + (TEST_PRNG(&prng) % 26);
            }
            uint8_t rbuf[BLOCK_SIZE];
            lfs3_file_read(&lfs3, &files[k],
                    rbuf, BLOCK_SIZE) => BLOCK_SIZE;
            assert(memcmp(rbuf, wbuf, BLOCK_SIZE) == 0);
        }
    }

    // try writing an additional block to all files, to make sure
    // things still work
    prng = wprng;
    for (lfs3_size_t i = 0; i < wsize; i += 1) {
        TEST_PRNG(&prng);
    }
    lfs3_file_seek(&lfs3, &files[0], 0, LFS3_SEEK_END) => wsize;
    uint8_t wbuf[BLOCK_SIZE];
    for (lfs3_size_t j = 0; j < BLOCK_SIZE; j++) {
        wbuf[j] = 'a' + (TEST_PRNG(&prng) % 26);
    }
    lfs3_file_write(&lfs3, &files[0],
            wbuf, BLOCK_SIZE) => BLOCK_SIZE;
    lfs3_file_flush(&lfs3, &files[0]) => 0;
    
    for (lfs3_size_t k = 1; k < N; k++) {
        lfs3_off_t wsize_ = BLOCK_SIZE + DELTA*k;
        while ((lfs3_soff_t)wsize_ < (lfs3_soff_t)BLOCK_SIZE) {
            wsize_ += BLOCK_SIZE;
        }
        prng = wprng;
        for (lfs3_size_t i = 0; i < wsize_; i += 1) {
            TEST_PRNG(&prng);
        }
        lfs3_file_seek(&lfs3, &files[k], 0, LFS3_SEEK_END) => wsize_;
        uint8_t wbuf[BLOCK_SIZE];
        for (lfs3_size_t j = 0; j < BLOCK_SIZE; j++) {
            wbuf[j] = 'a' + (TEST_PRNG(&prng) % 26);
        }
        lfs3_file_write(&lfs3, &files[k],
                wbuf, BLOCK_SIZE) => BLOCK_SIZE;
        lfs3_file_flush(&lfs3, &files[k]) => 0;
    }

    // check we can still read the files
    lfs3_file_rewind(&lfs3, &files[0]) => 0;
    prng = wprng;
    for (lfs3_off_t i = 0; i < wsize+BLOCK_SIZE; i += BLOCK_SIZE) {
        uint8_t wbuf[BLOCK_SIZE];
        for (lfs3_size_t j = 0; j < BLOCK_SIZE; j++) {
            wbuf[j] = 'a' + (TEST_PRNG(&prng) % 26);
        }
        uint8_t rbuf[BLOCK_SIZE];
        lfs3_file_read(&lfs3, &files[0],
                rbuf, BLOCK_SIZE) => BLOCK_SIZE;
        assert(memcmp(rbuf, wbuf, BLOCK_SIZE) == 0);
    }

    for (lfs3_size_t k = 1; k < N; k++) {
        lfs3_off_t wsize_ = BLOCK_SIZE + DELTA*k;
        while ((lfs3_soff_t)wsize_ < (lfs3_soff_t)BLOCK_SIZE) {
            wsize_ += BLOCK_SIZE;
        }
        lfs3_file_rewind(&lfs3, &files[k]) => 0;
        prng = wprng;
        for (lfs3_off_t i = 0; i < wsize_+BLOCK_SIZE; i += BLOCK_SIZE) {
            uint8_t wbuf[BLOCK_SIZE];
            for (lfs3_size_t j = 0; j < BLOCK_SIZE; j++) {
                wbuf[j] = 'a' + (TEST_PRNG(&prng) % 26);
            }
            uint8_t rbuf[BLOCK_SIZE];
            lfs3_file_read(&lfs3, &files[k],
                    rbuf, BLOCK_SIZE) => BLOCK_SIZE;
            assert(memcmp(rbuf, wbuf, BLOCK_SIZE) == 0);
        }
    }

    lfs3_file_close(&lfs3, &files[0]) => 0;
    for (lfs3_size_t k = 1; k < N; k++) {
        lfs3_file_desync(&lfs3, &files[k]) => 0;
        lfs3_file_close(&lfs3, &files[k]) => 0;
    }
    lfs3_unmount(&lfs3) => 0;
'''

# test bptrs are updated correctly, even when referenced by unrelated
# btrees, with various levels of erased-state
[cases.test_evict_t_bptr_shared_erased]
defines.CHUNK = 'BLOCK_SIZE/4'
defines.SIZE = ['1*CHUNK', '2*CHUNK', '3*CHUNK', '4*CHUNK']
defines.N = 4
defines.DELTA = ['+2*CHUNK', '+1*CHUNK', '-1*CHUNK', '-2*CHUNK']
defines.DESYNC = [false, true]
defines.SIBLING = [0]
# is a chunk prog aligned?
if = 'CHUNK % PROG_SIZE == 0'
code = '''
    lfs3_t lfs3;
    lfs3_format(&lfs3,
            LFS3_F_RDWR
                | ((GBMAP) ? LFS3_IFDEF_GBMAP(LFS3_F_GBMAP, -1) : 0),
            CFG) => 0;
    lfs3_mount(&lfs3, LFS3_M_RDWR, CFG) => 0;

    uint32_t prng = 42;

    // create a file
    lfs3_file_t files[N];
    lfs3_file_open(&lfs3, &files[0], "jellyfish",
            LFS3_O_RDWR | LFS3_O_CREAT | LFS3_O_EXCL) => 0;

    // write n blocks
    uint32_t wprng = prng;
    lfs3_off_t wsize = 0;
    while (wsize < SIZE) {
        uint8_t wbuf[CHUNK];
        for (lfs3_size_t j = 0; j < CHUNK; j++) {
            wbuf[j] = 'a' + (TEST_PRNG(&prng) % 26);
        }
        lfs3_file_write(&lfs3, &files[0], wbuf, CHUNK) => CHUNK;
        lfs3_file_sync(&lfs3, &files[0]) => 0;
        wsize += CHUNK;
    }

    // open N-1 other handles
    for (lfs3_size_t k = 1; k < N; k++) {
        lfs3_file_open(&lfs3, &files[k], "jellyfish", LFS3_O_RDWR) => 0;

        // use DELTA to derive a somewhat chaotic size for each file,
        // limited to at least CHUNK
        lfs3_off_t wsize_ = CHUNK + DELTA*k;
        while ((lfs3_soff_t)wsize_ < (lfs3_soff_t)CHUNK) {
            wsize_ += CHUNK;
        }
        // either truncate, or continue writing, note this should keep
        // any overlapping blocks
        if (wsize_ < wsize) {
            lfs3_file_truncate(&lfs3, &files[k], wsize_) => 0;
        } else {
            prng = wprng;
            for (lfs3_size_t i = 0; i < wsize; i += 1) {
                TEST_PRNG(&prng);
            }
            lfs3_file_seek(&lfs3, &files[k], 0, LFS3_SEEK_END) => wsize;
            for (lfs3_size_t i = wsize; i < wsize_; i += CHUNK) {
                uint8_t wbuf[CHUNK];
                for (lfs3_size_t j = 0; j < CHUNK; j++) {
                    wbuf[j] = 'a' + (TEST_PRNG(&prng) % 26);
                }
                lfs3_file_write(&lfs3, &files[k],
                        wbuf, CHUNK) => CHUNK;
            }
            lfs3_file_flush(&lfs3, &files[k]) => 0;
            // block evictions ignore desync, which risks complicating
            // things
            if (DESYNC) {
                lfs3_file_desync(&lfs3, &files[k]) => 0;
            }
        }
    }

    // get a block to evict
    lfs3_block_t victim = 0;
    lfs3_size_t count = 0;
    lfs3_trv_t trv;
    lfs3_trv_open(&lfs3, &trv, LFS3_T_RDONLY) => 0;
    for (lfs3_block_t i = 0;; i++) {
        assert(i < 2*BLOCK_COUNT);

        struct lfs3_tinfo tinfo;
        int err = lfs3_trv_read(&lfs3, &trv, &tinfo);
        assert(!err);

        if (tinfo.btype == LFS3_BTYPE_DATA) {
            if (count == SIBLING) {
                victim = tinfo.block;
                break;
            }

            count += 1;
        }
    }
    lfs3_trv_close(&lfs3, &trv) => 0;

    // try evicting
    lfs3_fs_mkbad(&lfs3, victim, LFS3_MKBAD_EVICT) => 0;

    // evicted block should no longer be in use, anywhere in the
    // filesystem
    lfs3_trv_open(&lfs3, &trv, LFS3_T_RDONLY) => 0;
    for (lfs3_block_t i = 0;; i++) {
        assert(i < 2*BLOCK_COUNT);

        struct lfs3_tinfo tinfo;
        int err = lfs3_trv_read(&lfs3, &trv, &tinfo);
        assert(!err || err == LFS3_ERR_NOENT);
        if (err == LFS3_ERR_NOENT) {
            break;
        }

        assert(tinfo.block != victim);
    }
    lfs3_trv_close(&lfs3, &trv) => 0;

    // check we can still read the files
    lfs3_file_rewind(&lfs3, &files[0]) => 0;
    prng = wprng;
    for (lfs3_off_t i = 0; i < wsize; i += CHUNK) {
        uint8_t wbuf[CHUNK];
        for (lfs3_size_t j = 0; j < CHUNK; j++) {
            wbuf[j] = 'a' + (TEST_PRNG(&prng) % 26);
        }
        uint8_t rbuf[CHUNK];
        lfs3_file_read(&lfs3, &files[0],
                rbuf, CHUNK) => CHUNK;
        assert(memcmp(rbuf, wbuf, CHUNK) == 0);
    }

    for (lfs3_size_t k = 1; k < N; k++) {
        lfs3_off_t wsize_ = CHUNK + DELTA*k;
        while ((lfs3_soff_t)wsize_ < (lfs3_soff_t)CHUNK) {
            wsize_ += CHUNK;
        }
        lfs3_file_rewind(&lfs3, &files[k]) => 0;
        prng = wprng;
        for (lfs3_off_t i = 0; i < wsize_; i += CHUNK) {
            uint8_t wbuf[CHUNK];
            for (lfs3_size_t j = 0; j < CHUNK; j++) {
                wbuf[j] = 'a' + (TEST_PRNG(&prng) % 26);
            }
            uint8_t rbuf[CHUNK];
            lfs3_file_read(&lfs3, &files[k],
                    rbuf, CHUNK) => CHUNK;
            assert(memcmp(rbuf, wbuf, CHUNK) == 0);
        }
    }

    // try writing an additional block to all files, to make sure
    // things still work
    prng = wprng;
    for (lfs3_size_t i = 0; i < wsize; i += 1) {
        TEST_PRNG(&prng);
    }
    lfs3_file_seek(&lfs3, &files[0], 0, LFS3_SEEK_END) => wsize;
    uint8_t wbuf[CHUNK];
    for (lfs3_size_t j = 0; j < CHUNK; j++) {
        wbuf[j] = 'a' + (TEST_PRNG(&prng) % 26);
    }
    lfs3_file_write(&lfs3, &files[0],
            wbuf, CHUNK) => CHUNK;
    lfs3_file_flush(&lfs3, &files[0]) => 0;
    
    for (lfs3_size_t k = 1; k < N; k++) {
        lfs3_off_t wsize_ = CHUNK + DELTA*k;
        while ((lfs3_soff_t)wsize_ < (lfs3_soff_t)CHUNK) {
            wsize_ += CHUNK;
        }
        prng = wprng;
        for (lfs3_size_t i = 0; i < wsize_; i += 1) {
            TEST_PRNG(&prng);
        }
        lfs3_file_seek(&lfs3, &files[k], 0, LFS3_SEEK_END) => wsize_;
        uint8_t wbuf[CHUNK];
        for (lfs3_size_t j = 0; j < CHUNK; j++) {
            wbuf[j] = 'a' + (TEST_PRNG(&prng) % 26);
        }
        lfs3_file_write(&lfs3, &files[k],
                wbuf, CHUNK) => CHUNK;
        lfs3_file_flush(&lfs3, &files[k]) => 0;
    }

    // check we can still read the files
    lfs3_file_rewind(&lfs3, &files[0]) => 0;
    prng = wprng;
    for (lfs3_off_t i = 0; i < wsize+CHUNK; i += CHUNK) {
        uint8_t wbuf[CHUNK];
        for (lfs3_size_t j = 0; j < CHUNK; j++) {
            wbuf[j] = 'a' + (TEST_PRNG(&prng) % 26);
        }
        uint8_t rbuf[CHUNK];
        lfs3_file_read(&lfs3, &files[0],
                rbuf, CHUNK) => CHUNK;
        assert(memcmp(rbuf, wbuf, CHUNK) == 0);
    }

    for (lfs3_size_t k = 1; k < N; k++) {
        lfs3_off_t wsize_ = CHUNK + DELTA*k;
        while ((lfs3_soff_t)wsize_ < (lfs3_soff_t)CHUNK) {
            wsize_ += CHUNK;
        }
        lfs3_file_rewind(&lfs3, &files[k]) => 0;
        prng = wprng;
        for (lfs3_off_t i = 0; i < wsize_+CHUNK; i += CHUNK) {
            uint8_t wbuf[CHUNK];
            for (lfs3_size_t j = 0; j < CHUNK; j++) {
                wbuf[j] = 'a' + (TEST_PRNG(&prng) % 26);
            }
            uint8_t rbuf[CHUNK];
            lfs3_file_read(&lfs3, &files[k],
                    rbuf, CHUNK) => CHUNK;
            assert(memcmp(rbuf, wbuf, CHUNK) == 0);
        }
    }

    lfs3_file_close(&lfs3, &files[0]) => 0;
    for (lfs3_size_t k = 1; k < N; k++) {
        lfs3_file_desync(&lfs3, &files[k]) => 0;
        lfs3_file_close(&lfs3, &files[k]) => 0;
    }
    lfs3_unmount(&lfs3) => 0;
'''



# these tests are just rude
#
# write to a file, but keep evicting the most recently written block

[cases.test_evict_rude_seq]
defines.SIZE = '8*BLOCK_SIZE'
defines.CHUNK = ['FCACHE_SIZE/2', '2*FCACHE_SIZE', 'BLOCK_SIZE']
# INIT=0 => no init
# INIT=1 => fill with data
# INIT=2 => truncate to size
defines.INIT = [0, 1, 2]
defines.SYNC = [false, true]
code = '''
    lfs3_t lfs3;
    lfs3_format(&lfs3,
            LFS3_F_RDWR
                | ((GBMAP) ? LFS3_IFDEF_GBMAP(LFS3_F_GBMAP, -1) : 0),
            CFG) => 0;
    lfs3_mount(&lfs3, LFS3_M_RDWR, CFG) => 0;

    // create a file
    lfs3_file_t file;
    lfs3_file_open(&lfs3, &file, "raccoonbutterflyfish",
            LFS3_O_RDWR | LFS3_O_CREAT | LFS3_O_EXCL) => 0;

    // simulate our file in ram
    uint8_t sim[SIZE];
    lfs3_off_t size;
    uint32_t prng = 42;
    if (INIT == 0) {
        size = 0;
    } else if (INIT == 1) {
        for (lfs3_size_t i = 0; i < SIZE; i++) {
            sim[i] = 'a' + (TEST_PRNG(&prng) % 26);
        }
        lfs3_file_write(&lfs3, &file, sim, SIZE) => SIZE;
        size = SIZE;
    } else {
        memset(sim, 0, SIZE);
        lfs3_file_truncate(&lfs3, &file, SIZE) => 0;
        size = SIZE;
    }
    // sync?
    if (SYNC) {
        lfs3_file_sync(&lfs3, &file) => 0;
    }

    // write to file
    lfs3_file_rewind(&lfs3, &file) => 0;
    for (lfs3_off_t i = 0; i < SIZE; i += CHUNK) {
        for (lfs3_size_t j = 0; j < CHUNK; j++) {
            sim[i+j] = 'a' + (TEST_PRNG(&prng) % 26);
        }
        lfs3_file_write(&lfs3, &file, &sim[i], CHUNK) => CHUNK;
        size = lfs3_max(size, i+CHUNK);

        // sync?
        if (SYNC) {
            lfs3_file_sync(&lfs3, &file) => 0;
        }

        // evict? so rude
        if (file.leaf.bptr.d.u.disk.block != 0) {
            lfs3_block_t victim = file.leaf.bptr.d.u.disk.block;

            // try evicting
            lfs3_fs_mkbad(&lfs3, victim, LFS3_MKBAD_EVICT) => 0;

            // evicted block should no longer be in use
            assert(file.leaf.bptr.d.u.disk.block != victim);

            // evicted block should no longer be in use, anywhere in the
            // filesystem
            lfs3_trv_t trv;
            lfs3_trv_open(&lfs3, &trv, LFS3_T_RDONLY) => 0;
            for (lfs3_block_t i = 0;; i++) {
                assert(i < 2*BLOCK_COUNT);

                struct lfs3_tinfo tinfo;
                int err = lfs3_trv_read(&lfs3, &trv, &tinfo);
                assert(!err || err == LFS3_ERR_NOENT);
                if (err == LFS3_ERR_NOENT) {
                    break;
                }

                assert(tinfo.block != victim);
            }
            lfs3_trv_close(&lfs3, &trv) => 0;
        }
    }

    // check we can still read the file
    for (int remount = 0; remount < 2; remount++) {
        // remount?
        if (remount) {
            lfs3_file_close(&lfs3, &file) => 0;
            lfs3_unmount(&lfs3) => 0;
            lfs3_mount(&lfs3, LFS3_M_RDWR, CFG) => 0;
            lfs3_file_open(&lfs3, &file, "raccoonbutterflyfish",
                    LFS3_O_RDONLY) => 0;
        }

        // is size correct?
        lfs3_file_size(&lfs3, &file) => size;
        // try reading
        uint8_t rbuf[2*SIZE];
        memset(rbuf, 0xaa, 2*SIZE);
        lfs3_file_rewind(&lfs3, &file) => 0;
        lfs3_file_read(&lfs3, &file, rbuf, 2*SIZE) => size;
        assert(memcmp(rbuf, sim, size) == 0);
    }

    lfs3_file_close(&lfs3, &file) => 0;
    lfs3_unmount(&lfs3) => 0;
'''

[cases.test_evict_rude_random]
defines.SIZE = '8*BLOCK_SIZE'
defines.CHUNK = ['FCACHE_SIZE/2', '2*FCACHE_SIZE', 'BLOCK_SIZE']
defines.N = 1000
# INIT=0 => no init
# INIT=1 => fill with data
# INIT=2 => truncate to size
defines.INIT = [0, 1, 2]
defines.SYNC = [false, true]
defines.SEED = 42
fuzz = 'SEED'
code = '''
    lfs3_t lfs3;
    lfs3_format(&lfs3,
            LFS3_F_RDWR
                | ((GBMAP) ? LFS3_IFDEF_GBMAP(LFS3_F_GBMAP, -1) : 0),
            CFG) => 0;
    lfs3_mount(&lfs3, LFS3_M_RDWR, CFG) => 0;

    // create a file
    lfs3_file_t file;
    lfs3_file_open(&lfs3, &file, "raccoonbutterflyfish",
            LFS3_O_RDWR | LFS3_O_CREAT | LFS3_O_EXCL) => 0;

    // simulate our file in ram
    uint8_t sim[SIZE];
    lfs3_off_t size;
    uint32_t prng = SEED;
    if (INIT == 0) {
        memset(sim, 0, SIZE);
        size = 0;
    } else if (INIT == 1) {
        for (lfs3_size_t i = 0; i < SIZE; i++) {
            sim[i] = 'a' + (TEST_PRNG(&prng) % 26);
        }
        lfs3_file_write(&lfs3, &file, sim, SIZE) => SIZE;
        size = SIZE;
    } else {
        memset(sim, 0, SIZE);
        lfs3_file_truncate(&lfs3, &file, SIZE) => 0;
        size = SIZE;
    }
    // sync?
    if (SYNC) {
        lfs3_file_sync(&lfs3, &file) => 0;
    }

    // write to file, randomly!
    for (lfs3_off_t i = 0; i < N; i += 1) {
        // choose a random offset
        lfs3_off_t off = TEST_PRNG(&prng) % (SIZE-(CHUNK-1));

        lfs3_file_seek(&lfs3, &file, off, LFS3_SEEK_SET) => off;
        for (lfs3_size_t j = 0; j < CHUNK; j++) {
            sim[off+j] = 'a' + (TEST_PRNG(&prng) % 26);
        }
        lfs3_file_write(&lfs3, &file, &sim[off], CHUNK) => CHUNK;
        size = lfs3_max(size, off+CHUNK);

        // sync?
        if (SYNC) {
            lfs3_file_sync(&lfs3, &file) => 0;
        }

        // evict? so rude
        if (file.leaf.bptr.d.u.disk.block != 0) {
            lfs3_block_t victim = file.leaf.bptr.d.u.disk.block;

            // try evicting
            lfs3_fs_mkbad(&lfs3, victim, LFS3_MKBAD_EVICT) => 0;

            // evicted block should no longer be in use
            assert(file.leaf.bptr.d.u.disk.block != victim);

            // evicted block should no longer be in use, anywhere in the
            // filesystem
            lfs3_trv_t trv;
            lfs3_trv_open(&lfs3, &trv, LFS3_T_RDONLY) => 0;
            for (lfs3_block_t i = 0;; i++) {
                assert(i < 2*BLOCK_COUNT);

                struct lfs3_tinfo tinfo;
                int err = lfs3_trv_read(&lfs3, &trv, &tinfo);
                assert(!err || err == LFS3_ERR_NOENT);
                if (err == LFS3_ERR_NOENT) {
                    break;
                }

                assert(tinfo.block != victim);
            }
            lfs3_trv_close(&lfs3, &trv) => 0;
        }
    }

    // check we can still read the file
    for (int remount = 0; remount < 2; remount++) {
        // remount?
        if (remount) {
            lfs3_file_close(&lfs3, &file) => 0;
            lfs3_unmount(&lfs3) => 0;
            lfs3_mount(&lfs3, LFS3_M_RDWR, CFG) => 0;
            lfs3_file_open(&lfs3, &file, "raccoonbutterflyfish",
                    LFS3_O_RDONLY) => 0;
        }

        // is size correct?
        lfs3_file_size(&lfs3, &file) => size;
        // try reading
        uint8_t rbuf[2*SIZE];
        memset(rbuf, 0xaa, 2*SIZE);
        lfs3_file_rewind(&lfs3, &file) => 0;
        lfs3_file_read(&lfs3, &file, rbuf, 2*SIZE) => size;
        assert(memcmp(rbuf, sim, size) == 0);
    }

    lfs3_file_close(&lfs3, &file) => 0;
    lfs3_unmount(&lfs3) => 0;
'''



# many/fuzz tests with random evictions
#
# randomly choose a block to evict every step
#

[cases.test_evict_spam_dir_many]
defines.N = [1, 2, 4, 8, 16, 32, 64, 128, 256]
defines.ESEED = 42
code = '''
    // test creating directories
    lfs3_t lfs3;
    lfs3_format(&lfs3,
            LFS3_F_RDWR
                | ((GBMAP) ? LFS3_IFDEF_GBMAP(LFS3_F_GBMAP, -1) : 0),
            CFG) => 0;
    lfs3_mount(&lfs3, LFS3_M_RDWR, CFG) => 0;
    // last seen block count
    lfs3_block_t n = 0;
    uint32_t eprng = ESEED;

    // make this many directories
    for (lfs3_size_t i = 0; i < N; i++) {
        char name[256];
        sprintf(name, "dir%03x", i);
        int err = lfs3_mkdir(&lfs3, name);
        assert(!err || (TEST_PLS && err == LFS3_ERR_EXIST));

        // randomly evict a block
        //
        // to try to minimize traversals we randomly choose a block
        // based on the _previous_ traversal, or the last
        // non-mrootanchor block if that fails
        lfs3_block_t victim_i = TEST_PRNG(&eprng) % lfs3_max(n, 1);
        lfs3_block_t victim = 2;
        lfs3_trv_t trv;
        lfs3_trv_open(&lfs3, &trv, LFS3_T_RDONLY) => 0;
        for (lfs3_block_t i = 0;; i++) {
            assert(i < 2*BLOCK_COUNT);

            struct lfs3_tinfo tinfo;
            int err = lfs3_trv_read(&lfs3, &trv, &tinfo);
            assert(!err || err == LFS3_ERR_NOENT);
            if (err == LFS3_ERR_NOENT) {
                break;
            }

            if (i <= victim_i && tinfo.block >= 2) {
                victim = tinfo.block;
            }
            n = i+1;
        }
        lfs3_trv_close(&lfs3, &trv) => 0;

        // try evicting
        lfs3_fs_mkbad(&lfs3, victim, LFS3_MKBAD_EVICT) => 0;

        // evicted block should no longer be in use, anywhere in the
        // filesystem
        lfs3_trv_open(&lfs3, &trv, LFS3_T_RDONLY) => 0;
        for (lfs3_block_t i = 0;; i++) {
            assert(i < 2*BLOCK_COUNT);

            struct lfs3_tinfo tinfo;
            int err = lfs3_trv_read(&lfs3, &trv, &tinfo);
            assert(!err || err == LFS3_ERR_NOENT);
            if (err == LFS3_ERR_NOENT) {
                break;
            }

            assert(tinfo.block != victim);
        }
        lfs3_trv_close(&lfs3, &trv) => 0;
    }

    for (int remount = 0; remount < 2; remount++) {
        // remount?
        if (remount) {
            lfs3_unmount(&lfs3) => 0;
            lfs3_mount(&lfs3, LFS3_M_RDONLY, CFG) => 0;
        }

        // grm should be zero here
        assert(lfs3.grm_p[0] == 0);

        // check that our mkdir worked
        for (lfs3_size_t i = 0; i < N; i++) {
            char name[256];
            sprintf(name, "dir%03x", i);
            struct lfs3_info info;
            lfs3_stat(&lfs3, name, &info) => 0;
            assert(strcmp(info.name, name) == 0);
            assert(info.type == LFS3_TYPE_DIR);
            assert(info.size == 0);
        }

        lfs3_dir_t dir;
        lfs3_dir_open(&lfs3, &dir, "/") => 0;
        struct lfs3_info info;
        lfs3_dir_read(&lfs3, &dir, &info) => 0;
        assert(strcmp(info.name, ".") == 0);
        assert(info.type == LFS3_TYPE_DIR);
        assert(info.size == 0);
        lfs3_dir_read(&lfs3, &dir, &info) => 0;
        assert(strcmp(info.name, "..") == 0);
        assert(info.type == LFS3_TYPE_DIR);
        assert(info.size == 0);
        for (lfs3_size_t i = 0; i < N; i++) {
            char name[256];
            sprintf(name, "dir%03x", i);
            lfs3_dir_read(&lfs3, &dir, &info) => 0;
            assert(strcmp(info.name, name) == 0);
            assert(info.type == LFS3_TYPE_DIR);
            assert(info.size == 0);
        }
        lfs3_dir_read(&lfs3, &dir, &info) => LFS3_ERR_NOENT;
        lfs3_dir_close(&lfs3, &dir) => 0;

        for (lfs3_size_t i = 0; i < N; i++) {
            char name[256];
            sprintf(name, "dir%03x", i);
            lfs3_dir_open(&lfs3, &dir, name) => 0;
            lfs3_dir_read(&lfs3, &dir, &info) => 0;
            assert(strcmp(info.name, ".") == 0);
            assert(info.type == LFS3_TYPE_DIR);
            assert(info.size == 0);
            lfs3_dir_read(&lfs3, &dir, &info) => 0;
            assert(strcmp(info.name, "..") == 0);
            assert(info.type == LFS3_TYPE_DIR);
            assert(info.size == 0);
            lfs3_dir_read(&lfs3, &dir, &info) => LFS3_ERR_NOENT;
            lfs3_dir_close(&lfs3, &dir) => 0;
        }
    }

    lfs3_unmount(&lfs3) => 0;
'''

[cases.test_evict_spam_dir_fuzz]
defines.N = [1, 2, 4, 8, 16, 32, 64, 128, 256]
defines.OPS = '2*N'
defines.SEED = 'range(10)'
defines.ESEED = '42+SEED'
fuzz = 'SEED'
code = '''
    // test fuzz with dirs
    lfs3_t lfs3;
    lfs3_format(&lfs3,
            LFS3_F_RDWR
                | ((GBMAP) ? LFS3_IFDEF_GBMAP(LFS3_F_GBMAP, -1) : 0),
            CFG) => 0;
    lfs3_mount(&lfs3, LFS3_M_RDWR, CFG) => 0;
    // last seen block count
    lfs3_block_t n = 0;
    uint32_t eprng = ESEED;

    // set up a simulation to compare against
    lfs3_size_t *sim = malloc(N*sizeof(lfs3_size_t));
    lfs3_size_t sim_size = 0;

    uint32_t prng = SEED;
    for (lfs3_size_t i = 0; i < OPS; i++) {
        // choose a pseudo-random op, either mkdir, remove, or rename
        uint8_t op = TEST_PRNG(&prng) % 3;

        if (op == 0 || sim_size == 0) {
            // choose a pseudo-random number, truncate to 3 hexadecimals
            lfs3_size_t x = TEST_PRNG(&prng) % N;
            // insert into our sim
            for (lfs3_size_t j = 0;; j++) {
                if (j >= sim_size || sim[j] >= x) {
                    // already seen?
                    if (j < sim_size && sim[j] == x) {
                        // do nothing
                    } else {
                        // insert
                        memmove(&sim[j+1], &sim[j],
                                (sim_size-j)*sizeof(lfs3_size_t));
                        sim_size += 1;
                        sim[j] = x;
                    }
                    break;
                }
            }

            // create a directory here
            char name[256];
            sprintf(name, "dir%03x", x);
            int err = lfs3_mkdir(&lfs3, name);
            assert(!err || err == LFS3_ERR_EXIST);

        } else if (op == 1) {
            // choose a pseudo-random entry to delete
            lfs3_size_t j = TEST_PRNG(&prng) % sim_size;
            lfs3_size_t x = sim[j];
            // delete from our sim
            memmove(&sim[j], &sim[j+1],
                    (sim_size-(j+1))*sizeof(lfs3_size_t));
            sim_size -= 1;

            // remove this directory
            char name[256];
            sprintf(name, "dir%03x", x);
            lfs3_remove(&lfs3, name) => 0;

        } else {
            // choose a pseudo-random entry to rename, and a pseudo-random
            // number to rename to
            lfs3_size_t j = TEST_PRNG(&prng) % sim_size;
            lfs3_size_t x = sim[j];
            lfs3_size_t y = TEST_PRNG(&prng) % N;
            for (lfs3_size_t k = 0;; k++) {
                if (k >= sim_size || sim[k] >= y) {
                    // already seen and not a noop?
                    if (k < sim_size && sim[k] == y && x != y) {
                        // just delete the original entry
                        memmove(&sim[j], &sim[j+1],
                                (sim_size-(j+1))*sizeof(lfs3_size_t));
                        sim_size -= 1;
                    } else {
                        // first delete
                        memmove(&sim[j], &sim[j+1],
                                (sim_size-(j+1))*sizeof(lfs3_size_t));
                        if (k > j) {
                            k -= 1;
                        }
                        // then insert
                        memmove(&sim[k+1], &sim[k],
                                (sim_size-1-k)*sizeof(lfs3_size_t));
                        sim[k] = y;
                    }
                    break;
                }
            }

            // rename this directory
            char old_name[256];
            sprintf(old_name, "dir%03x", x);
            char new_name[256];
            sprintf(new_name, "dir%03x", y);
            lfs3_rename(&lfs3, old_name, new_name) => 0;
        }

        // randomly evict a block
        //
        // to try to minimize traversals we randomly choose a block
        // based on the _previous_ traversal, or the last
        // non-mrootanchor block if that fails
        lfs3_block_t victim_i = TEST_PRNG(&eprng) % lfs3_max(n, 1);
        lfs3_block_t victim = 2;
        lfs3_trv_t trv;
        lfs3_trv_open(&lfs3, &trv, LFS3_T_RDONLY) => 0;
        for (lfs3_block_t i = 0;; i++) {
            assert(i < 2*BLOCK_COUNT);

            struct lfs3_tinfo tinfo;
            int err = lfs3_trv_read(&lfs3, &trv, &tinfo);
            assert(!err || err == LFS3_ERR_NOENT);
            if (err == LFS3_ERR_NOENT) {
                break;
            }

            if (i <= victim_i && tinfo.block >= 2) {
                victim = tinfo.block;
            }
            n = i+1;
        }
        lfs3_trv_close(&lfs3, &trv) => 0;

        // try evicting
        lfs3_fs_mkbad(&lfs3, victim, LFS3_MKBAD_EVICT) => 0;

        // evicted block should no longer be in use, anywhere in the
        // filesystem
        lfs3_trv_open(&lfs3, &trv, LFS3_T_RDONLY) => 0;
        for (lfs3_block_t i = 0;; i++) {
            assert(i < 2*BLOCK_COUNT);

            struct lfs3_tinfo tinfo;
            int err = lfs3_trv_read(&lfs3, &trv, &tinfo);
            assert(!err || err == LFS3_ERR_NOENT);
            if (err == LFS3_ERR_NOENT) {
                break;
            }

            assert(tinfo.block != victim);
        }
        lfs3_trv_close(&lfs3, &trv) => 0;
    }

    for (int remount = 0; remount < 2; remount++) {
        // remount?
        if (remount) {
            lfs3_unmount(&lfs3) => 0;
            lfs3_mount(&lfs3, LFS3_M_RDONLY, CFG) => 0;
        }

        // grm should be zero here
        assert(lfs3.grm_p[0] == 0);

        // test that our directories match our simulation
        for (lfs3_size_t j = 0; j < sim_size; j++) {
            char name[256];
            sprintf(name, "dir%03x", sim[j]);
            struct lfs3_info info;
            lfs3_stat(&lfs3, name, &info) => 0;
            char name2[256];
            sprintf(name2, "dir%03x", sim[j]);
            assert(strcmp(info.name, name2) == 0);
            assert(info.type == LFS3_TYPE_DIR);
            assert(info.size == 0);
        }

        lfs3_dir_t dir;
        lfs3_dir_open(&lfs3, &dir, "/") => 0;
        struct lfs3_info info;
        lfs3_dir_read(&lfs3, &dir, &info) => 0;
        assert(strcmp(info.name, ".") == 0);
        assert(info.type == LFS3_TYPE_DIR);
        assert(info.size == 0);
        lfs3_dir_read(&lfs3, &dir, &info) => 0;
        assert(strcmp(info.name, "..") == 0);
        assert(info.type == LFS3_TYPE_DIR);
        assert(info.size == 0);
        for (lfs3_size_t j = 0; j < sim_size; j++) {
            char name[256];
            sprintf(name, "dir%03x", sim[j]);
            lfs3_dir_read(&lfs3, &dir, &info) => 0;
            assert(strcmp(info.name, name) == 0);
            assert(info.type == LFS3_TYPE_DIR);
            assert(info.size == 0);
        }
        lfs3_dir_read(&lfs3, &dir, &info) => LFS3_ERR_NOENT;
        lfs3_dir_close(&lfs3, &dir) => 0;
    }

    // clean up sim/lfs3
    free(sim);
    lfs3_unmount(&lfs3) => 0;
'''

[cases.test_evict_spam_file_many]
defines.N = [1, 2, 4, 8, 16, 32, 64]
defines.SIZE = [
    '0',
    'FCACHE_SIZE/2',
    '2*FCACHE_SIZE',
    'BLOCK_SIZE/2',
    'BLOCK_SIZE',
    '2*BLOCK_SIZE',
    '4*BLOCK_SIZE',
]
defines.ESEED = 42
if = '(SIZE*N)/BLOCK_SIZE <= 32'
code = '''
    // test creating files
    lfs3_t lfs3;
    lfs3_format(&lfs3,
            LFS3_F_RDWR
                | ((GBMAP) ? LFS3_IFDEF_GBMAP(LFS3_F_GBMAP, -1) : 0),
            CFG) => 0;
    lfs3_mount(&lfs3, LFS3_M_RDWR, CFG) => 0;
    // last seen block count
    lfs3_block_t n = 0;
    uint32_t eprng = ESEED;

    // create this many files
    uint32_t prng = 42;
    for (lfs3_size_t i = 0; i < N; i++) {
        char name[256];
        sprintf(name, "amethyst%03x", i);

        uint8_t wbuf[SIZE];
        for (lfs3_size_t j = 0; j < SIZE; j++) {
            wbuf[j] = 'a' + (TEST_PRNG(&prng) % 26);
        }

        lfs3_file_t file;
        lfs3_file_open(&lfs3, &file, name,
                LFS3_O_WRONLY | LFS3_O_CREAT | LFS3_O_EXCL) => 0;
        lfs3_file_write(&lfs3, &file, wbuf, SIZE) => SIZE;
        lfs3_file_close(&lfs3, &file) => 0;

        // randomly evict a block
        //
        // to try to minimize traversals we randomly choose a block
        // based on the _previous_ traversal, or the last
        // non-mrootanchor block if that fails
        lfs3_block_t victim_i = TEST_PRNG(&eprng) % lfs3_max(n, 1);
        lfs3_block_t victim = 2;
        lfs3_trv_t trv;
        lfs3_trv_open(&lfs3, &trv, LFS3_T_RDONLY) => 0;
        for (lfs3_block_t i = 0;; i++) {
            assert(i < 2*BLOCK_COUNT);

            struct lfs3_tinfo tinfo;
            int err = lfs3_trv_read(&lfs3, &trv, &tinfo);
            assert(!err || err == LFS3_ERR_NOENT);
            if (err == LFS3_ERR_NOENT) {
                break;
            }

            if (i <= victim_i && tinfo.block >= 2) {
                victim = tinfo.block;
            }
            n = i+1;
        }
        lfs3_trv_close(&lfs3, &trv) => 0;

        // try evicting
        lfs3_fs_mkbad(&lfs3, victim, LFS3_MKBAD_EVICT) => 0;

        // evicted block should no longer be in use, anywhere in the
        // filesystem
        lfs3_trv_open(&lfs3, &trv, LFS3_T_RDONLY) => 0;
        for (lfs3_block_t i = 0;; i++) {
            assert(i < 2*BLOCK_COUNT);

            struct lfs3_tinfo tinfo;
            int err = lfs3_trv_read(&lfs3, &trv, &tinfo);
            assert(!err || err == LFS3_ERR_NOENT);
            if (err == LFS3_ERR_NOENT) {
                break;
            }

            assert(tinfo.block != victim);
        }
        lfs3_trv_close(&lfs3, &trv) => 0;
    }

    for (int remount = 0; remount < 2; remount++) {
        // remount?
        if (remount) {
            lfs3_unmount(&lfs3) => 0;
            lfs3_mount(&lfs3, LFS3_M_RDWR, CFG) => 0;
        }

        // check that our writes worked
        prng = 42;
        for (lfs3_size_t i = 0; i < N; i++) {
            // check with stat
            char name[256];
            sprintf(name, "amethyst%03x", i);
            struct lfs3_info info;
            lfs3_stat(&lfs3, name, &info) => 0;
            assert(strcmp(info.name, name) == 0);
            assert(info.type == LFS3_TYPE_REG);
            assert(info.size == SIZE);

            // try reading the file, note we reset prng above
            uint8_t wbuf[SIZE];
            for (lfs3_size_t j = 0; j < SIZE; j++) {
                wbuf[j] = 'a' + (TEST_PRNG(&prng) % 26);
            }

            lfs3_file_t file;
            uint8_t rbuf[SIZE];
            lfs3_file_open(&lfs3, &file, name, LFS3_O_RDONLY) => 0;
            lfs3_file_read(&lfs3, &file, rbuf, SIZE) => SIZE;
            assert(memcmp(rbuf, wbuf, SIZE) == 0);
            lfs3_file_close(&lfs3, &file) => 0;
        }
    }

    lfs3_unmount(&lfs3) => 0;
'''

[cases.test_evict_spam_file_fuzz]
defines.N = [1, 2, 4, 8, 16, 32, 64]
defines.OPS = '2*N'
defines.SIZE = [
    '0',
    'FCACHE_SIZE/2',
    '2*FCACHE_SIZE',
    'BLOCK_SIZE/2',
    'BLOCK_SIZE',
    '2*BLOCK_SIZE',
    '4*BLOCK_SIZE',
]
defines.SEED = 'range(10)'
defines.ESEED = '42+SEED'
fuzz = 'SEED'
if = '(SIZE*N)/BLOCK_SIZE <= 16'
code = '''
    // test fuzz with files
    lfs3_t lfs3;
    lfs3_format(&lfs3,
            LFS3_F_RDWR
                | ((GBMAP) ? LFS3_IFDEF_GBMAP(LFS3_F_GBMAP, -1) : 0),
            CFG) => 0;
    lfs3_mount(&lfs3, LFS3_M_RDWR, CFG) => 0;
    // last seen block count
    lfs3_block_t n = 0;
    uint32_t eprng = ESEED;

    // set up a simulation to compare against
    lfs3_size_t *sim = malloc(N*sizeof(lfs3_size_t));
    uint32_t *sim_prngs = malloc(N*sizeof(uint32_t));
    lfs3_size_t sim_size = 0;

    uint32_t prng = SEED;
    for (lfs3_size_t i = 0; i < OPS; i++) {
        // choose which operation to do
        uint8_t op = TEST_PRNG(&prng) % 3;

        // creating a new file?
        if (op == 0 || sim_size == 0) {
            // choose a pseudo-random number
            lfs3_size_t x = TEST_PRNG(&prng) % N;
            // associate each file with a prng that generates its contents
            uint32_t wprng = TEST_PRNG(&prng);

            // insert into our sim
            for (lfs3_size_t j = 0;; j++) {
                if (j >= sim_size || sim[j] >= x) {
                    // already seen?
                    if (j < sim_size && sim[j] == x) {
                        // new prng
                        sim_prngs[j] = wprng;
                    } else {
                        // insert
                        memmove(&sim[j+1], &sim[j],
                                (sim_size-j)*sizeof(lfs3_size_t));
                        memmove(&sim_prngs[j+1], &sim_prngs[j],
                                (sim_size-j)*sizeof(uint32_t));
                        sim_size += 1;
                        sim[j] = x;
                        sim_prngs[j] = wprng;
                    }
                    break;
                }
            }

            // create a file here
            char name[256];
            sprintf(name, "amethyst%03x", x);
            uint8_t wbuf[SIZE];
            for (lfs3_size_t j = 0; j < SIZE; j++) {
                wbuf[j] = 'a' + (TEST_PRNG(&wprng) % 26);
            }

            lfs3_file_t file;
            lfs3_file_open(&lfs3, &file, name,
                    LFS3_O_WRONLY | LFS3_O_CREAT | LFS3_O_TRUNC) => 0;
            lfs3_file_write(&lfs3, &file, wbuf, SIZE) => SIZE;
            lfs3_file_close(&lfs3, &file) => 0;

        // deleting a file?
        } else if (op == 1) {
            // choose a random file to delete
            lfs3_size_t j = TEST_PRNG(&prng) % sim_size;
            lfs3_size_t x = sim[j];
            // delete from our sim
            memmove(&sim[j], &sim[j+1],
                    (sim_size-(j+1))*sizeof(lfs3_size_t));
            memmove(&sim_prngs[j], &sim_prngs[j+1],
                    (sim_size-(j+1))*sizeof(uint32_t));
            sim_size -= 1;

            // delete this file
            char name[256];
            sprintf(name, "amethyst%03x", x);
            lfs3_remove(&lfs3, name) => 0;

        // renaming a file?
        } else {
            // choose a random file to rename, and a random number to
            // rename to
            lfs3_size_t j = TEST_PRNG(&prng) % sim_size;
            lfs3_size_t x = sim[j];
            lfs3_size_t y = TEST_PRNG(&prng) % N;
            uint32_t wprng = sim_prngs[j];

            // update our sim
            for (lfs3_size_t k = 0;; k++) {
                if (k >= sim_size || sim[k] >= y) {
                    // renaming and replacing
                    if (k < sim_size && sim[k] == y && x != y) {
                        // delete the original entry
                        memmove(&sim[j], &sim[j+1],
                                (sim_size-(j+1))*sizeof(lfs3_size_t));
                        memmove(&sim_prngs[j], &sim_prngs[j+1],
                                (sim_size-(j+1))*sizeof(uint32_t));
                        sim_size -= 1;
                        if (k > j) {
                            k -= 1;
                        }
                        // update the prng
                        sim_prngs[k] = wprng;
                    // just renaming
                    } else {
                        // first delete
                        memmove(&sim[j], &sim[j+1],
                                (sim_size-(j+1))*sizeof(lfs3_size_t));
                        memmove(&sim_prngs[j], &sim_prngs[j+1],
                                (sim_size-(j+1))*sizeof(uint32_t));
                        if (k > j) {
                            k -= 1;
                        }
                        // then insert
                        memmove(&sim[k+1], &sim[k],
                                (sim_size-1-k)*sizeof(lfs3_size_t));
                        memmove(&sim_prngs[k+1], &sim_prngs[k],
                                (sim_size-1-k)*sizeof(uint32_t));
                        sim[k] = y;
                        sim_prngs[k] = wprng;
                    }
                    break;
                }
            }

            // rename this file
            char old_name[256];
            sprintf(old_name, "amethyst%03x", x);
            char new_name[256];
            sprintf(new_name, "amethyst%03x", y);
            lfs3_rename(&lfs3, old_name, new_name) => 0;
        }

        // randomly evict a block
        //
        // to try to minimize traversals we randomly choose a block
        // based on the _previous_ traversal, or the last
        // non-mrootanchor block if that fails
        lfs3_block_t victim_i = TEST_PRNG(&eprng) % lfs3_max(n, 1);
        lfs3_block_t victim = 2;
        lfs3_trv_t trv;
        lfs3_trv_open(&lfs3, &trv, LFS3_T_RDONLY) => 0;
        for (lfs3_block_t i = 0;; i++) {
            assert(i < 2*BLOCK_COUNT);

            struct lfs3_tinfo tinfo;
            int err = lfs3_trv_read(&lfs3, &trv, &tinfo);
            assert(!err || err == LFS3_ERR_NOENT);
            if (err == LFS3_ERR_NOENT) {
                break;
            }

            if (i <= victim_i && tinfo.block >= 2) {
                victim = tinfo.block;
            }
            n = i+1;
        }
        lfs3_trv_close(&lfs3, &trv) => 0;

        // try evicting
        lfs3_fs_mkbad(&lfs3, victim, LFS3_MKBAD_EVICT) => 0;

        // evicted block should no longer be in use, anywhere in the
        // filesystem
        lfs3_trv_open(&lfs3, &trv, LFS3_T_RDONLY) => 0;
        for (lfs3_block_t i = 0;; i++) {
            assert(i < 2*BLOCK_COUNT);

            struct lfs3_tinfo tinfo;
            int err = lfs3_trv_read(&lfs3, &trv, &tinfo);
            assert(!err || err == LFS3_ERR_NOENT);
            if (err == LFS3_ERR_NOENT) {
                break;
            }

            assert(tinfo.block != victim);
        }
        lfs3_trv_close(&lfs3, &trv) => 0;
    }

    for (int remount = 0; remount < 2; remount++) {
        // remount?
        if (remount) {
            lfs3_unmount(&lfs3) => 0;
            lfs3_mount(&lfs3, LFS3_M_RDWR, CFG) => 0;
        }

        // check that our files match our simulation
        for (lfs3_size_t j = 0; j < sim_size; j++) {
            char name[256];
            sprintf(name, "amethyst%03x", sim[j]);
            struct lfs3_info info;
            lfs3_stat(&lfs3, name, &info) => 0;
            assert(strcmp(info.name, name) == 0);
            assert(info.type == LFS3_TYPE_REG);
            assert(info.size == SIZE);
        }

        lfs3_dir_t dir;
        lfs3_dir_open(&lfs3, &dir, "/") => 0;
        struct lfs3_info info;
        lfs3_dir_read(&lfs3, &dir, &info) => 0;
        assert(strcmp(info.name, ".") == 0);
        assert(info.type == LFS3_TYPE_DIR);
        assert(info.size == 0);
        lfs3_dir_read(&lfs3, &dir, &info) => 0;
        assert(strcmp(info.name, "..") == 0);
        assert(info.type == LFS3_TYPE_DIR);
        assert(info.size == 0);
        for (lfs3_size_t j = 0; j < sim_size; j++) {
            char name[256];
            sprintf(name, "amethyst%03x", sim[j]);
            lfs3_dir_read(&lfs3, &dir, &info) => 0;
            assert(strcmp(info.name, name) == 0);
            assert(info.type == LFS3_TYPE_REG);
            assert(info.size == SIZE);
        }
        lfs3_dir_read(&lfs3, &dir, &info) => LFS3_ERR_NOENT;
        lfs3_dir_close(&lfs3, &dir) => 0;

        // check the file contents
        for (lfs3_size_t j = 0; j < sim_size; j++) {
            char name[256];
            sprintf(name, "amethyst%03x", sim[j]);
            lfs3_file_t file;
            lfs3_file_open(&lfs3, &file, name, LFS3_O_RDONLY) => 0;

            uint32_t wprng = sim_prngs[j];
            uint8_t wbuf[SIZE];
            for (lfs3_size_t j = 0; j < SIZE; j++) {
                wbuf[j] = 'a' + (TEST_PRNG(&wprng) % 26);
            }

            uint8_t rbuf[SIZE];
            lfs3_file_read(&lfs3, &file, rbuf, SIZE) => SIZE;
            assert(memcmp(rbuf, wbuf, SIZE) == 0);
            lfs3_file_close(&lfs3, &file) => 0;
        }
    }

    // clean up sim/lfs3
    free(sim);
    free(sim_prngs);
    lfs3_unmount(&lfs3) => 0;
'''

[cases.test_evict_spam_fwrite_fuzz]
defines.OPS = 20
defines.SIZE = [
    'FCACHE_SIZE/2',
    '2*FCACHE_SIZE',
    'BLOCK_SIZE/2',
    'BLOCK_SIZE',
    '2*BLOCK_SIZE',
    '4*BLOCK_SIZE',
]
# chunk is more an upper limit here
defines.CHUNK = [32, 8, 1]
# INIT=0 => no init
# INIT=1 => fill with data
# INIT=2 => truncate to size
defines.INIT = [0, 1, 2]
defines.SYNC = [false, true]
defines.SEED = 'range(10)'
defines.ESEED = '42+SEED'
fuzz = 'SEED'
if = [
    'CHUNK <= SIZE',
    # this just saves testing time
    'SIZE <= 4*1024*FRAGMENT_SIZE',
]
code = '''
    // test with complex file writes
    lfs3_t lfs3;
    lfs3_format(&lfs3,
            LFS3_F_RDWR
                | ((GBMAP) ? LFS3_IFDEF_GBMAP(LFS3_F_GBMAP, -1) : 0),
            CFG) => 0;
    lfs3_mount(&lfs3, LFS3_M_RDWR, CFG) => 0;
    // last seen block count
    lfs3_block_t n = 0;
    uint32_t eprng = ESEED;

    // create a file
    lfs3_file_t file;
    lfs3_file_open(&lfs3, &file, "hello",
            LFS3_O_WRONLY | LFS3_O_CREAT | LFS3_O_EXCL) => 0;
    // simulate our file in ram
    uint8_t sim[SIZE];
    lfs3_off_t size;
    uint32_t prng = SEED;
    if (INIT == 0) {
        memset(sim, 0, SIZE);
        size = 0;
    } else if (INIT == 1) {
        for (lfs3_size_t i = 0; i < SIZE; i++) {
            sim[i] = 'a' + (TEST_PRNG(&prng) % 26);
        }
        lfs3_file_write(&lfs3, &file, sim, SIZE) => SIZE;
        size = SIZE;
    } else {
        memset(sim, 0, SIZE);
        lfs3_file_truncate(&lfs3, &file, SIZE) => 0;
        size = SIZE;
    }

    // sync?
    if (SYNC) {
        lfs3_file_sync(&lfs3, &file) => 0;
    }

    for (lfs3_size_t i = 0; i < OPS; i++) {
        // choose a random location
        lfs3_off_t off = TEST_PRNG(&prng) % SIZE;
        // and a random size, up to the chunk size
        lfs3_size_t chunk = lfs3_min(
                (TEST_PRNG(&prng) % (CHUNK+1-1)) + 1,
                SIZE - off);

        // update sim
        for (lfs3_size_t j = 0; j < chunk; j++) {
            sim[off+j] = 'a' + (TEST_PRNG(&prng) % 26);
        }
        size = lfs3_max(size, off+chunk);

        // update file
        lfs3_file_seek(&lfs3, &file, off, LFS3_SEEK_SET) => off;
        lfs3_file_write(&lfs3, &file, &sim[off], chunk) => chunk;

        // sync?
        if (SYNC) {
            lfs3_file_sync(&lfs3, &file) => 0;
        }

        // randomly evict a block
        //
        // to try to minimize traversals we randomly choose a block
        // based on the _previous_ traversal, or the last
        // non-mrootanchor block if that fails
        lfs3_block_t victim_i = TEST_PRNG(&eprng) % lfs3_max(n, 1);
        lfs3_block_t victim = 2;
        lfs3_trv_t trv;
        lfs3_trv_open(&lfs3, &trv, LFS3_T_RDONLY) => 0;
        for (lfs3_block_t i = 0;; i++) {
            assert(i < 2*BLOCK_COUNT);

            struct lfs3_tinfo tinfo;
            int err = lfs3_trv_read(&lfs3, &trv, &tinfo);
            assert(!err || err == LFS3_ERR_NOENT);
            if (err == LFS3_ERR_NOENT) {
                break;
            }

            if (i <= victim_i && tinfo.block >= 2) {
                victim = tinfo.block;
            }
            n = i+1;
        }
        lfs3_trv_close(&lfs3, &trv) => 0;

        // try evicting
        lfs3_fs_mkbad(&lfs3, victim, LFS3_MKBAD_EVICT) => 0;

        // evicted block should no longer be in use, anywhere in the
        // filesystem
        lfs3_trv_open(&lfs3, &trv, LFS3_T_RDONLY) => 0;
        for (lfs3_block_t i = 0;; i++) {
            assert(i < 2*BLOCK_COUNT);

            struct lfs3_tinfo tinfo;
            int err = lfs3_trv_read(&lfs3, &trv, &tinfo);
            assert(!err || err == LFS3_ERR_NOENT);
            if (err == LFS3_ERR_NOENT) {
                break;
            }

            assert(tinfo.block != victim);
        }
        lfs3_trv_close(&lfs3, &trv) => 0;
    }

    lfs3_file_close(&lfs3, &file) => 0;

    for (int remount = 0; remount < 2; remount++) {
        // remount?
        if (remount) {
            lfs3_unmount(&lfs3) => 0;
            lfs3_mount(&lfs3, LFS3_M_RDWR, CFG) => 0;
        }

        // check our file with stat
        struct lfs3_info info;
        lfs3_stat(&lfs3, "hello", &info) => 0;
        assert(strcmp(info.name, "hello") == 0);
        assert(info.type == LFS3_TYPE_REG);
        assert(info.size == size);

        // and with dir read
        lfs3_dir_t dir;
        lfs3_dir_open(&lfs3, &dir, "/") => 0;
        lfs3_dir_read(&lfs3, &dir, &info) => 0;
        assert(strcmp(info.name, ".") == 0);
        assert(info.type == LFS3_TYPE_DIR);
        assert(info.size == 0);
        lfs3_dir_read(&lfs3, &dir, &info) => 0;
        assert(strcmp(info.name, "..") == 0);
        assert(info.type == LFS3_TYPE_DIR);
        assert(info.size == 0);
        lfs3_dir_read(&lfs3, &dir, &info) => 0;
        assert(strcmp(info.name, "hello") == 0);
        assert(info.type == LFS3_TYPE_REG);
        assert(info.size == size);
        lfs3_dir_read(&lfs3, &dir, &info) => LFS3_ERR_NOENT;
        lfs3_dir_close(&lfs3, &dir) => 0;

        // try reading our file
        lfs3_file_open(&lfs3, &file, "hello", LFS3_O_RDONLY) => 0;
        // is size correct?
        lfs3_file_size(&lfs3, &file) => size;
        // try reading
        uint8_t rbuf[2*SIZE];
        memset(rbuf, 0xaa, 2*SIZE);
        lfs3_file_read(&lfs3, &file, rbuf, 2*SIZE) => size;
        // does our file match our simulation?
        assert(memcmp(rbuf, sim, size) == 0);
        lfs3_file_close(&lfs3, &file) => 0;
    }

    lfs3_unmount(&lfs3) => 0;
'''

[cases.test_evict_spam_uz_fuzz]
# you probably need to flush if you expect errors
defines.FLUSH = false
defines.N = [1, 2, 4, 8, 16, 32, 64]
defines.OPS = '2*N'
defines.SIZE = [
    '0',
    'FCACHE_SIZE/2',
    '2*FCACHE_SIZE',
    'BLOCK_SIZE/2',
    'BLOCK_SIZE',
    '2*BLOCK_SIZE',
    '4*BLOCK_SIZE',
]
defines.SEED = 'range(10)'
defines.ESEED = '42+SEED'
fuzz = 'SEED'
if = '(SIZE*N)/BLOCK_SIZE <= 16'
code = '''
    // test with uncreats, zombies, etc
    lfs3_t lfs3;
    lfs3_format(&lfs3,
            LFS3_F_RDWR
                | ((GBMAP) ? LFS3_IFDEF_GBMAP(LFS3_F_GBMAP, -1) : 0),
            CFG) => 0;
    lfs3_mount(&lfs3, LFS3_M_RDWR, CFG) => 0;
    // last seen block count
    lfs3_block_t n = 0;
    uint32_t eprng = ESEED;

    // set up a simulation to compare against
    lfs3_size_t *sim = malloc(N*sizeof(lfs3_size_t));
    uint32_t *sim_prngs = malloc(N*sizeof(uint32_t));
    bool *sim_isstickys = malloc(N*sizeof(bool));
    lfs3_size_t sim_size = 0;

    typedef struct sim_file {
        lfs3_size_t x;
        bool sticky;
        bool zombie;
        uint32_t prng;
        lfs3_file_t file;
    } sim_file_t;
    sim_file_t **sim_files = malloc(N*sizeof(sim_file_t*));
    lfs3_size_t sim_file_count = 0;

    uint32_t prng = SEED;
    for (lfs3_size_t i = 0; i < OPS; i++) {
    nonsense:;
        // choose which operation to do
        uint8_t op = TEST_PRNG(&prng) % 5;

        // open a new file?
        if (op == 0) {
            if (sim_file_count >= N) {
                goto nonsense;
            }
            // choose a pseudo-random number
            lfs3_size_t x = TEST_PRNG(&prng) % N;

            // already exists?
            bool exist = false;
            uint32_t wprng = 0;
            bool sticky = true;
            for (lfs3_size_t j = 0; j < sim_size; j++) {
                if (sim[j] == x) {
                    exist = true;
                    wprng = sim_prngs[j];
                    sticky = sim_isstickys[j];
                    break;
                }
            }
            // choose a random seed if we don't exist
            if (!exist) {
                wprng = TEST_PRNG(&prng);
                sticky = true;
            }

            lfs3_size_t j = sim_file_count;
            sim_files[j] = malloc(sizeof(sim_file_t));

            // open the actual file
            char name[256];
            sprintf(name, "batman%03x", x);
            lfs3_file_open(&lfs3, &sim_files[j]->file, name,
                    LFS3_O_RDWR
                        | LFS3_O_CREAT
                        | ((FLUSH) ? LFS3_O_FLUSH : 0)) => 0;

            // write some initial data if we don't exist
            if (!exist || sticky) {
                uint8_t wbuf[SIZE];
                uint32_t wprng_ = wprng;
                for (lfs3_size_t k = 0; k < SIZE; k++) {
                    wbuf[k] = 'a' + (TEST_PRNG(&wprng_) % 26);
                }
                lfs3_file_write(&lfs3, &sim_files[j]->file, wbuf, SIZE)
                        => SIZE;
            }

            // open in our sim
            sim_files[j]->x = x;
            sim_files[j]->sticky = sticky;
            sim_files[j]->zombie = false;
            sim_files[j]->prng = wprng;
            sim_file_count++;

            // insert into our sim
            for (lfs3_size_t k = 0;; k++) {
                if (k >= sim_size || sim[k] >= x) {
                    // already seen?
                    if (k < sim_size && sim[k] == x) {
                        // new prng
                        sim_prngs[k] = wprng;
                    } else {
                        // insert
                        memmove(&sim[k+1], &sim[k],
                                (sim_size-k)*sizeof(lfs3_size_t));
                        memmove(&sim_prngs[k+1], &sim_prngs[k],
                                (sim_size-k)*sizeof(uint32_t));
                        memmove(&sim_isstickys[k+1], &sim_isstickys[k],
                                (sim_size-k)*sizeof(bool));
                        sim_size += 1;
                        sim[k] = x;
                        sim_prngs[k] = wprng;
                        sim_isstickys[k] = sticky;
                    }
                    break;
                }
            }

        // write/rewrite a file?
        } else if (op == 1) {
            if (sim_file_count == 0) {
                goto nonsense;
            }
            // choose a random file handle
            lfs3_size_t j = TEST_PRNG(&prng) % sim_file_count;
            lfs3_size_t x = sim_files[j]->x;
            // choose a random seed
            uint32_t wprng = TEST_PRNG(&prng);

            // write to the file
            lfs3_file_rewind(&lfs3, &sim_files[j]->file) => 0;
            uint8_t wbuf[SIZE];
            uint32_t wprng_ = wprng;
            for (lfs3_size_t k = 0; k < SIZE; k++) {
                wbuf[k] = 'a' + (TEST_PRNG(&wprng_) % 26);
            }
            lfs3_file_write(&lfs3, &sim_files[j]->file, wbuf, SIZE) => SIZE;
            lfs3_file_sync(&lfs3, &sim_files[j]->file) => 0;

            // update sim
            sim_files[j]->prng = wprng;
            if (!sim_files[j]->zombie) {
                // update in our sim
                for (lfs3_size_t k = 0;; k++) {
                    if (sim[k] == x) {
                        // new prng
                        sim_prngs[k] = wprng;
                        // no longer sticky
                        sim_isstickys[k] = false;
                        break;
                    }
                }

                // update related sim files
                for (lfs3_size_t k = 0; k < sim_file_count; k++) {
                    if (sim_files[k]->x == x && !sim_files[k]->zombie) {
                        // new prng
                        sim_files[k]->prng = wprng;
                        // no longer sticky
                        sim_files[k]->sticky = false;
                    }
                }
            }

        // close a file?
        } else if (op == 2) {
            if (sim_file_count == 0) {
                goto nonsense;
            }
            // choose a random file handle
            lfs3_size_t j = TEST_PRNG(&prng) % sim_file_count;
            lfs3_size_t x = sim_files[j]->x;
            bool sticky = sim_files[j]->sticky;
            bool zombie = sim_files[j]->zombie;

            // this doesn't really test anything, but if we don't close
            // files eventually everything will end up zombies

            // close the file without affected disk
            lfs3_file_desync(&lfs3, &sim_files[j]->file) => 0;
            lfs3_file_close(&lfs3, &sim_files[j]->file) => 0;
            // clobber closed files to try to catch lingering references
            memset(&sim_files[j]->file, 0xcc, sizeof(lfs3_file_t));

            // remove from list
            free(sim_files[j]);
            sim_files[j] = sim_files[sim_file_count-1];
            sim_file_count -= 1;

            // update our sim
            if (sticky && !zombie) {
                // orphaned?
                bool orphan = true;
                for (lfs3_size_t k = 0; k < sim_file_count; k++) {
                    if (sim_files[k]->x == x && !sim_files[k]->zombie) {
                        orphan = false;
                    }
                }

                // if we were never synced, delete from sim
                if (orphan) {
                    for (lfs3_size_t k = 0;; k++) {
                        if (sim[k] == x) {
                            memmove(&sim[k], &sim[k+1],
                                    (sim_size-(k+1))*sizeof(lfs3_size_t));
                            memmove(&sim_prngs[k], &sim_prngs[k+1],
                                    (sim_size-(k+1))*sizeof(uint32_t));
                            memmove(&sim_isstickys[k], &sim_isstickys[k+1],
                                    (sim_size-(k+1))*sizeof(bool));
                            sim_size -= 1;
                            break;
                        }
                    }
                }
            }

        // remove a file?
        } else if (op == 3) {
            if (sim_size == 0) {
                goto nonsense;
            }
            // choose a random file to delete
            lfs3_size_t j = TEST_PRNG(&prng) % sim_size;
            lfs3_size_t x = sim[j];

            // delete this file
            char name[256];
            sprintf(name, "batman%03x", x);
            lfs3_remove(&lfs3, name) => 0;

            // delete from our sim
            memmove(&sim[j], &sim[j+1],
                    (sim_size-(j+1))*sizeof(lfs3_size_t));
            memmove(&sim_prngs[j], &sim_prngs[j+1],
                    (sim_size-(j+1))*sizeof(uint32_t));
            memmove(&sim_isstickys[j], &sim_isstickys[j+1],
                    (sim_size-(j+1))*sizeof(bool));
            sim_size -= 1;

            // mark any related sim files as zombied
            for (lfs3_size_t k = 0; k < sim_file_count; k++) {
                if (sim_files[k]->x == x) {
                    sim_files[k]->zombie = true;
                }
            }

        // rename a file?
        } else if (op == 4) {
            if (sim_size == 0) {
                goto nonsense;
            }
            // choose a random file to rename, and a random number to
            // rename to
            lfs3_size_t j = TEST_PRNG(&prng) % sim_size;
            lfs3_size_t x = sim[j];
            lfs3_size_t y = TEST_PRNG(&prng) % N;
            uint32_t wprng = sim_prngs[j];
            bool sticky = sim_isstickys[j];

            // rename this file
            char old_name[256];
            sprintf(old_name, "batman%03x", x);
            char new_name[256];
            sprintf(new_name, "batman%03x", y);
            lfs3_rename(&lfs3, old_name, new_name) => 0;

            // update our sim
            for (lfs3_size_t k = 0;; k++) {
                if (k >= sim_size || sim[k] >= y) {
                    // renaming and replacing
                    if (k < sim_size && sim[k] == y && x != y) {
                        // delete the original entry
                        memmove(&sim[j], &sim[j+1],
                                (sim_size-(j+1))*sizeof(lfs3_size_t));
                        memmove(&sim_prngs[j], &sim_prngs[j+1],
                                (sim_size-(j+1))*sizeof(uint32_t));
                        memmove(&sim_isstickys[j], &sim_isstickys[j+1],
                                (sim_size-(j+1))*sizeof(bool));
                        sim_size -= 1;
                        if (k > j) {
                            k -= 1;
                        }
                        // update the prng/sticky
                        sim_prngs[k] = wprng;
                        sim_isstickys[k] = sticky;
                    // just renaming
                    } else {
                        // first delete
                        memmove(&sim[j], &sim[j+1],
                                (sim_size-(j+1))*sizeof(lfs3_size_t));
                        memmove(&sim_prngs[j], &sim_prngs[j+1],
                                (sim_size-(j+1))*sizeof(uint32_t));
                        memmove(&sim_isstickys[j], &sim_isstickys[j+1],
                                (sim_size-(j+1))*sizeof(bool));
                        if (k > j) {
                            k -= 1;
                        }
                        // then insert
                        memmove(&sim[k+1], &sim[k],
                                (sim_size-1-k)*sizeof(lfs3_size_t));
                        memmove(&sim_prngs[k+1], &sim_prngs[k],
                                (sim_size-1-k)*sizeof(uint32_t));
                        memmove(&sim_isstickys[k+1], &sim_isstickys[k],
                                (sim_size-1-k)*sizeof(bool));
                        sim[k] = y;
                        sim_prngs[k] = wprng;
                        sim_isstickys[k] = sticky;
                    }
                    break;
                }
            }

            // update any related sim files
            for (lfs3_size_t k = 0; k < sim_file_count; k++) {
                // move source files
                if (sim_files[k]->x == x) {
                    sim_files[k]->x = y;

                // mark target files as zombied
                } else if (sim_files[k]->x == y) {
                    sim_files[k]->zombie = true;
                }
            }
        }

        // randomly evict a block
        //
        // to try to minimize traversals we randomly choose a block
        // based on the _previous_ traversal, or the last
        // non-mrootanchor block if that fails
        lfs3_block_t victim_i = TEST_PRNG(&eprng) % lfs3_max(n, 1);
        lfs3_block_t victim = 2;
        lfs3_trv_t trv;
        lfs3_trv_open(&lfs3, &trv, LFS3_T_RDONLY) => 0;
        for (lfs3_block_t i = 0;; i++) {
            assert(i < 2*BLOCK_COUNT);

            struct lfs3_tinfo tinfo;
            int err = lfs3_trv_read(&lfs3, &trv, &tinfo);
            assert(!err || err == LFS3_ERR_NOENT);
            if (err == LFS3_ERR_NOENT) {
                break;
            }

            if (i <= victim_i && tinfo.block >= 2) {
                victim = tinfo.block;
            }
            n = i+1;
        }
        lfs3_trv_close(&lfs3, &trv) => 0;

        // try evicting
        lfs3_fs_mkbad(&lfs3, victim, LFS3_MKBAD_EVICT) => 0;

        // evicted block should no longer be in use, anywhere in the
        // filesystem
        lfs3_trv_open(&lfs3, &trv, LFS3_T_RDONLY) => 0;
        for (lfs3_block_t i = 0;; i++) {
            assert(i < 2*BLOCK_COUNT);

            struct lfs3_tinfo tinfo;
            int err = lfs3_trv_read(&lfs3, &trv, &tinfo);
            assert(!err || err == LFS3_ERR_NOENT);
            if (err == LFS3_ERR_NOENT) {
                break;
            }

            assert(tinfo.block != victim);
        }
        lfs3_trv_close(&lfs3, &trv) => 0;
    }

    // check that disk matches our simulation
    for (lfs3_size_t j = 0; j < sim_size; j++) {
        char name[256];
        sprintf(name, "batman%03x", sim[j]);
        struct lfs3_info info;
        lfs3_stat(&lfs3, name, &info) => 0;
        assert(strcmp(info.name, name) == 0);
        if (sim_isstickys[j]) {
            assert(info.type == LFS3_TYPE_STICKYNOTE);
            assert(info.size == 0);
        } else {
            assert(info.type == LFS3_TYPE_REG);
            assert(info.size == SIZE);
        }
    }

    lfs3_dir_t dir;
    lfs3_dir_open(&lfs3, &dir, "/") => 0;
    struct lfs3_info info;
    lfs3_dir_read(&lfs3, &dir, &info) => 0;
    assert(strcmp(info.name, ".") == 0);
    assert(info.type == LFS3_TYPE_DIR);
    assert(info.size == 0);
    lfs3_dir_read(&lfs3, &dir, &info) => 0;
    assert(strcmp(info.name, "..") == 0);
    assert(info.type == LFS3_TYPE_DIR);
    assert(info.size == 0);
    for (lfs3_size_t j = 0; j < sim_size; j++) {
        char name[256];
        sprintf(name, "batman%03x", sim[j]);
        lfs3_dir_read(&lfs3, &dir, &info) => 0;
        assert(strcmp(info.name, name) == 0);
        if (sim_isstickys[j]) {
            assert(info.type == LFS3_TYPE_STICKYNOTE);
            assert(info.size == 0);
        } else {
            assert(info.type == LFS3_TYPE_REG);
            assert(info.size == SIZE);
        }
    }
    lfs3_dir_read(&lfs3, &dir, &info) => LFS3_ERR_NOENT;
    lfs3_dir_close(&lfs3, &dir) => 0;

    for (lfs3_size_t j = 0; j < sim_size; j++) {
        char name[256];
        sprintf(name, "batman%03x", sim[j]);
        lfs3_file_t file;
        lfs3_file_open(&lfs3, &file, name, LFS3_O_RDONLY) => 0;

        uint32_t wprng = sim_prngs[j];
        uint8_t wbuf[SIZE];
        for (lfs3_size_t j = 0; j < SIZE; j++) {
            wbuf[j] = 'a' + (TEST_PRNG(&wprng) % 26);
        }

        uint8_t rbuf[SIZE];
        if (sim_isstickys[j]) {
            lfs3_file_read(&lfs3, &file, rbuf, SIZE) => 0;
        } else {
            lfs3_file_read(&lfs3, &file, rbuf, SIZE) => SIZE;
            assert(memcmp(rbuf, wbuf, SIZE) == 0);
        }
        lfs3_file_close(&lfs3, &file) => 0;
    }

    // check that our file handles match our simulation
    for (lfs3_size_t j = 0; j < sim_file_count; j++) {
        uint32_t wprng = sim_files[j]->prng;
        uint8_t wbuf[SIZE];
        for (lfs3_size_t j = 0; j < SIZE; j++) {
            wbuf[j] = 'a' + (TEST_PRNG(&wprng) % 26);
        }

        lfs3_file_rewind(&lfs3, &sim_files[j]->file) => 0;
        uint8_t rbuf[SIZE];
        lfs3_file_read(&lfs3, &sim_files[j]->file, rbuf, SIZE) => SIZE;
        assert(memcmp(rbuf, wbuf, SIZE) == 0);
    }

    // clean up sim/lfs3
    free(sim);
    free(sim_prngs);
    free(sim_isstickys);
    for (lfs3_size_t j = 0; j < sim_file_count; j++) {
        lfs3_file_close(&lfs3, &sim_files[j]->file) => 0;
        free(sim_files[j]);
    }
    free(sim_files);
    lfs3_unmount(&lfs3) => 0;
'''

[cases.test_evict_spam_uzd_fuzz]
# you probably need to flush if you expect errors
defines.FLUSH = false
defines.N = [1, 2, 4, 8, 16, 32, 64]
defines.OPS = '2*N'
defines.SIZE = [
    '0',
    'FCACHE_SIZE/2',
    '2*FCACHE_SIZE',
    'BLOCK_SIZE/2',
    'BLOCK_SIZE',
    '2*BLOCK_SIZE',
    '4*BLOCK_SIZE',
]
defines.SEED = 'range(10)'
defines.ESEED = '42+SEED'
fuzz = 'SEED'
if = '(SIZE*N)/BLOCK_SIZE <= 16'
code = '''
    // test with uncreats, zombies, dirs, etc
    lfs3_t lfs3;
    lfs3_format(&lfs3,
            LFS3_F_RDWR
                | ((GBMAP) ? LFS3_IFDEF_GBMAP(LFS3_F_GBMAP, -1) : 0),
            CFG) => 0;
    lfs3_mount(&lfs3, LFS3_M_RDWR, CFG) => 0;
    // last seen block count
    lfs3_block_t n = 0;
    uint32_t eprng = ESEED;

    // set up a simulation to compare against
    lfs3_size_t *sim = malloc(N*sizeof(lfs3_size_t));
    uint32_t *sim_prngs = malloc(N*sizeof(uint32_t));
    bool *sim_isstickys = malloc(N*sizeof(bool));
    bool *sim_isdirs = malloc(N*sizeof(bool));
    lfs3_size_t sim_size = 0;

    typedef struct sim_file {
        lfs3_size_t x;
        bool sticky;
        bool zombie;
        uint32_t prng;
        lfs3_file_t file;
    } sim_file_t;
    sim_file_t **sim_files = malloc(N*sizeof(sim_file_t*));
    lfs3_size_t sim_file_count = 0;

    uint32_t prng = SEED;
    for (lfs3_size_t i = 0; i < OPS; i++) {
    nonsense:;
        // choose which operation to do
        uint8_t op = TEST_PRNG(&prng) % 8;

        // open a new file?
        if (op == 0) {
            if (sim_file_count >= N) {
                goto nonsense;
            }
            // choose a pseudo-random number
            lfs3_size_t x = TEST_PRNG(&prng) % N;

            // already exists?
            bool exist = true;
            uint32_t wprng = 0;
            bool sticky = true;
            for (lfs3_size_t j = 0; j < sim_size; j++) {
                if (sim[j] == x) {
                    if (sim_isdirs[j]) {
                        goto nonsense;
                    }
                    exist = true;
                    wprng = sim_prngs[j];
                    sticky = sim_isstickys[j];
                    break;
                }
            }
            // choose a random seed if we don't exist
            if (!exist) {
                wprng = TEST_PRNG(&prng);
                sticky = true;
            }

            lfs3_size_t j = sim_file_count;
            sim_files[j] = malloc(sizeof(sim_file_t));

            // open the actual file
            char name[256];
            sprintf(name, "batman%03x", x);
            lfs3_file_open(&lfs3, &sim_files[j]->file, name,
                    LFS3_O_RDWR
                        | LFS3_O_CREAT
                        | ((FLUSH) ? LFS3_O_FLUSH : 0)) => 0;

            // write some initial data if we don't exist
            if (!exist || sticky) {
                uint8_t wbuf[SIZE];
                uint32_t wprng_ = wprng;
                for (lfs3_size_t k = 0; k < SIZE; k++) {
                    wbuf[k] = 'a' + (TEST_PRNG(&wprng_) % 26);
                }
                lfs3_file_write(&lfs3, &sim_files[j]->file, wbuf, SIZE)
                        => SIZE;
            }

            // open in our sim
            sim_files[j]->x = x;
            sim_files[j]->sticky = sticky;
            sim_files[j]->zombie = false;
            sim_files[j]->prng = wprng;
            sim_file_count++;

            // insert into our sim
            for (lfs3_size_t k = 0;; k++) {
                if (k >= sim_size || sim[k] >= x) {
                    // already seen?
                    if (k < sim_size && sim[k] == x) {
                        // new prng
                        sim_prngs[k] = wprng;
                    } else {
                        // insert
                        memmove(&sim[k+1], &sim[k],
                                (sim_size-k)*sizeof(lfs3_size_t));
                        memmove(&sim_prngs[k+1], &sim_prngs[k],
                                (sim_size-k)*sizeof(uint32_t));
                        memmove(&sim_isstickys[k+1], &sim_isstickys[k],
                                (sim_size-k)*sizeof(bool));
                        memmove(&sim_isdirs[k+1], &sim_isdirs[k],
                                (sim_size-k)*sizeof(bool));
                        sim_size += 1;
                        sim[k] = x;
                        sim_prngs[k] = wprng;
                        sim_isstickys[k] = sticky;
                        sim_isdirs[k] = false;
                    }
                    break;
                }
            }

        // write/rewrite a file?
        } else if (op == 1) {
            if (sim_file_count == 0) {
                goto nonsense;
            }
            // choose a random file handle
            lfs3_size_t j = TEST_PRNG(&prng) % sim_file_count;
            lfs3_size_t x = sim_files[j]->x;
            // choose a random seed
            uint32_t wprng = TEST_PRNG(&prng);

            // write to the file
            lfs3_file_rewind(&lfs3, &sim_files[j]->file) => 0;
            uint8_t wbuf[SIZE];
            uint32_t wprng_ = wprng;
            for (lfs3_size_t k = 0; k < SIZE; k++) {
                wbuf[k] = 'a' + (TEST_PRNG(&wprng_) % 26);
            }
            lfs3_file_write(&lfs3, &sim_files[j]->file, wbuf, SIZE) => SIZE;
            lfs3_file_sync(&lfs3, &sim_files[j]->file) => 0;

            // update sim
            sim_files[j]->prng = wprng;
            if (!sim_files[j]->zombie) {
                // update in our sim
                for (lfs3_size_t k = 0;; k++) {
                    if (k >= sim_size || sim[k] >= x) {
                        // new prng
                        sim_prngs[k] = wprng;
                        // no longer sticky
                        sim_isstickys[k] = false;
                        break;
                    }
                }

                // update related sim files
                for (lfs3_size_t k = 0; k < sim_file_count; k++) {
                    if (sim_files[k]->x == x && !sim_files[k]->zombie) {
                        // new prng
                        sim_files[k]->prng = wprng;
                        // no longer sticky
                        sim_files[k]->sticky = false;
                    }
                }
            }

        // close a file?
        } else if (op == 2) {
            if (sim_file_count == 0) {
                goto nonsense;
            }
            // choose a random file handle
            lfs3_size_t j = TEST_PRNG(&prng) % sim_file_count;
            lfs3_size_t x = sim_files[j]->x;
            lfs3_size_t sticky = sim_files[j]->sticky;
            lfs3_size_t zombie = sim_files[j]->zombie;

            // this doesn't really test anything, but if we don't close
            // files eventually everything will end up zombies

            // close the file without affected disk
            lfs3_file_desync(&lfs3, &sim_files[j]->file) => 0;
            lfs3_file_close(&lfs3, &sim_files[j]->file) => 0;
            // clobber closed files to try to catch lingering references
            memset(&sim_files[j]->file, 0xcc, sizeof(lfs3_file_t));

            // remove from list
            free(sim_files[j]);
            sim_files[j] = sim_files[sim_file_count-1];
            sim_file_count -= 1;

            // update our sim
            if (sticky && !zombie) {
                // orphaned?
                bool orphan = true;
                for (lfs3_size_t k = 0; k < sim_file_count; k++) {
                    if (sim_files[k]->x == x && !sim_files[k]->zombie) {
                        orphan = false;
                    }
                }

                // if we were never synced, delete from sim
                if (orphan) {
                    for (lfs3_size_t k = 0;; k++) {
                        if (sim[k] == x) {
                            memmove(&sim[k], &sim[k+1],
                                    (sim_size-(k+1))*sizeof(lfs3_size_t));
                            memmove(&sim_prngs[k], &sim_prngs[k+1],
                                    (sim_size-(k+1))*sizeof(uint32_t));
                            memmove(&sim_isstickys[k], &sim_isstickys[k+1],
                                    (sim_size-(k+1))*sizeof(bool));
                            memmove(&sim_isdirs[k], &sim_isdirs[k+1],
                                    (sim_size-(k+1))*sizeof(bool));
                            sim_size -= 1;
                            break;
                        }
                    }
                }
            }

        // remove a file?
        } else if (op == 3) {
            if (sim_size == 0) {
                goto nonsense;
            }
            // choose a random file to delete
            lfs3_size_t j = TEST_PRNG(&prng) % sim_size;
            lfs3_size_t x = sim[j];

            // delete this file
            char name[256];
            sprintf(name, "batman%03x", x);
            lfs3_remove(&lfs3, name) => 0;

            // delete from our sim
            memmove(&sim[j], &sim[j+1],
                    (sim_size-(j+1))*sizeof(lfs3_size_t));
            memmove(&sim_prngs[j], &sim_prngs[j+1],
                    (sim_size-(j+1))*sizeof(uint32_t));
            memmove(&sim_isstickys[j], &sim_isstickys[j+1],
                    (sim_size-(j+1))*sizeof(bool));
            memmove(&sim_isdirs[j], &sim_isdirs[j+1],
                    (sim_size-(j+1))*sizeof(bool));
            sim_size -= 1;

            // mark any related sim files as zombied
            for (lfs3_size_t k = 0; k < sim_file_count; k++) {
                if (sim_files[k]->x == x) {
                    sim_files[k]->zombie = true;
                }
            }

        // rename a file?
        } else if (op == 4) {
            if (sim_size == 0) {
                goto nonsense;
            }
            // choose a random file to rename, and a random number to
            // rename to
            lfs3_size_t j = TEST_PRNG(&prng) % sim_size;
            lfs3_size_t x = sim[j];
            lfs3_size_t y = TEST_PRNG(&prng) % N;
            uint32_t wprng = sim_prngs[j];
            bool sticky = sim_isstickys[j];
            bool dir = sim_isdirs[j];

            for (lfs3_size_t k = 0;; k++) {
                if (k >= sim_size || sim[k] >= y) {
                    // renaming and replacing
                    if (k < sim_size && sim[k] == y && x != y) {
                        // type mismatch?
                        if (sim_isdirs[k] != dir) {
                            goto nonsense;
                        }
                    }
                    break;
                }
            }

            // rename this file
            char old_name[256];
            sprintf(old_name, "batman%03x", x);
            char new_name[256];
            sprintf(new_name, "batman%03x", y);
            lfs3_rename(&lfs3, old_name, new_name) => 0;

            // update our sim
            for (lfs3_size_t k = 0;; k++) {
                if (k >= sim_size || sim[k] >= y) {
                    // renaming and replacing
                    if (k < sim_size && sim[k] == y && x != y) {
                        // delete the original entry
                        memmove(&sim[j], &sim[j+1],
                                (sim_size-(j+1))*sizeof(lfs3_size_t));
                        memmove(&sim_prngs[j], &sim_prngs[j+1],
                                (sim_size-(j+1))*sizeof(uint32_t));
                        memmove(&sim_isstickys[j], &sim_isstickys[j+1],
                                (sim_size-(j+1))*sizeof(bool));
                        memmove(&sim_isdirs[j], &sim_isdirs[j+1],
                                (sim_size-(j+1))*sizeof(bool));
                        sim_size -= 1;
                        if (k > j) {
                            k -= 1;
                        }
                        // update the prng/sticky/dir
                        sim_prngs[k] = wprng;
                        sim_isstickys[k] = sticky;
                        sim_isdirs[k] = dir;
                    // just renaming
                    } else {
                        // first delete
                        memmove(&sim[j], &sim[j+1],
                                (sim_size-(j+1))*sizeof(lfs3_size_t));
                        memmove(&sim_prngs[j], &sim_prngs[j+1],
                                (sim_size-(j+1))*sizeof(uint32_t));
                        memmove(&sim_isstickys[j], &sim_isstickys[j+1],
                                (sim_size-(j+1))*sizeof(bool));
                        memmove(&sim_isdirs[j], &sim_isdirs[j+1],
                                (sim_size-(j+1))*sizeof(bool));
                        if (k > j) {
                            k -= 1;
                        }
                        // then insert
                        memmove(&sim[k+1], &sim[k],
                                (sim_size-1-k)*sizeof(lfs3_size_t));
                        memmove(&sim_prngs[k+1], &sim_prngs[k],
                                (sim_size-1-k)*sizeof(uint32_t));
                        memmove(&sim_isstickys[k+1], &sim_isstickys[k],
                                (sim_size-1-k)*sizeof(bool));
                        memmove(&sim_isdirs[k+1], &sim_isdirs[k],
                                (sim_size-1-k)*sizeof(bool));
                        sim[k] = y;
                        sim_prngs[k] = wprng;
                        sim_isstickys[k] = sticky;
                        sim_isdirs[k] = dir;
                    }
                    break;
                }
            }

            // update any related sim files
            for (lfs3_size_t k = 0; k < sim_file_count; k++) {
                // move source files
                if (sim_files[k]->x == x) {
                    sim_files[k]->x = y;

                // mark target files as zombied
                } else if (sim_files[k]->x == y) {
                    sim_files[k]->zombie = true;
                }
            }

        // toss a directory into the mix
        } else if (op == 5) {
            // choose a pseudo-random number
            lfs3_size_t x = TEST_PRNG(&prng) % N;

            for (lfs3_size_t k = 0;; k++) {
                if (k >= sim_size || sim[k] >= x) {
                    // already seen?
                    if (k < sim_size && sim[k] == x) {
                        goto nonsense;
                    }
                    break;
                }
            }

            // make the directory
            char name[256];
            sprintf(name, "batman%03x", x);
            lfs3_mkdir(&lfs3, name) => 0;

            // insert into our sim
            for (lfs3_size_t k = 0;; k++) {
                if (k >= sim_size || sim[k] >= x) {
                    // insert
                    memmove(&sim[k+1], &sim[k],
                            (sim_size-k)*sizeof(lfs3_size_t));
                    memmove(&sim_prngs[k+1], &sim_prngs[k],
                            (sim_size-k)*sizeof(uint32_t));
                    memmove(&sim_isstickys[k+1], &sim_isstickys[k],
                            (sim_size-k)*sizeof(bool));
                    memmove(&sim_isdirs[k+1], &sim_isdirs[k],
                            (sim_size-k)*sizeof(bool));
                    sim_size += 1;
                    sim[k] = x;
                    sim_prngs[k] = 0;
                    sim_isdirs[k] = true;
                    break;
                }
            }

            // mark any related sim files as zombied
            for (lfs3_size_t k = 0; k < sim_file_count; k++) {
                if (sim_files[k]->x == x) {
                    sim_files[k]->zombie = true;
                }
            }
        }

        // randomly evict a block
        //
        // to try to minimize traversals we randomly choose a block
        // based on the _previous_ traversal, or the last
        // non-mrootanchor block if that fails
        lfs3_block_t victim_i = TEST_PRNG(&eprng) % lfs3_max(n, 1);
        lfs3_block_t victim = 2;
        lfs3_trv_t trv;
        lfs3_trv_open(&lfs3, &trv, LFS3_T_RDONLY) => 0;
        for (lfs3_block_t i = 0;; i++) {
            assert(i < 2*BLOCK_COUNT);

            struct lfs3_tinfo tinfo;
            int err = lfs3_trv_read(&lfs3, &trv, &tinfo);
            assert(!err || err == LFS3_ERR_NOENT);
            if (err == LFS3_ERR_NOENT) {
                break;
            }

            if (i <= victim_i && tinfo.block >= 2) {
                victim = tinfo.block;
            }
            n = i+1;
        }
        lfs3_trv_close(&lfs3, &trv) => 0;

        // try evicting
        lfs3_fs_mkbad(&lfs3, victim, LFS3_MKBAD_EVICT) => 0;

        // evicted block should no longer be in use, anywhere in the
        // filesystem
        lfs3_trv_open(&lfs3, &trv, LFS3_T_RDONLY) => 0;
        for (lfs3_block_t i = 0;; i++) {
            assert(i < 2*BLOCK_COUNT);

            struct lfs3_tinfo tinfo;
            int err = lfs3_trv_read(&lfs3, &trv, &tinfo);
            assert(!err || err == LFS3_ERR_NOENT);
            if (err == LFS3_ERR_NOENT) {
                break;
            }

            assert(tinfo.block != victim);
        }
        lfs3_trv_close(&lfs3, &trv) => 0;
    }

    // check that disk matches our simulation
    for (lfs3_size_t j = 0; j < sim_size; j++) {
        char name[256];
        sprintf(name, "batman%03x", sim[j]);
        struct lfs3_info info;
        lfs3_stat(&lfs3, name, &info) => 0;
        assert(strcmp(info.name, name) == 0);
        if (sim_isdirs[j]) {
            assert(info.type == LFS3_TYPE_DIR);
            assert(info.size == 0);
        } else if (sim_isstickys[j]) {
            assert(info.type == LFS3_TYPE_STICKYNOTE);
            assert(info.size == 0);
        } else {
            assert(info.type == LFS3_TYPE_REG);
            assert(info.size == SIZE);
        }
    }

    lfs3_dir_t dir;
    lfs3_dir_open(&lfs3, &dir, "/") => 0;
    struct lfs3_info info;
    lfs3_dir_read(&lfs3, &dir, &info) => 0;
    assert(strcmp(info.name, ".") == 0);
    assert(info.type == LFS3_TYPE_DIR);
    assert(info.size == 0);
    lfs3_dir_read(&lfs3, &dir, &info) => 0;
    assert(strcmp(info.name, "..") == 0);
    assert(info.type == LFS3_TYPE_DIR);
    assert(info.size == 0);
    for (lfs3_size_t j = 0; j < sim_size; j++) {
        char name[256];
        sprintf(name, "batman%03x", sim[j]);
        lfs3_dir_read(&lfs3, &dir, &info) => 0;
        assert(strcmp(info.name, name) == 0);
        if (sim_isdirs[j]) {
            assert(info.type == LFS3_TYPE_DIR);
            assert(info.size == 0);
        } else if (sim_isstickys[j]) {
            assert(info.type == LFS3_TYPE_STICKYNOTE);
            assert(info.size == 0);
        } else {
            assert(info.type == LFS3_TYPE_REG);
            assert(info.size == SIZE);
        }
    }
    lfs3_dir_read(&lfs3, &dir, &info) => LFS3_ERR_NOENT;
    lfs3_dir_close(&lfs3, &dir) => 0;

    for (lfs3_size_t j = 0; j < sim_size; j++) {
        if (sim_isdirs[j]) {
            char name[256];
            sprintf(name, "batman%03x", sim[j]);
            lfs3_file_t file;
            lfs3_file_open(&lfs3, &file, name, LFS3_O_RDONLY)
                    => LFS3_ERR_ISDIR;

        } else {
            char name[256];
            sprintf(name, "batman%03x", sim[j]);
            lfs3_file_t file;
            lfs3_file_open(&lfs3, &file, name, LFS3_O_RDONLY) => 0;

            uint32_t wprng = sim_prngs[j];
            uint8_t wbuf[SIZE];
            for (lfs3_size_t j = 0; j < SIZE; j++) {
                wbuf[j] = 'a' + (TEST_PRNG(&wprng) % 26);
            }

            uint8_t rbuf[SIZE];
            if (sim_isstickys[j]) {
                lfs3_file_read(&lfs3, &file, rbuf, SIZE) => 0;
            } else {
                lfs3_file_read(&lfs3, &file, rbuf, SIZE) => SIZE;
                assert(memcmp(rbuf, wbuf, SIZE) == 0);
            }
            lfs3_file_close(&lfs3, &file) => 0;
        }
    }

    // check that our file handles match our simulation
    for (lfs3_size_t j = 0; j < sim_file_count; j++) {
        uint32_t wprng = sim_files[j]->prng;
        uint8_t wbuf[SIZE];
        for (lfs3_size_t j = 0; j < SIZE; j++) {
            wbuf[j] = 'a' + (TEST_PRNG(&wprng) % 26);
        }

        lfs3_file_rewind(&lfs3, &sim_files[j]->file) => 0;
        uint8_t rbuf[SIZE];
        lfs3_file_read(&lfs3, &sim_files[j]->file, rbuf, SIZE) => SIZE;
        assert(memcmp(rbuf, wbuf, SIZE) == 0);
    }

    // clean up sim/lfs3
    free(sim);
    free(sim_prngs);
    free(sim_isstickys);
    free(sim_isdirs);
    for (lfs3_size_t j = 0; j < sim_file_count; j++) {
        lfs3_file_close(&lfs3, &sim_files[j]->file) => 0;
        free(sim_files[j]);
    }
    free(sim_files);
    lfs3_unmount(&lfs3) => 0;
'''




# test repairing traversals

# test traversals with mdir repair

[cases.test_evict_repair_t_mdir_mroot]
defines.SIZE = 'FCACHE_SIZE/2'
defines.SIBLING = [0]
# force early relocations, this will cause the mroot anchor to extend
defines.BLOCK_RECYCLES = 0
# report as damaged to request repairs
defines.BADBLOCK_BEHAVIOR = 'LFS3_EMUBD_BADBLOCK_READDAMAGED'
code = '''
    lfs3_t lfs3;
    lfs3_format(&lfs3,
            LFS3_F_RDWR
                | ((GBMAP) ? LFS3_IFDEF_GBMAP(LFS3_F_GBMAP, -1) : 0),
            CFG) => 0;
    lfs3_mount(&lfs3, LFS3_M_RDWR, CFG) => 0;

    uint32_t prng = 42;

    // write to our mroot until it's not the mrootanchor
    //
    // we can't evict the mrootanchor
    lfs3_file_t file;
    lfs3_file_open(&lfs3, &file, "jellyfish",
            LFS3_O_RDWR | LFS3_O_CREAT | LFS3_O_EXCL) => 0;
    uint8_t wbuf[SIZE];
    while (file.h.mdir.r.blocks[0] == 0
            || file.h.mdir.r.blocks[0] == 1) {
        lfs3_file_rewind(&lfs3, &file) => 0;
        for (lfs3_size_t j = 0; j < SIZE; j++) {
            wbuf[j] = 'a' + (TEST_PRNG(&prng) % 26);
        }
        lfs3_file_write(&lfs3, &file, wbuf, SIZE) => SIZE;
        lfs3_file_sync(&lfs3, &file) => 0;
    }

    // get a block to evict
    lfs3_block_t victim = file.h.mdir.r.blocks[SIBLING];

    // make bad
    lfs3_emubd_mkbad(CFG, victim) => 0;
    // reset ck flags
    lfs3_fs_unck(&lfs3, LFS3_GC_CKMETA) => 0;

    // run gc to repair bad blocks
    lfs3_gc_t gc;
    lfs3_gc_open(&lfs3, &gc,
            LFS3_GC_WRONLY
                | LFS3_GC_CKMETA
                | LFS3_GC_REPAIRMETA) => 0;
    for (lfs3_block_t i = 0;; i++) {
        // a bit hacky, but this catches infinite loops
        assert(i < 2*BLOCK_COUNT);

        lfs3_sblock_t steps = lfs3_gc_write(&lfs3, &gc, 1);
        assert(steps >= 0);
        if (steps == 0) {
            break;
        }
    }
    lfs3_gc_close(&lfs3, &gc) => 0;

    // evicted block should no longer be in use
    assert(file.h.mdir.r.blocks[0] != victim);
    assert(file.h.mdir.r.blocks[1] != victim);

    // evicted block should no longer be in use, anywhere in the
    // filesystem
    lfs3_trv_t trv;
    lfs3_trv_open(&lfs3, &trv, LFS3_T_RDONLY) => 0;
    for (lfs3_block_t i = 0;; i++) {
        assert(i < 2*BLOCK_COUNT);

        struct lfs3_tinfo tinfo;
        int err = lfs3_trv_read(&lfs3, &trv, &tinfo);
        assert(!err || err == LFS3_ERR_NOENT);
        if (err == LFS3_ERR_NOENT) {
            break;
        }

        assert(tinfo.block != victim);
    }
    lfs3_trv_close(&lfs3, &trv) => 0;

    // check we can still read the file
    for (int remount = 0; remount < 2; remount++) {
        // remount?
        if (remount) {
            lfs3_file_close(&lfs3, &file) => 0;
            lfs3_unmount(&lfs3) => 0;
            lfs3_mount(&lfs3, LFS3_M_RDWR, CFG) => 0;
            lfs3_file_open(&lfs3, &file, "jellyfish", LFS3_O_RDONLY) => 0;
        }

        lfs3_file_rewind(&lfs3, &file) => 0;
        uint8_t rbuf[SIZE];
        lfs3_file_read(&lfs3, &file, rbuf, SIZE) => SIZE;
        assert(memcmp(rbuf, wbuf, SIZE) == 0);
    }

    lfs3_file_close(&lfs3, &file) => 0;
    lfs3_unmount(&lfs3) => 0;
'''

[cases.test_evict_repair_t_mrootanchor]
defines.SIZE = 'FCACHE_SIZE/2'
defines.SIBLING = [0, 1]
# report as damaged to request repairs
defines.BADBLOCK_BEHAVIOR = 'LFS3_EMUBD_BADBLOCK_READDAMAGED'
code = '''
    lfs3_t lfs3;
    lfs3_format(&lfs3,
            LFS3_F_RDWR
                | ((GBMAP) ? LFS3_IFDEF_GBMAP(LFS3_F_GBMAP, -1) : 0),
            CFG) => 0;
    lfs3_mount(&lfs3, LFS3_M_RDWR, CFG) => 0;

    uint32_t prng = 42;

    // write to our mroot
    lfs3_file_t file;
    lfs3_file_open(&lfs3, &file, "jellyfish",
            LFS3_O_RDWR | LFS3_O_CREAT | LFS3_O_EXCL) => 0;
    uint8_t wbuf[SIZE];
    for (lfs3_size_t j = 0; j < SIZE; j++) {
        wbuf[j] = 'a' + (TEST_PRNG(&prng) % 26);
    }
    lfs3_file_write(&lfs3, &file, wbuf, SIZE) => SIZE;
    lfs3_file_sync(&lfs3, &file) => 0;

    // get a block to evict
    lfs3_block_t victim = SIBLING;

    // make bad
    lfs3_emubd_mkbad(CFG, victim) => 0;
    // reset ck flags
    lfs3_fs_unck(&lfs3, LFS3_GC_CKMETA) => 0;

    // run gc to repair bad blocks
    lfs3_gc_t gc;
    lfs3_gc_open(&lfs3, &gc,
            LFS3_GC_WRONLY
                | LFS3_GC_CKMETA
                | LFS3_GC_REPAIRMETA) => 0;
    for (lfs3_block_t i = 0;; i++) {
        // a bit hacky, but this catches infinite loops
        assert(i < 2*BLOCK_COUNT);

        lfs3_sblock_t steps = lfs3_gc_write(&lfs3, &gc, 1);
        assert(steps >= 0);
        if (steps == 0) {
            break;
        }
    }
    lfs3_gc_close(&lfs3, &gc) => 0;

    // we can't fully evict the mroot anchor, but we can at least repair
    // damage by compacting
    assert(lfs3.mroot.r.blocks[0] != victim);

    // check we can still read the file
    for (int remount = 0; remount < 2; remount++) {
        // remount?
        if (remount) {
            lfs3_file_close(&lfs3, &file) => 0;
            lfs3_unmount(&lfs3) => 0;
            lfs3_mount(&lfs3, LFS3_M_RDWR, CFG) => 0;
            lfs3_file_open(&lfs3, &file, "jellyfish", LFS3_O_RDONLY) => 0;
        }

        lfs3_file_rewind(&lfs3, &file) => 0;
        uint8_t rbuf[SIZE];
        lfs3_file_read(&lfs3, &file, rbuf, SIZE) => SIZE;
        assert(memcmp(rbuf, wbuf, SIZE) == 0);
    }

    lfs3_file_close(&lfs3, &file) => 0;
    lfs3_unmount(&lfs3) => 0;
'''

[cases.test_evict_repair_t_mdir_mroot_extend]
defines.SIZE = 'FCACHE_SIZE/2'
defines.SIBLING = [0]
# force early relocations
defines.BLOCK_RECYCLES = 0
# report as damaged to request repairs
defines.BADBLOCK_BEHAVIOR = 'LFS3_EMUBD_BADBLOCK_READDAMAGED'
code = '''
    lfs3_t lfs3;
    lfs3_format(&lfs3,
            LFS3_F_RDWR
                | ((GBMAP) ? LFS3_IFDEF_GBMAP(LFS3_F_GBMAP, -1) : 0),
            CFG) => 0;
    lfs3_mount(&lfs3, LFS3_M_RDWR, CFG) => 0;

    uint32_t prng = 42;

    // write to our mroot until it's not the mrootanchor
    //
    // we can't evict the mrootanchor
    lfs3_file_t file;
    lfs3_file_open(&lfs3, &file, "jellyfish",
            LFS3_O_RDWR | LFS3_O_CREAT | LFS3_O_EXCL) => 0;
    uint8_t wbuf[SIZE];
    while (file.h.mdir.r.blocks[0] == 0
            || file.h.mdir.r.blocks[0] == 1) {
        lfs3_file_rewind(&lfs3, &file) => 0;
        for (lfs3_size_t j = 0; j < SIZE; j++) {
            wbuf[j] = 'a' + (TEST_PRNG(&prng) % 26);
        }
        lfs3_file_write(&lfs3, &file, wbuf, SIZE) => SIZE;
        lfs3_file_sync(&lfs3, &file) => 0;
    }

    // I'm too lazy to do this correctly, so just spam evictions until
    // we extend again
    lfs3_block_t victim = 0;
    for (lfs3_block_t i = 0;; i++) {
        // a bit hacky, but this catches infinite loops
        assert(i < 2*BLOCK_COUNT);

        lfs3_sblock_t count = lfs3_fs_usage(&lfs3);
        assert(count >= 0);
        if (count >= 6) {
            assert(count == 6 || count == 7); // maybe gbmap
            break;
        }

        // get a block to evict
        victim = file.h.mdir.r.blocks[SIBLING];

        // make bad
        lfs3_emubd_mkbad(CFG, victim) => 0;
        // reset ck flags
        lfs3_fs_unck(&lfs3, LFS3_GC_CKMETA) => 0;

        // run gc to repair bad blocks
        lfs3_gc_t gc;
        lfs3_gc_open(&lfs3, &gc,
                LFS3_GC_WRONLY
                    | LFS3_GC_CKMETA
                    | LFS3_GC_REPAIRMETA) => 0;
        for (lfs3_block_t i = 0;; i++) {
            // a bit hacky, but this catches infinite loops
            assert(i < 2*BLOCK_COUNT);

            lfs3_sblock_t steps = lfs3_gc_write(&lfs3, &gc, 1);
            assert(steps >= 0);
            if (steps == 0) {
                break;
            }
        }
        lfs3_gc_close(&lfs3, &gc) => 0;

        // evicted block should no longer be in use
        assert(file.h.mdir.r.blocks[0] != victim);
        assert(file.h.mdir.r.blocks[1] != victim);
    }

    // evicted block should no longer be in use, anywhere in the
    // filesystem
    lfs3_trv_t trv;
    lfs3_trv_open(&lfs3, &trv, LFS3_T_RDONLY) => 0;
    for (lfs3_block_t i = 0;; i++) {
        assert(i < 2*BLOCK_COUNT);

        struct lfs3_tinfo tinfo;
        int err = lfs3_trv_read(&lfs3, &trv, &tinfo);
        assert(!err || err == LFS3_ERR_NOENT);
        if (err == LFS3_ERR_NOENT) {
            break;
        }

        assert(tinfo.block != victim);
    }
    lfs3_trv_close(&lfs3, &trv) => 0;

    // check we can still read the file
    for (int remount = 0; remount < 2; remount++) {
        // remount?
        if (remount) {
            lfs3_file_close(&lfs3, &file) => 0;
            lfs3_unmount(&lfs3) => 0;
            lfs3_mount(&lfs3, LFS3_M_RDWR, CFG) => 0;
            lfs3_file_open(&lfs3, &file, "jellyfish", LFS3_O_RDONLY) => 0;
        }

        lfs3_file_rewind(&lfs3, &file) => 0;
        uint8_t rbuf[SIZE];
        lfs3_file_read(&lfs3, &file, rbuf, SIZE) => SIZE;
        assert(memcmp(rbuf, wbuf, SIZE) == 0);
    }

    lfs3_file_close(&lfs3, &file) => 0;
    lfs3_unmount(&lfs3) => 0;
'''

[cases.test_evict_repair_t_mdir_mroot_chain]
defines.SIZE = 'FCACHE_SIZE/2'
defines.SIBLING = [0]
# force early relocations
defines.BLOCK_RECYCLES = 0
# report as damaged to request repairs
defines.BADBLOCK_BEHAVIOR = 'LFS3_EMUBD_BADBLOCK_READDAMAGED'
code = '''
    lfs3_t lfs3;
    lfs3_format(&lfs3,
            LFS3_F_RDWR
                | ((GBMAP) ? LFS3_IFDEF_GBMAP(LFS3_F_GBMAP, -1) : 0),
            CFG) => 0;
    lfs3_mount(&lfs3, LFS3_M_RDWR, CFG) => 0;

    uint32_t prng = 42;

    // write to our mroot until it's not the mrootanchor
    //
    // we can't evict the mrootanchor
    lfs3_file_t file;
    lfs3_file_open(&lfs3, &file, "jellyfish",
            LFS3_O_RDWR | LFS3_O_CREAT | LFS3_O_EXCL) => 0;
    uint8_t wbuf[SIZE];
    while (file.h.mdir.r.blocks[0] == 0
            || file.h.mdir.r.blocks[0] == 1) {
        lfs3_file_rewind(&lfs3, &file) => 0;
        for (lfs3_size_t j = 0; j < SIZE; j++) {
            wbuf[j] = 'a' + (TEST_PRNG(&prng) % 26);
        }
        lfs3_file_write(&lfs3, &file, wbuf, SIZE) => SIZE;
        lfs3_file_sync(&lfs3, &file) => 0;
    }

    // I'm too lazy to do this correctly, so just spam evictions until
    // we extend again
    lfs3_block_t victim = 0;
    for (lfs3_block_t i = 0;; i++) {
        // a bit hacky, but this catches infinite loops
        assert(i < 2*BLOCK_COUNT);

        lfs3_sblock_t count = lfs3_fs_usage(&lfs3);
        assert(count >= 0);
        if (count >= 6) {
            assert(count == 6 || count == 7); // maybe gbmap
            break;
        }

        // get a block to evict
        victim = file.h.mdir.r.blocks[SIBLING];

        // make bad
        lfs3_emubd_mkbad(CFG, victim) => 0;
        // reset ck flags
        lfs3_fs_unck(&lfs3, LFS3_GC_CKMETA) => 0;

        // run gc to repair bad blocks
        lfs3_gc_t gc;
        lfs3_gc_open(&lfs3, &gc,
                LFS3_GC_WRONLY
                    | LFS3_GC_CKMETA
                    | LFS3_GC_REPAIRMETA) => 0;
        for (lfs3_block_t i = 0;; i++) {
            // a bit hacky, but this catches infinite loops
            assert(i < 2*BLOCK_COUNT);

            lfs3_sblock_t steps = lfs3_gc_write(&lfs3, &gc, 1);
            assert(steps >= 0);
            if (steps == 0) {
                break;
            }
        }
        lfs3_gc_close(&lfs3, &gc) => 0;

        // evicted block should no longer be in use
        assert(file.h.mdir.r.blocks[0] != victim);
        assert(file.h.mdir.r.blocks[1] != victim);
    }

    // now we want to evict the mdir in our mroot chain, that is neither
    // the active mroot, nor the mrootanchor
    lfs3_trv_t trv;
    lfs3_trv_open(&lfs3, &trv, LFS3_T_RDONLY) => 0;
    for (lfs3_block_t i = 0;; i++) {
        assert(i < 2*BLOCK_COUNT);

        struct lfs3_tinfo tinfo;
        int err = lfs3_trv_read(&lfs3, &trv, &tinfo);
        assert(!err || err == LFS3_ERR_NOENT);
        if (err == LFS3_ERR_NOENT) {
            break;
        }

        if (i == 2 + SIBLING) {
            victim = tinfo.block;
            break;
        }
    }
    lfs3_trv_close(&lfs3, &trv) => 0;
    
    // make bad
    lfs3_emubd_mkbad(CFG, victim) => 0;
    // reset ck flags
    lfs3_fs_unck(&lfs3, LFS3_GC_CKMETA) => 0;

    // run gc to repair bad blocks
    lfs3_gc_t gc;
    lfs3_gc_open(&lfs3, &gc,
            LFS3_GC_WRONLY
                | LFS3_GC_CKMETA
                | LFS3_GC_REPAIRMETA) => 0;
    for (lfs3_block_t i = 0;; i++) {
        // a bit hacky, but this catches infinite loops
        assert(i < 2*BLOCK_COUNT);

        lfs3_sblock_t steps = lfs3_gc_write(&lfs3, &gc, 1);
        assert(steps >= 0);
        if (steps == 0) {
            break;
        }
    }
    lfs3_gc_close(&lfs3, &gc) => 0;

    // evicted block should no longer be in use, anywhere in the
    // filesystem
    lfs3_trv_open(&lfs3, &trv, LFS3_T_RDONLY) => 0;
    for (lfs3_block_t i = 0;; i++) {
        assert(i < 2*BLOCK_COUNT);

        struct lfs3_tinfo tinfo;
        int err = lfs3_trv_read(&lfs3, &trv, &tinfo);
        assert(!err || err == LFS3_ERR_NOENT);
        if (err == LFS3_ERR_NOENT) {
            break;
        }

        assert(tinfo.block != victim);
    }
    lfs3_trv_close(&lfs3, &trv) => 0;

    // check we can still read the file
    for (int remount = 0; remount < 2; remount++) {
        // remount?
        if (remount) {
            lfs3_file_close(&lfs3, &file) => 0;
            lfs3_unmount(&lfs3) => 0;
            lfs3_mount(&lfs3, LFS3_M_RDWR, CFG) => 0;
            lfs3_file_open(&lfs3, &file, "jellyfish", LFS3_O_RDONLY) => 0;
        }

        lfs3_file_rewind(&lfs3, &file) => 0;
        uint8_t rbuf[SIZE];
        lfs3_file_read(&lfs3, &file, rbuf, SIZE) => SIZE;
        assert(memcmp(rbuf, wbuf, SIZE) == 0);
    }

    lfs3_file_close(&lfs3, &file) => 0;
    lfs3_unmount(&lfs3) => 0;
'''

[cases.test_evict_repair_t_mdir_mroot_split]
defines.SIZE = 'FCACHE_SIZE/2'
defines.SIBLING = [0, 2]
# force early relocations
defines.BLOCK_RECYCLES = 0
# report as damaged to request repairs
defines.BADBLOCK_BEHAVIOR = 'LFS3_EMUBD_BADBLOCK_READDAMAGED'
in = 'lfs3.c'
code = '''
    lfs3_t lfs3;
    lfs3_format(&lfs3,
            LFS3_F_RDWR
                | ((GBMAP) ? LFS3_IFDEF_GBMAP(LFS3_F_GBMAP, -1) : 0),
            CFG) => 0;
    lfs3_mount(&lfs3, LFS3_M_RDWR, CFG) => 0;

    uint32_t prng = 42;

    // create two files
    lfs3_file_t file1;
    lfs3_file_open(&lfs3, &file1, "jellyfish",
            LFS3_O_RDWR | LFS3_O_CREAT | LFS3_O_EXCL) => 0;
    uint8_t wbuf1[SIZE];
    for (lfs3_size_t j = 0; j < SIZE; j++) {
        wbuf1[j] = 'a' + (TEST_PRNG(&prng) % 26);
    }
    lfs3_file_write(&lfs3, &file1, wbuf1, SIZE) => SIZE;
    lfs3_file_sync(&lfs3, &file1) => 0;

    lfs3_file_t file2;
    lfs3_file_open(&lfs3, &file2, "octopus",
            LFS3_O_RDWR | LFS3_O_CREAT | LFS3_O_EXCL) => 0;
    uint8_t wbuf2[SIZE];
    for (lfs3_size_t j = 0; j < SIZE; j++) {
        wbuf2[j] = 'a' + (TEST_PRNG(&prng) % 26);
    }
    lfs3_file_write(&lfs3, &file2, wbuf2, SIZE) => SIZE;
    lfs3_file_sync(&lfs3, &file2) => 0;

    // write to our mroot until it's not the mrootanchor
    //
    // we can't evict the mrootanchor
    while (file1.h.mdir.r.blocks[0] == 0
            || file1.h.mdir.r.blocks[0] == 1) {
        lfs3_file_rewind(&lfs3, &file1) => 0;
        for (lfs3_size_t j = 0; j < SIZE; j++) {
            wbuf1[j] = 'a' + (TEST_PRNG(&prng) % 26);
        }
        lfs3_file_write(&lfs3, &file1, wbuf1, SIZE) => SIZE;
        lfs3_file_sync(&lfs3, &file1) => 0;
    }

    // create enough files to both compact and split
    lfs3_size_t i = 0;
    while (true) {
        // we should not have split yet
        assert(lfs3.mtree.weight == 0);
        // we need internals to check this
        lfs3_ssize_t estimate = lfs3_mdir_estimate__(&lfs3,
                &file1.h.mdir, -1, -1,
                NULL);
        assert(estimate >= 0);
        if ((file1.h.mdir.r.eoff & 0x7fffffff) > GC_COMPACTMETA_THRESH
                && estimate > BLOCK_SIZE/2) {
            break;
        }

        char name[256];
        sprintf(name, "medusaaaaaaaaaaaaaaaa%03x", i);
        lfs3_file_t file;
        lfs3_file_open(&lfs3, &file, name,
                LFS3_O_WRONLY | LFS3_O_CREAT | LFS3_O_EXCL) => 0;
        lfs3_file_close(&lfs3, &file) => 0;
        i += 1;
    }

    // get a block to evict
    lfs3_block_t victim = (SIBLING < 2)
            ? file1.h.mdir.r.blocks[SIBLING % 2]
            : file2.h.mdir.r.blocks[SIBLING % 2];

    // make bad
    lfs3_emubd_mkbad(CFG, victim) => 0;
    // reset ck flags
    lfs3_fs_unck(&lfs3, LFS3_GC_CKMETA) => 0;

    // run gc to repair bad blocks
    lfs3_gc_t gc;
    lfs3_gc_open(&lfs3, &gc,
            LFS3_GC_WRONLY
                | LFS3_GC_CKMETA
                | LFS3_GC_REPAIRMETA) => 0;
    for (lfs3_block_t i = 0;; i++) {
        // a bit hacky, but this catches infinite loops
        assert(i < 2*BLOCK_COUNT);

        lfs3_sblock_t steps = lfs3_gc_write(&lfs3, &gc, 1);
        assert(steps >= 0);
        if (steps == 0) {
            break;
        }
    }
    lfs3_gc_close(&lfs3, &gc) => 0;

    // evicted block should no longer be in use
    assert(file1.h.mdir.r.blocks[0] != victim);
    assert(file1.h.mdir.r.blocks[1] != victim);
    assert(file2.h.mdir.r.blocks[0] != victim);
    assert(file2.h.mdir.r.blocks[1] != victim);
    
    // evicted block should no longer be in use, anywhere in the
    // filesystem
    lfs3_trv_t trv;
    lfs3_trv_open(&lfs3, &trv, LFS3_T_RDONLY) => 0;
    for (lfs3_block_t i = 0;; i++) {
        assert(i < 2*BLOCK_COUNT);

        struct lfs3_tinfo tinfo;
        int err = lfs3_trv_read(&lfs3, &trv, &tinfo);
        assert(!err || err == LFS3_ERR_NOENT);
        if (err == LFS3_ERR_NOENT) {
            break;
        }

        assert(tinfo.block != victim);
    }
    lfs3_trv_close(&lfs3, &trv) => 0;

    // check we can still read the files
    for (int remount = 0; remount < 2; remount++) {
        // remount?
        if (remount) {
            lfs3_file_close(&lfs3, &file1) => 0;
            lfs3_file_close(&lfs3, &file2) => 0;
            lfs3_unmount(&lfs3) => 0;
            lfs3_mount(&lfs3, LFS3_M_RDWR, CFG) => 0;
            lfs3_file_open(&lfs3, &file1, "jellyfish", LFS3_O_RDONLY) => 0;
            lfs3_file_open(&lfs3, &file2, "octopus", LFS3_O_RDONLY) => 0;
        }

        lfs3_file_rewind(&lfs3, &file1) => 0;
        uint8_t rbuf[SIZE];
        lfs3_file_read(&lfs3, &file1, rbuf, SIZE) => SIZE;
        assert(memcmp(rbuf, wbuf1, SIZE) == 0);

        lfs3_file_rewind(&lfs3, &file2) => 0;
        lfs3_file_read(&lfs3, &file2, rbuf, SIZE) => SIZE;
        assert(memcmp(rbuf, wbuf2, SIZE) == 0);
    }

    lfs3_file_close(&lfs3, &file1) => 0;
    lfs3_file_close(&lfs3, &file2) => 0;
    lfs3_unmount(&lfs3) => 0;
'''

[cases.test_evict_repair_t_mdir_mtree]
defines.SIZE = 'FCACHE_SIZE/2'
defines.SIBLING = [0, 2, 4]
# report as damaged to request repairs
defines.BADBLOCK_BEHAVIOR = 'LFS3_EMUBD_BADBLOCK_READDAMAGED'
code = '''
    lfs3_t lfs3;
    lfs3_format(&lfs3,
            LFS3_F_RDWR
                | ((GBMAP) ? LFS3_IFDEF_GBMAP(LFS3_F_GBMAP, -1) : 0),
            CFG) => 0;
    lfs3_mount(&lfs3, LFS3_M_RDWR, CFG) => 0;

    uint32_t prng = 42;

    // create three files
    lfs3_file_t file1;
    lfs3_file_open(&lfs3, &file1, "cuttlefish",
            LFS3_O_RDWR | LFS3_O_CREAT | LFS3_O_EXCL) => 0;
    uint8_t wbuf1[SIZE];
    for (lfs3_size_t j = 0; j < SIZE; j++) {
        wbuf1[j] = 'a' + (TEST_PRNG(&prng) % 26);
    }
    lfs3_file_write(&lfs3, &file1, wbuf1, SIZE) => SIZE;
    lfs3_file_sync(&lfs3, &file1) => 0;

    lfs3_file_t file2;
    lfs3_file_open(&lfs3, &file2, "jellyfish",
            LFS3_O_RDWR | LFS3_O_CREAT | LFS3_O_EXCL) => 0;
    uint8_t wbuf2[SIZE];
    for (lfs3_size_t j = 0; j < SIZE; j++) {
        wbuf2[j] = 'a' + (TEST_PRNG(&prng) % 26);
    }
    lfs3_file_write(&lfs3, &file2, wbuf2, SIZE) => SIZE;
    lfs3_file_sync(&lfs3, &file2) => 0;

    lfs3_file_t file3;
    lfs3_file_open(&lfs3, &file3, "octopus",
            LFS3_O_RDWR | LFS3_O_CREAT | LFS3_O_EXCL) => 0;
    uint8_t wbuf3[SIZE];
    for (lfs3_size_t j = 0; j < SIZE; j++) {
        wbuf3[j] = 'a' + (TEST_PRNG(&prng) % 26);
    }
    lfs3_file_write(&lfs3, &file3, wbuf3, SIZE) => SIZE;
    lfs3_file_sync(&lfs3, &file3) => 0;

    // create enough files for mroot to split twice
    lfs3_size_t i = 0;
    while (lfs3.mtree.weight == 0) {
        char name[256];
        sprintf(name, "hydroid%03x", i);
        lfs3_file_t file;
        lfs3_file_open(&lfs3, &file, name,
                LFS3_O_WRONLY | LFS3_O_CREAT | LFS3_O_EXCL) => 0;
        lfs3_file_close(&lfs3, &file) => 0;
        i += 1;
    }

    i = 0;
    lfs3_size_t orig = lfs3.mtree.weight;
    while (lfs3.mtree.weight == orig) {
        char name[256];
        sprintf(name, "medusa%03x", i);
        lfs3_file_t file;
        lfs3_file_open(&lfs3, &file, name,
                LFS3_O_WRONLY | LFS3_O_CREAT | LFS3_O_EXCL) => 0;
        lfs3_file_close(&lfs3, &file) => 0;
        i += 1;
    }

    // get a block to evict
    lfs3_block_t victim
            = (SIBLING < 2)
                ? file1.h.mdir.r.blocks[SIBLING % 2]
            : (SIBLING < 4)
                ? file2.h.mdir.r.blocks[SIBLING % 2]
                : file3.h.mdir.r.blocks[SIBLING % 2];

    // make bad
    lfs3_emubd_mkbad(CFG, victim) => 0;
    // reset ck flags
    lfs3_fs_unck(&lfs3, LFS3_GC_CKMETA) => 0;

    // run gc to repair bad blocks
    lfs3_gc_t gc;
    lfs3_gc_open(&lfs3, &gc,
            LFS3_GC_WRONLY
                | LFS3_GC_CKMETA
                | LFS3_GC_REPAIRMETA) => 0;
    for (lfs3_block_t i = 0;; i++) {
        // a bit hacky, but this catches infinite loops
        assert(i < 2*BLOCK_COUNT);

        lfs3_sblock_t steps = lfs3_gc_write(&lfs3, &gc, 1);
        assert(steps >= 0);
        if (steps == 0) {
            break;
        }
    }
    lfs3_gc_close(&lfs3, &gc) => 0;

    // evicted block should no longer be in use
    assert(file1.h.mdir.r.blocks[0] != victim);
    assert(file1.h.mdir.r.blocks[1] != victim);
    assert(file2.h.mdir.r.blocks[0] != victim);
    assert(file2.h.mdir.r.blocks[1] != victim);
    assert(file3.h.mdir.r.blocks[0] != victim);
    assert(file3.h.mdir.r.blocks[1] != victim);

    // evicted block should no longer be in use, anywhere in the
    // filesystem
    lfs3_trv_t trv;
    lfs3_trv_open(&lfs3, &trv, LFS3_T_RDONLY) => 0;
    for (lfs3_block_t i = 0;; i++) {
        assert(i < 2*BLOCK_COUNT);

        struct lfs3_tinfo tinfo;
        int err = lfs3_trv_read(&lfs3, &trv, &tinfo);
        assert(!err || err == LFS3_ERR_NOENT);
        if (err == LFS3_ERR_NOENT) {
            break;
        }

        assert(tinfo.block != victim);
    }
    lfs3_trv_close(&lfs3, &trv) => 0;

    // check we can still read the files
    for (int remount = 0; remount < 2; remount++) {
        // remount?
        if (remount) {
            lfs3_file_close(&lfs3, &file1) => 0;
            lfs3_file_close(&lfs3, &file2) => 0;
            lfs3_file_close(&lfs3, &file3) => 0;
            lfs3_unmount(&lfs3) => 0;
            lfs3_mount(&lfs3, LFS3_M_RDWR, CFG) => 0;
            lfs3_file_open(&lfs3, &file1, "cuttlefish", LFS3_O_RDONLY) => 0;
            lfs3_file_open(&lfs3, &file2, "jellyfish", LFS3_O_RDONLY) => 0;
            lfs3_file_open(&lfs3, &file3, "octopus", LFS3_O_RDONLY) => 0;
        }

        lfs3_file_rewind(&lfs3, &file1) => 0;
        uint8_t rbuf[SIZE];
        lfs3_file_read(&lfs3, &file1, rbuf, SIZE) => SIZE;
        assert(memcmp(rbuf, wbuf1, SIZE) == 0);

        lfs3_file_rewind(&lfs3, &file2) => 0;
        lfs3_file_read(&lfs3, &file2, rbuf, SIZE) => SIZE;
        assert(memcmp(rbuf, wbuf2, SIZE) == 0);

        lfs3_file_rewind(&lfs3, &file3) => 0;
        lfs3_file_read(&lfs3, &file3, rbuf, SIZE) => SIZE;
        assert(memcmp(rbuf, wbuf3, SIZE) == 0);
    }

    lfs3_file_close(&lfs3, &file1) => 0;
    lfs3_file_close(&lfs3, &file2) => 0;
    lfs3_file_close(&lfs3, &file3) => 0;
    lfs3_unmount(&lfs3) => 0;
'''

[cases.test_evict_repair_t_mdir_mtree_split]
defines.SIZE = 'FCACHE_SIZE/2'
defines.SIBLING = [0, 2, 4, 6]
# report as damaged to request repairs
defines.BADBLOCK_BEHAVIOR = 'LFS3_EMUBD_BADBLOCK_READDAMAGED'
in = 'lfs3.c'
code = '''
    lfs3_t lfs3;
    lfs3_format(&lfs3,
            LFS3_F_RDWR
                | ((GBMAP) ? LFS3_IFDEF_GBMAP(LFS3_F_GBMAP, -1) : 0),
            CFG) => 0;
    lfs3_mount(&lfs3, LFS3_M_RDWR, CFG) => 0;

    uint32_t prng = 42;

    // create four files
    lfs3_file_t file1;
    lfs3_file_open(&lfs3, &file1, "cuttlefish",
            LFS3_O_RDWR | LFS3_O_CREAT | LFS3_O_EXCL) => 0;
    uint8_t wbuf1[SIZE];
    for (lfs3_size_t j = 0; j < SIZE; j++) {
        wbuf1[j] = 'a' + (TEST_PRNG(&prng) % 26);
    }
    lfs3_file_write(&lfs3, &file1, wbuf1, SIZE) => SIZE;
    lfs3_file_sync(&lfs3, &file1) => 0;

    lfs3_file_t file2;
    lfs3_file_open(&lfs3, &file2, "jellyfish",
            LFS3_O_RDWR | LFS3_O_CREAT | LFS3_O_EXCL) => 0;
    uint8_t wbuf2[SIZE];
    for (lfs3_size_t j = 0; j < SIZE; j++) {
        wbuf2[j] = 'a' + (TEST_PRNG(&prng) % 26);
    }
    lfs3_file_write(&lfs3, &file2, wbuf2, SIZE) => SIZE;
    lfs3_file_sync(&lfs3, &file2) => 0;

    lfs3_file_t file3;
    lfs3_file_open(&lfs3, &file3, "octopus",
            LFS3_O_RDWR | LFS3_O_CREAT | LFS3_O_EXCL) => 0;
    uint8_t wbuf3[SIZE];
    for (lfs3_size_t j = 0; j < SIZE; j++) {
        wbuf3[j] = 'a' + (TEST_PRNG(&prng) % 26);
    }
    lfs3_file_write(&lfs3, &file3, wbuf3, SIZE) => SIZE;
    lfs3_file_sync(&lfs3, &file3) => 0;

    lfs3_file_t file4;
    lfs3_file_open(&lfs3, &file4, "squid",
            LFS3_O_RDWR | LFS3_O_CREAT | LFS3_O_EXCL) => 0;
    uint8_t wbuf4[SIZE];
    for (lfs3_size_t j = 0; j < SIZE; j++) {
        wbuf4[j] = 'a' + (TEST_PRNG(&prng) % 26);
    }
    lfs3_file_write(&lfs3, &file4, wbuf4, SIZE) => SIZE;
    lfs3_file_sync(&lfs3, &file4) => 0;

    // create enough files for mroot to split twice
    lfs3_size_t i = 0;
    while (lfs3.mtree.weight == 0) {
        char name[256];
        sprintf(name, "hydroid%03x", i);
        lfs3_file_t file;
        lfs3_file_open(&lfs3, &file, name,
                LFS3_O_WRONLY | LFS3_O_CREAT | LFS3_O_EXCL) => 0;
        lfs3_file_close(&lfs3, &file) => 0;
        i += 1;
    }

    i = 0;
    lfs3_size_t orig = lfs3.mtree.weight;
    while (lfs3.mtree.weight == orig) {
        char name[256];
        sprintf(name, "polyp%03x", i);
        lfs3_file_t file;
        lfs3_file_open(&lfs3, &file, name,
                LFS3_O_WRONLY | LFS3_O_CREAT | LFS3_O_EXCL) => 0;
        lfs3_file_close(&lfs3, &file) => 0;
        i += 1;
    }

    // create enough files to both compact and split
    i = 0;
    orig = lfs3.mtree.weight;
    while (true) {
        // we should not have split yet
        assert(lfs3.mtree.weight == orig);
        // we need internals to check this
        lfs3_ssize_t estimate = lfs3_mdir_estimate__(&lfs3,
                &file2.h.mdir, -1, -1,
                NULL);
        assert(estimate >= 0);
        if ((file2.h.mdir.r.eoff & 0x7fffffff) > GC_COMPACTMETA_THRESH
                && estimate > BLOCK_SIZE/2) {
            break;
        }

        char name[256];
        sprintf(name, "medusaaaaaaaaaaaaaaaa%03x", i);
        lfs3_file_t file;
        lfs3_file_open(&lfs3, &file, name,
                LFS3_O_WRONLY | LFS3_O_CREAT | LFS3_O_EXCL) => 0;
        lfs3_file_close(&lfs3, &file) => 0;
        i += 1;
    }

    // get a block to evict
    lfs3_block_t victim
            = (SIBLING < 2)
                ? file1.h.mdir.r.blocks[SIBLING % 2]
            : (SIBLING < 4)
                ? file2.h.mdir.r.blocks[SIBLING % 2]
            : (SIBLING < 6)
                ? file3.h.mdir.r.blocks[SIBLING % 2]
                : file4.h.mdir.r.blocks[SIBLING % 2];

    // make bad
    lfs3_emubd_mkbad(CFG, victim) => 0;
    // reset ck flags
    lfs3_fs_unck(&lfs3, LFS3_GC_CKMETA) => 0;

    // run gc to repair bad blocks
    lfs3_gc_t gc;
    lfs3_gc_open(&lfs3, &gc,
            LFS3_GC_WRONLY
                | LFS3_GC_CKMETA
                | LFS3_GC_REPAIRMETA) => 0;
    for (lfs3_block_t i = 0;; i++) {
        // a bit hacky, but this catches infinite loops
        assert(i < 2*BLOCK_COUNT);

        lfs3_sblock_t steps = lfs3_gc_write(&lfs3, &gc, 1);
        assert(steps >= 0);
        if (steps == 0) {
            break;
        }
    }
    lfs3_gc_close(&lfs3, &gc) => 0;

    // evicted block should no longer be in use
    assert(file1.h.mdir.r.blocks[0] != victim);
    assert(file1.h.mdir.r.blocks[1] != victim);
    assert(file2.h.mdir.r.blocks[0] != victim);
    assert(file2.h.mdir.r.blocks[1] != victim);
    assert(file3.h.mdir.r.blocks[0] != victim);
    assert(file3.h.mdir.r.blocks[1] != victim);
    assert(file4.h.mdir.r.blocks[0] != victim);
    assert(file4.h.mdir.r.blocks[1] != victim);

    // check we can still read the files
    for (int remount = 0; remount < 2; remount++) {
        // remount?
        if (remount) {
            lfs3_file_close(&lfs3, &file1) => 0;
            lfs3_file_close(&lfs3, &file2) => 0;
            lfs3_file_close(&lfs3, &file3) => 0;
            lfs3_file_close(&lfs3, &file4) => 0;
            lfs3_unmount(&lfs3) => 0;
            lfs3_mount(&lfs3, LFS3_M_RDWR, CFG) => 0;
            lfs3_file_open(&lfs3, &file1, "cuttlefish", LFS3_O_RDONLY) => 0;
            lfs3_file_open(&lfs3, &file2, "jellyfish", LFS3_O_RDONLY) => 0;
            lfs3_file_open(&lfs3, &file3, "octopus", LFS3_O_RDONLY) => 0;
            lfs3_file_open(&lfs3, &file4, "squid", LFS3_O_RDONLY) => 0;
        }

        lfs3_file_rewind(&lfs3, &file1) => 0;
        uint8_t rbuf[SIZE];
        lfs3_file_read(&lfs3, &file1, rbuf, SIZE) => SIZE;
        assert(memcmp(rbuf, wbuf1, SIZE) == 0);

        lfs3_file_rewind(&lfs3, &file2) => 0;
        lfs3_file_read(&lfs3, &file2, rbuf, SIZE) => SIZE;
        assert(memcmp(rbuf, wbuf2, SIZE) == 0);

        lfs3_file_rewind(&lfs3, &file3) => 0;
        lfs3_file_read(&lfs3, &file3, rbuf, SIZE) => SIZE;
        assert(memcmp(rbuf, wbuf3, SIZE) == 0);

        lfs3_file_rewind(&lfs3, &file4) => 0;
        lfs3_file_read(&lfs3, &file4, rbuf, SIZE) => SIZE;
        assert(memcmp(rbuf, wbuf4, SIZE) == 0);
    }

    lfs3_file_close(&lfs3, &file1) => 0;
    lfs3_file_close(&lfs3, &file2) => 0;
    lfs3_file_close(&lfs3, &file3) => 0;
    lfs3_file_close(&lfs3, &file4) => 0;
    lfs3_unmount(&lfs3) => 0;
'''


# test traversals with btree repair

[cases.test_evict_repair_t_btree_mtree]
defines.SIZE = 'FCACHE_SIZE/2'
# force early relocations
defines.BLOCK_RECYCLES = 0
# report as damaged to request repairs
defines.BADBLOCK_BEHAVIOR = 'LFS3_EMUBD_BADBLOCK_READDAMAGED'
code = '''
    lfs3_t lfs3;
    lfs3_format(&lfs3,
            LFS3_F_RDWR
                | ((GBMAP) ? LFS3_IFDEF_GBMAP(LFS3_F_GBMAP, -1) : 0),
            CFG) => 0;
    lfs3_mount(&lfs3, LFS3_M_RDWR, CFG) => 0;

    uint32_t prng = 42;

    // create two files
    lfs3_file_t file1;
    lfs3_file_open(&lfs3, &file1, "jellyfish",
            LFS3_O_RDWR | LFS3_O_CREAT | LFS3_O_EXCL) => 0;
    uint8_t wbuf1[SIZE];
    for (lfs3_size_t j = 0; j < SIZE; j++) {
        wbuf1[j] = 'a' + (TEST_PRNG(&prng) % 26);
    }
    lfs3_file_write(&lfs3, &file1, wbuf1, SIZE) => SIZE;
    lfs3_file_sync(&lfs3, &file1) => 0;

    lfs3_file_t file2;
    lfs3_file_open(&lfs3, &file2, "octopus",
            LFS3_O_RDWR | LFS3_O_CREAT | LFS3_O_EXCL) => 0;
    uint8_t wbuf2[SIZE];
    for (lfs3_size_t j = 0; j < SIZE; j++) {
        wbuf2[j] = 'a' + (TEST_PRNG(&prng) % 26);
    }
    lfs3_file_write(&lfs3, &file2, wbuf2, SIZE) => SIZE;
    lfs3_file_sync(&lfs3, &file2) => 0;

    // create more files until we have an mtree
    lfs3_size_t i = 0;
    // hack, don't use the internals like this
    while (lfs3.mtree.weight == 0) {
        char name[256];
        sprintf(name, "medusaaaaaaaaaaaaaaaa%03x", i);
        lfs3_file_t file;
        lfs3_file_open(&lfs3, &file, name,
                LFS3_O_WRONLY | LFS3_O_CREAT | LFS3_O_EXCL) => 0;
        // let's speed this up a bit
        file.h.mdir.r.eoff = -1;
        lfs3_file_close(&lfs3, &file) => 0;
        i += 1;
    }

    // get a block to evict
    lfs3_block_t victim = lfs3.mtree.blocks[0];

    // make bad
    lfs3_emubd_mkbad(CFG, victim) => 0;
    // reset ck flags
    lfs3_fs_unck(&lfs3, LFS3_GC_CKMETA) => 0;

    // run gc to repair bad blocks
    lfs3_gc_t gc;
    lfs3_gc_open(&lfs3, &gc,
            LFS3_GC_WRONLY
                | LFS3_GC_CKMETA
                | LFS3_GC_REPAIRMETA) => 0;
    for (lfs3_block_t i = 0;; i++) {
        // a bit hacky, but this catches infinite loops
        assert(i < 2*BLOCK_COUNT);

        lfs3_sblock_t steps = lfs3_gc_write(&lfs3, &gc, 1);
        assert(steps >= 0);
        if (steps == 0) {
            break;
        }
    }
    lfs3_gc_close(&lfs3, &gc) => 0;

    // evicted block should no longer be in use
    assert(lfs3.mtree.blocks[0] != victim);

    // evicted block should no longer be in use, anywhere in the
    // filesystem
    lfs3_trv_t trv;
    lfs3_trv_open(&lfs3, &trv, LFS3_T_RDONLY) => 0;
    for (lfs3_block_t i = 0;; i++) {
        assert(i < 2*BLOCK_COUNT);

        struct lfs3_tinfo tinfo;
        int err = lfs3_trv_read(&lfs3, &trv, &tinfo);
        assert(!err || err == LFS3_ERR_NOENT);
        if (err == LFS3_ERR_NOENT) {
            break;
        }

        assert(tinfo.block != victim);
    }
    lfs3_trv_close(&lfs3, &trv) => 0;

    // check we can still read the files
    for (int remount = 0; remount < 2; remount++) {
        // remount?
        if (remount) {
            lfs3_file_close(&lfs3, &file1) => 0;
            lfs3_file_close(&lfs3, &file2) => 0;
            lfs3_unmount(&lfs3) => 0;
            lfs3_mount(&lfs3, LFS3_M_RDWR, CFG) => 0;
            lfs3_file_open(&lfs3, &file1, "jellyfish", LFS3_O_RDONLY) => 0;
            lfs3_file_open(&lfs3, &file2, "octopus", LFS3_O_RDONLY) => 0;
        }

        lfs3_file_rewind(&lfs3, &file1) => 0;
        uint8_t rbuf[SIZE];
        lfs3_file_read(&lfs3, &file1, rbuf, SIZE) => SIZE;
        assert(memcmp(rbuf, wbuf1, SIZE) == 0);

        lfs3_file_rewind(&lfs3, &file2) => 0;
        lfs3_file_read(&lfs3, &file2, rbuf, SIZE) => SIZE;
        assert(memcmp(rbuf, wbuf2, SIZE) == 0);
    }

    lfs3_file_close(&lfs3, &file1) => 0;
    lfs3_file_close(&lfs3, &file2) => 0;
    lfs3_unmount(&lfs3) => 0;
'''

[cases.test_evict_repair_t_btree_mtree_mtree]
defines.SIZE = 'FCACHE_SIZE/2'
defines.SMACK = 20
defines.SIBLING = [0, 1, 2]
# force early relocations
defines.BLOCK_RECYCLES = 0
# report as damaged to request repairs
defines.BADBLOCK_BEHAVIOR = 'LFS3_EMUBD_BADBLOCK_READDAMAGED'
code = '''
    lfs3_t lfs3;
    lfs3_format(&lfs3,
            LFS3_F_RDWR
                | ((GBMAP) ? LFS3_IFDEF_GBMAP(LFS3_F_GBMAP, -1) : 0),
            CFG) => 0;
    lfs3_mount(&lfs3, LFS3_M_RDWR, CFG) => 0;

    uint32_t prng = 42;

    // create two files
    lfs3_file_t file1;
    lfs3_file_open(&lfs3, &file1, "jellyfish",
            LFS3_O_RDWR | LFS3_O_CREAT | LFS3_O_EXCL) => 0;
    uint8_t wbuf1[SIZE];
    for (lfs3_size_t j = 0; j < SIZE; j++) {
        wbuf1[j] = 'a' + (TEST_PRNG(&prng) % 26);
    }
    lfs3_file_write(&lfs3, &file1, wbuf1, SIZE) => SIZE;
    lfs3_file_sync(&lfs3, &file1) => 0;

    lfs3_file_t file2;
    lfs3_file_open(&lfs3, &file2, "octopus",
            LFS3_O_RDWR | LFS3_O_CREAT | LFS3_O_EXCL) => 0;
    uint8_t wbuf2[SIZE];
    for (lfs3_size_t j = 0; j < SIZE; j++) {
        wbuf2[j] = 'a' + (TEST_PRNG(&prng) % 26);
    }
    lfs3_file_write(&lfs3, &file2, wbuf2, SIZE) => SIZE;
    lfs3_file_sync(&lfs3, &file2) => 0;

    // create more files until we have at least 3 btree nodes
    lfs3_size_t i = 0;
    while (true) {
        lfs3_size_t count = 0;
        lfs3_trv_t trv;
        lfs3_trv_open(&lfs3, &trv, LFS3_T_RDONLY) => 0;
        for (lfs3_block_t i = 0;; i++) {
            assert(i < 2*BLOCK_COUNT);

            struct lfs3_tinfo tinfo;
            int err = lfs3_trv_read(&lfs3, &trv, &tinfo);
            assert(!err || err == LFS3_ERR_NOENT);
            if (err == LFS3_ERR_NOENT) {
                break;
            }

            if (tinfo.btype == LFS3_BTYPE_BTREE) {
                count += 1;
            }
        }
        lfs3_trv_close(&lfs3, &trv) => 0;
        if (count >= 3 + ((GBMAP) ? 1 : 0)) {
            break;
        }

        for (lfs3_size_t k = 0; k < SMACK; k++) {
            char name[256];
            sprintf(name, "medusaaaaaaaaaaaaaaaa%03x", i);
            lfs3_file_t file;
            lfs3_file_open(&lfs3, &file, name,
                    LFS3_O_WRONLY | LFS3_O_CREAT | LFS3_O_EXCL) => 0;
            lfs3_file_close(&lfs3, &file) => 0;
            i += 1;
        }
    }

    // get a block to evict
    lfs3_block_t victim = 0;
    lfs3_size_t count = 0;
    lfs3_trv_t trv;
    lfs3_trv_open(&lfs3, &trv, LFS3_T_RDONLY) => 0;
    for (lfs3_block_t i = 0;; i++) {
        assert(i < 2*BLOCK_COUNT);

        struct lfs3_tinfo tinfo;
        int err = lfs3_trv_read(&lfs3, &trv, &tinfo);
        assert(!err);

        if (tinfo.btype == LFS3_BTYPE_BTREE) {
            if (count == SIBLING) {
                victim = tinfo.block;
                break;
            }

            count += 1;
        }
    }
    lfs3_trv_close(&lfs3, &trv) => 0;

    // make bad
    lfs3_emubd_mkbad(CFG, victim) => 0;
    // reset ck flags
    lfs3_fs_unck(&lfs3, LFS3_GC_CKMETA) => 0;

    // run gc to repair bad blocks
    lfs3_gc_t gc;
    lfs3_gc_open(&lfs3, &gc,
            LFS3_GC_WRONLY
                | LFS3_GC_CKMETA
                | LFS3_GC_REPAIRMETA) => 0;
    for (lfs3_block_t i = 0;; i++) {
        // a bit hacky, but this catches infinite loops
        assert(i < 2*BLOCK_COUNT);

        lfs3_sblock_t steps = lfs3_gc_write(&lfs3, &gc, 1);
        assert(steps >= 0);
        if (steps == 0) {
            break;
        }
    }
    lfs3_gc_close(&lfs3, &gc) => 0;

    // evicted block should no longer be in use, anywhere in the
    // filesystem
    lfs3_trv_open(&lfs3, &trv, LFS3_T_RDONLY) => 0;
    for (lfs3_block_t i = 0;; i++) {
        assert(i < 2*BLOCK_COUNT);

        struct lfs3_tinfo tinfo;
        int err = lfs3_trv_read(&lfs3, &trv, &tinfo);
        assert(!err || err == LFS3_ERR_NOENT);
        if (err == LFS3_ERR_NOENT) {
            break;
        }

        assert(tinfo.block != victim);
    }
    lfs3_trv_close(&lfs3, &trv) => 0;

    // check we can still read the files
    for (int remount = 0; remount < 2; remount++) {
        // remount?
        if (remount) {
            lfs3_file_close(&lfs3, &file1) => 0;
            lfs3_file_close(&lfs3, &file2) => 0;
            lfs3_unmount(&lfs3) => 0;
            lfs3_mount(&lfs3, LFS3_M_RDWR, CFG) => 0;
            lfs3_file_open(&lfs3, &file1, "jellyfish", LFS3_O_RDONLY) => 0;
            lfs3_file_open(&lfs3, &file2, "octopus", LFS3_O_RDONLY) => 0;
        }

        lfs3_file_rewind(&lfs3, &file1) => 0;
        uint8_t rbuf[SIZE];
        lfs3_file_read(&lfs3, &file1, rbuf, SIZE) => SIZE;
        assert(memcmp(rbuf, wbuf1, SIZE) == 0);

        lfs3_file_rewind(&lfs3, &file2) => 0;
        lfs3_file_read(&lfs3, &file2, rbuf, SIZE) => SIZE;
        assert(memcmp(rbuf, wbuf2, SIZE) == 0);
    }

    lfs3_file_close(&lfs3, &file1) => 0;
    lfs3_file_close(&lfs3, &file2) => 0;
    lfs3_unmount(&lfs3) => 0;
'''

[cases.test_evict_repair_t_btree_btree]
defines.CHUNK = 'BLOCK_SIZE/8'
defines.SYNC = [false, true]
defines.CLOSE = [false, true]
defines.SIBLING = [0]
# prevent block crystallization
defines.CRYSTAL_THRESH = -1
# report as damaged to request repairs
defines.BADBLOCK_BEHAVIOR = 'LFS3_EMUBD_BADBLOCK_READDAMAGED'
code = '''
    lfs3_t lfs3;
    lfs3_format(&lfs3,
            LFS3_F_RDWR
                | ((GBMAP) ? LFS3_IFDEF_GBMAP(LFS3_F_GBMAP, -1) : 0),
            CFG) => 0;
    lfs3_mount(&lfs3, LFS3_M_RDWR, CFG) => 0;

    uint32_t prng = 42;

    // create a file
    lfs3_file_t file;
    lfs3_file_open(&lfs3, &file, "jellyfish",
            LFS3_O_RDWR | LFS3_O_CREAT | LFS3_O_EXCL) => 0;

    // write to our file until we have a btree
    uint32_t wprng = prng;
    lfs3_off_t wsize = 0;
    // hack, don't use the internals like this
    while ((file.bshrub.trunk & 0x80000000)
            || file.bshrub.weight == 0) {
        uint8_t wbuf[CHUNK];
        for (lfs3_size_t j = 0; j < CHUNK; j++) {
            wbuf[j] = 'a' + (TEST_PRNG(&prng) % 26);
        }
        lfs3_file_write(&lfs3, &file, wbuf, CHUNK) => CHUNK;
        if (SYNC) {
            lfs3_file_sync(&lfs3, &file) => 0;
        }
        wsize += CHUNK;
    }

    // close during eviction? this makes sure eviction can find the
    // btree on disk
    if (CLOSE) {
        lfs3_file_close(&lfs3, &file) => 0;
    }

    // get a block to evict
    lfs3_block_t victim = file.bshrub.blocks[0];

    // make bad
    lfs3_emubd_mkbad(CFG, victim) => 0;
    // reset ck flags
    lfs3_fs_unck(&lfs3, LFS3_GC_CKMETA) => 0;

    // run gc to repair bad blocks
    lfs3_gc_t gc;
    lfs3_gc_open(&lfs3, &gc,
            LFS3_GC_WRONLY
                | LFS3_GC_CKMETA
                | LFS3_GC_REPAIRMETA) => 0;
    for (lfs3_block_t i = 0;; i++) {
        // a bit hacky, but this catches infinite loops
        assert(i < 2*BLOCK_COUNT);

        lfs3_sblock_t steps = lfs3_gc_write(&lfs3, &gc, 1);
        assert(steps >= 0);
        if (steps == 0) {
            break;
        }
    }
    lfs3_gc_close(&lfs3, &gc) => 0;

    // reopen if necessary
    if (CLOSE) {
        lfs3_file_open(&lfs3, &file, "jellyfish", LFS3_O_RDWR) => 0;
    }

    // evicted block should no longer be in use
    assert(file.bshrub.blocks[0] != victim);

    // evicted block should no longer be in use, anywhere in the
    // filesystem
    lfs3_trv_t trv;
    lfs3_trv_open(&lfs3, &trv, LFS3_T_RDONLY) => 0;
    for (lfs3_block_t i = 0;; i++) {
        assert(i < 2*BLOCK_COUNT);

        struct lfs3_tinfo tinfo;
        int err = lfs3_trv_read(&lfs3, &trv, &tinfo);
        assert(!err || err == LFS3_ERR_NOENT);
        if (err == LFS3_ERR_NOENT) {
            break;
        }

        assert(tinfo.block != victim);
    }
    lfs3_trv_close(&lfs3, &trv) => 0;

    // check we can still read the file
    for (int remount = 0; remount < 2; remount++) {
        // remount?
        if (remount) {
            lfs3_file_close(&lfs3, &file) => 0;
            lfs3_unmount(&lfs3) => 0;
            lfs3_mount(&lfs3, LFS3_M_RDWR, CFG) => 0;
            lfs3_file_open(&lfs3, &file, "jellyfish", LFS3_O_RDONLY) => 0;
        }

        lfs3_file_rewind(&lfs3, &file) => 0;
        prng = wprng;
        for (lfs3_off_t i = 0; i < wsize; i += CHUNK) {
            uint8_t wbuf[CHUNK];
            for (lfs3_size_t j = 0; j < CHUNK; j++) {
                wbuf[j] = 'a' + (TEST_PRNG(&prng) % 26);
            }
            uint8_t rbuf[CHUNK];
            lfs3_file_read(&lfs3, &file, rbuf, CHUNK) => CHUNK;
            assert(memcmp(rbuf, wbuf, CHUNK) == 0);
        }
    }

    lfs3_file_close(&lfs3, &file) => 0;
    lfs3_unmount(&lfs3) => 0;
'''

[cases.test_evict_repair_t_btree_btree_btree]
defines.CHUNK = 'BLOCK_SIZE/8'
defines.SYNC = [false, true]
defines.CLOSE = [false, true]
defines.SIBLING = [0, 1, 2]
# prevent block crystallization
defines.CRYSTAL_THRESH = -1
# report as damaged to request repairs
defines.BADBLOCK_BEHAVIOR = 'LFS3_EMUBD_BADBLOCK_READDAMAGED'
code = '''
    lfs3_t lfs3;
    lfs3_format(&lfs3,
            LFS3_F_RDWR
                | ((GBMAP) ? LFS3_IFDEF_GBMAP(LFS3_F_GBMAP, -1) : 0),
            CFG) => 0;
    lfs3_mount(&lfs3, LFS3_M_RDWR, CFG) => 0;

    uint32_t prng = 42;

    // create a file
    lfs3_file_t file;
    lfs3_file_open(&lfs3, &file, "jellyfish",
            LFS3_O_RDWR | LFS3_O_CREAT | LFS3_O_EXCL) => 0;

    // write to our file until we are a btree and have at least
    // 3 btree nodes
    uint32_t wprng = prng;
    lfs3_off_t wsize = 0;
    while (true) {
        if (!(file.bshrub.trunk & 0x80000000)) {
            lfs3_size_t count = 0;
            lfs3_trv_t trv;
            lfs3_trv_open(&lfs3, &trv, LFS3_T_RDONLY) => 0;
            for (lfs3_block_t i = 0;; i++) {
                assert(i < 2*BLOCK_COUNT);

                struct lfs3_tinfo tinfo;
                int err = lfs3_trv_read(&lfs3, &trv, &tinfo);
                assert(!err || err == LFS3_ERR_NOENT);
                if (err == LFS3_ERR_NOENT) {
                    break;
                }

                if (tinfo.btype == LFS3_BTYPE_BTREE) {
                    count += 1;
                }
            }
            lfs3_trv_close(&lfs3, &trv) => 0;
            if (count >= 3 + ((GBMAP) ? 1 : 0)) {
                break;
            }
        }

        uint8_t wbuf[CHUNK];
        for (lfs3_size_t j = 0; j < CHUNK; j++) {
            wbuf[j] = 'a' + (TEST_PRNG(&prng) % 26);
        }
        lfs3_file_write(&lfs3, &file, wbuf, CHUNK) => CHUNK;
        if (SYNC) {
            lfs3_file_sync(&lfs3, &file) => 0;
        }
        wsize += CHUNK;
    }

    // close during gc? this makes sure gc can find the btree on disk
    if (CLOSE) {
        lfs3_file_close(&lfs3, &file) => 0;
    }

    // get a block to evict
    lfs3_block_t victim = 0;
    lfs3_size_t count = 0;
    lfs3_trv_t trv;
    lfs3_trv_open(&lfs3, &trv, LFS3_T_RDONLY) => 0;
    for (lfs3_block_t i = 0;; i++) {
        assert(i < 2*BLOCK_COUNT);

        struct lfs3_tinfo tinfo;
        int err = lfs3_trv_read(&lfs3, &trv, &tinfo);
        assert(!err);

        if (tinfo.btype == LFS3_BTYPE_BTREE) {
            if (count == SIBLING) {
                victim = tinfo.block;
                break;
            }

            count += 1;
        }
    }
    lfs3_trv_close(&lfs3, &trv) => 0;

    // make bad
    lfs3_emubd_mkbad(CFG, victim) => 0;
    // reset ck flags
    lfs3_fs_unck(&lfs3, LFS3_GC_CKMETA) => 0;

    // run gc to repair bad blocks
    lfs3_gc_t gc;
    lfs3_gc_open(&lfs3, &gc,
            LFS3_GC_WRONLY
                | LFS3_GC_CKMETA
                | LFS3_GC_REPAIRMETA) => 0;
    for (lfs3_block_t i = 0;; i++) {
        // a bit hacky, but this catches infinite loops
        assert(i < 2*BLOCK_COUNT);

        lfs3_sblock_t steps = lfs3_gc_write(&lfs3, &gc, 1);
        assert(steps >= 0);
        if (steps == 0) {
            break;
        }
    }
    lfs3_gc_close(&lfs3, &gc) => 0;

    // reopen if necessary
    if (CLOSE) {
        lfs3_file_open(&lfs3, &file, "jellyfish", LFS3_O_RDWR) => 0;
    }

    // evicted block should no longer be in use, anywhere in the
    // filesystem
    lfs3_trv_open(&lfs3, &trv, LFS3_T_RDONLY) => 0;
    for (lfs3_block_t i = 0;; i++) {
        assert(i < 2*BLOCK_COUNT);

        struct lfs3_tinfo tinfo;
        int err = lfs3_trv_read(&lfs3, &trv, &tinfo);
        assert(!err || err == LFS3_ERR_NOENT);
        if (err == LFS3_ERR_NOENT) {
            break;
        }

        assert(tinfo.block != victim);
    }
    lfs3_trv_close(&lfs3, &trv) => 0;

    // check we can still read the file
    for (int remount = 0; remount < 2; remount++) {
        // remount?
        if (remount) {
            lfs3_file_close(&lfs3, &file) => 0;
            lfs3_unmount(&lfs3) => 0;
            lfs3_mount(&lfs3, LFS3_M_RDWR, CFG) => 0;
            lfs3_file_open(&lfs3, &file, "jellyfish", LFS3_O_RDONLY) => 0;
        }

        lfs3_file_rewind(&lfs3, &file) => 0;
        prng = wprng;
        for (lfs3_off_t i = 0; i < wsize; i += CHUNK) {
            uint8_t wbuf[CHUNK];
            for (lfs3_size_t j = 0; j < CHUNK; j++) {
                wbuf[j] = 'a' + (TEST_PRNG(&prng) % 26);
            }
            uint8_t rbuf[CHUNK];
            lfs3_file_read(&lfs3, &file, rbuf, CHUNK) => CHUNK;
            assert(memcmp(rbuf, wbuf, CHUNK) == 0);
        }
    }

    lfs3_file_close(&lfs3, &file) => 0;
    lfs3_unmount(&lfs3) => 0;
'''

[cases.test_evict_repair_t_btree_bshrub]
defines.CHUNK = 'BLOCK_SIZE/8'
defines.SYNC = [false, true]
defines.CLOSE = [false, true]
defines.SIBLING = [0, 1, 2]
# prevent block crystallization
defines.CRYSTAL_THRESH = -1
# report as damaged to request repairs
defines.BADBLOCK_BEHAVIOR = 'LFS3_EMUBD_BADBLOCK_READDAMAGED'
code = '''
    lfs3_t lfs3;
    lfs3_format(&lfs3,
            LFS3_F_RDWR
                | ((GBMAP) ? LFS3_IFDEF_GBMAP(LFS3_F_GBMAP, -1) : 0),
            CFG) => 0;
    lfs3_mount(&lfs3, LFS3_M_RDWR, CFG) => 0;

    uint32_t prng = 42;

    // create a file
    lfs3_file_t file;
    lfs3_file_open(&lfs3, &file, "jellyfish",
            LFS3_O_RDWR | LFS3_O_CREAT | LFS3_O_EXCL) => 0;

    // write to our file until we are a bshrub and have at least
    // 3 btree node
    uint32_t wprng = prng;
    lfs3_off_t wsize = 0;
    while (true) {
        if (file.bshrub.trunk & 0x80000000) {
            lfs3_size_t count = 0;
            lfs3_trv_t trv;
            lfs3_trv_open(&lfs3, &trv, LFS3_T_RDONLY) => 0;
            for (lfs3_block_t i = 0;; i++) {
                assert(i < 2*BLOCK_COUNT);

                struct lfs3_tinfo tinfo;
                int err = lfs3_trv_read(&lfs3, &trv, &tinfo);
                assert(!err || err == LFS3_ERR_NOENT);
                if (err == LFS3_ERR_NOENT) {
                    break;
                }

                if (tinfo.btype == LFS3_BTYPE_BTREE) {
                    count += 1;
                }
            }
            lfs3_trv_close(&lfs3, &trv) => 0;
            if (count >= 3 + ((GBMAP) ? 1 : 0)) {
                break;
            }
        }

        uint8_t wbuf[CHUNK];
        for (lfs3_size_t j = 0; j < CHUNK; j++) {
            wbuf[j] = 'a' + (TEST_PRNG(&prng) % 26);
        }
        lfs3_file_write(&lfs3, &file, wbuf, CHUNK) => CHUNK;
        if (SYNC) {
            lfs3_file_sync(&lfs3, &file) => 0;
        }
        wsize += CHUNK;
    }

    // close during gc? this makes sure gc can find the btree on disk
    if (CLOSE) {
        lfs3_file_close(&lfs3, &file) => 0;
    }

    // get a block to evict
    lfs3_block_t victim = 0;
    lfs3_size_t count = 0;
    lfs3_trv_t trv;
    lfs3_trv_open(&lfs3, &trv, LFS3_T_RDONLY) => 0;
    for (lfs3_block_t i = 0;; i++) {
        assert(i < 2*BLOCK_COUNT);

        struct lfs3_tinfo tinfo;
        int err = lfs3_trv_read(&lfs3, &trv, &tinfo);
        assert(!err);

        if (tinfo.btype == LFS3_BTYPE_BTREE) {
            if (count == SIBLING) {
                victim = tinfo.block;
                break;
            }

            count += 1;
        }
    }
    lfs3_trv_close(&lfs3, &trv) => 0;

    // make bad
    lfs3_emubd_mkbad(CFG, victim) => 0;
    // reset ck flags
    lfs3_fs_unck(&lfs3, LFS3_GC_CKMETA) => 0;

    // run gc to repair bad blocks
    lfs3_gc_t gc;
    lfs3_gc_open(&lfs3, &gc,
            LFS3_GC_WRONLY
                | LFS3_GC_CKMETA
                | LFS3_GC_REPAIRMETA) => 0;
    for (lfs3_block_t i = 0;; i++) {
        // a bit hacky, but this catches infinite loops
        assert(i < 2*BLOCK_COUNT);

        lfs3_sblock_t steps = lfs3_gc_write(&lfs3, &gc, 1);
        assert(steps >= 0);
        if (steps == 0) {
            break;
        }
    }
    lfs3_gc_close(&lfs3, &gc) => 0;

    // reopen if necessary
    if (CLOSE) {
        lfs3_file_open(&lfs3, &file, "jellyfish", LFS3_O_RDWR) => 0;
    }

    // evicted block should no longer be in use, anywhere in the
    // filesystem
    lfs3_trv_open(&lfs3, &trv, LFS3_T_RDONLY) => 0;
    for (lfs3_block_t i = 0;; i++) {
        assert(i < 2*BLOCK_COUNT);

        struct lfs3_tinfo tinfo;
        int err = lfs3_trv_read(&lfs3, &trv, &tinfo);
        assert(!err || err == LFS3_ERR_NOENT);
        if (err == LFS3_ERR_NOENT) {
            break;
        }

        assert(tinfo.block != victim);
    }
    lfs3_trv_close(&lfs3, &trv) => 0;

    // check we can still read the file
    for (int remount = 0; remount < 2; remount++) {
        // remount?
        if (remount) {
            lfs3_file_close(&lfs3, &file) => 0;
            lfs3_unmount(&lfs3) => 0;
            lfs3_mount(&lfs3, LFS3_M_RDWR, CFG) => 0;
            lfs3_file_open(&lfs3, &file, "jellyfish", LFS3_O_RDONLY) => 0;
        }

        lfs3_file_rewind(&lfs3, &file) => 0;
        prng = wprng;
        for (lfs3_off_t i = 0; i < wsize; i += CHUNK) {
            uint8_t wbuf[CHUNK];
            for (lfs3_size_t j = 0; j < CHUNK; j++) {
                wbuf[j] = 'a' + (TEST_PRNG(&prng) % 26);
            }
            uint8_t rbuf[CHUNK];
            lfs3_file_read(&lfs3, &file, rbuf, CHUNK) => CHUNK;
            assert(memcmp(rbuf, wbuf, CHUNK) == 0);
        }
    }

    lfs3_file_close(&lfs3, &file) => 0;
    lfs3_unmount(&lfs3) => 0;
'''

[cases.test_evict_repair_t_btree_btree_broadcast]
defines.CHUNK = 'BLOCK_SIZE/8'
defines.HANDLESET = 'range(0x8)'
defines.SIBLING = [0]
# prevent block crystallization
defines.CRYSTAL_THRESH = -1
# report as damaged to request repairs
defines.BADBLOCK_BEHAVIOR = 'LFS3_EMUBD_BADBLOCK_READDAMAGED'
code = '''
    lfs3_t lfs3;
    lfs3_format(&lfs3,
            LFS3_F_RDWR
                | ((GBMAP) ? LFS3_IFDEF_GBMAP(LFS3_F_GBMAP, -1) : 0),
            CFG) => 0;
    lfs3_mount(&lfs3, LFS3_M_RDWR, CFG) => 0;

    uint32_t prng = 42;

    // create a file
    lfs3_file_t file;
    lfs3_file_open(&lfs3, &file, "jellyfish",
            LFS3_O_RDWR | LFS3_O_CREAT | LFS3_O_EXCL) => 0;

    // write to our file until we have a btree
    uint32_t wprng = prng;
    lfs3_off_t wsize = 0;
    // hack, don't use the internals like this
    while ((file.bshrub.trunk & 0x80000000)
            || file.bshrub.weight == 0) {
        uint8_t wbuf[CHUNK];
        for (lfs3_size_t j = 0; j < CHUNK; j++) {
            wbuf[j] = 'a' + (TEST_PRNG(&prng) % 26);
        }
        lfs3_file_write(&lfs3, &file, wbuf, CHUNK) => CHUNK;
        lfs3_file_sync(&lfs3, &file) => 0;
        wsize += CHUNK;
    }

    // open a couple other handles in different states
    lfs3_file_t file_mutated;
    if (HANDLESET & 0x1) {
        lfs3_file_open(&lfs3, &file_mutated, "jellyfish", LFS3_O_RDWR) => 0;
        lfs3_file_write(&lfs3, &file_mutated, "!", 1) => 1;
        lfs3_file_flush(&lfs3, &file_mutated) => 0;
    }

    lfs3_file_t file_desync;
    if (HANDLESET & 0x2) {
        lfs3_file_open(&lfs3, &file_desync, "jellyfish",
                LFS3_O_RDWR | LFS3_O_DESYNC) => 0;
    }

    lfs3_file_t file_desync_mutated;
    if (HANDLESET & 0x4) {
        lfs3_file_open(&lfs3, &file_desync_mutated, "jellyfish",
                LFS3_O_RDWR | LFS3_O_DESYNC) => 0;
        lfs3_file_write(&lfs3, &file_desync_mutated, "!", 1) => 1;
        lfs3_file_flush(&lfs3, &file_desync_mutated) => 0;
    }

    // get a block to evict
    lfs3_block_t victim = file.bshrub.blocks[0];

    // make bad
    lfs3_emubd_mkbad(CFG, victim) => 0;
    // reset ck flags
    lfs3_fs_unck(&lfs3, LFS3_GC_CKMETA) => 0;

    // run gc to repair bad blocks
    lfs3_gc_t gc;
    lfs3_gc_open(&lfs3, &gc,
            LFS3_GC_WRONLY
                | LFS3_GC_CKMETA
                | LFS3_GC_REPAIRMETA) => 0;
    for (lfs3_block_t i = 0;; i++) {
        // a bit hacky, but this catches infinite loops
        assert(i < 2*BLOCK_COUNT);

        lfs3_sblock_t steps = lfs3_gc_write(&lfs3, &gc, 1);
        assert(steps >= 0);
        if (steps == 0) {
            break;
        }
    }
    lfs3_gc_close(&lfs3, &gc) => 0;

    // evicted block should no longer be in use
    assert(file.bshrub.blocks[0] != victim);

    // evicted block should no longer be in use, anywhere in the
    // filesystem
    lfs3_trv_t trv;
    lfs3_trv_open(&lfs3, &trv, LFS3_T_RDONLY) => 0;
    for (lfs3_block_t i = 0;; i++) {
        assert(i < 2*BLOCK_COUNT);

        struct lfs3_tinfo tinfo;
        int err = lfs3_trv_read(&lfs3, &trv, &tinfo);
        assert(!err || err == LFS3_ERR_NOENT);
        if (err == LFS3_ERR_NOENT) {
            break;
        }

        assert(tinfo.block != victim);
    }
    lfs3_trv_close(&lfs3, &trv) => 0;

    if (HANDLESET & 0x1) {
        // file_mutated should still be a different bshrub
        assert(file_mutated.bshrub.blocks[0]
                    != file.bshrub.blocks[0]
                || (file_mutated.bshrub.trunk & 0x7fffffff)
                    != (file.bshrub.trunk & 0x7fffffff));
    }

    if (HANDLESET & 0x2) {
        // file_desync should be the same bshrub, yes we ignore desync
        // here!
        assert(file_desync.bshrub.blocks[0]
                    == file.bshrub.blocks[0]
                && (file_desync.bshrub.trunk & 0x7fffffff)
                    == (file.bshrub.trunk & 0x7fffffff));
    }

    if (HANDLESET & 0x4) {
        // file_desync_mutated should still be a different bshrub
        assert(file_desync_mutated.bshrub.blocks[0]
                    != file.bshrub.blocks[0]
                || (file_desync_mutated.bshrub.trunk & 0x7fffffff)
                    != (file.bshrub.trunk & 0x7fffffff));
    }

    // check we can still read the files
    lfs3_file_rewind(&lfs3, &file) => 0;
    prng = wprng;
    for (lfs3_off_t i = 0; i < wsize; i += CHUNK) {
        uint8_t wbuf[CHUNK];
        for (lfs3_size_t j = 0; j < CHUNK; j++) {
            wbuf[j] = 'a' + (TEST_PRNG(&prng) % 26);
        }
        uint8_t rbuf[CHUNK];
        lfs3_file_read(&lfs3, &file, rbuf, CHUNK) => CHUNK;
        assert(memcmp(rbuf, wbuf, CHUNK) == 0);
    }

    if (HANDLESET & 0x1) {
        lfs3_file_rewind(&lfs3, &file_mutated) => 0;
        prng = wprng;
        for (lfs3_off_t i = 0; i < wsize; i += CHUNK) {
            uint8_t wbuf[CHUNK];
            for (lfs3_size_t j = 0; j < CHUNK; j++) {
                wbuf[j] = 'a' + (TEST_PRNG(&prng) % 26);
            }
            if (i == 0) {
                wbuf[0] = '!';
            }
            uint8_t rbuf[CHUNK];
            lfs3_file_read(&lfs3, &file_mutated, rbuf, CHUNK) => CHUNK;
            assert(memcmp(rbuf, wbuf, CHUNK) == 0);
        }
    }

    if (HANDLESET & 0x2) {
        lfs3_file_rewind(&lfs3, &file_desync) => 0;
        prng = wprng;
        for (lfs3_off_t i = 0; i < wsize; i += CHUNK) {
            uint8_t wbuf[CHUNK];
            for (lfs3_size_t j = 0; j < CHUNK; j++) {
                wbuf[j] = 'a' + (TEST_PRNG(&prng) % 26);
            }
            uint8_t rbuf[CHUNK];
            lfs3_file_read(&lfs3, &file_desync, rbuf, CHUNK) => CHUNK;
            assert(memcmp(rbuf, wbuf, CHUNK) == 0);
        }
    }

    if (HANDLESET & 0x4) {
        lfs3_file_rewind(&lfs3, &file_desync_mutated) => 0;
        prng = wprng;
        for (lfs3_off_t i = 0; i < wsize; i += CHUNK) {
            uint8_t wbuf[CHUNK];
            for (lfs3_size_t j = 0; j < CHUNK; j++) {
                wbuf[j] = 'a' + (TEST_PRNG(&prng) % 26);
            }
            if (i == 0) {
                wbuf[0] = '!';
            }
            uint8_t rbuf[CHUNK];
            lfs3_file_read(&lfs3, &file_desync_mutated, rbuf, CHUNK) => CHUNK;
            assert(memcmp(rbuf, wbuf, CHUNK) == 0);
        }
    }

    lfs3_file_close(&lfs3, &file) => 0;
    if (HANDLESET & 0x1) {
        lfs3_file_desync(&lfs3, &file_mutated) => 0;
        lfs3_file_close(&lfs3, &file_mutated) => 0;
    }
    if (HANDLESET & 0x2) {
        lfs3_file_close(&lfs3, &file_desync) => 0;
    }
    if (HANDLESET & 0x4) {
        lfs3_file_close(&lfs3, &file_desync_mutated) => 0;
    }
    lfs3_unmount(&lfs3) => 0;
'''

[cases.test_evict_repair_t_btree_btree_btree_broadcast]
defines.CHUNK = 'BLOCK_SIZE/8'
defines.HANDLESET = 'range(0x8)'
defines.SIBLING = [0, 1, 2]
# prevent block crystallization
defines.CRYSTAL_THRESH = -1
# report as damaged to request repairs
defines.BADBLOCK_BEHAVIOR = 'LFS3_EMUBD_BADBLOCK_READDAMAGED'
code = '''
    lfs3_t lfs3;
    lfs3_format(&lfs3,
            LFS3_F_RDWR
                | ((GBMAP) ? LFS3_IFDEF_GBMAP(LFS3_F_GBMAP, -1) : 0),
            CFG) => 0;
    lfs3_mount(&lfs3, LFS3_M_RDWR, CFG) => 0;

    uint32_t prng = 42;

    // create a file
    lfs3_file_t file;
    lfs3_file_open(&lfs3, &file, "jellyfish",
            LFS3_O_RDWR | LFS3_O_CREAT | LFS3_O_EXCL) => 0;

    // write to our file until we are a btree and have at least
    // 3 btree nodes
    uint32_t wprng = prng;
    lfs3_off_t wsize = 0;
    while (true) {
        if (!(file.bshrub.trunk & 0x80000000)) {
            lfs3_size_t count = 0;
            lfs3_trv_t trv;
            lfs3_trv_open(&lfs3, &trv, LFS3_T_RDONLY) => 0;
            for (lfs3_block_t i = 0;; i++) {
                assert(i < 2*BLOCK_COUNT);

                struct lfs3_tinfo tinfo;
                int err = lfs3_trv_read(&lfs3, &trv, &tinfo);
                assert(!err || err == LFS3_ERR_NOENT);
                if (err == LFS3_ERR_NOENT) {
                    break;
                }

                if (tinfo.btype == LFS3_BTYPE_BTREE) {
                    count += 1;
                }
            }
            lfs3_trv_close(&lfs3, &trv) => 0;
            if (count >= 3 + ((GBMAP) ? 1 : 0)) {
                break;
            }
        }

        uint8_t wbuf[CHUNK];
        for (lfs3_size_t j = 0; j < CHUNK; j++) {
            wbuf[j] = 'a' + (TEST_PRNG(&prng) % 26);
        }
        lfs3_file_write(&lfs3, &file, wbuf, CHUNK) => CHUNK;
        lfs3_file_sync(&lfs3, &file) => 0;
        wsize += CHUNK;
    }

    // open a couple other handles in different states
    lfs3_file_t file_mutated;
    if (HANDLESET & 0x1) {
        lfs3_file_open(&lfs3, &file_mutated, "jellyfish", LFS3_O_RDWR) => 0;
        lfs3_file_write(&lfs3, &file_mutated, "!", 1) => 1;
        lfs3_file_flush(&lfs3, &file_mutated) => 0;
    }

    lfs3_file_t file_desync;
    if (HANDLESET & 0x2) {
        lfs3_file_open(&lfs3, &file_desync, "jellyfish",
                LFS3_O_RDWR | LFS3_O_DESYNC) => 0;
    }

    lfs3_file_t file_desync_mutated;
    if (HANDLESET & 0x4) {
        lfs3_file_open(&lfs3, &file_desync_mutated, "jellyfish",
                LFS3_O_RDWR | LFS3_O_DESYNC) => 0;
        lfs3_file_write(&lfs3, &file_desync_mutated, "!", 1) => 1;
        lfs3_file_flush(&lfs3, &file_desync_mutated) => 0;
    }

    // get a block to evict
    lfs3_block_t victim = 0;
    lfs3_size_t count = 0;
    lfs3_trv_t trv;
    lfs3_trv_open(&lfs3, &trv, LFS3_T_RDONLY) => 0;
    for (lfs3_block_t i = 0;; i++) {
        assert(i < 2*BLOCK_COUNT);

        struct lfs3_tinfo tinfo;
        int err = lfs3_trv_read(&lfs3, &trv, &tinfo);
        assert(!err);

        if (tinfo.btype == LFS3_BTYPE_BTREE) {
            if (count == SIBLING) {
                victim = tinfo.block;
                break;
            }

            count += 1;
        }
    }
    lfs3_trv_close(&lfs3, &trv) => 0;

    // make bad
    lfs3_emubd_mkbad(CFG, victim) => 0;
    // reset ck flags
    lfs3_fs_unck(&lfs3, LFS3_GC_CKMETA) => 0;

    // run gc to repair bad blocks
    lfs3_gc_t gc;
    lfs3_gc_open(&lfs3, &gc,
            LFS3_GC_WRONLY
                | LFS3_GC_CKMETA
                | LFS3_GC_REPAIRMETA) => 0;
    for (lfs3_block_t i = 0;; i++) {
        // a bit hacky, but this catches infinite loops
        assert(i < 2*BLOCK_COUNT);

        lfs3_sblock_t steps = lfs3_gc_write(&lfs3, &gc, 1);
        assert(steps >= 0);
        if (steps == 0) {
            break;
        }
    }
    lfs3_gc_close(&lfs3, &gc) => 0;

    // evicted block should no longer be in use, anywhere in the
    // filesystem
    lfs3_trv_open(&lfs3, &trv, LFS3_T_RDONLY) => 0;
    for (lfs3_block_t i = 0;; i++) {
        assert(i < 2*BLOCK_COUNT);

        struct lfs3_tinfo tinfo;
        int err = lfs3_trv_read(&lfs3, &trv, &tinfo);
        assert(!err || err == LFS3_ERR_NOENT);
        if (err == LFS3_ERR_NOENT) {
            break;
        }

        assert(tinfo.block != victim);
    }
    lfs3_trv_close(&lfs3, &trv) => 0;

    if (HANDLESET & 0x1) {
        // file_mutated should still be a different bshrub
        assert(file_mutated.bshrub.blocks[0]
                    != file.bshrub.blocks[0]
                || (file_mutated.bshrub.trunk & 0x7fffffff)
                    != (file.bshrub.trunk & 0x7fffffff));
    }

    if (HANDLESET & 0x2) {
        // file_desync should be the same bshrub, yes we ignore desync
        // here!
        assert(file_desync.bshrub.blocks[0]
                    == file.bshrub.blocks[0]
                && (file_desync.bshrub.trunk & 0x7fffffff)
                    == (file.bshrub.trunk & 0x7fffffff));
    }

    if (HANDLESET & 0x4) {
        // file_desync_mutated should still be a different bshrub
        assert(file_desync_mutated.bshrub.blocks[0]
                    != file.bshrub.blocks[0]
                || (file_desync_mutated.bshrub.trunk & 0x7fffffff)
                    != (file.bshrub.trunk & 0x7fffffff));
    }

    // check we can still read the files
    lfs3_file_rewind(&lfs3, &file) => 0;
    prng = wprng;
    for (lfs3_off_t i = 0; i < wsize; i += CHUNK) {
        uint8_t wbuf[CHUNK];
        for (lfs3_size_t j = 0; j < CHUNK; j++) {
            wbuf[j] = 'a' + (TEST_PRNG(&prng) % 26);
        }
        uint8_t rbuf[CHUNK];
        lfs3_file_read(&lfs3, &file, rbuf, CHUNK) => CHUNK;
        assert(memcmp(rbuf, wbuf, CHUNK) == 0);
    }

    if (HANDLESET & 0x1) {
        lfs3_file_rewind(&lfs3, &file_mutated) => 0;
        prng = wprng;
        for (lfs3_off_t i = 0; i < wsize; i += CHUNK) {
            uint8_t wbuf[CHUNK];
            for (lfs3_size_t j = 0; j < CHUNK; j++) {
                wbuf[j] = 'a' + (TEST_PRNG(&prng) % 26);
            }
            if (i == 0) {
                wbuf[0] = '!';
            }
            uint8_t rbuf[CHUNK];
            lfs3_file_read(&lfs3, &file_mutated, rbuf, CHUNK) => CHUNK;
            assert(memcmp(rbuf, wbuf, CHUNK) == 0);
        }
    }

    if (HANDLESET & 0x2) {
        lfs3_file_rewind(&lfs3, &file_desync) => 0;
        prng = wprng;
        for (lfs3_off_t i = 0; i < wsize; i += CHUNK) {
            uint8_t wbuf[CHUNK];
            for (lfs3_size_t j = 0; j < CHUNK; j++) {
                wbuf[j] = 'a' + (TEST_PRNG(&prng) % 26);
            }
            uint8_t rbuf[CHUNK];
            lfs3_file_read(&lfs3, &file_desync, rbuf, CHUNK) => CHUNK;
            assert(memcmp(rbuf, wbuf, CHUNK) == 0);
        }
    }

    if (HANDLESET & 0x4) {
        lfs3_file_rewind(&lfs3, &file_desync_mutated) => 0;
        prng = wprng;
        for (lfs3_off_t i = 0; i < wsize; i += CHUNK) {
            uint8_t wbuf[CHUNK];
            for (lfs3_size_t j = 0; j < CHUNK; j++) {
                wbuf[j] = 'a' + (TEST_PRNG(&prng) % 26);
            }
            if (i == 0) {
                wbuf[0] = '!';
            }
            uint8_t rbuf[CHUNK];
            lfs3_file_read(&lfs3, &file_desync_mutated, rbuf, CHUNK) => CHUNK;
            assert(memcmp(rbuf, wbuf, CHUNK) == 0);
        }
    }

    lfs3_file_close(&lfs3, &file) => 0;
    if (HANDLESET & 0x1) {
        lfs3_file_desync(&lfs3, &file_mutated) => 0;
        lfs3_file_close(&lfs3, &file_mutated) => 0;
    }
    if (HANDLESET & 0x2) {
        lfs3_file_close(&lfs3, &file_desync) => 0;
    }
    if (HANDLESET & 0x4) {
        lfs3_file_close(&lfs3, &file_desync_mutated) => 0;
    }
    lfs3_unmount(&lfs3) => 0;
'''

[cases.test_evict_repair_t_btree_bshrub_broadcast]
defines.CHUNK = 'BLOCK_SIZE/8'
defines.HANDLESET = 'range(0x8)'
defines.SIBLING = [0, 1, 2]
# prevent block crystallization
defines.CRYSTAL_THRESH = -1
# report as damaged to request repairs
defines.BADBLOCK_BEHAVIOR = 'LFS3_EMUBD_BADBLOCK_READDAMAGED'
code = '''
    lfs3_t lfs3;
    lfs3_format(&lfs3,
            LFS3_F_RDWR
                | ((GBMAP) ? LFS3_IFDEF_GBMAP(LFS3_F_GBMAP, -1) : 0),
            CFG) => 0;
    lfs3_mount(&lfs3, LFS3_M_RDWR, CFG) => 0;

    uint32_t prng = 42;

    // create a file
    lfs3_file_t file;
    lfs3_file_open(&lfs3, &file, "jellyfish",
            LFS3_O_RDWR | LFS3_O_CREAT | LFS3_O_EXCL) => 0;

    // write to our file until we are a bshrub and have at least
    // 3 btree node
    uint32_t wprng = prng;
    lfs3_off_t wsize = 0;
    while (true) {
        if (file.bshrub.trunk & 0x80000000) {
            lfs3_size_t count = 0;
            lfs3_trv_t trv;
            lfs3_trv_open(&lfs3, &trv, LFS3_T_RDONLY) => 0;
            for (lfs3_block_t i = 0;; i++) {
                assert(i < 2*BLOCK_COUNT);

                struct lfs3_tinfo tinfo;
                int err = lfs3_trv_read(&lfs3, &trv, &tinfo);
                assert(!err || err == LFS3_ERR_NOENT);
                if (err == LFS3_ERR_NOENT) {
                    break;
                }

                if (tinfo.btype == LFS3_BTYPE_BTREE) {
                    count += 1;
                }
            }
            lfs3_trv_close(&lfs3, &trv) => 0;
            if (count >= 3 + ((GBMAP) ? 1 : 0)) {
                break;
            }
        }

        uint8_t wbuf[CHUNK];
        for (lfs3_size_t j = 0; j < CHUNK; j++) {
            wbuf[j] = 'a' + (TEST_PRNG(&prng) % 26);
        }
        lfs3_file_write(&lfs3, &file, wbuf, CHUNK) => CHUNK;
        lfs3_file_sync(&lfs3, &file) => 0;
        wsize += CHUNK;
    }

    // open a couple other handles in different states
    lfs3_file_t file_mutated;
    if (HANDLESET & 0x1) {
        lfs3_file_open(&lfs3, &file_mutated, "jellyfish", LFS3_O_RDWR) => 0;
        lfs3_file_write(&lfs3, &file_mutated, "!", 1) => 1;
        lfs3_file_flush(&lfs3, &file_mutated) => 0;
    }

    lfs3_file_t file_desync;
    if (HANDLESET & 0x2) {
        lfs3_file_open(&lfs3, &file_desync, "jellyfish",
                LFS3_O_RDWR | LFS3_O_DESYNC) => 0;
    }

    lfs3_file_t file_desync_mutated;
    if (HANDLESET & 0x4) {
        lfs3_file_open(&lfs3, &file_desync_mutated, "jellyfish",
                LFS3_O_RDWR | LFS3_O_DESYNC) => 0;
        lfs3_file_write(&lfs3, &file_desync_mutated, "!", 1) => 1;
        lfs3_file_flush(&lfs3, &file_desync_mutated) => 0;
    }

    // get a block to evict
    lfs3_block_t victim = 0;
    lfs3_size_t count = 0;
    lfs3_trv_t trv;
    lfs3_trv_open(&lfs3, &trv, LFS3_T_RDONLY) => 0;
    for (lfs3_block_t i = 0;; i++) {
        assert(i < 2*BLOCK_COUNT);

        struct lfs3_tinfo tinfo;
        int err = lfs3_trv_read(&lfs3, &trv, &tinfo);
        assert(!err);

        if (tinfo.btype == LFS3_BTYPE_BTREE) {
            if (count == SIBLING) {
                victim = tinfo.block;
                break;
            }

            count += 1;
        }
    }
    lfs3_trv_close(&lfs3, &trv) => 0;

    // make bad
    lfs3_emubd_mkbad(CFG, victim) => 0;
    // reset ck flags
    lfs3_fs_unck(&lfs3, LFS3_GC_CKMETA) => 0;

    // run gc to repair bad blocks
    lfs3_gc_t gc;
    lfs3_gc_open(&lfs3, &gc,
            LFS3_GC_WRONLY
                | LFS3_GC_CKMETA
                | LFS3_GC_REPAIRMETA) => 0;
    for (lfs3_block_t i = 0;; i++) {
        // a bit hacky, but this catches infinite loops
        assert(i < 2*BLOCK_COUNT);

        lfs3_sblock_t steps = lfs3_gc_write(&lfs3, &gc, 1);
        assert(steps >= 0);
        if (steps == 0) {
            break;
        }
    }
    lfs3_gc_close(&lfs3, &gc) => 0;

    // evicted block should no longer be in use, anywhere in the
    // filesystem
    lfs3_trv_open(&lfs3, &trv, LFS3_T_RDONLY) => 0;
    for (lfs3_block_t i = 0;; i++) {
        assert(i < 2*BLOCK_COUNT);

        struct lfs3_tinfo tinfo;
        int err = lfs3_trv_read(&lfs3, &trv, &tinfo);
        assert(!err || err == LFS3_ERR_NOENT);
        if (err == LFS3_ERR_NOENT) {
            break;
        }

        assert(tinfo.block != victim);
    }
    lfs3_trv_close(&lfs3, &trv) => 0;

    if (HANDLESET & 0x1) {
        // file_mutated should still be a different bshrub
        assert(file_mutated.bshrub.blocks[0]
                    != file.bshrub.blocks[0]
                || (file_mutated.bshrub.trunk & 0x7fffffff)
                    != (file.bshrub.trunk & 0x7fffffff));
    }

    if (HANDLESET & 0x2) {
        // file_desync should be the same bshrub, yes we ignore desync
        // here!
        assert(file_desync.bshrub.blocks[0]
                    == file.bshrub.blocks[0]
                && (file_desync.bshrub.trunk & 0x7fffffff)
                    == (file.bshrub.trunk & 0x7fffffff));
    }

    if (HANDLESET & 0x4) {
        // file_desync_mutated should still be a different bshrub
        assert(file_desync_mutated.bshrub.blocks[0]
                    != file.bshrub.blocks[0]
                || (file_desync_mutated.bshrub.trunk & 0x7fffffff)
                    != (file.bshrub.trunk & 0x7fffffff));
    }

    // check we can still read the files
    lfs3_file_rewind(&lfs3, &file) => 0;
    prng = wprng;
    for (lfs3_off_t i = 0; i < wsize; i += CHUNK) {
        uint8_t wbuf[CHUNK];
        for (lfs3_size_t j = 0; j < CHUNK; j++) {
            wbuf[j] = 'a' + (TEST_PRNG(&prng) % 26);
        }
        uint8_t rbuf[CHUNK];
        lfs3_file_read(&lfs3, &file, rbuf, CHUNK) => CHUNK;
        assert(memcmp(rbuf, wbuf, CHUNK) == 0);
    }

    if (HANDLESET & 0x1) {
        lfs3_file_rewind(&lfs3, &file_mutated) => 0;
        prng = wprng;
        for (lfs3_off_t i = 0; i < wsize; i += CHUNK) {
            uint8_t wbuf[CHUNK];
            for (lfs3_size_t j = 0; j < CHUNK; j++) {
                wbuf[j] = 'a' + (TEST_PRNG(&prng) % 26);
            }
            if (i == 0) {
                wbuf[0] = '!';
            }
            uint8_t rbuf[CHUNK];
            lfs3_file_read(&lfs3, &file_mutated, rbuf, CHUNK) => CHUNK;
            assert(memcmp(rbuf, wbuf, CHUNK) == 0);
        }
    }

    if (HANDLESET & 0x2) {
        lfs3_file_rewind(&lfs3, &file_desync) => 0;
        prng = wprng;
        for (lfs3_off_t i = 0; i < wsize; i += CHUNK) {
            uint8_t wbuf[CHUNK];
            for (lfs3_size_t j = 0; j < CHUNK; j++) {
                wbuf[j] = 'a' + (TEST_PRNG(&prng) % 26);
            }
            uint8_t rbuf[CHUNK];
            lfs3_file_read(&lfs3, &file_desync, rbuf, CHUNK) => CHUNK;
            assert(memcmp(rbuf, wbuf, CHUNK) == 0);
        }
    }

    if (HANDLESET & 0x4) {
        lfs3_file_rewind(&lfs3, &file_desync_mutated) => 0;
        prng = wprng;
        for (lfs3_off_t i = 0; i < wsize; i += CHUNK) {
            uint8_t wbuf[CHUNK];
            for (lfs3_size_t j = 0; j < CHUNK; j++) {
                wbuf[j] = 'a' + (TEST_PRNG(&prng) % 26);
            }
            if (i == 0) {
                wbuf[0] = '!';
            }
            uint8_t rbuf[CHUNK];
            lfs3_file_read(&lfs3, &file_desync_mutated, rbuf, CHUNK) => CHUNK;
            assert(memcmp(rbuf, wbuf, CHUNK) == 0);
        }
    }

    lfs3_file_close(&lfs3, &file) => 0;
    if (HANDLESET & 0x1) {
        lfs3_file_desync(&lfs3, &file_mutated) => 0;
        lfs3_file_close(&lfs3, &file_mutated) => 0;
    }
    if (HANDLESET & 0x2) {
        lfs3_file_close(&lfs3, &file_desync) => 0;
    }
    if (HANDLESET & 0x4) {
        lfs3_file_close(&lfs3, &file_desync_mutated) => 0;
    }
    lfs3_unmount(&lfs3) => 0;
'''

[cases.test_evict_repair_t_btree_gbmap]
defines.CHUNK = 'BLOCK_SIZE'
defines.SIZE = ['1*CHUNK', '3*CHUNK', '20*CHUNK']
defines.SYNC = [false, true]
# report as damaged to request repairs
defines.BADBLOCK_BEHAVIOR = 'LFS3_EMUBD_BADBLOCK_READDAMAGED'
if = 'GBMAP'
ifdef = 'LFS3_GBMAP'
code = '''
    lfs3_t lfs3;
    lfs3_format(&lfs3, LFS3_F_RDWR | LFS3_F_GBMAP, CFG) => 0;
    lfs3_mount(&lfs3, LFS3_M_RDWR, CFG) => 0;

    uint32_t prng = 42;

    // create a file
    lfs3_file_t file;
    lfs3_file_open(&lfs3, &file, "jellyfish",
            LFS3_O_RDWR | LFS3_O_CREAT | LFS3_O_EXCL) => 0;

    // write to our file
    //
    // we use a really annoying write pattern to try to fragment the gbmap
    uint32_t wprng = prng;
    lfs3_off_t wsize = 0;
    while (wsize < SIZE) {
        uint8_t wbuf[CHUNK];
        for (lfs3_size_t j = 0; j < CHUNK; j++) {
            wbuf[j] = 'a' + (TEST_PRNG(&prng) % 26);
        }
        lfs3_file_write(&lfs3, &file, wbuf, CHUNK) => CHUNK;
        lfs3_soff_t off = lfs3_file_seek(&lfs3, &file, -CHUNK, LFS3_SEEK_END);
        assert(off >= 0);
        for (lfs3_size_t j = 0; j < CHUNK; j++) {
            wbuf[j] = 'a' + (TEST_PRNG(&prng) % 26);
        }
        lfs3_file_write(&lfs3, &file, wbuf, CHUNK) => CHUNK;
        if (SYNC) {
            lfs3_file_sync(&lfs3, &file) => 0;
        }
        wsize += CHUNK;
    }

    // get a block to evict
    lfs3_block_t victim = lfs3.gbmap.b.blocks[0];

    // make bad
    lfs3_emubd_mkbad(CFG, victim) => 0;
    // reset ck flags
    lfs3_fs_unck(&lfs3, LFS3_GC_CKMETA) => 0;

    // run gc to repair bad blocks
    lfs3_gc_t gc;
    lfs3_gc_open(&lfs3, &gc,
            LFS3_GC_WRONLY
                | LFS3_GC_CKMETA
                | LFS3_GC_REPAIRMETA) => 0;
    for (lfs3_block_t i = 0;; i++) {
        // a bit hacky, but this catches infinite loops
        assert(i < 2*BLOCK_COUNT);

        lfs3_sblock_t steps = lfs3_gc_write(&lfs3, &gc, 1);
        assert(steps >= 0);
        if (steps == 0) {
            break;
        }
    }
    lfs3_gc_close(&lfs3, &gc) => 0;

    // evicted block should no longer be in use
    assert(lfs3.gbmap.b.blocks[0] != victim);

    // evicted block should no longer be in use, anywhere in the
    // filesystem
    lfs3_trv_t trv;
    lfs3_trv_open(&lfs3, &trv, LFS3_T_RDONLY) => 0;
    for (lfs3_block_t i = 0;; i++) {
        assert(i < 2*BLOCK_COUNT);

        struct lfs3_tinfo tinfo;
        int err = lfs3_trv_read(&lfs3, &trv, &tinfo);
        assert(!err || err == LFS3_ERR_NOENT);
        if (err == LFS3_ERR_NOENT) {
            break;
        }

        assert(tinfo.block != victim);
    }
    lfs3_trv_close(&lfs3, &trv) => 0;

    // check we can still read the file
    lfs3_file_rewind(&lfs3, &file) => 0;
    prng = wprng;
    for (lfs3_off_t i = 0; i < wsize; i += CHUNK) {
        uint8_t wbuf[CHUNK];
        for (lfs3_size_t j = 0; j < CHUNK; j++) {
            TEST_PRNG(&prng);
        }
        for (lfs3_size_t j = 0; j < CHUNK; j++) {
            wbuf[j] = 'a' + (TEST_PRNG(&prng) % 26);
        }
        uint8_t rbuf[CHUNK];
        lfs3_file_read(&lfs3, &file, rbuf, CHUNK) => CHUNK;
        assert(memcmp(rbuf, wbuf, CHUNK) == 0);
    }

    // try creating a second file to make sure the gbmap wasn't
    // corrupted
    lfs3_file_t file2;
    lfs3_file_open(&lfs3, &file2, "octopus",
            LFS3_O_RDWR | LFS3_O_CREAT | LFS3_O_EXCL) => 0;
    for (lfs3_off_t i = 0; i < wsize/2; i += CHUNK) {
        uint8_t wbuf[CHUNK];
        for (lfs3_size_t j = 0; j < CHUNK; j++) {
            wbuf[j] = 'a' + (TEST_PRNG(&prng) % 26);
        }
        lfs3_file_write(&lfs3, &file2, wbuf, CHUNK) => CHUNK;
    }
    lfs3_file_sync(&lfs3, &file2) => 0;

    // check we can still read the files
    for (int remount = 0; remount < 2; remount++) {
        // remount?
        if (remount) {
            lfs3_file_close(&lfs3, &file) => 0;
            lfs3_file_close(&lfs3, &file2) => 0;
            lfs3_unmount(&lfs3) => 0;
            lfs3_mount(&lfs3, LFS3_M_RDWR, CFG) => 0;
            lfs3_file_open(&lfs3, &file, "jellyfish", LFS3_O_RDONLY) => 0;
            lfs3_file_open(&lfs3, &file2, "octopus", LFS3_O_RDONLY) => 0;
        }

        lfs3_file_rewind(&lfs3, &file) => 0;
        prng = wprng;
        for (lfs3_off_t i = 0; i < wsize; i += CHUNK) {
            uint8_t wbuf[CHUNK];
            for (lfs3_size_t j = 0; j < CHUNK; j++) {
                TEST_PRNG(&prng);
            }
            for (lfs3_size_t j = 0; j < CHUNK; j++) {
                wbuf[j] = 'a' + (TEST_PRNG(&prng) % 26);
            }
            uint8_t rbuf[CHUNK];
            lfs3_file_read(&lfs3, &file, rbuf, CHUNK) => CHUNK;
            assert(memcmp(rbuf, wbuf, CHUNK) == 0);
        }

        lfs3_file_rewind(&lfs3, &file2) => 0;
        for (lfs3_off_t i = 0; i < wsize/2; i += CHUNK) {
            uint8_t wbuf[CHUNK];
            for (lfs3_size_t j = 0; j < CHUNK; j++) {
                wbuf[j] = 'a' + (TEST_PRNG(&prng) % 26);
            }
            uint8_t rbuf[CHUNK];
            lfs3_file_read(&lfs3, &file2, rbuf, CHUNK) => CHUNK;
            assert(memcmp(rbuf, wbuf, CHUNK) == 0);
        }
    }

    lfs3_file_close(&lfs3, &file) => 0;
    lfs3_file_close(&lfs3, &file2) => 0;
    lfs3_unmount(&lfs3) => 0;
'''

[cases.test_evict_repair_t_btree_gbmap_gbmap]
defines.CHUNK = 'BLOCK_SIZE'
defines.SYNC = [false, true]
defines.SIBLING = [0, 1, 2]
# report as damaged to request repairs
defines.BADBLOCK_BEHAVIOR = 'LFS3_EMUBD_BADBLOCK_READDAMAGED'
if = 'GBMAP'
ifdef = 'LFS3_GBMAP'
in = 'lfs3.c'
code = '''
    lfs3_t lfs3;
    lfs3_format(&lfs3, LFS3_F_RDWR | LFS3_F_GBMAP, CFG) => 0;
    lfs3_mount(&lfs3, LFS3_M_RDWR, CFG) => 0;

    uint32_t prng = 42;

    // create a file
    lfs3_file_t file;
    lfs3_file_open(&lfs3, &file, "jellyfish",
            LFS3_O_RDWR | LFS3_O_CREAT | LFS3_O_EXCL) => 0;

    // write to our file until we have at least 3 btree nodes
    //
    // we use a really annoying write pattern to try to fragment the gbmap
    uint32_t wprng = prng;
    lfs3_off_t wsize = 0;
    while (true) {
        lfs3_size_t count = 0;
        lfs3_trv_t trv;
        lfs3_trv_open(&lfs3, &trv, LFS3_T_RDONLY) => 0;
        for (lfs3_block_t i = 0;; i++) {
            assert(i < 2*BLOCK_COUNT);

            struct lfs3_tinfo tinfo;
            int err = lfs3_trv_read(&lfs3, &trv, &tinfo);
            assert(!err || err == LFS3_ERR_NOENT);
            if (err == LFS3_ERR_NOENT) {
                break;
            }

            if (tinfo.btype == LFS3_BTYPE_BTREE
                    // bit of a hack
                    && trv.t.h.mdir.mid == LFS3_MID_GBMAP) {
                count += 1;
            }
        }
        lfs3_trv_close(&lfs3, &trv) => 0;
        if (count >= 3) {
            break;
        }

        uint8_t wbuf[CHUNK];
        for (lfs3_size_t j = 0; j < CHUNK; j++) {
            wbuf[j] = 'a' + (TEST_PRNG(&prng) % 26);
        }
        lfs3_file_write(&lfs3, &file, wbuf, CHUNK) => CHUNK;
        lfs3_soff_t off = lfs3_file_seek(&lfs3, &file, -CHUNK, LFS3_SEEK_END);
        assert(off >= 0);
        for (lfs3_size_t j = 0; j < CHUNK; j++) {
            wbuf[j] = 'a' + (TEST_PRNG(&prng) % 26);
        }
        lfs3_file_write(&lfs3, &file, wbuf, CHUNK) => CHUNK;
        if (SYNC) {
            lfs3_file_sync(&lfs3, &file) => 0;
        }
        wsize += CHUNK;
    }

    // get a block to evict
    lfs3_block_t victim = 0;
    lfs3_size_t count = 0;
    lfs3_trv_t trv;
    lfs3_trv_open(&lfs3, &trv, LFS3_T_RDONLY) => 0;
    for (lfs3_block_t i = 0;; i++) {
        assert(i < 2*BLOCK_COUNT);

        struct lfs3_tinfo tinfo;
        int err = lfs3_trv_read(&lfs3, &trv, &tinfo);
        assert(!err);

        if (tinfo.btype == LFS3_BTYPE_BTREE
                // bit of a hack
                && trv.t.h.mdir.mid == LFS3_MID_GBMAP) {
            if (count == SIBLING) {
                victim = tinfo.block;
                break;
            }

            count += 1;
        }
    }
    lfs3_trv_close(&lfs3, &trv) => 0;

    // make bad
    lfs3_emubd_mkbad(CFG, victim) => 0;
    // reset ck flags
    lfs3_fs_unck(&lfs3, LFS3_GC_CKMETA) => 0;

    // run gc to repair bad blocks
    lfs3_gc_t gc;
    lfs3_gc_open(&lfs3, &gc,
            LFS3_GC_WRONLY
                | LFS3_GC_CKMETA
                | LFS3_GC_REPAIRMETA) => 0;
    for (lfs3_block_t i = 0;; i++) {
        // a bit hacky, but this catches infinite loops
        assert(i < 2*BLOCK_COUNT);

        lfs3_sblock_t steps = lfs3_gc_write(&lfs3, &gc, 1);
        assert(steps >= 0);
        if (steps == 0) {
            break;
        }
    }
    lfs3_gc_close(&lfs3, &gc) => 0;

    // evicted block should no longer be in use, anywhere in the
    // filesystem
    lfs3_trv_open(&lfs3, &trv, LFS3_T_RDONLY) => 0;
    for (lfs3_block_t i = 0;; i++) {
        assert(i < 2*BLOCK_COUNT);

        struct lfs3_tinfo tinfo;
        int err = lfs3_trv_read(&lfs3, &trv, &tinfo);
        assert(!err || err == LFS3_ERR_NOENT);
        if (err == LFS3_ERR_NOENT) {
            break;
        }

        assert(tinfo.block != victim);
    }
    lfs3_trv_close(&lfs3, &trv) => 0;

    // check we can still read the file
    lfs3_file_rewind(&lfs3, &file) => 0;
    prng = wprng;
    for (lfs3_off_t i = 0; i < wsize; i += CHUNK) {
        uint8_t wbuf[CHUNK];
        for (lfs3_size_t j = 0; j < CHUNK; j++) {
            TEST_PRNG(&prng);
        }
        for (lfs3_size_t j = 0; j < CHUNK; j++) {
            wbuf[j] = 'a' + (TEST_PRNG(&prng) % 26);
        }
        uint8_t rbuf[CHUNK];
        lfs3_file_read(&lfs3, &file, rbuf, CHUNK) => CHUNK;
        assert(memcmp(rbuf, wbuf, CHUNK) == 0);
    }

    // try creating a second file to make sure the gbmap wasn't
    // corrupted
    lfs3_file_t file2;
    lfs3_file_open(&lfs3, &file2, "octopus",
            LFS3_O_RDWR | LFS3_O_CREAT | LFS3_O_EXCL) => 0;
    for (lfs3_off_t i = 0; i < wsize/2; i += CHUNK) {
        uint8_t wbuf[CHUNK];
        for (lfs3_size_t j = 0; j < CHUNK; j++) {
            wbuf[j] = 'a' + (TEST_PRNG(&prng) % 26);
        }
        lfs3_file_write(&lfs3, &file2, wbuf, CHUNK) => CHUNK;
    }
    lfs3_file_sync(&lfs3, &file2) => 0;

    // check we can still read the files
    for (int remount = 0; remount < 2; remount++) {
        // remount?
        if (remount) {
            lfs3_file_close(&lfs3, &file) => 0;
            lfs3_file_close(&lfs3, &file2) => 0;
            lfs3_unmount(&lfs3) => 0;
            lfs3_mount(&lfs3, LFS3_M_RDWR, CFG) => 0;
            lfs3_file_open(&lfs3, &file, "jellyfish", LFS3_O_RDONLY) => 0;
            lfs3_file_open(&lfs3, &file2, "octopus", LFS3_O_RDONLY) => 0;
        }

        lfs3_file_rewind(&lfs3, &file) => 0;
        prng = wprng;
        for (lfs3_off_t i = 0; i < wsize; i += CHUNK) {
            uint8_t wbuf[CHUNK];
            for (lfs3_size_t j = 0; j < CHUNK; j++) {
                TEST_PRNG(&prng);
            }
            for (lfs3_size_t j = 0; j < CHUNK; j++) {
                wbuf[j] = 'a' + (TEST_PRNG(&prng) % 26);
            }
            uint8_t rbuf[CHUNK];
            lfs3_file_read(&lfs3, &file, rbuf, CHUNK) => CHUNK;
            assert(memcmp(rbuf, wbuf, CHUNK) == 0);
        }

        lfs3_file_rewind(&lfs3, &file2) => 0;
        for (lfs3_off_t i = 0; i < wsize/2; i += CHUNK) {
            uint8_t wbuf[CHUNK];
            for (lfs3_size_t j = 0; j < CHUNK; j++) {
                wbuf[j] = 'a' + (TEST_PRNG(&prng) % 26);
            }
            uint8_t rbuf[CHUNK];
            lfs3_file_read(&lfs3, &file2, rbuf, CHUNK) => CHUNK;
            assert(memcmp(rbuf, wbuf, CHUNK) == 0);
        }
    }

    lfs3_file_close(&lfs3, &file) => 0;
    lfs3_file_close(&lfs3, &file2) => 0;
    lfs3_unmount(&lfs3) => 0;
'''

[cases.test_evict_repair_t_btree_gbmap_p]
defines.CHUNK = 'BLOCK_SIZE'
defines.SIZE = ['1*CHUNK', '3*CHUNK', '20*CHUNK']
# need to disable shrubs to avoid accidental gbmap syncs
defines.SHRUB_SIZE = 0
# report as damaged to request repairs
defines.BADBLOCK_BEHAVIOR = 'LFS3_EMUBD_BADBLOCK_READDAMAGED'
if = 'GBMAP'
ifdef = 'LFS3_GBMAP'
code = '''
    lfs3_t lfs3;
    lfs3_format(&lfs3, LFS3_F_RDWR | LFS3_F_GBMAP, CFG) => 0;
    lfs3_mount(&lfs3, LFS3_M_RDWR, CFG) => 0;

    uint32_t prng = 42;

    // create two files
    lfs3_file_t file;
    lfs3_file_open(&lfs3, &file, "jellyfish",
            LFS3_O_RDWR | LFS3_O_CREAT | LFS3_O_EXCL) => 0;

    // write to our file
    //
    // we use a really annoying write pattern to try to fragment the gbmap
    uint32_t wprng = prng;
    lfs3_off_t wsize = 0;
    while (wsize < SIZE) {
        uint8_t wbuf[CHUNK];
        for (lfs3_size_t j = 0; j < CHUNK; j++) {
            wbuf[j] = 'a' + (TEST_PRNG(&prng) % 26);
        }
        lfs3_file_write(&lfs3, &file, wbuf, CHUNK) => CHUNK;
        lfs3_soff_t off = lfs3_file_seek(&lfs3, &file, -CHUNK, LFS3_SEEK_END);
        assert(off >= 0);
        for (lfs3_size_t j = 0; j < CHUNK; j++) {
            wbuf[j] = 'a' + (TEST_PRNG(&prng) % 26);
        }
        lfs3_file_write(&lfs3, &file, wbuf, CHUNK) => CHUNK;
        if (SYNC) {
            lfs3_file_sync(&lfs3, &file) => 0;
        }
        wsize += CHUNK;
    }

    // create an unrelated file, as a side effect this commits the gbmap
    // to disk
    lfs3_file_t file3;
    lfs3_file_open(&lfs3, &file3, "cuttlefish",
            LFS3_O_RDWR | LFS3_O_CREAT | LFS3_O_EXCL) => 0;
    lfs3_file_close(&lfs3, &file3) => 0;
    assert(lfs3.gbmap.b.blocks[0] == lfs3.gbmap.b_p.blocks[0]);

    // keep track of gbmap_p
    lfs3_btree_t b_p = lfs3.gbmap.b_p;

    // truncate our file to free up space and get rid of all the
    // fragmented gbmap ranges
    lfs3_file_truncate(&lfs3, &file, 0) => 0;
    lfs3_file_seek(&lfs3, &file, 0, LFS3_SEEK_SET) => 0;
    wprng = prng;
    wsize = 0;

    // force a lookgbmap scan, _after_ we truncate
    lfs3.gbmap.known = 0;
    lfs3.flags |= LFS3_I_LOOKAHEAD;

    // rewrite our file to force a lookgbmap scan and hopefully compact
    // the gbmap
    //
    // note _write_ triggers lookgbmap scans, but we don't commit the
    // gbmap until _sync_
    //
    // we also need to avoid any mdir commits (open+creat, shrubs, etc)!
    // littlefs is very opportunistic!
    while (lfs3.gbmap.b.blocks[0] == lfs3.gbmap.b_p.blocks[0]) {
        uint8_t wbuf[CHUNK];
        for (lfs3_size_t j = 0; j < CHUNK; j++) {
            wbuf[j] = 'a' + (TEST_PRNG(&prng) % 26);
        }
        lfs3_file_write(&lfs3, &file, wbuf, CHUNK) => CHUNK;
        wsize += CHUNK;
        // don't sync!
    }

    // at this point, gbmap and gbmap_p should differ, with gbmap
    // compacted and gbmap_p uncompacted
    assert(lfs3.gbmap.b.blocks[0] != lfs3.gbmap.b_p.blocks[0]);
    assert(lfs3.gbmap.b_p.blocks[0] == b_p.blocks[0]);

    // get a block to evict
    lfs3_block_t victim = lfs3.gbmap.b_p.blocks[0];

    // make bad
    lfs3_emubd_mkbad(CFG, victim) => 0;
    // reset ck flags
    lfs3_fs_unck(&lfs3, LFS3_GC_CKMETA) => 0;

    // run gc to repair bad blocks
    lfs3_gc_t gc;
    lfs3_gc_open(&lfs3, &gc,
            LFS3_GC_WRONLY
                | LFS3_GC_CKMETA
                | LFS3_GC_REPAIRMETA) => 0;
    for (lfs3_block_t i = 0;; i++) {
        // a bit hacky, but this catches infinite loops
        assert(i < 2*BLOCK_COUNT);

        lfs3_sblock_t steps = lfs3_gc_write(&lfs3, &gc, 1);
        assert(steps >= 0);
        if (steps == 0) {
            break;
        }
    }
    lfs3_gc_close(&lfs3, &gc) => 0;

    // evicted block should no longer be in use
    assert(lfs3.gbmap.b_p.blocks[0] != victim);

    // evicted block should no longer be in use, anywhere in the
    // filesystem
    lfs3_trv_t trv;
    lfs3_trv_open(&lfs3, &trv, LFS3_T_RDONLY) => 0;
    for (lfs3_block_t i = 0;; i++) {
        assert(i < 2*BLOCK_COUNT);

        struct lfs3_tinfo tinfo;
        int err = lfs3_trv_read(&lfs3, &trv, &tinfo);
        assert(!err || err == LFS3_ERR_NOENT);
        if (err == LFS3_ERR_NOENT) {
            break;
        }

        assert(tinfo.block != victim);
    }
    lfs3_trv_close(&lfs3, &trv) => 0;

    // gbmap_p should have been discarded, so these should be the same
    assert(lfs3.gbmap.b_p.blocks[0] == lfs3.gbmap.b.blocks[0]);

    // check we can still read the file
    lfs3_file_rewind(&lfs3, &file) => 0;
    prng = wprng;
    for (lfs3_off_t i = 0; i < wsize; i += CHUNK) {
        uint8_t wbuf[CHUNK];
        for (lfs3_size_t j = 0; j < CHUNK; j++) {
            wbuf[j] = 'a' + (TEST_PRNG(&prng) % 26);
        }
        uint8_t rbuf[CHUNK];
        lfs3_file_read(&lfs3, &file, rbuf, CHUNK) => CHUNK;
        assert(memcmp(rbuf, wbuf, CHUNK) == 0);
    }

    // try creating a second file to make sure the gbmap wasn't
    // corrupted
    lfs3_file_t file2;
    lfs3_file_open(&lfs3, &file2, "octopus",
            LFS3_O_RDWR | LFS3_O_CREAT | LFS3_O_EXCL) => 0;
    for (lfs3_off_t i = 0; i < wsize/4; i += CHUNK) {
        uint8_t wbuf[CHUNK];
        for (lfs3_size_t j = 0; j < CHUNK; j++) {
            wbuf[j] = 'a' + (TEST_PRNG(&prng) % 26);
        }
        lfs3_file_write(&lfs3, &file2, wbuf, CHUNK) => CHUNK;
    }
    lfs3_file_sync(&lfs3, &file2) => 0;

    // check we can still read the files
    for (int remount = 0; remount < 2; remount++) {
        // remount?
        if (remount) {
            lfs3_file_close(&lfs3, &file) => 0;
            lfs3_file_close(&lfs3, &file2) => 0;
            lfs3_unmount(&lfs3) => 0;
            lfs3_mount(&lfs3, LFS3_M_RDWR, CFG) => 0;
            lfs3_file_open(&lfs3, &file, "jellyfish", LFS3_O_RDONLY) => 0;
            lfs3_file_open(&lfs3, &file2, "octopus", LFS3_O_RDONLY) => 0;
        }

        lfs3_file_rewind(&lfs3, &file) => 0;
        prng = wprng;
        for (lfs3_off_t i = 0; i < wsize; i += CHUNK) {
            uint8_t wbuf[CHUNK];
            for (lfs3_size_t j = 0; j < CHUNK; j++) {
                wbuf[j] = 'a' + (TEST_PRNG(&prng) % 26);
            }
            uint8_t rbuf[CHUNK];
            lfs3_file_read(&lfs3, &file, rbuf, CHUNK) => CHUNK;
            assert(memcmp(rbuf, wbuf, CHUNK) == 0);
        }

        lfs3_file_rewind(&lfs3, &file2) => 0;
        for (lfs3_off_t i = 0; i < wsize/4; i += CHUNK) {
            uint8_t wbuf[CHUNK];
            for (lfs3_size_t j = 0; j < CHUNK; j++) {
                wbuf[j] = 'a' + (TEST_PRNG(&prng) % 26);
            }
            uint8_t rbuf[CHUNK];
            lfs3_file_read(&lfs3, &file2, rbuf, CHUNK) => CHUNK;
            assert(memcmp(rbuf, wbuf, CHUNK) == 0);
        }
    }

    lfs3_file_close(&lfs3, &file) => 0;
    lfs3_file_close(&lfs3, &file2) => 0;
    lfs3_unmount(&lfs3) => 0;
'''

[cases.test_evict_repair_t_btree_gbmap_p_gbmap_p]
defines.CHUNK = 'BLOCK_SIZE'
defines.SIBLING = [0, 1, 2]
# need to disable shrubs to avoid accidental gbmap syncs
defines.SHRUB_SIZE = 0
# report as damaged to request repairs
defines.BADBLOCK_BEHAVIOR = 'LFS3_EMUBD_BADBLOCK_READDAMAGED'
if = 'GBMAP'
ifdef = 'LFS3_GBMAP'
in = 'lfs3.c'
code = '''
    lfs3_t lfs3;
    lfs3_format(&lfs3, LFS3_F_RDWR | LFS3_F_GBMAP, CFG) => 0;
    lfs3_mount(&lfs3, LFS3_M_RDWR, CFG) => 0;

    uint32_t prng = 42;

    // create two files
    lfs3_file_t file;
    lfs3_file_open(&lfs3, &file, "jellyfish",
            LFS3_O_RDWR | LFS3_O_CREAT | LFS3_O_EXCL) => 0;

    // write to our file until we have at least 3 btree nodes
    //
    // we use a really annoying write pattern to try to fragment the gbmap
    uint32_t wprng = prng;
    lfs3_off_t wsize = 0;
    while (true) {
        lfs3_size_t count = 0;
        lfs3_trv_t trv;
        lfs3_trv_open(&lfs3, &trv, LFS3_T_RDONLY) => 0;
        for (lfs3_block_t i = 0;; i++) {
            assert(i < 2*BLOCK_COUNT);

            struct lfs3_tinfo tinfo;
            int err = lfs3_trv_read(&lfs3, &trv, &tinfo);
            assert(!err || err == LFS3_ERR_NOENT);
            if (err == LFS3_ERR_NOENT) {
                break;
            }

            if (tinfo.btype == LFS3_BTYPE_BTREE
                    // bit of a hack
                    && trv.t.h.mdir.mid == LFS3_MID_GBMAP) {
                count += 1;
            }
        }
        lfs3_trv_close(&lfs3, &trv) => 0;
        if (count >= 3) {
            break;
        }

        uint8_t wbuf[CHUNK];
        for (lfs3_size_t j = 0; j < CHUNK; j++) {
            wbuf[j] = 'a' + (TEST_PRNG(&prng) % 26);
        }
        lfs3_file_write(&lfs3, &file, wbuf, CHUNK) => CHUNK;
        lfs3_soff_t off = lfs3_file_seek(&lfs3, &file, -CHUNK, LFS3_SEEK_END);
        assert(off >= 0);
        for (lfs3_size_t j = 0; j < CHUNK; j++) {
            wbuf[j] = 'a' + (TEST_PRNG(&prng) % 26);
        }
        lfs3_file_write(&lfs3, &file, wbuf, CHUNK) => CHUNK;
        if (SYNC) {
            lfs3_file_sync(&lfs3, &file) => 0;
        }
        wsize += CHUNK;
    }

    // create an unrelated file, as a side effect this commits the gbmap
    // to disk
    lfs3_file_t file3;
    lfs3_file_open(&lfs3, &file3, "cuttlefish",
            LFS3_O_RDWR | LFS3_O_CREAT | LFS3_O_EXCL) => 0;
    lfs3_file_close(&lfs3, &file3) => 0;
    assert(lfs3.gbmap.b.blocks[0] == lfs3.gbmap.b_p.blocks[0]);

    // keep track of gbmap_p
    lfs3_btree_t b_p = lfs3.gbmap.b_p;

    // truncate our file to free up space and get rid of all the
    // fragmented gbmap ranges
    lfs3_file_truncate(&lfs3, &file, 0) => 0;
    lfs3_file_seek(&lfs3, &file, 0, LFS3_SEEK_SET) => 0;
    wprng = prng;
    wsize = 0;

    // force a lookgbmap scan, _after_ we truncate
    lfs3.gbmap.known = 0;
    lfs3.flags |= LFS3_I_LOOKAHEAD;

    // rewrite our file to force a lookgbmap scan and hopefully compact
    // the gbmap
    //
    // note _write_ triggers lookgbmap scans, but we don't commit the
    // gbmap until _sync_
    //
    // we also need to avoid any mdir commits (open+creat, shrubs, etc)!
    // littlefs is very opportunistic!
    while (lfs3.gbmap.b.blocks[0] == lfs3.gbmap.b_p.blocks[0]) {
        uint8_t wbuf[CHUNK];
        for (lfs3_size_t j = 0; j < CHUNK; j++) {
            wbuf[j] = 'a' + (TEST_PRNG(&prng) % 26);
        }
        lfs3_file_write(&lfs3, &file, wbuf, CHUNK) => CHUNK;
        wsize += CHUNK;
        // don't sync!
    }

    // at this point, gbmap and gbmap_p should differ, with gbmap
    // compacted and gbmap_p uncompacted
    assert(lfs3.gbmap.b.blocks[0] != lfs3.gbmap.b_p.blocks[0]);
    assert(lfs3.gbmap.b_p.blocks[0] == b_p.blocks[0]);

    // get a block to evict
    lfs3_block_t victim = 0;
    lfs3_size_t count = 0;
    lfs3_trv_t trv;
    lfs3_trv_open(&lfs3, &trv, LFS3_T_RDONLY) => 0;
    for (lfs3_block_t i = 0;; i++) {
        assert(i < 2*BLOCK_COUNT);

        struct lfs3_tinfo tinfo;
        int err = lfs3_trv_read(&lfs3, &trv, &tinfo);
        assert(!err);

        if (tinfo.btype == LFS3_BTYPE_BTREE
                // bit of a hack
                && trv.t.h.mdir.mid == LFS3_MID_GBMAP_P) {
            if (count == SIBLING) {
                victim = tinfo.block;
                break;
            }

            count += 1;
        }
    }
    lfs3_trv_close(&lfs3, &trv) => 0;

    // make bad
    lfs3_emubd_mkbad(CFG, victim) => 0;
    // reset ck flags
    lfs3_fs_unck(&lfs3, LFS3_GC_CKMETA) => 0;

    // run gc to repair bad blocks
    lfs3_gc_t gc;
    lfs3_gc_open(&lfs3, &gc,
            LFS3_GC_WRONLY
                | LFS3_GC_CKMETA
                | LFS3_GC_REPAIRMETA) => 0;
    for (lfs3_block_t i = 0;; i++) {
        // a bit hacky, but this catches infinite loops
        assert(i < 2*BLOCK_COUNT);

        lfs3_sblock_t steps = lfs3_gc_write(&lfs3, &gc, 1);
        assert(steps >= 0);
        if (steps == 0) {
            break;
        }
    }
    lfs3_gc_close(&lfs3, &gc) => 0;

    // evicted block should no longer be in use, anywhere in the
    // filesystem
    lfs3_trv_open(&lfs3, &trv, LFS3_T_RDONLY) => 0;
    for (lfs3_block_t i = 0;; i++) {
        assert(i < 2*BLOCK_COUNT);

        struct lfs3_tinfo tinfo;
        int err = lfs3_trv_read(&lfs3, &trv, &tinfo);
        assert(!err || err == LFS3_ERR_NOENT);
        if (err == LFS3_ERR_NOENT) {
            break;
        }

        assert(tinfo.block != victim);
    }
    lfs3_trv_close(&lfs3, &trv) => 0;

    // gbmap_p should have been discarded, so these should be the same
    assert(lfs3.gbmap.b_p.blocks[0] == lfs3.gbmap.b.blocks[0]);

    // check we can still read the file
    lfs3_file_rewind(&lfs3, &file) => 0;
    prng = wprng;
    for (lfs3_off_t i = 0; i < wsize; i += CHUNK) {
        uint8_t wbuf[CHUNK];
        for (lfs3_size_t j = 0; j < CHUNK; j++) {
            wbuf[j] = 'a' + (TEST_PRNG(&prng) % 26);
        }
        uint8_t rbuf[CHUNK];
        lfs3_file_read(&lfs3, &file, rbuf, CHUNK) => CHUNK;
        assert(memcmp(rbuf, wbuf, CHUNK) == 0);
    }

    // try creating a second file to make sure the gbmap wasn't
    // corrupted
    lfs3_file_t file2;
    lfs3_file_open(&lfs3, &file2, "octopus",
            LFS3_O_RDWR | LFS3_O_CREAT | LFS3_O_EXCL) => 0;
    for (lfs3_off_t i = 0; i < wsize/4; i += CHUNK) {
        uint8_t wbuf[CHUNK];
        for (lfs3_size_t j = 0; j < CHUNK; j++) {
            wbuf[j] = 'a' + (TEST_PRNG(&prng) % 26);
        }
        lfs3_file_write(&lfs3, &file2, wbuf, CHUNK) => CHUNK;
    }
    lfs3_file_sync(&lfs3, &file2) => 0;

    // check we can still read the files
    for (int remount = 0; remount < 2; remount++) {
        // remount?
        if (remount) {
            lfs3_file_close(&lfs3, &file) => 0;
            lfs3_file_close(&lfs3, &file2) => 0;
            lfs3_unmount(&lfs3) => 0;
            lfs3_mount(&lfs3, LFS3_M_RDWR, CFG) => 0;
            lfs3_file_open(&lfs3, &file, "jellyfish", LFS3_O_RDONLY) => 0;
            lfs3_file_open(&lfs3, &file2, "octopus", LFS3_O_RDONLY) => 0;
        }

        lfs3_file_rewind(&lfs3, &file) => 0;
        prng = wprng;
        for (lfs3_off_t i = 0; i < wsize; i += CHUNK) {
            uint8_t wbuf[CHUNK];
            for (lfs3_size_t j = 0; j < CHUNK; j++) {
                wbuf[j] = 'a' + (TEST_PRNG(&prng) % 26);
            }
            uint8_t rbuf[CHUNK];
            lfs3_file_read(&lfs3, &file, rbuf, CHUNK) => CHUNK;
            assert(memcmp(rbuf, wbuf, CHUNK) == 0);
        }

        lfs3_file_rewind(&lfs3, &file2) => 0;
        for (lfs3_off_t i = 0; i < wsize/4; i += CHUNK) {
            uint8_t wbuf[CHUNK];
            for (lfs3_size_t j = 0; j < CHUNK; j++) {
                wbuf[j] = 'a' + (TEST_PRNG(&prng) % 26);
            }
            uint8_t rbuf[CHUNK];
            lfs3_file_read(&lfs3, &file2, rbuf, CHUNK) => CHUNK;
            assert(memcmp(rbuf, wbuf, CHUNK) == 0);
        }
    }

    lfs3_file_close(&lfs3, &file) => 0;
    lfs3_file_close(&lfs3, &file2) => 0;
    lfs3_unmount(&lfs3) => 0;
'''


# test traversals with bptr repair

[cases.test_evict_repair_t_bptr]
defines.SYNC = [false, true]
defines.CLOSE = [false, true]
# report as damaged to request repairs
defines.BADBLOCK_BEHAVIOR = 'LFS3_EMUBD_BADBLOCK_READDAMAGED'
code = '''
    lfs3_t lfs3;
    lfs3_format(&lfs3,
            LFS3_F_RDWR
                | ((GBMAP) ? LFS3_IFDEF_GBMAP(LFS3_F_GBMAP, -1) : 0),
            CFG) => 0;
    lfs3_mount(&lfs3, LFS3_M_RDWR, CFG) => 0;

    uint32_t prng = 42;

    // create a file
    lfs3_file_t file;
    lfs3_file_open(&lfs3, &file, "jellyfish",
            LFS3_O_RDWR | LFS3_O_CREAT | LFS3_O_EXCL) => 0;

    // write a block
    uint32_t wprng = prng;
    lfs3_off_t wsize = 0;
    uint8_t wbuf[BLOCK_SIZE];
    for (lfs3_size_t j = 0; j < BLOCK_SIZE; j++) {
        wbuf[j] = 'a' + (TEST_PRNG(&prng) % 26);
    }
    lfs3_file_write(&lfs3, &file, wbuf, BLOCK_SIZE) => BLOCK_SIZE;
    if (SYNC) {
        lfs3_file_sync(&lfs3, &file) => 0;
    }

    // close during eviction? this makes sure eviction can find the
    // btree on disk
    if (CLOSE) {
        lfs3_file_close(&lfs3, &file) => 0;
    }

    // get a block to evict
    lfs3_block_t victim = file.leaf.bptr.d.u.disk.block;

    // this block should be in use
    lfs3_trv_t trv;
    lfs3_trv_open(&lfs3, &trv, LFS3_T_RDONLY) => 0;
    for (lfs3_block_t i = 0;; i++) {
        assert(i < 2*BLOCK_COUNT);

        struct lfs3_tinfo tinfo;
        int err = lfs3_trv_read(&lfs3, &trv, &tinfo);
        assert(!err);

        if (tinfo.block == victim) {
            assert(tinfo.btype == LFS3_BTYPE_DATA);
            break;
        }
    }
    lfs3_trv_close(&lfs3, &trv) => 0;

    // make bad
    lfs3_emubd_mkbad(CFG, victim) => 0;
    // reset ck flags
    lfs3_fs_unck(&lfs3, LFS3_GC_CKDATA) => 0;

    // run gc to repair bad blocks
    lfs3_gc_t gc;
    lfs3_gc_open(&lfs3, &gc,
            LFS3_GC_WRONLY
                | LFS3_GC_CKDATA
                | LFS3_GC_REPAIRDATA) => 0;
    for (lfs3_block_t i = 0;; i++) {
        // a bit hacky, but this catches infinite loops
        assert(i < 2*BLOCK_COUNT);

        lfs3_sblock_t steps = lfs3_gc_write(&lfs3, &gc, 1);
        assert(steps >= 0);
        if (steps == 0) {
            break;
        }
    }
    lfs3_gc_close(&lfs3, &gc) => 0;

    // reopen if necessary
    if (CLOSE) {
        lfs3_file_open(&lfs3, &file, "jellyfish", LFS3_O_RDWR) => 0;
    }

    // evicted block should no longer be in use
    assert(file.leaf.bptr.d.u.disk.block != victim);

    // evicted block should no longer be in use, anywhere in the
    // filesystem
    lfs3_trv_open(&lfs3, &trv, LFS3_T_RDONLY) => 0;
    for (lfs3_block_t i = 0;; i++) {
        assert(i < 2*BLOCK_COUNT);

        struct lfs3_tinfo tinfo;
        int err = lfs3_trv_read(&lfs3, &trv, &tinfo);
        assert(!err || err == LFS3_ERR_NOENT);
        if (err == LFS3_ERR_NOENT) {
            break;
        }

        assert(tinfo.block != victim);
    }
    lfs3_trv_close(&lfs3, &trv) => 0;

    // check we can still read the file
    for (int remount = 0; remount < 2; remount++) {
        // remount?
        if (remount) {
            lfs3_file_close(&lfs3, &file) => 0;
            lfs3_unmount(&lfs3) => 0;
            lfs3_mount(&lfs3, LFS3_M_RDWR, CFG) => 0;
            lfs3_file_open(&lfs3, &file, "jellyfish", LFS3_O_RDONLY) => 0;
        }

        lfs3_file_rewind(&lfs3, &file) => 0;
        prng = wprng;
        for (lfs3_off_t i = 0; i < wsize; i += BLOCK_SIZE) {
            uint8_t wbuf[BLOCK_SIZE];
            for (lfs3_size_t j = 0; j < BLOCK_SIZE; j++) {\
                wbuf[j] = 'a' + (TEST_PRNG(&prng) % 26);
            }
            uint8_t rbuf[BLOCK_SIZE];
            lfs3_file_read(&lfs3, &file, rbuf, BLOCK_SIZE) => BLOCK_SIZE;
            assert(memcmp(rbuf, wbuf, BLOCK_SIZE) == 0);
        }
    }

    lfs3_file_close(&lfs3, &file) => 0;
    lfs3_unmount(&lfs3) => 0;
'''

[cases.test_evict_repair_t_bptr_btree]
defines.SYNC = [false, true]
defines.CLOSE = [false, true]
# encourage a btree
defines.SHRUB_SIZE = 0
# report as damaged to request repairs
defines.BADBLOCK_BEHAVIOR = 'LFS3_EMUBD_BADBLOCK_READDAMAGED'
code = '''
    lfs3_t lfs3;
    lfs3_format(&lfs3,
            LFS3_F_RDWR
                | ((GBMAP) ? LFS3_IFDEF_GBMAP(LFS3_F_GBMAP, -1) : 0),
            CFG) => 0;
    lfs3_mount(&lfs3, LFS3_M_RDWR, CFG) => 0;

    uint32_t prng = 42;

    // create a file
    lfs3_file_t file;
    lfs3_file_open(&lfs3, &file, "jellyfish",
            LFS3_O_RDWR | LFS3_O_CREAT | LFS3_O_EXCL) => 0;

    // write to our file until we are a btree and have at least
    // 3 btree node
    uint32_t wprng = prng;
    lfs3_off_t wsize = 0;
    while (true) {
        if (!(file.bshrub.trunk & 0x80000000)) {
            lfs3_size_t count = 0;
            lfs3_trv_t trv;
            lfs3_trv_open(&lfs3, &trv, LFS3_T_RDONLY) => 0;
            for (lfs3_block_t i = 0;; i++) {
                assert(i < 2*BLOCK_COUNT);

                struct lfs3_tinfo tinfo;
                int err = lfs3_trv_read(&lfs3, &trv, &tinfo);
                assert(!err || err == LFS3_ERR_NOENT);
                if (err == LFS3_ERR_NOENT) {
                    break;
                }

                if (tinfo.btype == LFS3_BTYPE_BTREE) {
                    count += 1;
                }
            }
            lfs3_trv_close(&lfs3, &trv) => 0;
            if (count >= 3 + ((GBMAP) ? 1 : 0)) {
                break;
            }
        }

        uint8_t wbuf[BLOCK_SIZE];
        for (lfs3_size_t j = 0; j < BLOCK_SIZE; j++) {
            wbuf[j] = 'a' + (TEST_PRNG(&prng) % 26);
        }
        lfs3_file_write(&lfs3, &file, wbuf, BLOCK_SIZE) => BLOCK_SIZE;
        if (SYNC) {
            lfs3_file_sync(&lfs3, &file) => 0;
        }
        wsize += BLOCK_SIZE;
    }

    // close during eviction? this makes sure eviction can find the
    // btree on disk
    if (CLOSE) {
        lfs3_file_close(&lfs3, &file) => 0;
    }

    // get a block to evict
    lfs3_block_t victim = file.leaf.bptr.d.u.disk.block;

    // this block should be in use
    lfs3_trv_t trv;
    lfs3_trv_open(&lfs3, &trv, LFS3_T_RDONLY) => 0;
    for (lfs3_block_t i = 0;; i++) {
        assert(i < 2*BLOCK_COUNT);

        struct lfs3_tinfo tinfo;
        int err = lfs3_trv_read(&lfs3, &trv, &tinfo);
        assert(!err);

        if (tinfo.block == victim) {
            assert(tinfo.btype == LFS3_BTYPE_DATA);
            break;
        }
    }
    lfs3_trv_close(&lfs3, &trv) => 0;

    // make bad
    lfs3_emubd_mkbad(CFG, victim) => 0;
    // reset ck flags
    lfs3_fs_unck(&lfs3, LFS3_GC_CKDATA) => 0;

    // run gc to repair bad blocks
    lfs3_gc_t gc;
    lfs3_gc_open(&lfs3, &gc,
            LFS3_GC_WRONLY
                | LFS3_GC_CKDATA
                | LFS3_GC_REPAIRDATA) => 0;
    for (lfs3_block_t i = 0;; i++) {
        // a bit hacky, but this catches infinite loops
        assert(i < 2*BLOCK_COUNT);

        lfs3_sblock_t steps = lfs3_gc_write(&lfs3, &gc, 1);
        assert(steps >= 0);
        if (steps == 0) {
            break;
        }
    }
    lfs3_gc_close(&lfs3, &gc) => 0;

    // reopen if necessary
    if (CLOSE) {
        lfs3_file_open(&lfs3, &file, "jellyfish", LFS3_O_RDWR) => 0;
    }

    // evicted block should no longer be in use
    assert(file.leaf.bptr.d.u.disk.block != victim);

    // evicted block should no longer be in use, anywhere in the
    // filesystem
    lfs3_trv_open(&lfs3, &trv, LFS3_T_RDONLY) => 0;
    for (lfs3_block_t i = 0;; i++) {
        assert(i < 2*BLOCK_COUNT);

        struct lfs3_tinfo tinfo;
        int err = lfs3_trv_read(&lfs3, &trv, &tinfo);
        assert(!err || err == LFS3_ERR_NOENT);
        if (err == LFS3_ERR_NOENT) {
            break;
        }

        assert(tinfo.block != victim);
    }
    lfs3_trv_close(&lfs3, &trv) => 0;

    // check we can still read the file
    for (int remount = 0; remount < 2; remount++) {
        // remount?
        if (remount) {
            lfs3_file_close(&lfs3, &file) => 0;
            lfs3_unmount(&lfs3) => 0;
            lfs3_mount(&lfs3, LFS3_M_RDWR, CFG) => 0;
            lfs3_file_open(&lfs3, &file, "jellyfish", LFS3_O_RDONLY) => 0;
        }

        lfs3_file_rewind(&lfs3, &file) => 0;
        prng = wprng;
        for (lfs3_off_t i = 0; i < wsize; i += BLOCK_SIZE) {
            uint8_t wbuf[BLOCK_SIZE];
            for (lfs3_size_t j = 0; j < BLOCK_SIZE; j++) {\
                wbuf[j] = 'a' + (TEST_PRNG(&prng) % 26);
            }
            uint8_t rbuf[BLOCK_SIZE];
            lfs3_file_read(&lfs3, &file, rbuf, BLOCK_SIZE) => BLOCK_SIZE;
            assert(memcmp(rbuf, wbuf, BLOCK_SIZE) == 0);
        }
    }

    lfs3_file_close(&lfs3, &file) => 0;
    lfs3_unmount(&lfs3) => 0;
'''

[cases.test_evict_repair_t_bptr_bshrub]
defines.SYNC = [false, true]
defines.CLOSE = [false, true]
# report as damaged to request repairs
defines.BADBLOCK_BEHAVIOR = 'LFS3_EMUBD_BADBLOCK_READDAMAGED'
code = '''
    lfs3_t lfs3;
    lfs3_format(&lfs3,
            LFS3_F_RDWR
                | ((GBMAP) ? LFS3_IFDEF_GBMAP(LFS3_F_GBMAP, -1) : 0),
            CFG) => 0;
    lfs3_mount(&lfs3, LFS3_M_RDWR, CFG) => 0;

    uint32_t prng = 42;

    // create a file
    lfs3_file_t file;
    lfs3_file_open(&lfs3, &file, "jellyfish",
            LFS3_O_RDWR | LFS3_O_CREAT | LFS3_O_EXCL) => 0;

    // write to our file until we are a bshrub and have at least
    // 3 btree node
    uint32_t wprng = prng;
    lfs3_off_t wsize = 0;
    while (true) {
        if (file.bshrub.trunk & 0x80000000) {
            lfs3_size_t count = 0;
            lfs3_trv_t trv;
            lfs3_trv_open(&lfs3, &trv, LFS3_T_RDONLY) => 0;
            for (lfs3_block_t i = 0;; i++) {
                assert(i < 2*BLOCK_COUNT);

                struct lfs3_tinfo tinfo;
                int err = lfs3_trv_read(&lfs3, &trv, &tinfo);
                assert(!err || err == LFS3_ERR_NOENT);
                if (err == LFS3_ERR_NOENT) {
                    break;
                }

                if (tinfo.btype == LFS3_BTYPE_BTREE) {
                    count += 1;
                }
            }
            lfs3_trv_close(&lfs3, &trv) => 0;
            if (count >= 3 + ((GBMAP) ? 1 : 0)) {
                break;
            }
        }

        uint8_t wbuf[BLOCK_SIZE];
        for (lfs3_size_t j = 0; j < BLOCK_SIZE; j++) {
            wbuf[j] = 'a' + (TEST_PRNG(&prng) % 26);
        }
        lfs3_file_write(&lfs3, &file, wbuf, BLOCK_SIZE) => BLOCK_SIZE;
        if (SYNC) {
            lfs3_file_sync(&lfs3, &file) => 0;
        }
        wsize += BLOCK_SIZE;
    }

    // close during eviction? this makes sure eviction can find the
    // btree on disk
    if (CLOSE) {
        lfs3_file_close(&lfs3, &file) => 0;
    }

    // get a block to evict
    lfs3_block_t victim = file.leaf.bptr.d.u.disk.block;

    // this block should be in use
    lfs3_trv_t trv;
    lfs3_trv_open(&lfs3, &trv, LFS3_T_RDONLY) => 0;
    for (lfs3_block_t i = 0;; i++) {
        assert(i < 2*BLOCK_COUNT);

        struct lfs3_tinfo tinfo;
        int err = lfs3_trv_read(&lfs3, &trv, &tinfo);
        assert(!err);

        if (tinfo.block == victim) {
            assert(tinfo.btype == LFS3_BTYPE_DATA);
            break;
        }
    }
    lfs3_trv_close(&lfs3, &trv) => 0;

    // make bad
    lfs3_emubd_mkbad(CFG, victim) => 0;
    // reset ck flags
    lfs3_fs_unck(&lfs3, LFS3_GC_CKDATA) => 0;

    // run gc to repair bad blocks
    lfs3_gc_t gc;
    lfs3_gc_open(&lfs3, &gc,
            LFS3_GC_WRONLY
                | LFS3_GC_CKDATA
                | LFS3_GC_REPAIRDATA) => 0;
    for (lfs3_block_t i = 0;; i++) {
        // a bit hacky, but this catches infinite loops
        assert(i < 2*BLOCK_COUNT);

        lfs3_sblock_t steps = lfs3_gc_write(&lfs3, &gc, 1);
        assert(steps >= 0);
        if (steps == 0) {
            break;
        }
    }
    lfs3_gc_close(&lfs3, &gc) => 0;

    // reopen if necessary
    if (CLOSE) {
        lfs3_file_open(&lfs3, &file, "jellyfish", LFS3_O_RDWR) => 0;
    }

    // evicted block should no longer be in use
    assert(file.leaf.bptr.d.u.disk.block != victim);

    // evicted block should no longer be in use, anywhere in the
    // filesystem
    lfs3_trv_open(&lfs3, &trv, LFS3_T_RDONLY) => 0;
    for (lfs3_block_t i = 0;; i++) {
        assert(i < 2*BLOCK_COUNT);

        struct lfs3_tinfo tinfo;
        int err = lfs3_trv_read(&lfs3, &trv, &tinfo);
        assert(!err || err == LFS3_ERR_NOENT);
        if (err == LFS3_ERR_NOENT) {
            break;
        }

        assert(tinfo.block != victim);
    }
    lfs3_trv_close(&lfs3, &trv) => 0;

    // check we can still read the file
    for (int remount = 0; remount < 2; remount++) {
        // remount?
        if (remount) {
            lfs3_file_close(&lfs3, &file) => 0;
            lfs3_unmount(&lfs3) => 0;
            lfs3_mount(&lfs3, LFS3_M_RDWR, CFG) => 0;
            lfs3_file_open(&lfs3, &file, "jellyfish", LFS3_O_RDONLY) => 0;
        }

        lfs3_file_rewind(&lfs3, &file) => 0;
        prng = wprng;
        for (lfs3_off_t i = 0; i < wsize; i += BLOCK_SIZE) {
            uint8_t wbuf[BLOCK_SIZE];
            for (lfs3_size_t j = 0; j < BLOCK_SIZE; j++) {\
                wbuf[j] = 'a' + (TEST_PRNG(&prng) % 26);
            }
            uint8_t rbuf[BLOCK_SIZE];
            lfs3_file_read(&lfs3, &file, rbuf, BLOCK_SIZE) => BLOCK_SIZE;
            assert(memcmp(rbuf, wbuf, BLOCK_SIZE) == 0);
        }
    }

    lfs3_file_close(&lfs3, &file) => 0;
    lfs3_unmount(&lfs3) => 0;
'''

# test evicting a dag block
[cases.test_evict_repair_t_bptr_dag]
defines.SYNC = [false, true]
defines.CLOSE = [false, true]
# report as damaged to request repairs
defines.BADBLOCK_BEHAVIOR = 'LFS3_EMUBD_BADBLOCK_READDAMAGED'
code = '''
    lfs3_t lfs3;
    lfs3_format(&lfs3,
            LFS3_F_RDWR
                | ((GBMAP) ? LFS3_IFDEF_GBMAP(LFS3_F_GBMAP, -1) : 0),
            CFG) => 0;
    lfs3_mount(&lfs3, LFS3_M_RDWR, CFG) => 0;

    uint32_t prng = 42;

    // create a file
    lfs3_file_t file;
    lfs3_file_open(&lfs3, &file, "jellyfish",
            LFS3_O_RDWR | LFS3_O_CREAT | LFS3_O_EXCL) => 0;

    // write a block
    uint8_t wbuf[BLOCK_SIZE];
    for (lfs3_size_t j = 0; j < BLOCK_SIZE; j++) {
        wbuf[j] = 'a' + (TEST_PRNG(&prng) % 26);
    }
    lfs3_file_write(&lfs3, &file, wbuf, BLOCK_SIZE) => BLOCK_SIZE;
    lfs3_file_flush(&lfs3, &file) => 0;
    // write a single byte in the middle of the block, forcing a dag
    wbuf[BLOCK_SIZE/2] = '!';
    lfs3_file_seek(&lfs3, &file, BLOCK_SIZE/2, LFS3_SEEK_SET)
            => BLOCK_SIZE/2;
    lfs3_file_write(&lfs3, &file, &wbuf[BLOCK_SIZE/2], 1) => 1;
    lfs3_file_flush(&lfs3, &file) => 0;
    // sync?
    if (SYNC) {
        lfs3_file_sync(&lfs3, &file) => 0;
    }

    // read to get the bptr back in our leaf
    uint8_t rbuf[BLOCK_SIZE];
    lfs3_file_seek(&lfs3, &file, -1, LFS3_SEEK_END) => BLOCK_SIZE-1;
    lfs3_file_read(&lfs3, &file, rbuf, 1) => 1;

    // close during eviction? this makes sure eviction can find the
    // btree on disk
    if (CLOSE) {
        lfs3_file_close(&lfs3, &file) => 0;
    }

    // get a block to evict
    lfs3_block_t victim = file.leaf.bptr.d.u.disk.block;

    // this block should be in use, twice! because it's a dag
    lfs3_trv_t trv;
    lfs3_size_t count = 0;
    lfs3_trv_open(&lfs3, &trv, LFS3_T_RDONLY) => 0;
    for (lfs3_block_t i = 0;; i++) {
        assert(i < 2*BLOCK_COUNT);

        struct lfs3_tinfo tinfo;
        int err = lfs3_trv_read(&lfs3, &trv, &tinfo);
        assert(!err || err == LFS3_ERR_NOENT);
        if (err == LFS3_ERR_NOENT) {
            break;
        }

        if (tinfo.btype == LFS3_BTYPE_DATA) {
            assert(tinfo.block == victim);
            count += 1;
        }
    }
    lfs3_trv_close(&lfs3, &trv) => 0;
    assert(count == 2);

    // make bad
    lfs3_emubd_mkbad(CFG, victim) => 0;
    // reset ck flags
    lfs3_fs_unck(&lfs3, LFS3_GC_CKDATA) => 0;

    // run gc to repair bad blocks
    lfs3_gc_t gc;
    lfs3_gc_open(&lfs3, &gc,
            LFS3_GC_WRONLY
                | LFS3_GC_CKDATA
                | LFS3_GC_REPAIRDATA) => 0;
    for (lfs3_block_t i = 0;; i++) {
        // a bit hacky, but this catches infinite loops
        assert(i < 2*BLOCK_COUNT);

        lfs3_sblock_t steps = lfs3_gc_write(&lfs3, &gc, 1);
        assert(steps >= 0);
        if (steps == 0) {
            break;
        }
    }
    lfs3_gc_close(&lfs3, &gc) => 0;

    // reopen if necessary
    if (CLOSE) {
        lfs3_file_open(&lfs3, &file, "jellyfish", LFS3_O_RDWR) => 0;
    }

    // evicted block should no longer be in use
    assert(file.leaf.bptr.d.u.disk.block != victim);

    // evicted block should no longer be in use, anywhere in the
    // filesystem
    lfs3_trv_open(&lfs3, &trv, LFS3_T_RDONLY) => 0;
    for (lfs3_block_t i = 0;; i++) {
        assert(i < 2*BLOCK_COUNT);

        struct lfs3_tinfo tinfo;
        int err = lfs3_trv_read(&lfs3, &trv, &tinfo);
        assert(!err || err == LFS3_ERR_NOENT);
        if (err == LFS3_ERR_NOENT) {
            break;
        }

        assert(tinfo.block != victim);
    }
    lfs3_trv_close(&lfs3, &trv) => 0;

    // but we should still only have one block in use, twice, because
    // it's a dag
    lfs3_block_t block = 0;
    count = 0;
    lfs3_trv_open(&lfs3, &trv, LFS3_T_RDONLY) => 0;
    for (lfs3_block_t i = 0;; i++) {
        assert(i < 2*BLOCK_COUNT);

        struct lfs3_tinfo tinfo;
        int err = lfs3_trv_read(&lfs3, &trv, &tinfo);
        assert(!err || err == LFS3_ERR_NOENT);
        if (err == LFS3_ERR_NOENT) {
            break;
        }

        if (tinfo.btype == LFS3_BTYPE_DATA) {
            assert(!block || tinfo.block == block);
            count += 1;
        }
    }
    lfs3_trv_close(&lfs3, &trv) => 0;
    assert(count == 2);

    // check we can still read the file
    for (int remount = 0; remount < 2; remount++) {
        // remount?
        if (remount) {
            lfs3_file_close(&lfs3, &file) => 0;
            lfs3_unmount(&lfs3) => 0;
            lfs3_mount(&lfs3, LFS3_M_RDWR, CFG) => 0;
            lfs3_file_open(&lfs3, &file, "jellyfish", LFS3_O_RDONLY) => 0;
        }

        lfs3_file_rewind(&lfs3, &file) => 0;
        lfs3_file_read(&lfs3, &file, rbuf, BLOCK_SIZE) => BLOCK_SIZE;
        assert(memcmp(rbuf, wbuf, BLOCK_SIZE) == 0);
    }

    lfs3_file_close(&lfs3, &file) => 0;
    lfs3_unmount(&lfs3) => 0;
'''

# test evicting a partially erased block
#
# Well, we can't use erased state in evicted blocks, but we shouldn't
# explode or anything
#
[cases.test_evict_repair_t_bptr_erased]
defines.SYNC = [false, true]
# report as damaged to request repairs
defines.BADBLOCK_BEHAVIOR = 'LFS3_EMUBD_BADBLOCK_READDAMAGED'
# is half a block prog aligned?
if = '(BLOCK_SIZE/2) % PROG_SIZE == 0'
in = 'lfs3.c'
code = '''
    lfs3_t lfs3;
    lfs3_format(&lfs3,
            LFS3_F_RDWR
                | ((GBMAP) ? LFS3_IFDEF_GBMAP(LFS3_F_GBMAP, -1) : 0),
            CFG) => 0;
    lfs3_mount(&lfs3, LFS3_M_RDWR, CFG) => 0;

    uint32_t prng = 42;

    // create a file
    lfs3_file_t file;
    lfs3_file_open(&lfs3, &file, "jellyfish",
            LFS3_O_RDWR | LFS3_O_CREAT | LFS3_O_EXCL) => 0;

    // write half a block
    uint8_t wbuf[BLOCK_SIZE];
    for (lfs3_size_t j = 0; j < BLOCK_SIZE/2; j++) {
        wbuf[j] = 'a' + (TEST_PRNG(&prng) % 26);
    }
    lfs3_file_write(&lfs3, &file, wbuf, BLOCK_SIZE/2) => BLOCK_SIZE/2;
    lfs3_file_flush(&lfs3, &file) => 0;
    // sync?
    if (SYNC) {
        lfs3_file_sync(&lfs3, &file) => 0;
    }

    // get a block to evict
    lfs3_block_t victim = file.leaf.bptr.d.u.disk.block;

    // this block should be in use
    lfs3_trv_t trv;
    lfs3_trv_open(&lfs3, &trv, LFS3_T_RDONLY) => 0;
    for (lfs3_block_t i = 0;; i++) {
        assert(i < 2*BLOCK_COUNT);

        struct lfs3_tinfo tinfo;
        int err = lfs3_trv_read(&lfs3, &trv, &tinfo);
        assert(!err);

        if (tinfo.block == victim) {
            assert(tinfo.btype == LFS3_BTYPE_DATA);
            break;
        }
    }
    lfs3_trv_close(&lfs3, &trv) => 0;

    // the block should be marked as erased
    assert(lfs3_bptr_iserased(&file.leaf.bptr));

    // make bad
    lfs3_emubd_mkbad(CFG, victim) => 0;
    // reset ck flags
    lfs3_fs_unck(&lfs3, LFS3_GC_CKDATA) => 0;

    // run gc to repair bad blocks
    lfs3_gc_t gc;
    lfs3_gc_open(&lfs3, &gc,
            LFS3_GC_WRONLY
                | LFS3_GC_CKDATA
                | LFS3_GC_REPAIRDATA) => 0;
    for (lfs3_block_t i = 0;; i++) {
        // a bit hacky, but this catches infinite loops
        assert(i < 2*BLOCK_COUNT);

        lfs3_sblock_t steps = lfs3_gc_write(&lfs3, &gc, 1);
        assert(steps >= 0);
        if (steps == 0) {
            break;
        }
    }
    lfs3_gc_close(&lfs3, &gc) => 0;

    // evicted block should no longer be in use
    assert(file.leaf.bptr.d.u.disk.block != victim);

    // evicted block should no longer be in use, anywhere in the
    // filesystem
    lfs3_trv_open(&lfs3, &trv, LFS3_T_RDONLY) => 0;
    for (lfs3_block_t i = 0;; i++) {
        assert(i < 2*BLOCK_COUNT);

        struct lfs3_tinfo tinfo;
        int err = lfs3_trv_read(&lfs3, &trv, &tinfo);
        assert(!err || err == LFS3_ERR_NOENT);
        if (err == LFS3_ERR_NOENT) {
            break;
        }

        assert(tinfo.block != victim);
    }
    lfs3_trv_close(&lfs3, &trv) => 0;

    // we should no longer be marked as erased
    assert(file.leaf.bptr.d.u.disk.block == 0
            || !lfs3_bptr_iserased(&file.leaf.bptr));

    // check we can still read the file
    uint8_t rbuf[BLOCK_SIZE];
    lfs3_file_rewind(&lfs3, &file) => 0;
    lfs3_file_read(&lfs3, &file, rbuf, BLOCK_SIZE) => BLOCK_SIZE/2;
    assert(memcmp(rbuf, wbuf, BLOCK_SIZE/2) == 0);

    // finish writing the block to try to make sure nothing is funky
    for (lfs3_size_t j = 0; j < BLOCK_SIZE/2; j++) {
        wbuf[(BLOCK_SIZE/2)+j] = 'a' + (TEST_PRNG(&prng) % 26);
    }
    lfs3_file_write(&lfs3, &file,
            &wbuf[BLOCK_SIZE/2], BLOCK_SIZE/2) => BLOCK_SIZE/2;
    lfs3_file_flush(&lfs3, &file) => 0;
    // sync?
    if (SYNC) {
        lfs3_file_sync(&lfs3, &file) => 0;
    }

    // check we can still read the file
    for (int remount = 0; remount < 2; remount++) {
        // remount?
        if (remount) {
            lfs3_file_close(&lfs3, &file) => 0;
            lfs3_unmount(&lfs3) => 0;
            lfs3_mount(&lfs3, LFS3_M_RDWR, CFG) => 0;
            lfs3_file_open(&lfs3, &file, "jellyfish", LFS3_O_RDONLY) => 0;
        }

        lfs3_file_rewind(&lfs3, &file) => 0;
        lfs3_file_read(&lfs3, &file, rbuf, BLOCK_SIZE) => BLOCK_SIZE;
        assert(memcmp(rbuf, wbuf, BLOCK_SIZE) == 0);
    }

    lfs3_file_close(&lfs3, &file) => 0;
    lfs3_unmount(&lfs3) => 0;
'''

# test bshrubs with evicted bptrs are broadcast correctly
[cases.test_evict_repair_t_bptr_broadcast]
defines.SIZE = [
    '1*BLOCK_SIZE', '2*BLOCK_SIZE', '3*BLOCK_SIZE', '4*BLOCK_SIZE'
]
defines.HANDLESET = 'range(0x8)'
defines.SIBLING = [0, 1, 2, 3]
# report as damaged to request repairs
defines.BADBLOCK_BEHAVIOR = 'LFS3_EMUBD_BADBLOCK_READDAMAGED'
if = 'SIBLING < SIZE/BLOCK_SIZE'
code = '''
    lfs3_t lfs3;
    lfs3_format(&lfs3,
            LFS3_F_RDWR
                | ((GBMAP) ? LFS3_IFDEF_GBMAP(LFS3_F_GBMAP, -1) : 0),
            CFG) => 0;
    lfs3_mount(&lfs3, LFS3_M_RDWR, CFG) => 0;

    uint32_t prng = 42;

    // create a file
    lfs3_file_t file;
    lfs3_file_open(&lfs3, &file, "jellyfish",
            LFS3_O_RDWR | LFS3_O_CREAT | LFS3_O_EXCL) => 0;

    // write n blocks
    uint32_t wprng = prng;
    lfs3_off_t wsize = 0;
    while (wsize < SIZE) {
        uint8_t wbuf[BLOCK_SIZE];
        for (lfs3_size_t j = 0; j < BLOCK_SIZE; j++) {
            wbuf[j] = 'a' + (TEST_PRNG(&prng) % 26);
        }
        lfs3_file_write(&lfs3, &file, wbuf, BLOCK_SIZE) => BLOCK_SIZE;
        lfs3_file_sync(&lfs3, &file) => 0;
        wsize += BLOCK_SIZE;
    }

    // open a couple other handles in different states
    lfs3_file_t file_mutated;
    if (HANDLESET & 0x1) {
        lfs3_file_open(&lfs3, &file_mutated, "jellyfish", LFS3_O_RDWR) => 0;
        lfs3_file_write(&lfs3, &file_mutated, "!", 1) => 1;
        lfs3_file_flush(&lfs3, &file_mutated) => 0;
    }

    lfs3_file_t file_desync;
    if (HANDLESET & 0x2) {
        lfs3_file_open(&lfs3, &file_desync, "jellyfish",
                LFS3_O_RDWR | LFS3_O_DESYNC) => 0;
    }

    lfs3_file_t file_desync_mutated;
    if (HANDLESET & 0x4) {
        lfs3_file_open(&lfs3, &file_desync_mutated, "jellyfish",
                LFS3_O_RDWR | LFS3_O_DESYNC) => 0;
        lfs3_file_write(&lfs3, &file_desync_mutated, "!", 1) => 1;
        lfs3_file_flush(&lfs3, &file_desync_mutated) => 0;
    }

    // get a block to evict
    lfs3_block_t victim = 0;
    lfs3_size_t count = 0;
    lfs3_trv_t trv;
    lfs3_trv_open(&lfs3, &trv, LFS3_T_RDONLY) => 0;
    for (lfs3_block_t i = 0;; i++) {
        assert(i < 2*BLOCK_COUNT);

        struct lfs3_tinfo tinfo;
        int err = lfs3_trv_read(&lfs3, &trv, &tinfo);
        assert(!err);

        if (tinfo.btype == LFS3_BTYPE_DATA) {
            if (count == SIBLING) {
                victim = tinfo.block;
                break;
            }

            count += 1;
        }
    }
    lfs3_trv_close(&lfs3, &trv) => 0;

    // make bad
    lfs3_emubd_mkbad(CFG, victim) => 0;
    // reset ck flags
    lfs3_fs_unck(&lfs3, LFS3_GC_CKDATA) => 0;

    // run gc to repair bad blocks
    lfs3_gc_t gc;
    lfs3_gc_open(&lfs3, &gc,
            LFS3_GC_WRONLY
                | LFS3_GC_CKDATA
                | LFS3_GC_REPAIRDATA) => 0;
    for (lfs3_block_t i = 0;; i++) {
        // a bit hacky, but this catches infinite loops
        assert(i < 2*BLOCK_COUNT);

        lfs3_sblock_t steps = lfs3_gc_write(&lfs3, &gc, 1);
        assert(steps >= 0);
        if (steps == 0) {
            break;
        }
    }
    lfs3_gc_close(&lfs3, &gc) => 0;

    // evicted block should no longer be in use, anywhere in the
    // filesystem
    lfs3_trv_open(&lfs3, &trv, LFS3_T_RDONLY) => 0;
    for (lfs3_block_t i = 0;; i++) {
        assert(i < 2*BLOCK_COUNT);

        struct lfs3_tinfo tinfo;
        int err = lfs3_trv_read(&lfs3, &trv, &tinfo);
        assert(!err || err == LFS3_ERR_NOENT);
        if (err == LFS3_ERR_NOENT) {
            break;
        }

        assert(tinfo.block != victim);
    }
    lfs3_trv_close(&lfs3, &trv) => 0;

    if (HANDLESET & 0x1) {
        // file_mutated should still be a different bshrub
        assert(file_mutated.bshrub.blocks[0]
                    != file.bshrub.blocks[0]
                || (file_mutated.bshrub.trunk & 0x7fffffff)
                    != (file.bshrub.trunk & 0x7fffffff));
    }

    if (HANDLESET & 0x2) {
        // file_desync should be the same bshrub, yes we ignore desync
        // here!
        assert(file_desync.bshrub.blocks[0]
                    == file.bshrub.blocks[0]
                && (file_desync.bshrub.trunk & 0x7fffffff)
                    == (file.bshrub.trunk & 0x7fffffff));
    }

    if (HANDLESET & 0x4) {
        // file_desync_mutated should still be a different bshrub
        assert(file_desync_mutated.bshrub.blocks[0]
                    != file.bshrub.blocks[0]
                || (file_desync_mutated.bshrub.trunk & 0x7fffffff)
                    != (file.bshrub.trunk & 0x7fffffff));
    }

    // check we can still read the files
    lfs3_file_rewind(&lfs3, &file) => 0;
    prng = wprng;
    for (lfs3_off_t i = 0; i < wsize; i += BLOCK_SIZE) {
        uint8_t wbuf[BLOCK_SIZE];
        for (lfs3_size_t j = 0; j < BLOCK_SIZE; j++) {
            wbuf[j] = 'a' + (TEST_PRNG(&prng) % 26);
        }
        uint8_t rbuf[BLOCK_SIZE];
        lfs3_file_read(&lfs3, &file,
                rbuf, BLOCK_SIZE) => BLOCK_SIZE;
        assert(memcmp(rbuf, wbuf, BLOCK_SIZE) == 0);
    }

    if (HANDLESET & 0x1) {
        lfs3_file_rewind(&lfs3, &file_mutated) => 0;
        prng = wprng;
        for (lfs3_off_t i = 0; i < wsize; i += BLOCK_SIZE) {
            uint8_t wbuf[BLOCK_SIZE];
            for (lfs3_size_t j = 0; j < BLOCK_SIZE; j++) {
                wbuf[j] = 'a' + (TEST_PRNG(&prng) % 26);
            }
            if (i == 0) {
                wbuf[0] = '!';
            }
            uint8_t rbuf[BLOCK_SIZE];
            lfs3_file_read(&lfs3, &file_mutated,
                    rbuf, BLOCK_SIZE) => BLOCK_SIZE;
            assert(memcmp(rbuf, wbuf, BLOCK_SIZE) == 0);
        }
    }

    if (HANDLESET & 0x2) {
        lfs3_file_rewind(&lfs3, &file_desync) => 0;
        prng = wprng;
        for (lfs3_off_t i = 0; i < wsize; i += BLOCK_SIZE) {
            uint8_t wbuf[BLOCK_SIZE];
            for (lfs3_size_t j = 0; j < BLOCK_SIZE; j++) {
                wbuf[j] = 'a' + (TEST_PRNG(&prng) % 26);
            }
            uint8_t rbuf[BLOCK_SIZE];
            lfs3_file_read(&lfs3, &file_desync,
                    rbuf, BLOCK_SIZE) => BLOCK_SIZE;
            assert(memcmp(rbuf, wbuf, BLOCK_SIZE) == 0);
        }
    }

    if (HANDLESET & 0x4) {
        lfs3_file_rewind(&lfs3, &file_desync_mutated) => 0;
        prng = wprng;
        for (lfs3_off_t i = 0; i < wsize; i += BLOCK_SIZE) {
            uint8_t wbuf[BLOCK_SIZE];
            for (lfs3_size_t j = 0; j < BLOCK_SIZE; j++) {
                wbuf[j] = 'a' + (TEST_PRNG(&prng) % 26);
            }
            if (i == 0) {
                wbuf[0] = '!';
            }
            uint8_t rbuf[BLOCK_SIZE];
            lfs3_file_read(&lfs3, &file_desync_mutated,
                    rbuf, BLOCK_SIZE) => BLOCK_SIZE;
            assert(memcmp(rbuf, wbuf, BLOCK_SIZE) == 0);
        }
    }

    lfs3_file_close(&lfs3, &file) => 0;
    if (HANDLESET & 0x1) {
        lfs3_file_desync(&lfs3, &file_mutated) => 0;
        lfs3_file_close(&lfs3, &file_mutated) => 0;
    }
    if (HANDLESET & 0x2) {
        lfs3_file_close(&lfs3, &file_desync) => 0;
    }
    if (HANDLESET & 0x4) {
        lfs3_file_close(&lfs3, &file_desync_mutated) => 0;
    }
    lfs3_unmount(&lfs3) => 0;
'''

# test bptrs are updated correctly, even when referenced by unrelated
# btrees
[cases.test_evict_repair_t_bptr_shared]
defines.SIZE = [
    '1*BLOCK_SIZE', '2*BLOCK_SIZE', '3*BLOCK_SIZE', '4*BLOCK_SIZE'
]
defines.N = 4
defines.DELTA = [
    '+2*BLOCK_SIZE', '+1*BLOCK_SIZE', '-1*BLOCK_SIZE', '-2*BLOCK_SIZE'
]
defines.DESYNC = [false, true]
defines.SIBLING = [0, 1, 2, 3]
# report as damaged to request repairs
defines.BADBLOCK_BEHAVIOR = 'LFS3_EMUBD_BADBLOCK_READDAMAGED'
if = 'SIBLING < SIZE/BLOCK_SIZE'
code = '''
    lfs3_t lfs3;
    lfs3_format(&lfs3,
            LFS3_F_RDWR
                | ((GBMAP) ? LFS3_IFDEF_GBMAP(LFS3_F_GBMAP, -1) : 0),
            CFG) => 0;
    lfs3_mount(&lfs3, LFS3_M_RDWR, CFG) => 0;

    uint32_t prng = 42;

    // create a file
    lfs3_file_t files[N];
    lfs3_file_open(&lfs3, &files[0], "jellyfish",
            LFS3_O_RDWR | LFS3_O_CREAT | LFS3_O_EXCL) => 0;

    // write n blocks
    uint32_t wprng = prng;
    lfs3_off_t wsize = 0;
    while (wsize < SIZE) {
        uint8_t wbuf[BLOCK_SIZE];
        for (lfs3_size_t j = 0; j < BLOCK_SIZE; j++) {
            wbuf[j] = 'a' + (TEST_PRNG(&prng) % 26);
        }
        lfs3_file_write(&lfs3, &files[0], wbuf, BLOCK_SIZE) => BLOCK_SIZE;
        lfs3_file_sync(&lfs3, &files[0]) => 0;
        wsize += BLOCK_SIZE;
    }

    // open N-1 other handles
    for (lfs3_size_t k = 1; k < N; k++) {
        lfs3_file_open(&lfs3, &files[k], "jellyfish", LFS3_O_RDWR) => 0;

        // use DELTA to derive a somewhat chaotic size for each file,
        // limited to at least BLOCK_SIZE
        lfs3_off_t wsize_ = BLOCK_SIZE + DELTA*k;
        while ((lfs3_soff_t)wsize_ < (lfs3_soff_t)BLOCK_SIZE) {
            wsize_ += BLOCK_SIZE;
        }
        // either truncate, or continue writing, note this should keep
        // any overlapping blocks
        if (wsize_ < wsize) {
            lfs3_file_truncate(&lfs3, &files[k], wsize_) => 0;
        } else {
            prng = wprng;
            for (lfs3_size_t i = 0; i < wsize; i += 1) {
                TEST_PRNG(&prng);
            }
            lfs3_file_seek(&lfs3, &files[k], 0, LFS3_SEEK_END) => wsize;
            for (lfs3_size_t i = wsize; i < wsize_; i += BLOCK_SIZE) {
                uint8_t wbuf[BLOCK_SIZE];
                for (lfs3_size_t j = 0; j < BLOCK_SIZE; j++) {
                    wbuf[j] = 'a' + (TEST_PRNG(&prng) % 26);
                }
                lfs3_file_write(&lfs3, &files[k],
                        wbuf, BLOCK_SIZE) => BLOCK_SIZE;
            }
            lfs3_file_flush(&lfs3, &files[k]) => 0;
            // block evictions ignore desync, which risks complicating
            // things
            if (DESYNC) {
                lfs3_file_desync(&lfs3, &files[k]) => 0;
            }
        }
    }

    // get a block to evict
    lfs3_block_t victim = 0;
    lfs3_size_t count = 0;
    lfs3_trv_t trv;
    lfs3_trv_open(&lfs3, &trv, LFS3_T_RDONLY) => 0;
    for (lfs3_block_t i = 0;; i++) {
        assert(i < 2*BLOCK_COUNT);

        struct lfs3_tinfo tinfo;
        int err = lfs3_trv_read(&lfs3, &trv, &tinfo);
        assert(!err);

        if (tinfo.btype == LFS3_BTYPE_DATA) {
            if (count == SIBLING) {
                victim = tinfo.block;
                break;
            }

            count += 1;
        }
    }
    lfs3_trv_close(&lfs3, &trv) => 0;

    // make bad
    lfs3_emubd_mkbad(CFG, victim) => 0;
    // reset ck flags
    lfs3_fs_unck(&lfs3, LFS3_GC_CKDATA) => 0;

    // run gc to repair bad blocks
    lfs3_gc_t gc;
    lfs3_gc_open(&lfs3, &gc,
            LFS3_GC_WRONLY
                | LFS3_GC_CKDATA
                | LFS3_GC_REPAIRDATA) => 0;
    for (lfs3_block_t i = 0;; i++) {
        // a bit hacky, but this catches infinite loops
        assert(i < 2*BLOCK_COUNT);

        lfs3_sblock_t steps = lfs3_gc_write(&lfs3, &gc, 1);
        assert(steps >= 0);
        if (steps == 0) {
            break;
        }
    }
    lfs3_gc_close(&lfs3, &gc) => 0;

    // evicted block should no longer be in use, anywhere in the
    // filesystem
    lfs3_trv_open(&lfs3, &trv, LFS3_T_RDONLY) => 0;
    for (lfs3_block_t i = 0;; i++) {
        assert(i < 2*BLOCK_COUNT);

        struct lfs3_tinfo tinfo;
        int err = lfs3_trv_read(&lfs3, &trv, &tinfo);
        assert(!err || err == LFS3_ERR_NOENT);
        if (err == LFS3_ERR_NOENT) {
            break;
        }

        assert(tinfo.block != victim);
    }
    lfs3_trv_close(&lfs3, &trv) => 0;

    // check we can still read the files
    lfs3_file_rewind(&lfs3, &files[0]) => 0;
    prng = wprng;
    for (lfs3_off_t i = 0; i < wsize; i += BLOCK_SIZE) {
        uint8_t wbuf[BLOCK_SIZE];
        for (lfs3_size_t j = 0; j < BLOCK_SIZE; j++) {
            wbuf[j] = 'a' + (TEST_PRNG(&prng) % 26);
        }
        uint8_t rbuf[BLOCK_SIZE];
        lfs3_file_read(&lfs3, &files[0],
                rbuf, BLOCK_SIZE) => BLOCK_SIZE;
        assert(memcmp(rbuf, wbuf, BLOCK_SIZE) == 0);
    }

    for (lfs3_size_t k = 1; k < N; k++) {
        lfs3_off_t wsize_ = BLOCK_SIZE + DELTA*k;
        while ((lfs3_soff_t)wsize_ < (lfs3_soff_t)BLOCK_SIZE) {
            wsize_ += BLOCK_SIZE;
        }
        lfs3_file_rewind(&lfs3, &files[k]) => 0;
        prng = wprng;
        for (lfs3_off_t i = 0; i < wsize_; i += BLOCK_SIZE) {
            uint8_t wbuf[BLOCK_SIZE];
            for (lfs3_size_t j = 0; j < BLOCK_SIZE; j++) {
                wbuf[j] = 'a' + (TEST_PRNG(&prng) % 26);
            }
            uint8_t rbuf[BLOCK_SIZE];
            lfs3_file_read(&lfs3, &files[k],
                    rbuf, BLOCK_SIZE) => BLOCK_SIZE;
            assert(memcmp(rbuf, wbuf, BLOCK_SIZE) == 0);
        }
    }

    // try writing an additional block to all files, to make sure
    // things still work
    prng = wprng;
    for (lfs3_size_t i = 0; i < wsize; i += 1) {
        TEST_PRNG(&prng);
    }
    lfs3_file_seek(&lfs3, &files[0], 0, LFS3_SEEK_END) => wsize;
    uint8_t wbuf[BLOCK_SIZE];
    for (lfs3_size_t j = 0; j < BLOCK_SIZE; j++) {
        wbuf[j] = 'a' + (TEST_PRNG(&prng) % 26);
    }
    lfs3_file_write(&lfs3, &files[0],
            wbuf, BLOCK_SIZE) => BLOCK_SIZE;
    lfs3_file_flush(&lfs3, &files[0]) => 0;
    
    for (lfs3_size_t k = 1; k < N; k++) {
        lfs3_off_t wsize_ = BLOCK_SIZE + DELTA*k;
        while ((lfs3_soff_t)wsize_ < (lfs3_soff_t)BLOCK_SIZE) {
            wsize_ += BLOCK_SIZE;
        }
        prng = wprng;
        for (lfs3_size_t i = 0; i < wsize_; i += 1) {
            TEST_PRNG(&prng);
        }
        lfs3_file_seek(&lfs3, &files[k], 0, LFS3_SEEK_END) => wsize_;
        uint8_t wbuf[BLOCK_SIZE];
        for (lfs3_size_t j = 0; j < BLOCK_SIZE; j++) {
            wbuf[j] = 'a' + (TEST_PRNG(&prng) % 26);
        }
        lfs3_file_write(&lfs3, &files[k],
                wbuf, BLOCK_SIZE) => BLOCK_SIZE;
        lfs3_file_flush(&lfs3, &files[k]) => 0;
    }

    // check we can still read the files
    lfs3_file_rewind(&lfs3, &files[0]) => 0;
    prng = wprng;
    for (lfs3_off_t i = 0; i < wsize+BLOCK_SIZE; i += BLOCK_SIZE) {
        uint8_t wbuf[BLOCK_SIZE];
        for (lfs3_size_t j = 0; j < BLOCK_SIZE; j++) {
            wbuf[j] = 'a' + (TEST_PRNG(&prng) % 26);
        }
        uint8_t rbuf[BLOCK_SIZE];
        lfs3_file_read(&lfs3, &files[0],
                rbuf, BLOCK_SIZE) => BLOCK_SIZE;
        assert(memcmp(rbuf, wbuf, BLOCK_SIZE) == 0);
    }

    for (lfs3_size_t k = 1; k < N; k++) {
        lfs3_off_t wsize_ = BLOCK_SIZE + DELTA*k;
        while ((lfs3_soff_t)wsize_ < (lfs3_soff_t)BLOCK_SIZE) {
            wsize_ += BLOCK_SIZE;
        }
        lfs3_file_rewind(&lfs3, &files[k]) => 0;
        prng = wprng;
        for (lfs3_off_t i = 0; i < wsize_+BLOCK_SIZE; i += BLOCK_SIZE) {
            uint8_t wbuf[BLOCK_SIZE];
            for (lfs3_size_t j = 0; j < BLOCK_SIZE; j++) {
                wbuf[j] = 'a' + (TEST_PRNG(&prng) % 26);
            }
            uint8_t rbuf[BLOCK_SIZE];
            lfs3_file_read(&lfs3, &files[k],
                    rbuf, BLOCK_SIZE) => BLOCK_SIZE;
            assert(memcmp(rbuf, wbuf, BLOCK_SIZE) == 0);
        }
    }

    lfs3_file_close(&lfs3, &files[0]) => 0;
    for (lfs3_size_t k = 1; k < N; k++) {
        lfs3_file_desync(&lfs3, &files[k]) => 0;
        lfs3_file_close(&lfs3, &files[k]) => 0;
    }
    lfs3_unmount(&lfs3) => 0;
'''

# test bptrs are updated correctly, even when referenced by unrelated
# btrees, with various levels of erased-state
[cases.test_evict_repair_t_bptr_shared_erased]
defines.CHUNK = 'BLOCK_SIZE/4'
defines.SIZE = ['1*CHUNK', '2*CHUNK', '3*CHUNK', '4*CHUNK']
defines.N = 4
defines.DELTA = ['+2*CHUNK', '+1*CHUNK', '-1*CHUNK', '-2*CHUNK']
defines.DESYNC = [false, true]
defines.SIBLING = [0]
# report as damaged to request repairs
defines.BADBLOCK_BEHAVIOR = 'LFS3_EMUBD_BADBLOCK_READDAMAGED'
# is a chunk prog aligned?
if = 'CHUNK % PROG_SIZE == 0'
code = '''
    lfs3_t lfs3;
    lfs3_format(&lfs3,
            LFS3_F_RDWR
                | ((GBMAP) ? LFS3_IFDEF_GBMAP(LFS3_F_GBMAP, -1) : 0),
            CFG) => 0;
    lfs3_mount(&lfs3, LFS3_M_RDWR, CFG) => 0;

    uint32_t prng = 42;

    // create a file
    lfs3_file_t files[N];
    lfs3_file_open(&lfs3, &files[0], "jellyfish",
            LFS3_O_RDWR | LFS3_O_CREAT | LFS3_O_EXCL) => 0;

    // write n blocks
    uint32_t wprng = prng;
    lfs3_off_t wsize = 0;
    while (wsize < SIZE) {
        uint8_t wbuf[CHUNK];
        for (lfs3_size_t j = 0; j < CHUNK; j++) {
            wbuf[j] = 'a' + (TEST_PRNG(&prng) % 26);
        }
        lfs3_file_write(&lfs3, &files[0], wbuf, CHUNK) => CHUNK;
        lfs3_file_sync(&lfs3, &files[0]) => 0;
        wsize += CHUNK;
    }

    // open N-1 other handles
    for (lfs3_size_t k = 1; k < N; k++) {
        lfs3_file_open(&lfs3, &files[k], "jellyfish", LFS3_O_RDWR) => 0;

        // use DELTA to derive a somewhat chaotic size for each file,
        // limited to at least CHUNK
        lfs3_off_t wsize_ = CHUNK + DELTA*k;
        while ((lfs3_soff_t)wsize_ < (lfs3_soff_t)CHUNK) {
            wsize_ += CHUNK;
        }
        // either truncate, or continue writing, note this should keep
        // any overlapping blocks
        if (wsize_ < wsize) {
            lfs3_file_truncate(&lfs3, &files[k], wsize_) => 0;
        } else {
            prng = wprng;
            for (lfs3_size_t i = 0; i < wsize; i += 1) {
                TEST_PRNG(&prng);
            }
            lfs3_file_seek(&lfs3, &files[k], 0, LFS3_SEEK_END) => wsize;
            for (lfs3_size_t i = wsize; i < wsize_; i += CHUNK) {
                uint8_t wbuf[CHUNK];
                for (lfs3_size_t j = 0; j < CHUNK; j++) {
                    wbuf[j] = 'a' + (TEST_PRNG(&prng) % 26);
                }
                lfs3_file_write(&lfs3, &files[k],
                        wbuf, CHUNK) => CHUNK;
            }
            lfs3_file_flush(&lfs3, &files[k]) => 0;
            // block evictions ignore desync, which risks complicating
            // things
            if (DESYNC) {
                lfs3_file_desync(&lfs3, &files[k]) => 0;
            }
        }
    }

    // get a block to evict
    lfs3_block_t victim = 0;
    lfs3_size_t count = 0;
    lfs3_trv_t trv;
    lfs3_trv_open(&lfs3, &trv, LFS3_T_RDONLY) => 0;
    for (lfs3_block_t i = 0;; i++) {
        assert(i < 2*BLOCK_COUNT);

        struct lfs3_tinfo tinfo;
        int err = lfs3_trv_read(&lfs3, &trv, &tinfo);
        assert(!err);

        if (tinfo.btype == LFS3_BTYPE_DATA) {
            if (count == SIBLING) {
                victim = tinfo.block;
                break;
            }

            count += 1;
        }
    }
    lfs3_trv_close(&lfs3, &trv) => 0;

    // make bad
    lfs3_emubd_mkbad(CFG, victim) => 0;
    // reset ck flags
    lfs3_fs_unck(&lfs3, LFS3_GC_CKDATA) => 0;

    // run gc to repair bad blocks
    lfs3_gc_t gc;
    lfs3_gc_open(&lfs3, &gc,
            LFS3_GC_WRONLY
                | LFS3_GC_CKDATA
                | LFS3_GC_REPAIRDATA) => 0;
    for (lfs3_block_t i = 0;; i++) {
        // a bit hacky, but this catches infinite loops
        assert(i < 2*BLOCK_COUNT);

        lfs3_sblock_t steps = lfs3_gc_write(&lfs3, &gc, 1);
        assert(steps >= 0);
        if (steps == 0) {
            break;
        }
    }
    lfs3_gc_close(&lfs3, &gc) => 0;

    // evicted block should no longer be in use, anywhere in the
    // filesystem
    lfs3_trv_open(&lfs3, &trv, LFS3_T_RDONLY) => 0;
    for (lfs3_block_t i = 0;; i++) {
        assert(i < 2*BLOCK_COUNT);

        struct lfs3_tinfo tinfo;
        int err = lfs3_trv_read(&lfs3, &trv, &tinfo);
        assert(!err || err == LFS3_ERR_NOENT);
        if (err == LFS3_ERR_NOENT) {
            break;
        }

        assert(tinfo.block != victim);
    }
    lfs3_trv_close(&lfs3, &trv) => 0;

    // check we can still read the files
    lfs3_file_rewind(&lfs3, &files[0]) => 0;
    prng = wprng;
    for (lfs3_off_t i = 0; i < wsize; i += CHUNK) {
        uint8_t wbuf[CHUNK];
        for (lfs3_size_t j = 0; j < CHUNK; j++) {
            wbuf[j] = 'a' + (TEST_PRNG(&prng) % 26);
        }
        uint8_t rbuf[CHUNK];
        lfs3_file_read(&lfs3, &files[0],
                rbuf, CHUNK) => CHUNK;
        assert(memcmp(rbuf, wbuf, CHUNK) == 0);
    }

    for (lfs3_size_t k = 1; k < N; k++) {
        lfs3_off_t wsize_ = CHUNK + DELTA*k;
        while ((lfs3_soff_t)wsize_ < (lfs3_soff_t)CHUNK) {
            wsize_ += CHUNK;
        }
        lfs3_file_rewind(&lfs3, &files[k]) => 0;
        prng = wprng;
        for (lfs3_off_t i = 0; i < wsize_; i += CHUNK) {
            uint8_t wbuf[CHUNK];
            for (lfs3_size_t j = 0; j < CHUNK; j++) {
                wbuf[j] = 'a' + (TEST_PRNG(&prng) % 26);
            }
            uint8_t rbuf[CHUNK];
            lfs3_file_read(&lfs3, &files[k],
                    rbuf, CHUNK) => CHUNK;
            assert(memcmp(rbuf, wbuf, CHUNK) == 0);
        }
    }

    // try writing an additional block to all files, to make sure
    // things still work
    prng = wprng;
    for (lfs3_size_t i = 0; i < wsize; i += 1) {
        TEST_PRNG(&prng);
    }
    lfs3_file_seek(&lfs3, &files[0], 0, LFS3_SEEK_END) => wsize;
    uint8_t wbuf[CHUNK];
    for (lfs3_size_t j = 0; j < CHUNK; j++) {
        wbuf[j] = 'a' + (TEST_PRNG(&prng) % 26);
    }
    lfs3_file_write(&lfs3, &files[0],
            wbuf, CHUNK) => CHUNK;
    lfs3_file_flush(&lfs3, &files[0]) => 0;
    
    for (lfs3_size_t k = 1; k < N; k++) {
        lfs3_off_t wsize_ = CHUNK + DELTA*k;
        while ((lfs3_soff_t)wsize_ < (lfs3_soff_t)CHUNK) {
            wsize_ += CHUNK;
        }
        prng = wprng;
        for (lfs3_size_t i = 0; i < wsize_; i += 1) {
            TEST_PRNG(&prng);
        }
        lfs3_file_seek(&lfs3, &files[k], 0, LFS3_SEEK_END) => wsize_;
        uint8_t wbuf[CHUNK];
        for (lfs3_size_t j = 0; j < CHUNK; j++) {
            wbuf[j] = 'a' + (TEST_PRNG(&prng) % 26);
        }
        lfs3_file_write(&lfs3, &files[k],
                wbuf, CHUNK) => CHUNK;
        lfs3_file_flush(&lfs3, &files[k]) => 0;
    }

    // check we can still read the files
    lfs3_file_rewind(&lfs3, &files[0]) => 0;
    prng = wprng;
    for (lfs3_off_t i = 0; i < wsize+CHUNK; i += CHUNK) {
        uint8_t wbuf[CHUNK];
        for (lfs3_size_t j = 0; j < CHUNK; j++) {
            wbuf[j] = 'a' + (TEST_PRNG(&prng) % 26);
        }
        uint8_t rbuf[CHUNK];
        lfs3_file_read(&lfs3, &files[0],
                rbuf, CHUNK) => CHUNK;
        assert(memcmp(rbuf, wbuf, CHUNK) == 0);
    }

    for (lfs3_size_t k = 1; k < N; k++) {
        lfs3_off_t wsize_ = CHUNK + DELTA*k;
        while ((lfs3_soff_t)wsize_ < (lfs3_soff_t)CHUNK) {
            wsize_ += CHUNK;
        }
        lfs3_file_rewind(&lfs3, &files[k]) => 0;
        prng = wprng;
        for (lfs3_off_t i = 0; i < wsize_+CHUNK; i += CHUNK) {
            uint8_t wbuf[CHUNK];
            for (lfs3_size_t j = 0; j < CHUNK; j++) {
                wbuf[j] = 'a' + (TEST_PRNG(&prng) % 26);
            }
            uint8_t rbuf[CHUNK];
            lfs3_file_read(&lfs3, &files[k],
                    rbuf, CHUNK) => CHUNK;
            assert(memcmp(rbuf, wbuf, CHUNK) == 0);
        }
    }

    lfs3_file_close(&lfs3, &files[0]) => 0;
    for (lfs3_size_t k = 1; k < N; k++) {
        lfs3_file_desync(&lfs3, &files[k]) => 0;
        lfs3_file_close(&lfs3, &files[k]) => 0;
    }
    lfs3_unmount(&lfs3) => 0;
'''



# these tests are just rude
#
# write to a file, but keep evicting the most recently written block

[cases.test_evict_repair_rude_seq]
defines.SIZE = '8*BLOCK_SIZE'
defines.CHUNK = ['FCACHE_SIZE/2', '2*FCACHE_SIZE', 'BLOCK_SIZE']
# INIT=0 => no init
# INIT=1 => fill with data
# INIT=2 => truncate to size
defines.INIT = [0, 1, 2]
defines.SYNC = [false, true]
# report as damaged to request repairs
defines.BADBLOCK_BEHAVIOR = 'LFS3_EMUBD_BADBLOCK_READDAMAGED'
code = '''
    lfs3_t lfs3;
    lfs3_format(&lfs3,
            LFS3_F_RDWR
                | ((GBMAP) ? LFS3_IFDEF_GBMAP(LFS3_F_GBMAP, -1) : 0),
            CFG) => 0;
    lfs3_mount(&lfs3, LFS3_M_RDWR, CFG) => 0;

    // create a file
    lfs3_file_t file;
    lfs3_file_open(&lfs3, &file, "raccoonbutterflyfish",
            LFS3_O_RDWR | LFS3_O_CREAT | LFS3_O_EXCL) => 0;

    // simulate our file in ram
    uint8_t sim[SIZE];
    lfs3_off_t size;
    uint32_t prng = 42;
    if (INIT == 0) {
        size = 0;
    } else if (INIT == 1) {
        for (lfs3_size_t i = 0; i < SIZE; i++) {
            sim[i] = 'a' + (TEST_PRNG(&prng) % 26);
        }
        lfs3_file_write(&lfs3, &file, sim, SIZE) => SIZE;
        size = SIZE;
    } else {
        memset(sim, 0, SIZE);
        lfs3_file_truncate(&lfs3, &file, SIZE) => 0;
        size = SIZE;
    }
    // sync?
    if (SYNC) {
        lfs3_file_sync(&lfs3, &file) => 0;
    }

    // write to file
    lfs3_file_rewind(&lfs3, &file) => 0;
    for (lfs3_off_t i = 0; i < SIZE; i += CHUNK) {
        for (lfs3_size_t j = 0; j < CHUNK; j++) {
            sim[i+j] = 'a' + (TEST_PRNG(&prng) % 26);
        }
        lfs3_file_write(&lfs3, &file, &sim[i], CHUNK) => CHUNK;
        size = lfs3_max(size, i+CHUNK);

        // sync?
        if (SYNC) {
            lfs3_file_sync(&lfs3, &file) => 0;
        }

        // evict? so rude
        if (file.leaf.bptr.d.u.disk.block != 0) {
            lfs3_block_t victim = file.leaf.bptr.d.u.disk.block;

            // make bad
            lfs3_emubd_mkbad(CFG, victim) => 0;
            // reset ck flags
            lfs3_fs_unck(&lfs3, LFS3_GC_CKDATA) => 0;

            // run gc to repair bad blocks
            lfs3_gc_t gc;
            lfs3_gc_open(&lfs3, &gc,
                    LFS3_GC_WRONLY
                        | LFS3_GC_CKDATA
                        | LFS3_GC_REPAIRDATA) => 0;
            for (lfs3_block_t i = 0;; i++) {
                // a bit hacky, but this catches infinite loops
                //
                // note this takes longer than the evict tests because
                // we need a second pass to clear the ckmeta/ckdata
                // flags
                assert(i < 4*BLOCK_COUNT);

                lfs3_sblock_t steps = lfs3_gc_write(&lfs3, &gc, 1);
                assert(steps >= 0);
                if (steps == 0) {
                    break;
                }
            }
            lfs3_gc_close(&lfs3, &gc) => 0;

            // make the block good again
            lfs3_emubd_mkgood(CFG, victim) => 0;

            // evicted block should no longer be in use
            assert(file.leaf.bptr.d.u.disk.block != victim);

            // evicted block should no longer be in use, anywhere in the
            // filesystem
            lfs3_trv_t trv;
            lfs3_trv_open(&lfs3, &trv, LFS3_T_RDONLY) => 0;
            for (lfs3_block_t i = 0;; i++) {
                assert(i < 2*BLOCK_COUNT);

                struct lfs3_tinfo tinfo;
                int err = lfs3_trv_read(&lfs3, &trv, &tinfo);
                assert(!err || err == LFS3_ERR_NOENT);
                if (err == LFS3_ERR_NOENT) {
                    break;
                }

                assert(tinfo.block != victim);
            }
            lfs3_trv_close(&lfs3, &trv) => 0;
        }
    }

    // check we can still read the file
    for (int remount = 0; remount < 2; remount++) {
        // remount?
        if (remount) {
            lfs3_file_close(&lfs3, &file) => 0;
            lfs3_unmount(&lfs3) => 0;
            lfs3_mount(&lfs3, LFS3_M_RDWR, CFG) => 0;
            lfs3_file_open(&lfs3, &file, "raccoonbutterflyfish",
                    LFS3_O_RDONLY) => 0;
        }

        // is size correct?
        lfs3_file_size(&lfs3, &file) => size;
        // try reading
        uint8_t rbuf[2*SIZE];
        memset(rbuf, 0xaa, 2*SIZE);
        lfs3_file_rewind(&lfs3, &file) => 0;
        lfs3_file_read(&lfs3, &file, rbuf, 2*SIZE) => size;
        assert(memcmp(rbuf, sim, size) == 0);
    }

    lfs3_file_close(&lfs3, &file) => 0;
    lfs3_unmount(&lfs3) => 0;
'''

[cases.test_evict_repair_rude_random]
defines.SIZE = '8*BLOCK_SIZE'
defines.CHUNK = ['FCACHE_SIZE/2', '2*FCACHE_SIZE', 'BLOCK_SIZE']
defines.N = 1000
# INIT=0 => no init
# INIT=1 => fill with data
# INIT=2 => truncate to size
defines.INIT = [0, 1, 2]
defines.SYNC = [false, true]
defines.SEED = 42
# report as damaged to request repairs
defines.BADBLOCK_BEHAVIOR = 'LFS3_EMUBD_BADBLOCK_READDAMAGED'
fuzz = 'SEED'
code = '''
    lfs3_t lfs3;
    lfs3_format(&lfs3,
            LFS3_F_RDWR
                | ((GBMAP) ? LFS3_IFDEF_GBMAP(LFS3_F_GBMAP, -1) : 0),
            CFG) => 0;
    lfs3_mount(&lfs3, LFS3_M_RDWR, CFG) => 0;

    // create a file
    lfs3_file_t file;
    lfs3_file_open(&lfs3, &file, "raccoonbutterflyfish",
            LFS3_O_RDWR | LFS3_O_CREAT | LFS3_O_EXCL) => 0;

    // simulate our file in ram
    uint8_t sim[SIZE];
    lfs3_off_t size;
    uint32_t prng = SEED;
    if (INIT == 0) {
        memset(sim, 0, SIZE);
        size = 0;
    } else if (INIT == 1) {
        for (lfs3_size_t i = 0; i < SIZE; i++) {
            sim[i] = 'a' + (TEST_PRNG(&prng) % 26);
        }
        lfs3_file_write(&lfs3, &file, sim, SIZE) => SIZE;
        size = SIZE;
    } else {
        memset(sim, 0, SIZE);
        lfs3_file_truncate(&lfs3, &file, SIZE) => 0;
        size = SIZE;
    }
    // sync?
    if (SYNC) {
        lfs3_file_sync(&lfs3, &file) => 0;
    }

    // write to file, randomly!
    for (lfs3_off_t i = 0; i < N; i += 1) {
        // choose a random offset
        lfs3_off_t off = TEST_PRNG(&prng) % (SIZE-(CHUNK-1));

        lfs3_file_seek(&lfs3, &file, off, LFS3_SEEK_SET) => off;
        for (lfs3_size_t j = 0; j < CHUNK; j++) {
            sim[off+j] = 'a' + (TEST_PRNG(&prng) % 26);
        }
        lfs3_file_write(&lfs3, &file, &sim[off], CHUNK) => CHUNK;
        size = lfs3_max(size, off+CHUNK);

        // sync?
        if (SYNC) {
            lfs3_file_sync(&lfs3, &file) => 0;
        }

        // evict? so rude
        if (file.leaf.bptr.d.u.disk.block != 0) {
            lfs3_block_t victim = file.leaf.bptr.d.u.disk.block;

            // make bad
            lfs3_emubd_mkbad(CFG, victim) => 0;
            // reset ck flags
            lfs3_fs_unck(&lfs3, LFS3_GC_CKDATA) => 0;

            // run gc to repair bad blocks
            lfs3_gc_t gc;
            lfs3_gc_open(&lfs3, &gc,
                    LFS3_GC_WRONLY
                        | LFS3_GC_CKDATA
                        | LFS3_GC_REPAIRDATA) => 0;
            for (lfs3_block_t i = 0;; i++) {
                // a bit hacky, but this catches infinite loops
                //
                // note this takes longer than the evict tests because
                // we need a second pass to clear the ckmeta/ckdata
                // flags
                assert(i < 4*BLOCK_COUNT);

                lfs3_sblock_t steps = lfs3_gc_write(&lfs3, &gc, 1);
                assert(steps >= 0);
                if (steps == 0) {
                    break;
                }
            }
            lfs3_gc_close(&lfs3, &gc) => 0;

            // make the block good again
            lfs3_emubd_mkgood(CFG, victim) => 0;

            // evicted block should no longer be in use
            assert(file.leaf.bptr.d.u.disk.block != victim);

            // evicted block should no longer be in use, anywhere in the
            // filesystem
            lfs3_trv_t trv;
            lfs3_trv_open(&lfs3, &trv, LFS3_T_RDONLY) => 0;
            for (lfs3_block_t i = 0;; i++) {
                assert(i < 2*BLOCK_COUNT);

                struct lfs3_tinfo tinfo;
                int err = lfs3_trv_read(&lfs3, &trv, &tinfo);
                assert(!err || err == LFS3_ERR_NOENT);
                if (err == LFS3_ERR_NOENT) {
                    break;
                }

                assert(tinfo.block != victim);
            }
            lfs3_trv_close(&lfs3, &trv) => 0;
        }
    }

    // check we can still read the file
    for (int remount = 0; remount < 2; remount++) {
        // remount?
        if (remount) {
            lfs3_file_close(&lfs3, &file) => 0;
            lfs3_unmount(&lfs3) => 0;
            lfs3_mount(&lfs3, LFS3_M_RDWR, CFG) => 0;
            lfs3_file_open(&lfs3, &file, "raccoonbutterflyfish",
                    LFS3_O_RDONLY) => 0;
        }

        // is size correct?
        lfs3_file_size(&lfs3, &file) => size;
        // try reading
        uint8_t rbuf[2*SIZE];
        memset(rbuf, 0xaa, 2*SIZE);
        lfs3_file_rewind(&lfs3, &file) => 0;
        lfs3_file_read(&lfs3, &file, rbuf, 2*SIZE) => size;
        assert(memcmp(rbuf, sim, size) == 0);
    }

    lfs3_file_close(&lfs3, &file) => 0;
    lfs3_unmount(&lfs3) => 0;
'''



# many/fuzz tests with random repairs
#
# randomly choose a block to evict every step
#

[cases.test_evict_repair_spam_dir_many]
defines.N = [1, 2, 4, 8, 16, 32, 64, 128, 256]
defines.ESEED = 42
# report as damaged to request repairs
defines.BADBLOCK_BEHAVIOR = 'LFS3_EMUBD_BADBLOCK_READDAMAGED'
code = '''
    // test creating directories
    lfs3_t lfs3;
    lfs3_format(&lfs3,
            LFS3_F_RDWR
                | ((GBMAP) ? LFS3_IFDEF_GBMAP(LFS3_F_GBMAP, -1) : 0),
            CFG) => 0;
    lfs3_mount(&lfs3, LFS3_M_RDWR, CFG) => 0;
    // last seen block count
    lfs3_block_t n = 0;
    uint32_t eprng = ESEED;

    // make this many directories
    for (lfs3_size_t i = 0; i < N; i++) {
        char name[256];
        sprintf(name, "dir%03x", i);
        int err = lfs3_mkdir(&lfs3, name);
        assert(!err || (TEST_PLS && err == LFS3_ERR_EXIST));

        // randomly evict a block
        //
        // to try to minimize traversals we randomly choose a block
        // based on the _previous_ traversal, or the last
        // non-mrootanchor block if that fails
        lfs3_block_t victim_i = TEST_PRNG(&eprng) % lfs3_max(n, 1);
        lfs3_block_t victim = 2;
        lfs3_trv_t trv;
        lfs3_trv_open(&lfs3, &trv, LFS3_T_RDONLY) => 0;
        for (lfs3_block_t i = 0;; i++) {
            assert(i < 2*BLOCK_COUNT);

            struct lfs3_tinfo tinfo;
            int err = lfs3_trv_read(&lfs3, &trv, &tinfo);
            assert(!err || err == LFS3_ERR_NOENT);
            if (err == LFS3_ERR_NOENT) {
                break;
            }

            if (i <= victim_i && tinfo.block >= 2) {
                victim = tinfo.block;
            }
            n = i+1;
        }
        lfs3_trv_close(&lfs3, &trv) => 0;

        // make bad
        lfs3_emubd_mkbad(CFG, victim) => 0;
        // reset ck flags
        lfs3_fs_unck(&lfs3, LFS3_GC_CKDATA) => 0;

        // run gc to repair bad blocks
        lfs3_gc_t gc;
        lfs3_gc_open(&lfs3, &gc,
                LFS3_GC_WRONLY
                    | LFS3_GC_CKDATA
                    | LFS3_GC_REPAIRDATA) => 0;
        for (lfs3_block_t i = 0;; i++) {
            // a bit hacky, but this catches infinite loops
            assert(i < 2*BLOCK_COUNT);

            lfs3_sblock_t steps = lfs3_gc_write(&lfs3, &gc, 1);
            assert(steps >= 0);
            if (steps == 0) {
                break;
            }
        }
        lfs3_gc_close(&lfs3, &gc) => 0;

        // make the block good again
        lfs3_emubd_mkgood(CFG, victim) => 0;

        // evicted block should no longer be in use, anywhere in the
        // filesystem
        lfs3_trv_open(&lfs3, &trv, LFS3_T_RDONLY) => 0;
        for (lfs3_block_t i = 0;; i++) {
            assert(i < 2*BLOCK_COUNT);

            struct lfs3_tinfo tinfo;
            int err = lfs3_trv_read(&lfs3, &trv, &tinfo);
            assert(!err || err == LFS3_ERR_NOENT);
            if (err == LFS3_ERR_NOENT) {
                break;
            }

            // hmm, ok, one exception is mdir's spare blocks,
            // these don't actually store anything at rest
            if (!(tinfo.btype == LFS3_BTYPE_MDIR
                    && trv.blocks[1] == -1)) {
                assert(tinfo.block != victim);
            }
        }
        lfs3_trv_close(&lfs3, &trv) => 0;
    }

    for (int remount = 0; remount < 2; remount++) {
        // remount?
        if (remount) {
            lfs3_unmount(&lfs3) => 0;
            lfs3_mount(&lfs3, LFS3_M_RDONLY, CFG) => 0;
        }

        // grm should be zero here
        assert(lfs3.grm_p[0] == 0);

        // check that our mkdir worked
        for (lfs3_size_t i = 0; i < N; i++) {
            char name[256];
            sprintf(name, "dir%03x", i);
            struct lfs3_info info;
            lfs3_stat(&lfs3, name, &info) => 0;
            assert(strcmp(info.name, name) == 0);
            assert(info.type == LFS3_TYPE_DIR);
            assert(info.size == 0);
        }

        lfs3_dir_t dir;
        lfs3_dir_open(&lfs3, &dir, "/") => 0;
        struct lfs3_info info;
        lfs3_dir_read(&lfs3, &dir, &info) => 0;
        assert(strcmp(info.name, ".") == 0);
        assert(info.type == LFS3_TYPE_DIR);
        assert(info.size == 0);
        lfs3_dir_read(&lfs3, &dir, &info) => 0;
        assert(strcmp(info.name, "..") == 0);
        assert(info.type == LFS3_TYPE_DIR);
        assert(info.size == 0);
        for (lfs3_size_t i = 0; i < N; i++) {
            char name[256];
            sprintf(name, "dir%03x", i);
            lfs3_dir_read(&lfs3, &dir, &info) => 0;
            assert(strcmp(info.name, name) == 0);
            assert(info.type == LFS3_TYPE_DIR);
            assert(info.size == 0);
        }
        lfs3_dir_read(&lfs3, &dir, &info) => LFS3_ERR_NOENT;
        lfs3_dir_close(&lfs3, &dir) => 0;

        for (lfs3_size_t i = 0; i < N; i++) {
            char name[256];
            sprintf(name, "dir%03x", i);
            lfs3_dir_open(&lfs3, &dir, name) => 0;
            lfs3_dir_read(&lfs3, &dir, &info) => 0;
            assert(strcmp(info.name, ".") == 0);
            assert(info.type == LFS3_TYPE_DIR);
            assert(info.size == 0);
            lfs3_dir_read(&lfs3, &dir, &info) => 0;
            assert(strcmp(info.name, "..") == 0);
            assert(info.type == LFS3_TYPE_DIR);
            assert(info.size == 0);
            lfs3_dir_read(&lfs3, &dir, &info) => LFS3_ERR_NOENT;
            lfs3_dir_close(&lfs3, &dir) => 0;
        }
    }

    lfs3_unmount(&lfs3) => 0;
'''

[cases.test_evict_repair_spam_dir_fuzz]
defines.N = [1, 2, 4, 8, 16, 32, 64, 128, 256]
defines.OPS = '2*N'
defines.SEED = 'range(10)'
defines.ESEED = '42+SEED'
# report as damaged to request repairs
defines.BADBLOCK_BEHAVIOR = 'LFS3_EMUBD_BADBLOCK_READDAMAGED'
fuzz = 'SEED'
code = '''
    // test fuzz with dirs
    lfs3_t lfs3;
    lfs3_format(&lfs3,
            LFS3_F_RDWR
                | ((GBMAP) ? LFS3_IFDEF_GBMAP(LFS3_F_GBMAP, -1) : 0),
            CFG) => 0;
    lfs3_mount(&lfs3, LFS3_M_RDWR, CFG) => 0;
    // last seen block count
    lfs3_block_t n = 0;
    uint32_t eprng = ESEED;

    // set up a simulation to compare against
    lfs3_size_t *sim = malloc(N*sizeof(lfs3_size_t));
    lfs3_size_t sim_size = 0;

    uint32_t prng = SEED;
    for (lfs3_size_t i = 0; i < OPS; i++) {
        // choose a pseudo-random op, either mkdir, remove, or rename
        uint8_t op = TEST_PRNG(&prng) % 3;

        if (op == 0 || sim_size == 0) {
            // choose a pseudo-random number, truncate to 3 hexadecimals
            lfs3_size_t x = TEST_PRNG(&prng) % N;
            // insert into our sim
            for (lfs3_size_t j = 0;; j++) {
                if (j >= sim_size || sim[j] >= x) {
                    // already seen?
                    if (j < sim_size && sim[j] == x) {
                        // do nothing
                    } else {
                        // insert
                        memmove(&sim[j+1], &sim[j],
                                (sim_size-j)*sizeof(lfs3_size_t));
                        sim_size += 1;
                        sim[j] = x;
                    }
                    break;
                }
            }

            // create a directory here
            char name[256];
            sprintf(name, "dir%03x", x);
            int err = lfs3_mkdir(&lfs3, name);
            assert(!err || err == LFS3_ERR_EXIST);

        } else if (op == 1) {
            // choose a pseudo-random entry to delete
            lfs3_size_t j = TEST_PRNG(&prng) % sim_size;
            lfs3_size_t x = sim[j];
            // delete from our sim
            memmove(&sim[j], &sim[j+1],
                    (sim_size-(j+1))*sizeof(lfs3_size_t));
            sim_size -= 1;

            // remove this directory
            char name[256];
            sprintf(name, "dir%03x", x);
            lfs3_remove(&lfs3, name) => 0;

        } else {
            // choose a pseudo-random entry to rename, and a pseudo-random
            // number to rename to
            lfs3_size_t j = TEST_PRNG(&prng) % sim_size;
            lfs3_size_t x = sim[j];
            lfs3_size_t y = TEST_PRNG(&prng) % N;
            for (lfs3_size_t k = 0;; k++) {
                if (k >= sim_size || sim[k] >= y) {
                    // already seen and not a noop?
                    if (k < sim_size && sim[k] == y && x != y) {
                        // just delete the original entry
                        memmove(&sim[j], &sim[j+1],
                                (sim_size-(j+1))*sizeof(lfs3_size_t));
                        sim_size -= 1;
                    } else {
                        // first delete
                        memmove(&sim[j], &sim[j+1],
                                (sim_size-(j+1))*sizeof(lfs3_size_t));
                        if (k > j) {
                            k -= 1;
                        }
                        // then insert
                        memmove(&sim[k+1], &sim[k],
                                (sim_size-1-k)*sizeof(lfs3_size_t));
                        sim[k] = y;
                    }
                    break;
                }
            }

            // rename this directory
            char old_name[256];
            sprintf(old_name, "dir%03x", x);
            char new_name[256];
            sprintf(new_name, "dir%03x", y);
            lfs3_rename(&lfs3, old_name, new_name) => 0;
        }

        // randomly evict a block
        //
        // to try to minimize traversals we randomly choose a block
        // based on the _previous_ traversal, or the last
        // non-mrootanchor block if that fails
        lfs3_block_t victim_i = TEST_PRNG(&eprng) % lfs3_max(n, 1);
        lfs3_block_t victim = 2;
        lfs3_trv_t trv;
        lfs3_trv_open(&lfs3, &trv, LFS3_T_RDONLY) => 0;
        for (lfs3_block_t i = 0;; i++) {
            assert(i < 2*BLOCK_COUNT);

            struct lfs3_tinfo tinfo;
            int err = lfs3_trv_read(&lfs3, &trv, &tinfo);
            assert(!err || err == LFS3_ERR_NOENT);
            if (err == LFS3_ERR_NOENT) {
                break;
            }

            if (i <= victim_i && tinfo.block >= 2) {
                victim = tinfo.block;
            }
            n = i+1;
        }
        lfs3_trv_close(&lfs3, &trv) => 0;

        // make bad
        lfs3_emubd_mkbad(CFG, victim) => 0;
        // reset ck flags
        lfs3_fs_unck(&lfs3, LFS3_GC_CKDATA) => 0;

        // run gc to repair bad blocks
        lfs3_gc_t gc;
        lfs3_gc_open(&lfs3, &gc,
                LFS3_GC_WRONLY
                    | LFS3_GC_CKDATA
                    | LFS3_GC_REPAIRDATA) => 0;
        for (lfs3_block_t i = 0;; i++) {
            // a bit hacky, but this catches infinite loops
            assert(i < 2*BLOCK_COUNT);

            lfs3_sblock_t steps = lfs3_gc_write(&lfs3, &gc, 1);
            assert(steps >= 0);
            if (steps == 0) {
                break;
            }
        }
        lfs3_gc_close(&lfs3, &gc) => 0;

        // make the block good again
        lfs3_emubd_mkgood(CFG, victim) => 0;

        // evicted block should no longer be in use, anywhere in the
        // filesystem
        lfs3_trv_open(&lfs3, &trv, LFS3_T_RDONLY) => 0;
        for (lfs3_block_t i = 0;; i++) {
            assert(i < 2*BLOCK_COUNT);

            struct lfs3_tinfo tinfo;
            int err = lfs3_trv_read(&lfs3, &trv, &tinfo);
            assert(!err || err == LFS3_ERR_NOENT);
            if (err == LFS3_ERR_NOENT) {
                break;
            }

            // hmm, ok, one exception is mdir's spare blocks,
            // these don't actually store anything at rest
            if (!(tinfo.btype == LFS3_BTYPE_MDIR
                    && trv.blocks[1] == -1)) {
                assert(tinfo.block != victim);
            }
        }
        lfs3_trv_close(&lfs3, &trv) => 0;
    }

    for (int remount = 0; remount < 2; remount++) {
        // remount?
        if (remount) {
            lfs3_unmount(&lfs3) => 0;
            lfs3_mount(&lfs3, LFS3_M_RDONLY, CFG) => 0;
        }

        // grm should be zero here
        assert(lfs3.grm_p[0] == 0);

        // test that our directories match our simulation
        for (lfs3_size_t j = 0; j < sim_size; j++) {
            char name[256];
            sprintf(name, "dir%03x", sim[j]);
            struct lfs3_info info;
            lfs3_stat(&lfs3, name, &info) => 0;
            char name2[256];
            sprintf(name2, "dir%03x", sim[j]);
            assert(strcmp(info.name, name2) == 0);
            assert(info.type == LFS3_TYPE_DIR);
            assert(info.size == 0);
        }

        lfs3_dir_t dir;
        lfs3_dir_open(&lfs3, &dir, "/") => 0;
        struct lfs3_info info;
        lfs3_dir_read(&lfs3, &dir, &info) => 0;
        assert(strcmp(info.name, ".") == 0);
        assert(info.type == LFS3_TYPE_DIR);
        assert(info.size == 0);
        lfs3_dir_read(&lfs3, &dir, &info) => 0;
        assert(strcmp(info.name, "..") == 0);
        assert(info.type == LFS3_TYPE_DIR);
        assert(info.size == 0);
        for (lfs3_size_t j = 0; j < sim_size; j++) {
            char name[256];
            sprintf(name, "dir%03x", sim[j]);
            lfs3_dir_read(&lfs3, &dir, &info) => 0;
            assert(strcmp(info.name, name) == 0);
            assert(info.type == LFS3_TYPE_DIR);
            assert(info.size == 0);
        }
        lfs3_dir_read(&lfs3, &dir, &info) => LFS3_ERR_NOENT;
        lfs3_dir_close(&lfs3, &dir) => 0;
    }

    // clean up sim/lfs3
    free(sim);
    lfs3_unmount(&lfs3) => 0;
'''

[cases.test_evict_repair_spam_file_many]
defines.N = [1, 2, 4, 8, 16, 32, 64]
defines.SIZE = [
    '0',
    'FCACHE_SIZE/2',
    '2*FCACHE_SIZE',
    'BLOCK_SIZE/2',
    'BLOCK_SIZE',
    '2*BLOCK_SIZE',
    '4*BLOCK_SIZE',
]
defines.ESEED = 42
# report as damaged to request repairs
defines.BADBLOCK_BEHAVIOR = 'LFS3_EMUBD_BADBLOCK_READDAMAGED'
if = '(SIZE*N)/BLOCK_SIZE <= 32'
code = '''
    // test creating files
    lfs3_t lfs3;
    lfs3_format(&lfs3,
            LFS3_F_RDWR
                | ((GBMAP) ? LFS3_IFDEF_GBMAP(LFS3_F_GBMAP, -1) : 0),
            CFG) => 0;
    lfs3_mount(&lfs3, LFS3_M_RDWR, CFG) => 0;
    // last seen block count
    lfs3_block_t n = 0;
    uint32_t eprng = ESEED;

    // create this many files
    uint32_t prng = 42;
    for (lfs3_size_t i = 0; i < N; i++) {
        char name[256];
        sprintf(name, "amethyst%03x", i);

        uint8_t wbuf[SIZE];
        for (lfs3_size_t j = 0; j < SIZE; j++) {
            wbuf[j] = 'a' + (TEST_PRNG(&prng) % 26);
        }

        lfs3_file_t file;
        lfs3_file_open(&lfs3, &file, name,
                LFS3_O_WRONLY | LFS3_O_CREAT | LFS3_O_EXCL) => 0;
        lfs3_file_write(&lfs3, &file, wbuf, SIZE) => SIZE;
        lfs3_file_close(&lfs3, &file) => 0;

        // randomly evict a block
        //
        // to try to minimize traversals we randomly choose a block
        // based on the _previous_ traversal, or the last
        // non-mrootanchor block if that fails
        lfs3_block_t victim_i = TEST_PRNG(&eprng) % lfs3_max(n, 1);
        lfs3_block_t victim = 2;
        lfs3_trv_t trv;
        lfs3_trv_open(&lfs3, &trv, LFS3_T_RDONLY) => 0;
        for (lfs3_block_t i = 0;; i++) {
            assert(i < 2*BLOCK_COUNT);

            struct lfs3_tinfo tinfo;
            int err = lfs3_trv_read(&lfs3, &trv, &tinfo);
            assert(!err || err == LFS3_ERR_NOENT);
            if (err == LFS3_ERR_NOENT) {
                break;
            }

            if (i <= victim_i && tinfo.block >= 2) {
                victim = tinfo.block;
            }
            n = i+1;
        }
        lfs3_trv_close(&lfs3, &trv) => 0;

        // make bad
        lfs3_emubd_mkbad(CFG, victim) => 0;
        // reset ck flags
        lfs3_fs_unck(&lfs3, LFS3_GC_CKDATA) => 0;

        // run gc to repair bad blocks
        lfs3_gc_t gc;
        lfs3_gc_open(&lfs3, &gc,
                LFS3_GC_WRONLY
                    | LFS3_GC_CKDATA
                    | LFS3_GC_REPAIRDATA) => 0;
        for (lfs3_block_t i = 0;; i++) {
            // a bit hacky, but this catches infinite loops
            assert(i < 2*BLOCK_COUNT);

            lfs3_sblock_t steps = lfs3_gc_write(&lfs3, &gc, 1);
            assert(steps >= 0);
            if (steps == 0) {
                break;
            }
        }
        lfs3_gc_close(&lfs3, &gc) => 0;

        // make the block good again
        lfs3_emubd_mkgood(CFG, victim) => 0;

        // evicted block should no longer be in use, anywhere in the
        // filesystem
        lfs3_trv_open(&lfs3, &trv, LFS3_T_RDONLY) => 0;
        for (lfs3_block_t i = 0;; i++) {
            assert(i < 2*BLOCK_COUNT);

            struct lfs3_tinfo tinfo;
            int err = lfs3_trv_read(&lfs3, &trv, &tinfo);
            assert(!err || err == LFS3_ERR_NOENT);
            if (err == LFS3_ERR_NOENT) {
                break;
            }

            // hmm, ok, one exception is mdir's spare blocks,
            // these don't actually store anything at rest
            if (!(tinfo.btype == LFS3_BTYPE_MDIR
                    && trv.blocks[1] == -1)) {
                assert(tinfo.block != victim);
            }
        }
        lfs3_trv_close(&lfs3, &trv) => 0;
    }

    for (int remount = 0; remount < 2; remount++) {
        // remount?
        if (remount) {
            lfs3_unmount(&lfs3) => 0;
            lfs3_mount(&lfs3, LFS3_M_RDWR, CFG) => 0;
        }

        // check that our writes worked
        prng = 42;
        for (lfs3_size_t i = 0; i < N; i++) {
            // check with stat
            char name[256];
            sprintf(name, "amethyst%03x", i);
            struct lfs3_info info;
            lfs3_stat(&lfs3, name, &info) => 0;
            assert(strcmp(info.name, name) == 0);
            assert(info.type == LFS3_TYPE_REG);
            assert(info.size == SIZE);

            // try reading the file, note we reset prng above
            uint8_t wbuf[SIZE];
            for (lfs3_size_t j = 0; j < SIZE; j++) {
                wbuf[j] = 'a' + (TEST_PRNG(&prng) % 26);
            }

            lfs3_file_t file;
            uint8_t rbuf[SIZE];
            lfs3_file_open(&lfs3, &file, name, LFS3_O_RDONLY) => 0;
            lfs3_file_read(&lfs3, &file, rbuf, SIZE) => SIZE;
            assert(memcmp(rbuf, wbuf, SIZE) == 0);
            lfs3_file_close(&lfs3, &file) => 0;
        }
    }

    lfs3_unmount(&lfs3) => 0;
'''

[cases.test_evict_repair_spam_file_fuzz]
defines.N = [1, 2, 4, 8, 16, 32, 64]
defines.OPS = '2*N'
defines.SIZE = [
    '0',
    'FCACHE_SIZE/2',
    '2*FCACHE_SIZE',
    'BLOCK_SIZE/2',
    'BLOCK_SIZE',
    '2*BLOCK_SIZE',
    '4*BLOCK_SIZE',
]
defines.SEED = 'range(10)'
defines.ESEED = '42+SEED'
# report as damaged to request repairs
defines.BADBLOCK_BEHAVIOR = 'LFS3_EMUBD_BADBLOCK_READDAMAGED'
fuzz = 'SEED'
if = '(SIZE*N)/BLOCK_SIZE <= 16'
code = '''
    // test fuzz with files
    lfs3_t lfs3;
    lfs3_format(&lfs3,
            LFS3_F_RDWR
                | ((GBMAP) ? LFS3_IFDEF_GBMAP(LFS3_F_GBMAP, -1) : 0),
            CFG) => 0;
    lfs3_mount(&lfs3, LFS3_M_RDWR, CFG) => 0;
    // last seen block count
    lfs3_block_t n = 0;
    uint32_t eprng = ESEED;

    // set up a simulation to compare against
    lfs3_size_t *sim = malloc(N*sizeof(lfs3_size_t));
    uint32_t *sim_prngs = malloc(N*sizeof(uint32_t));
    lfs3_size_t sim_size = 0;

    uint32_t prng = SEED;
    for (lfs3_size_t i = 0; i < OPS; i++) {
        // choose which operation to do
        uint8_t op = TEST_PRNG(&prng) % 3;

        // creating a new file?
        if (op == 0 || sim_size == 0) {
            // choose a pseudo-random number
            lfs3_size_t x = TEST_PRNG(&prng) % N;
            // associate each file with a prng that generates its contents
            uint32_t wprng = TEST_PRNG(&prng);

            // insert into our sim
            for (lfs3_size_t j = 0;; j++) {
                if (j >= sim_size || sim[j] >= x) {
                    // already seen?
                    if (j < sim_size && sim[j] == x) {
                        // new prng
                        sim_prngs[j] = wprng;
                    } else {
                        // insert
                        memmove(&sim[j+1], &sim[j],
                                (sim_size-j)*sizeof(lfs3_size_t));
                        memmove(&sim_prngs[j+1], &sim_prngs[j],
                                (sim_size-j)*sizeof(uint32_t));
                        sim_size += 1;
                        sim[j] = x;
                        sim_prngs[j] = wprng;
                    }
                    break;
                }
            }

            // create a file here
            char name[256];
            sprintf(name, "amethyst%03x", x);
            uint8_t wbuf[SIZE];
            for (lfs3_size_t j = 0; j < SIZE; j++) {
                wbuf[j] = 'a' + (TEST_PRNG(&wprng) % 26);
            }

            lfs3_file_t file;
            lfs3_file_open(&lfs3, &file, name,
                    LFS3_O_WRONLY | LFS3_O_CREAT | LFS3_O_TRUNC) => 0;
            lfs3_file_write(&lfs3, &file, wbuf, SIZE) => SIZE;
            lfs3_file_close(&lfs3, &file) => 0;

        // deleting a file?
        } else if (op == 1) {
            // choose a random file to delete
            lfs3_size_t j = TEST_PRNG(&prng) % sim_size;
            lfs3_size_t x = sim[j];
            // delete from our sim
            memmove(&sim[j], &sim[j+1],
                    (sim_size-(j+1))*sizeof(lfs3_size_t));
            memmove(&sim_prngs[j], &sim_prngs[j+1],
                    (sim_size-(j+1))*sizeof(uint32_t));
            sim_size -= 1;

            // delete this file
            char name[256];
            sprintf(name, "amethyst%03x", x);
            lfs3_remove(&lfs3, name) => 0;

        // renaming a file?
        } else {
            // choose a random file to rename, and a random number to
            // rename to
            lfs3_size_t j = TEST_PRNG(&prng) % sim_size;
            lfs3_size_t x = sim[j];
            lfs3_size_t y = TEST_PRNG(&prng) % N;
            uint32_t wprng = sim_prngs[j];

            // update our sim
            for (lfs3_size_t k = 0;; k++) {
                if (k >= sim_size || sim[k] >= y) {
                    // renaming and replacing
                    if (k < sim_size && sim[k] == y && x != y) {
                        // delete the original entry
                        memmove(&sim[j], &sim[j+1],
                                (sim_size-(j+1))*sizeof(lfs3_size_t));
                        memmove(&sim_prngs[j], &sim_prngs[j+1],
                                (sim_size-(j+1))*sizeof(uint32_t));
                        sim_size -= 1;
                        if (k > j) {
                            k -= 1;
                        }
                        // update the prng
                        sim_prngs[k] = wprng;
                    // just renaming
                    } else {
                        // first delete
                        memmove(&sim[j], &sim[j+1],
                                (sim_size-(j+1))*sizeof(lfs3_size_t));
                        memmove(&sim_prngs[j], &sim_prngs[j+1],
                                (sim_size-(j+1))*sizeof(uint32_t));
                        if (k > j) {
                            k -= 1;
                        }
                        // then insert
                        memmove(&sim[k+1], &sim[k],
                                (sim_size-1-k)*sizeof(lfs3_size_t));
                        memmove(&sim_prngs[k+1], &sim_prngs[k],
                                (sim_size-1-k)*sizeof(uint32_t));
                        sim[k] = y;
                        sim_prngs[k] = wprng;
                    }
                    break;
                }
            }

            // rename this file
            char old_name[256];
            sprintf(old_name, "amethyst%03x", x);
            char new_name[256];
            sprintf(new_name, "amethyst%03x", y);
            lfs3_rename(&lfs3, old_name, new_name) => 0;
        }

        // randomly evict a block
        //
        // to try to minimize traversals we randomly choose a block
        // based on the _previous_ traversal, or the last
        // non-mrootanchor block if that fails
        lfs3_block_t victim_i = TEST_PRNG(&eprng) % lfs3_max(n, 1);
        lfs3_block_t victim = 2;
        lfs3_trv_t trv;
        lfs3_trv_open(&lfs3, &trv, LFS3_T_RDONLY) => 0;
        for (lfs3_block_t i = 0;; i++) {
            assert(i < 2*BLOCK_COUNT);

            struct lfs3_tinfo tinfo;
            int err = lfs3_trv_read(&lfs3, &trv, &tinfo);
            assert(!err || err == LFS3_ERR_NOENT);
            if (err == LFS3_ERR_NOENT) {
                break;
            }

            if (i <= victim_i && tinfo.block >= 2) {
                victim = tinfo.block;
            }
            n = i+1;
        }
        lfs3_trv_close(&lfs3, &trv) => 0;

        // make bad
        lfs3_emubd_mkbad(CFG, victim) => 0;
        // reset ck flags
        lfs3_fs_unck(&lfs3, LFS3_GC_CKDATA) => 0;

        // run gc to repair bad blocks
        lfs3_gc_t gc;
        lfs3_gc_open(&lfs3, &gc,
                LFS3_GC_WRONLY
                    | LFS3_GC_CKDATA
                    | LFS3_GC_REPAIRDATA) => 0;
        for (lfs3_block_t i = 0;; i++) {
            // a bit hacky, but this catches infinite loops
            assert(i < 2*BLOCK_COUNT);

            lfs3_sblock_t steps = lfs3_gc_write(&lfs3, &gc, 1);
            assert(steps >= 0);
            if (steps == 0) {
                break;
            }
        }
        lfs3_gc_close(&lfs3, &gc) => 0;

        // make the block good again
        lfs3_emubd_mkgood(CFG, victim) => 0;

        // evicted block should no longer be in use, anywhere in the
        // filesystem
        lfs3_trv_open(&lfs3, &trv, LFS3_T_RDONLY) => 0;
        for (lfs3_block_t i = 0;; i++) {
            assert(i < 2*BLOCK_COUNT);

            struct lfs3_tinfo tinfo;
            int err = lfs3_trv_read(&lfs3, &trv, &tinfo);
            assert(!err || err == LFS3_ERR_NOENT);
            if (err == LFS3_ERR_NOENT) {
                break;
            }

            // hmm, ok, one exception is mdir's spare blocks,
            // these don't actually store anything at rest
            if (!(tinfo.btype == LFS3_BTYPE_MDIR
                    && trv.blocks[1] == -1)) {
                assert(tinfo.block != victim);
            }
        }
        lfs3_trv_close(&lfs3, &trv) => 0;
    }

    for (int remount = 0; remount < 2; remount++) {
        // remount?
        if (remount) {
            lfs3_unmount(&lfs3) => 0;
            lfs3_mount(&lfs3, LFS3_M_RDWR, CFG) => 0;
        }

        // check that our files match our simulation
        for (lfs3_size_t j = 0; j < sim_size; j++) {
            char name[256];
            sprintf(name, "amethyst%03x", sim[j]);
            struct lfs3_info info;
            lfs3_stat(&lfs3, name, &info) => 0;
            assert(strcmp(info.name, name) == 0);
            assert(info.type == LFS3_TYPE_REG);
            assert(info.size == SIZE);
        }

        lfs3_dir_t dir;
        lfs3_dir_open(&lfs3, &dir, "/") => 0;
        struct lfs3_info info;
        lfs3_dir_read(&lfs3, &dir, &info) => 0;
        assert(strcmp(info.name, ".") == 0);
        assert(info.type == LFS3_TYPE_DIR);
        assert(info.size == 0);
        lfs3_dir_read(&lfs3, &dir, &info) => 0;
        assert(strcmp(info.name, "..") == 0);
        assert(info.type == LFS3_TYPE_DIR);
        assert(info.size == 0);
        for (lfs3_size_t j = 0; j < sim_size; j++) {
            char name[256];
            sprintf(name, "amethyst%03x", sim[j]);
            lfs3_dir_read(&lfs3, &dir, &info) => 0;
            assert(strcmp(info.name, name) == 0);
            assert(info.type == LFS3_TYPE_REG);
            assert(info.size == SIZE);
        }
        lfs3_dir_read(&lfs3, &dir, &info) => LFS3_ERR_NOENT;
        lfs3_dir_close(&lfs3, &dir) => 0;

        // check the file contents
        for (lfs3_size_t j = 0; j < sim_size; j++) {
            char name[256];
            sprintf(name, "amethyst%03x", sim[j]);
            lfs3_file_t file;
            lfs3_file_open(&lfs3, &file, name, LFS3_O_RDONLY) => 0;

            uint32_t wprng = sim_prngs[j];
            uint8_t wbuf[SIZE];
            for (lfs3_size_t j = 0; j < SIZE; j++) {
                wbuf[j] = 'a' + (TEST_PRNG(&wprng) % 26);
            }

            uint8_t rbuf[SIZE];
            lfs3_file_read(&lfs3, &file, rbuf, SIZE) => SIZE;
            assert(memcmp(rbuf, wbuf, SIZE) == 0);
            lfs3_file_close(&lfs3, &file) => 0;
        }
    }

    // clean up sim/lfs3
    free(sim);
    free(sim_prngs);
    lfs3_unmount(&lfs3) => 0;
'''

[cases.test_evict_repair_spam_fwrite_fuzz]
defines.OPS = 20
defines.SIZE = [
    'FCACHE_SIZE/2',
    '2*FCACHE_SIZE',
    'BLOCK_SIZE/2',
    'BLOCK_SIZE',
    '2*BLOCK_SIZE',
    '4*BLOCK_SIZE',
]
# chunk is more an upper limit here
defines.CHUNK = [32, 8, 1]
# INIT=0 => no init
# INIT=1 => fill with data
# INIT=2 => truncate to size
defines.INIT = [0, 1, 2]
defines.SYNC = [false, true]
defines.SEED = 'range(10)'
defines.ESEED = '42+SEED'
# report as damaged to request repairs
defines.BADBLOCK_BEHAVIOR = 'LFS3_EMUBD_BADBLOCK_READDAMAGED'
fuzz = 'SEED'
if = [
    'CHUNK <= SIZE',
    # this just saves testing time
    'SIZE <= 4*1024*FRAGMENT_SIZE',
]
code = '''
    // test with complex file writes
    lfs3_t lfs3;
    lfs3_format(&lfs3,
            LFS3_F_RDWR
                | ((GBMAP) ? LFS3_IFDEF_GBMAP(LFS3_F_GBMAP, -1) : 0),
            CFG) => 0;
    lfs3_mount(&lfs3, LFS3_M_RDWR, CFG) => 0;
    // last seen block count
    lfs3_block_t n = 0;
    uint32_t eprng = ESEED;

    // create a file
    lfs3_file_t file;
    lfs3_file_open(&lfs3, &file, "hello",
            LFS3_O_WRONLY | LFS3_O_CREAT | LFS3_O_EXCL) => 0;
    // simulate our file in ram
    uint8_t sim[SIZE];
    lfs3_off_t size;
    uint32_t prng = SEED;
    if (INIT == 0) {
        memset(sim, 0, SIZE);
        size = 0;
    } else if (INIT == 1) {
        for (lfs3_size_t i = 0; i < SIZE; i++) {
            sim[i] = 'a' + (TEST_PRNG(&prng) % 26);
        }
        lfs3_file_write(&lfs3, &file, sim, SIZE) => SIZE;
        size = SIZE;
    } else {
        memset(sim, 0, SIZE);
        lfs3_file_truncate(&lfs3, &file, SIZE) => 0;
        size = SIZE;
    }

    // sync?
    if (SYNC) {
        lfs3_file_sync(&lfs3, &file) => 0;
    }

    for (lfs3_size_t i = 0; i < OPS; i++) {
        // choose a random location
        lfs3_off_t off = TEST_PRNG(&prng) % SIZE;
        // and a random size, up to the chunk size
        lfs3_size_t chunk = lfs3_min(
                (TEST_PRNG(&prng) % (CHUNK+1-1)) + 1,
                SIZE - off);

        // update sim
        for (lfs3_size_t j = 0; j < chunk; j++) {
            sim[off+j] = 'a' + (TEST_PRNG(&prng) % 26);
        }
        size = lfs3_max(size, off+chunk);

        // update file
        lfs3_file_seek(&lfs3, &file, off, LFS3_SEEK_SET) => off;
        lfs3_file_write(&lfs3, &file, &sim[off], chunk) => chunk;

        // sync?
        if (SYNC) {
            lfs3_file_sync(&lfs3, &file) => 0;
        }

        // randomly evict a block
        //
        // to try to minimize traversals we randomly choose a block
        // based on the _previous_ traversal, or the last
        // non-mrootanchor block if that fails
        lfs3_block_t victim_i = TEST_PRNG(&eprng) % lfs3_max(n, 1);
        lfs3_block_t victim = 2;
        lfs3_trv_t trv;
        lfs3_trv_open(&lfs3, &trv, LFS3_T_RDONLY) => 0;
        for (lfs3_block_t i = 0;; i++) {
            assert(i < 2*BLOCK_COUNT);

            struct lfs3_tinfo tinfo;
            int err = lfs3_trv_read(&lfs3, &trv, &tinfo);
            assert(!err || err == LFS3_ERR_NOENT);
            if (err == LFS3_ERR_NOENT) {
                break;
            }

            if (i <= victim_i && tinfo.block >= 2) {
                victim = tinfo.block;
            }
            n = i+1;
        }
        lfs3_trv_close(&lfs3, &trv) => 0;

        // make bad
        lfs3_emubd_mkbad(CFG, victim) => 0;
        // reset ck flags
        lfs3_fs_unck(&lfs3, LFS3_GC_CKDATA) => 0;

        // run gc to repair bad blocks
        lfs3_gc_t gc;
        lfs3_gc_open(&lfs3, &gc,
                LFS3_GC_WRONLY
                    | LFS3_GC_CKDATA
                    | LFS3_GC_REPAIRDATA) => 0;
        for (lfs3_block_t i = 0;; i++) {
            // a bit hacky, but this catches infinite loops
            assert(i < 2*BLOCK_COUNT);

            lfs3_sblock_t steps = lfs3_gc_write(&lfs3, &gc, 1);
            assert(steps >= 0);
            if (steps == 0) {
                break;
            }
        }
        lfs3_gc_close(&lfs3, &gc) => 0;

        // make the block good again
        lfs3_emubd_mkgood(CFG, victim) => 0;

        // evicted block should no longer be in use, anywhere in the
        // filesystem
        lfs3_trv_open(&lfs3, &trv, LFS3_T_RDONLY) => 0;
        for (lfs3_block_t i = 0;; i++) {
            assert(i < 2*BLOCK_COUNT);

            struct lfs3_tinfo tinfo;
            int err = lfs3_trv_read(&lfs3, &trv, &tinfo);
            assert(!err || err == LFS3_ERR_NOENT);
            if (err == LFS3_ERR_NOENT) {
                break;
            }

            // hmm, ok, one exception is mdir's spare blocks,
            // these don't actually store anything at rest
            if (!(tinfo.btype == LFS3_BTYPE_MDIR
                    && trv.blocks[1] == -1)) {
                assert(tinfo.block != victim);
            }
        }
        lfs3_trv_close(&lfs3, &trv) => 0;
    }

    lfs3_file_close(&lfs3, &file) => 0;

    for (int remount = 0; remount < 2; remount++) {
        // remount?
        if (remount) {
            lfs3_unmount(&lfs3) => 0;
            lfs3_mount(&lfs3, LFS3_M_RDWR, CFG) => 0;
        }

        // check our file with stat
        struct lfs3_info info;
        lfs3_stat(&lfs3, "hello", &info) => 0;
        assert(strcmp(info.name, "hello") == 0);
        assert(info.type == LFS3_TYPE_REG);
        assert(info.size == size);

        // and with dir read
        lfs3_dir_t dir;
        lfs3_dir_open(&lfs3, &dir, "/") => 0;
        lfs3_dir_read(&lfs3, &dir, &info) => 0;
        assert(strcmp(info.name, ".") == 0);
        assert(info.type == LFS3_TYPE_DIR);
        assert(info.size == 0);
        lfs3_dir_read(&lfs3, &dir, &info) => 0;
        assert(strcmp(info.name, "..") == 0);
        assert(info.type == LFS3_TYPE_DIR);
        assert(info.size == 0);
        lfs3_dir_read(&lfs3, &dir, &info) => 0;
        assert(strcmp(info.name, "hello") == 0);
        assert(info.type == LFS3_TYPE_REG);
        assert(info.size == size);
        lfs3_dir_read(&lfs3, &dir, &info) => LFS3_ERR_NOENT;
        lfs3_dir_close(&lfs3, &dir) => 0;

        // try reading our file
        lfs3_file_open(&lfs3, &file, "hello", LFS3_O_RDONLY) => 0;
        // is size correct?
        lfs3_file_size(&lfs3, &file) => size;
        // try reading
        uint8_t rbuf[2*SIZE];
        memset(rbuf, 0xaa, 2*SIZE);
        lfs3_file_read(&lfs3, &file, rbuf, 2*SIZE) => size;
        // does our file match our simulation?
        assert(memcmp(rbuf, sim, size) == 0);
        lfs3_file_close(&lfs3, &file) => 0;
    }

    lfs3_unmount(&lfs3) => 0;
'''

[cases.test_evict_repair_spam_uz_fuzz]
# you probably need to flush if you expect errors
defines.FLUSH = false
defines.N = [1, 2, 4, 8, 16, 32, 64]
defines.OPS = '2*N'
defines.SIZE = [
    '0',
    'FCACHE_SIZE/2',
    '2*FCACHE_SIZE',
    'BLOCK_SIZE/2',
    'BLOCK_SIZE',
    '2*BLOCK_SIZE',
    '4*BLOCK_SIZE',
]
defines.SEED = 'range(10)'
defines.ESEED = '42+SEED'
# report as damaged to request repairs
defines.BADBLOCK_BEHAVIOR = 'LFS3_EMUBD_BADBLOCK_READDAMAGED'
fuzz = 'SEED'
if = '(SIZE*N)/BLOCK_SIZE <= 16'
code = '''
    // test with uncreats, zombies, etc
    lfs3_t lfs3;
    lfs3_format(&lfs3,
            LFS3_F_RDWR
                | ((GBMAP) ? LFS3_IFDEF_GBMAP(LFS3_F_GBMAP, -1) : 0),
            CFG) => 0;
    lfs3_mount(&lfs3, LFS3_M_RDWR, CFG) => 0;
    // last seen block count
    lfs3_block_t n = 0;
    uint32_t eprng = ESEED;

    // set up a simulation to compare against
    lfs3_size_t *sim = malloc(N*sizeof(lfs3_size_t));
    uint32_t *sim_prngs = malloc(N*sizeof(uint32_t));
    bool *sim_isstickys = malloc(N*sizeof(bool));
    lfs3_size_t sim_size = 0;

    typedef struct sim_file {
        lfs3_size_t x;
        bool sticky;
        bool zombie;
        uint32_t prng;
        lfs3_file_t file;
    } sim_file_t;
    sim_file_t **sim_files = malloc(N*sizeof(sim_file_t*));
    lfs3_size_t sim_file_count = 0;

    uint32_t prng = SEED;
    for (lfs3_size_t i = 0; i < OPS; i++) {
    nonsense:;
        // choose which operation to do
        uint8_t op = TEST_PRNG(&prng) % 5;

        // open a new file?
        if (op == 0) {
            if (sim_file_count >= N) {
                goto nonsense;
            }
            // choose a pseudo-random number
            lfs3_size_t x = TEST_PRNG(&prng) % N;

            // already exists?
            bool exist = false;
            uint32_t wprng = 0;
            bool sticky = true;
            for (lfs3_size_t j = 0; j < sim_size; j++) {
                if (sim[j] == x) {
                    exist = true;
                    wprng = sim_prngs[j];
                    sticky = sim_isstickys[j];
                    break;
                }
            }
            // choose a random seed if we don't exist
            if (!exist) {
                wprng = TEST_PRNG(&prng);
                sticky = true;
            }

            lfs3_size_t j = sim_file_count;
            sim_files[j] = malloc(sizeof(sim_file_t));

            // open the actual file
            char name[256];
            sprintf(name, "batman%03x", x);
            lfs3_file_open(&lfs3, &sim_files[j]->file, name,
                    LFS3_O_RDWR
                        | LFS3_O_CREAT
                        | ((FLUSH) ? LFS3_O_FLUSH : 0)) => 0;

            // write some initial data if we don't exist
            if (!exist || sticky) {
                uint8_t wbuf[SIZE];
                uint32_t wprng_ = wprng;
                for (lfs3_size_t k = 0; k < SIZE; k++) {
                    wbuf[k] = 'a' + (TEST_PRNG(&wprng_) % 26);
                }
                lfs3_file_write(&lfs3, &sim_files[j]->file, wbuf, SIZE)
                        => SIZE;
            }

            // open in our sim
            sim_files[j]->x = x;
            sim_files[j]->sticky = sticky;
            sim_files[j]->zombie = false;
            sim_files[j]->prng = wprng;
            sim_file_count++;

            // insert into our sim
            for (lfs3_size_t k = 0;; k++) {
                if (k >= sim_size || sim[k] >= x) {
                    // already seen?
                    if (k < sim_size && sim[k] == x) {
                        // new prng
                        sim_prngs[k] = wprng;
                    } else {
                        // insert
                        memmove(&sim[k+1], &sim[k],
                                (sim_size-k)*sizeof(lfs3_size_t));
                        memmove(&sim_prngs[k+1], &sim_prngs[k],
                                (sim_size-k)*sizeof(uint32_t));
                        memmove(&sim_isstickys[k+1], &sim_isstickys[k],
                                (sim_size-k)*sizeof(bool));
                        sim_size += 1;
                        sim[k] = x;
                        sim_prngs[k] = wprng;
                        sim_isstickys[k] = sticky;
                    }
                    break;
                }
            }

        // write/rewrite a file?
        } else if (op == 1) {
            if (sim_file_count == 0) {
                goto nonsense;
            }
            // choose a random file handle
            lfs3_size_t j = TEST_PRNG(&prng) % sim_file_count;
            lfs3_size_t x = sim_files[j]->x;
            // choose a random seed
            uint32_t wprng = TEST_PRNG(&prng);

            // write to the file
            lfs3_file_rewind(&lfs3, &sim_files[j]->file) => 0;
            uint8_t wbuf[SIZE];
            uint32_t wprng_ = wprng;
            for (lfs3_size_t k = 0; k < SIZE; k++) {
                wbuf[k] = 'a' + (TEST_PRNG(&wprng_) % 26);
            }
            lfs3_file_write(&lfs3, &sim_files[j]->file, wbuf, SIZE) => SIZE;
            lfs3_file_sync(&lfs3, &sim_files[j]->file) => 0;

            // update sim
            sim_files[j]->prng = wprng;
            if (!sim_files[j]->zombie) {
                // update in our sim
                for (lfs3_size_t k = 0;; k++) {
                    if (sim[k] == x) {
                        // new prng
                        sim_prngs[k] = wprng;
                        // no longer sticky
                        sim_isstickys[k] = false;
                        break;
                    }
                }

                // update related sim files
                for (lfs3_size_t k = 0; k < sim_file_count; k++) {
                    if (sim_files[k]->x == x && !sim_files[k]->zombie) {
                        // new prng
                        sim_files[k]->prng = wprng;
                        // no longer sticky
                        sim_files[k]->sticky = false;
                    }
                }
            }

        // close a file?
        } else if (op == 2) {
            if (sim_file_count == 0) {
                goto nonsense;
            }
            // choose a random file handle
            lfs3_size_t j = TEST_PRNG(&prng) % sim_file_count;
            lfs3_size_t x = sim_files[j]->x;
            bool sticky = sim_files[j]->sticky;
            bool zombie = sim_files[j]->zombie;

            // this doesn't really test anything, but if we don't close
            // files eventually everything will end up zombies

            // close the file without affected disk
            lfs3_file_desync(&lfs3, &sim_files[j]->file) => 0;
            lfs3_file_close(&lfs3, &sim_files[j]->file) => 0;
            // clobber closed files to try to catch lingering references
            memset(&sim_files[j]->file, 0xcc, sizeof(lfs3_file_t));

            // remove from list
            free(sim_files[j]);
            sim_files[j] = sim_files[sim_file_count-1];
            sim_file_count -= 1;

            // update our sim
            if (sticky && !zombie) {
                // orphaned?
                bool orphan = true;
                for (lfs3_size_t k = 0; k < sim_file_count; k++) {
                    if (sim_files[k]->x == x && !sim_files[k]->zombie) {
                        orphan = false;
                    }
                }

                // if we were never synced, delete from sim
                if (orphan) {
                    for (lfs3_size_t k = 0;; k++) {
                        if (sim[k] == x) {
                            memmove(&sim[k], &sim[k+1],
                                    (sim_size-(k+1))*sizeof(lfs3_size_t));
                            memmove(&sim_prngs[k], &sim_prngs[k+1],
                                    (sim_size-(k+1))*sizeof(uint32_t));
                            memmove(&sim_isstickys[k], &sim_isstickys[k+1],
                                    (sim_size-(k+1))*sizeof(bool));
                            sim_size -= 1;
                            break;
                        }
                    }
                }
            }

        // remove a file?
        } else if (op == 3) {
            if (sim_size == 0) {
                goto nonsense;
            }
            // choose a random file to delete
            lfs3_size_t j = TEST_PRNG(&prng) % sim_size;
            lfs3_size_t x = sim[j];

            // delete this file
            char name[256];
            sprintf(name, "batman%03x", x);
            lfs3_remove(&lfs3, name) => 0;

            // delete from our sim
            memmove(&sim[j], &sim[j+1],
                    (sim_size-(j+1))*sizeof(lfs3_size_t));
            memmove(&sim_prngs[j], &sim_prngs[j+1],
                    (sim_size-(j+1))*sizeof(uint32_t));
            memmove(&sim_isstickys[j], &sim_isstickys[j+1],
                    (sim_size-(j+1))*sizeof(bool));
            sim_size -= 1;

            // mark any related sim files as zombied
            for (lfs3_size_t k = 0; k < sim_file_count; k++) {
                if (sim_files[k]->x == x) {
                    sim_files[k]->zombie = true;
                }
            }

        // rename a file?
        } else if (op == 4) {
            if (sim_size == 0) {
                goto nonsense;
            }
            // choose a random file to rename, and a random number to
            // rename to
            lfs3_size_t j = TEST_PRNG(&prng) % sim_size;
            lfs3_size_t x = sim[j];
            lfs3_size_t y = TEST_PRNG(&prng) % N;
            uint32_t wprng = sim_prngs[j];
            bool sticky = sim_isstickys[j];

            // rename this file
            char old_name[256];
            sprintf(old_name, "batman%03x", x);
            char new_name[256];
            sprintf(new_name, "batman%03x", y);
            lfs3_rename(&lfs3, old_name, new_name) => 0;

            // update our sim
            for (lfs3_size_t k = 0;; k++) {
                if (k >= sim_size || sim[k] >= y) {
                    // renaming and replacing
                    if (k < sim_size && sim[k] == y && x != y) {
                        // delete the original entry
                        memmove(&sim[j], &sim[j+1],
                                (sim_size-(j+1))*sizeof(lfs3_size_t));
                        memmove(&sim_prngs[j], &sim_prngs[j+1],
                                (sim_size-(j+1))*sizeof(uint32_t));
                        memmove(&sim_isstickys[j], &sim_isstickys[j+1],
                                (sim_size-(j+1))*sizeof(bool));
                        sim_size -= 1;
                        if (k > j) {
                            k -= 1;
                        }
                        // update the prng/sticky
                        sim_prngs[k] = wprng;
                        sim_isstickys[k] = sticky;
                    // just renaming
                    } else {
                        // first delete
                        memmove(&sim[j], &sim[j+1],
                                (sim_size-(j+1))*sizeof(lfs3_size_t));
                        memmove(&sim_prngs[j], &sim_prngs[j+1],
                                (sim_size-(j+1))*sizeof(uint32_t));
                        memmove(&sim_isstickys[j], &sim_isstickys[j+1],
                                (sim_size-(j+1))*sizeof(bool));
                        if (k > j) {
                            k -= 1;
                        }
                        // then insert
                        memmove(&sim[k+1], &sim[k],
                                (sim_size-1-k)*sizeof(lfs3_size_t));
                        memmove(&sim_prngs[k+1], &sim_prngs[k],
                                (sim_size-1-k)*sizeof(uint32_t));
                        memmove(&sim_isstickys[k+1], &sim_isstickys[k],
                                (sim_size-1-k)*sizeof(bool));
                        sim[k] = y;
                        sim_prngs[k] = wprng;
                        sim_isstickys[k] = sticky;
                    }
                    break;
                }
            }

            // update any related sim files
            for (lfs3_size_t k = 0; k < sim_file_count; k++) {
                // move source files
                if (sim_files[k]->x == x) {
                    sim_files[k]->x = y;

                // mark target files as zombied
                } else if (sim_files[k]->x == y) {
                    sim_files[k]->zombie = true;
                }
            }
        }

        // randomly evict a block
        //
        // to try to minimize traversals we randomly choose a block
        // based on the _previous_ traversal, or the last
        // non-mrootanchor block if that fails
        lfs3_block_t victim_i = TEST_PRNG(&eprng) % lfs3_max(n, 1);
        lfs3_block_t victim = 2;
        lfs3_trv_t trv;
        lfs3_trv_open(&lfs3, &trv, LFS3_T_RDONLY) => 0;
        for (lfs3_block_t i = 0;; i++) {
            assert(i < 2*BLOCK_COUNT);

            struct lfs3_tinfo tinfo;
            int err = lfs3_trv_read(&lfs3, &trv, &tinfo);
            assert(!err || err == LFS3_ERR_NOENT);
            if (err == LFS3_ERR_NOENT) {
                break;
            }

            if (i <= victim_i && tinfo.block >= 2) {
                victim = tinfo.block;
            }
            n = i+1;
        }
        lfs3_trv_close(&lfs3, &trv) => 0;

        // make bad
        lfs3_emubd_mkbad(CFG, victim) => 0;
        // reset ck flags
        lfs3_fs_unck(&lfs3, LFS3_GC_CKDATA) => 0;

        // run gc to repair bad blocks
        lfs3_gc_t gc;
        lfs3_gc_open(&lfs3, &gc,
                LFS3_GC_WRONLY
                    | LFS3_GC_CKDATA
                    | LFS3_GC_REPAIRDATA) => 0;
        for (lfs3_block_t i = 0;; i++) {
            // a bit hacky, but this catches infinite loops
            assert(i < 2*BLOCK_COUNT);

            lfs3_sblock_t steps = lfs3_gc_write(&lfs3, &gc, 1);
            assert(steps >= 0);
            if (steps == 0) {
                break;
            }
        }
        lfs3_gc_close(&lfs3, &gc) => 0;

        // make the block good again
        lfs3_emubd_mkgood(CFG, victim) => 0;

        // evicted block should no longer be in use, anywhere in the
        // filesystem
        lfs3_trv_open(&lfs3, &trv, LFS3_T_RDONLY) => 0;
        for (lfs3_block_t i = 0;; i++) {
            assert(i < 2*BLOCK_COUNT);

            struct lfs3_tinfo tinfo;
            int err = lfs3_trv_read(&lfs3, &trv, &tinfo);
            assert(!err || err == LFS3_ERR_NOENT);
            if (err == LFS3_ERR_NOENT) {
                break;
            }

            // hmm, ok, one exception is mdir's spare blocks,
            // these don't actually store anything at rest
            if (!(tinfo.btype == LFS3_BTYPE_MDIR
                    && trv.blocks[1] == -1)) {
                assert(tinfo.block != victim);
            }
        }
        lfs3_trv_close(&lfs3, &trv) => 0;
    }

    // check that disk matches our simulation
    for (lfs3_size_t j = 0; j < sim_size; j++) {
        char name[256];
        sprintf(name, "batman%03x", sim[j]);
        struct lfs3_info info;
        lfs3_stat(&lfs3, name, &info) => 0;
        assert(strcmp(info.name, name) == 0);
        if (sim_isstickys[j]) {
            assert(info.type == LFS3_TYPE_STICKYNOTE);
            assert(info.size == 0);
        } else {
            assert(info.type == LFS3_TYPE_REG);
            assert(info.size == SIZE);
        }
    }

    lfs3_dir_t dir;
    lfs3_dir_open(&lfs3, &dir, "/") => 0;
    struct lfs3_info info;
    lfs3_dir_read(&lfs3, &dir, &info) => 0;
    assert(strcmp(info.name, ".") == 0);
    assert(info.type == LFS3_TYPE_DIR);
    assert(info.size == 0);
    lfs3_dir_read(&lfs3, &dir, &info) => 0;
    assert(strcmp(info.name, "..") == 0);
    assert(info.type == LFS3_TYPE_DIR);
    assert(info.size == 0);
    for (lfs3_size_t j = 0; j < sim_size; j++) {
        char name[256];
        sprintf(name, "batman%03x", sim[j]);
        lfs3_dir_read(&lfs3, &dir, &info) => 0;
        assert(strcmp(info.name, name) == 0);
        if (sim_isstickys[j]) {
            assert(info.type == LFS3_TYPE_STICKYNOTE);
            assert(info.size == 0);
        } else {
            assert(info.type == LFS3_TYPE_REG);
            assert(info.size == SIZE);
        }
    }
    lfs3_dir_read(&lfs3, &dir, &info) => LFS3_ERR_NOENT;
    lfs3_dir_close(&lfs3, &dir) => 0;

    for (lfs3_size_t j = 0; j < sim_size; j++) {
        char name[256];
        sprintf(name, "batman%03x", sim[j]);
        lfs3_file_t file;
        lfs3_file_open(&lfs3, &file, name, LFS3_O_RDONLY) => 0;

        uint32_t wprng = sim_prngs[j];
        uint8_t wbuf[SIZE];
        for (lfs3_size_t j = 0; j < SIZE; j++) {
            wbuf[j] = 'a' + (TEST_PRNG(&wprng) % 26);
        }

        uint8_t rbuf[SIZE];
        if (sim_isstickys[j]) {
            lfs3_file_read(&lfs3, &file, rbuf, SIZE) => 0;
        } else {
            lfs3_file_read(&lfs3, &file, rbuf, SIZE) => SIZE;
            assert(memcmp(rbuf, wbuf, SIZE) == 0);
        }
        lfs3_file_close(&lfs3, &file) => 0;
    }

    // check that our file handles match our simulation
    for (lfs3_size_t j = 0; j < sim_file_count; j++) {
        uint32_t wprng = sim_files[j]->prng;
        uint8_t wbuf[SIZE];
        for (lfs3_size_t j = 0; j < SIZE; j++) {
            wbuf[j] = 'a' + (TEST_PRNG(&wprng) % 26);
        }

        lfs3_file_rewind(&lfs3, &sim_files[j]->file) => 0;
        uint8_t rbuf[SIZE];
        lfs3_file_read(&lfs3, &sim_files[j]->file, rbuf, SIZE) => SIZE;
        assert(memcmp(rbuf, wbuf, SIZE) == 0);
    }

    // clean up sim/lfs3
    free(sim);
    free(sim_prngs);
    free(sim_isstickys);
    for (lfs3_size_t j = 0; j < sim_file_count; j++) {
        lfs3_file_close(&lfs3, &sim_files[j]->file) => 0;
        free(sim_files[j]);
    }
    free(sim_files);
    lfs3_unmount(&lfs3) => 0;
'''

[cases.test_evict_repair_spam_uzd_fuzz]
# you probably need to flush if you expect errors
defines.FLUSH = false
defines.N = [1, 2, 4, 8, 16, 32, 64]
defines.OPS = '2*N'
defines.SIZE = [
    '0',
    'FCACHE_SIZE/2',
    '2*FCACHE_SIZE',
    'BLOCK_SIZE/2',
    'BLOCK_SIZE',
    '2*BLOCK_SIZE',
    '4*BLOCK_SIZE',
]
defines.SEED = 'range(10)'
defines.ESEED = '42+SEED'
# report as damaged to request repairs
defines.BADBLOCK_BEHAVIOR = 'LFS3_EMUBD_BADBLOCK_READDAMAGED'
fuzz = 'SEED'
if = '(SIZE*N)/BLOCK_SIZE <= 16'
code = '''
    // test with uncreats, zombies, dirs, etc
    lfs3_t lfs3;
    lfs3_format(&lfs3,
            LFS3_F_RDWR
                | ((GBMAP) ? LFS3_IFDEF_GBMAP(LFS3_F_GBMAP, -1) : 0),
            CFG) => 0;
    lfs3_mount(&lfs3, LFS3_M_RDWR, CFG) => 0;
    // last seen block count
    lfs3_block_t n = 0;
    uint32_t eprng = ESEED;

    // set up a simulation to compare against
    lfs3_size_t *sim = malloc(N*sizeof(lfs3_size_t));
    uint32_t *sim_prngs = malloc(N*sizeof(uint32_t));
    bool *sim_isstickys = malloc(N*sizeof(bool));
    bool *sim_isdirs = malloc(N*sizeof(bool));
    lfs3_size_t sim_size = 0;

    typedef struct sim_file {
        lfs3_size_t x;
        bool sticky;
        bool zombie;
        uint32_t prng;
        lfs3_file_t file;
    } sim_file_t;
    sim_file_t **sim_files = malloc(N*sizeof(sim_file_t*));
    lfs3_size_t sim_file_count = 0;

    uint32_t prng = SEED;
    for (lfs3_size_t i = 0; i < OPS; i++) {
    nonsense:;
        // choose which operation to do
        uint8_t op = TEST_PRNG(&prng) % 8;

        // open a new file?
        if (op == 0) {
            if (sim_file_count >= N) {
                goto nonsense;
            }
            // choose a pseudo-random number
            lfs3_size_t x = TEST_PRNG(&prng) % N;

            // already exists?
            bool exist = true;
            uint32_t wprng = 0;
            bool sticky = true;
            for (lfs3_size_t j = 0; j < sim_size; j++) {
                if (sim[j] == x) {
                    if (sim_isdirs[j]) {
                        goto nonsense;
                    }
                    exist = true;
                    wprng = sim_prngs[j];
                    sticky = sim_isstickys[j];
                    break;
                }
            }
            // choose a random seed if we don't exist
            if (!exist) {
                wprng = TEST_PRNG(&prng);
                sticky = true;
            }

            lfs3_size_t j = sim_file_count;
            sim_files[j] = malloc(sizeof(sim_file_t));

            // open the actual file
            char name[256];
            sprintf(name, "batman%03x", x);
            lfs3_file_open(&lfs3, &sim_files[j]->file, name,
                    LFS3_O_RDWR
                        | LFS3_O_CREAT
                        | ((FLUSH) ? LFS3_O_FLUSH : 0)) => 0;

            // write some initial data if we don't exist
            if (!exist || sticky) {
                uint8_t wbuf[SIZE];
                uint32_t wprng_ = wprng;
                for (lfs3_size_t k = 0; k < SIZE; k++) {
                    wbuf[k] = 'a' + (TEST_PRNG(&wprng_) % 26);
                }
                lfs3_file_write(&lfs3, &sim_files[j]->file, wbuf, SIZE)
                        => SIZE;
            }

            // open in our sim
            sim_files[j]->x = x;
            sim_files[j]->sticky = sticky;
            sim_files[j]->zombie = false;
            sim_files[j]->prng = wprng;
            sim_file_count++;

            // insert into our sim
            for (lfs3_size_t k = 0;; k++) {
                if (k >= sim_size || sim[k] >= x) {
                    // already seen?
                    if (k < sim_size && sim[k] == x) {
                        // new prng
                        sim_prngs[k] = wprng;
                    } else {
                        // insert
                        memmove(&sim[k+1], &sim[k],
                                (sim_size-k)*sizeof(lfs3_size_t));
                        memmove(&sim_prngs[k+1], &sim_prngs[k],
                                (sim_size-k)*sizeof(uint32_t));
                        memmove(&sim_isstickys[k+1], &sim_isstickys[k],
                                (sim_size-k)*sizeof(bool));
                        memmove(&sim_isdirs[k+1], &sim_isdirs[k],
                                (sim_size-k)*sizeof(bool));
                        sim_size += 1;
                        sim[k] = x;
                        sim_prngs[k] = wprng;
                        sim_isstickys[k] = sticky;
                        sim_isdirs[k] = false;
                    }
                    break;
                }
            }

        // write/rewrite a file?
        } else if (op == 1) {
            if (sim_file_count == 0) {
                goto nonsense;
            }
            // choose a random file handle
            lfs3_size_t j = TEST_PRNG(&prng) % sim_file_count;
            lfs3_size_t x = sim_files[j]->x;
            // choose a random seed
            uint32_t wprng = TEST_PRNG(&prng);

            // write to the file
            lfs3_file_rewind(&lfs3, &sim_files[j]->file) => 0;
            uint8_t wbuf[SIZE];
            uint32_t wprng_ = wprng;
            for (lfs3_size_t k = 0; k < SIZE; k++) {
                wbuf[k] = 'a' + (TEST_PRNG(&wprng_) % 26);
            }
            lfs3_file_write(&lfs3, &sim_files[j]->file, wbuf, SIZE) => SIZE;
            lfs3_file_sync(&lfs3, &sim_files[j]->file) => 0;

            // update sim
            sim_files[j]->prng = wprng;
            if (!sim_files[j]->zombie) {
                // update in our sim
                for (lfs3_size_t k = 0;; k++) {
                    if (k >= sim_size || sim[k] >= x) {
                        // new prng
                        sim_prngs[k] = wprng;
                        // no longer sticky
                        sim_isstickys[k] = false;
                        break;
                    }
                }

                // update related sim files
                for (lfs3_size_t k = 0; k < sim_file_count; k++) {
                    if (sim_files[k]->x == x && !sim_files[k]->zombie) {
                        // new prng
                        sim_files[k]->prng = wprng;
                        // no longer sticky
                        sim_files[k]->sticky = false;
                    }
                }
            }

        // close a file?
        } else if (op == 2) {
            if (sim_file_count == 0) {
                goto nonsense;
            }
            // choose a random file handle
            lfs3_size_t j = TEST_PRNG(&prng) % sim_file_count;
            lfs3_size_t x = sim_files[j]->x;
            lfs3_size_t sticky = sim_files[j]->sticky;
            lfs3_size_t zombie = sim_files[j]->zombie;

            // this doesn't really test anything, but if we don't close
            // files eventually everything will end up zombies

            // close the file without affected disk
            lfs3_file_desync(&lfs3, &sim_files[j]->file) => 0;
            lfs3_file_close(&lfs3, &sim_files[j]->file) => 0;
            // clobber closed files to try to catch lingering references
            memset(&sim_files[j]->file, 0xcc, sizeof(lfs3_file_t));

            // remove from list
            free(sim_files[j]);
            sim_files[j] = sim_files[sim_file_count-1];
            sim_file_count -= 1;

            // update our sim
            if (sticky && !zombie) {
                // orphaned?
                bool orphan = true;
                for (lfs3_size_t k = 0; k < sim_file_count; k++) {
                    if (sim_files[k]->x == x && !sim_files[k]->zombie) {
                        orphan = false;
                    }
                }

                // if we were never synced, delete from sim
                if (orphan) {
                    for (lfs3_size_t k = 0;; k++) {
                        if (sim[k] == x) {
                            memmove(&sim[k], &sim[k+1],
                                    (sim_size-(k+1))*sizeof(lfs3_size_t));
                            memmove(&sim_prngs[k], &sim_prngs[k+1],
                                    (sim_size-(k+1))*sizeof(uint32_t));
                            memmove(&sim_isstickys[k], &sim_isstickys[k+1],
                                    (sim_size-(k+1))*sizeof(bool));
                            memmove(&sim_isdirs[k], &sim_isdirs[k+1],
                                    (sim_size-(k+1))*sizeof(bool));
                            sim_size -= 1;
                            break;
                        }
                    }
                }
            }

        // remove a file?
        } else if (op == 3) {
            if (sim_size == 0) {
                goto nonsense;
            }
            // choose a random file to delete
            lfs3_size_t j = TEST_PRNG(&prng) % sim_size;
            lfs3_size_t x = sim[j];

            // delete this file
            char name[256];
            sprintf(name, "batman%03x", x);
            lfs3_remove(&lfs3, name) => 0;

            // delete from our sim
            memmove(&sim[j], &sim[j+1],
                    (sim_size-(j+1))*sizeof(lfs3_size_t));
            memmove(&sim_prngs[j], &sim_prngs[j+1],
                    (sim_size-(j+1))*sizeof(uint32_t));
            memmove(&sim_isstickys[j], &sim_isstickys[j+1],
                    (sim_size-(j+1))*sizeof(bool));
            memmove(&sim_isdirs[j], &sim_isdirs[j+1],
                    (sim_size-(j+1))*sizeof(bool));
            sim_size -= 1;

            // mark any related sim files as zombied
            for (lfs3_size_t k = 0; k < sim_file_count; k++) {
                if (sim_files[k]->x == x) {
                    sim_files[k]->zombie = true;
                }
            }

        // rename a file?
        } else if (op == 4) {
            if (sim_size == 0) {
                goto nonsense;
            }
            // choose a random file to rename, and a random number to
            // rename to
            lfs3_size_t j = TEST_PRNG(&prng) % sim_size;
            lfs3_size_t x = sim[j];
            lfs3_size_t y = TEST_PRNG(&prng) % N;
            uint32_t wprng = sim_prngs[j];
            bool sticky = sim_isstickys[j];
            bool dir = sim_isdirs[j];

            for (lfs3_size_t k = 0;; k++) {
                if (k >= sim_size || sim[k] >= y) {
                    // renaming and replacing
                    if (k < sim_size && sim[k] == y && x != y) {
                        // type mismatch?
                        if (sim_isdirs[k] != dir) {
                            goto nonsense;
                        }
                    }
                    break;
                }
            }

            // rename this file
            char old_name[256];
            sprintf(old_name, "batman%03x", x);
            char new_name[256];
            sprintf(new_name, "batman%03x", y);
            lfs3_rename(&lfs3, old_name, new_name) => 0;

            // update our sim
            for (lfs3_size_t k = 0;; k++) {
                if (k >= sim_size || sim[k] >= y) {
                    // renaming and replacing
                    if (k < sim_size && sim[k] == y && x != y) {
                        // delete the original entry
                        memmove(&sim[j], &sim[j+1],
                                (sim_size-(j+1))*sizeof(lfs3_size_t));
                        memmove(&sim_prngs[j], &sim_prngs[j+1],
                                (sim_size-(j+1))*sizeof(uint32_t));
                        memmove(&sim_isstickys[j], &sim_isstickys[j+1],
                                (sim_size-(j+1))*sizeof(bool));
                        memmove(&sim_isdirs[j], &sim_isdirs[j+1],
                                (sim_size-(j+1))*sizeof(bool));
                        sim_size -= 1;
                        if (k > j) {
                            k -= 1;
                        }
                        // update the prng/sticky/dir
                        sim_prngs[k] = wprng;
                        sim_isstickys[k] = sticky;
                        sim_isdirs[k] = dir;
                    // just renaming
                    } else {
                        // first delete
                        memmove(&sim[j], &sim[j+1],
                                (sim_size-(j+1))*sizeof(lfs3_size_t));
                        memmove(&sim_prngs[j], &sim_prngs[j+1],
                                (sim_size-(j+1))*sizeof(uint32_t));
                        memmove(&sim_isstickys[j], &sim_isstickys[j+1],
                                (sim_size-(j+1))*sizeof(bool));
                        memmove(&sim_isdirs[j], &sim_isdirs[j+1],
                                (sim_size-(j+1))*sizeof(bool));
                        if (k > j) {
                            k -= 1;
                        }
                        // then insert
                        memmove(&sim[k+1], &sim[k],
                                (sim_size-1-k)*sizeof(lfs3_size_t));
                        memmove(&sim_prngs[k+1], &sim_prngs[k],
                                (sim_size-1-k)*sizeof(uint32_t));
                        memmove(&sim_isstickys[k+1], &sim_isstickys[k],
                                (sim_size-1-k)*sizeof(bool));
                        memmove(&sim_isdirs[k+1], &sim_isdirs[k],
                                (sim_size-1-k)*sizeof(bool));
                        sim[k] = y;
                        sim_prngs[k] = wprng;
                        sim_isstickys[k] = sticky;
                        sim_isdirs[k] = dir;
                    }
                    break;
                }
            }

            // update any related sim files
            for (lfs3_size_t k = 0; k < sim_file_count; k++) {
                // move source files
                if (sim_files[k]->x == x) {
                    sim_files[k]->x = y;

                // mark target files as zombied
                } else if (sim_files[k]->x == y) {
                    sim_files[k]->zombie = true;
                }
            }

        // toss a directory into the mix
        } else if (op == 5) {
            // choose a pseudo-random number
            lfs3_size_t x = TEST_PRNG(&prng) % N;

            for (lfs3_size_t k = 0;; k++) {
                if (k >= sim_size || sim[k] >= x) {
                    // already seen?
                    if (k < sim_size && sim[k] == x) {
                        goto nonsense;
                    }
                    break;
                }
            }

            // make the directory
            char name[256];
            sprintf(name, "batman%03x", x);
            lfs3_mkdir(&lfs3, name) => 0;

            // insert into our sim
            for (lfs3_size_t k = 0;; k++) {
                if (k >= sim_size || sim[k] >= x) {
                    // insert
                    memmove(&sim[k+1], &sim[k],
                            (sim_size-k)*sizeof(lfs3_size_t));
                    memmove(&sim_prngs[k+1], &sim_prngs[k],
                            (sim_size-k)*sizeof(uint32_t));
                    memmove(&sim_isstickys[k+1], &sim_isstickys[k],
                            (sim_size-k)*sizeof(bool));
                    memmove(&sim_isdirs[k+1], &sim_isdirs[k],
                            (sim_size-k)*sizeof(bool));
                    sim_size += 1;
                    sim[k] = x;
                    sim_prngs[k] = 0;
                    sim_isdirs[k] = true;
                    break;
                }
            }

            // mark any related sim files as zombied
            for (lfs3_size_t k = 0; k < sim_file_count; k++) {
                if (sim_files[k]->x == x) {
                    sim_files[k]->zombie = true;
                }
            }
        }

        // randomly evict a block
        //
        // to try to minimize traversals we randomly choose a block
        // based on the _previous_ traversal, or the last
        // non-mrootanchor block if that fails
        lfs3_block_t victim_i = TEST_PRNG(&eprng) % lfs3_max(n, 1);
        lfs3_block_t victim = 2;
        lfs3_trv_t trv;
        lfs3_trv_open(&lfs3, &trv, LFS3_T_RDONLY) => 0;
        for (lfs3_block_t i = 0;; i++) {
            assert(i < 2*BLOCK_COUNT);

            struct lfs3_tinfo tinfo;
            int err = lfs3_trv_read(&lfs3, &trv, &tinfo);
            assert(!err || err == LFS3_ERR_NOENT);
            if (err == LFS3_ERR_NOENT) {
                break;
            }

            if (i <= victim_i && tinfo.block >= 2) {
                victim = tinfo.block;
            }
            n = i+1;
        }
        lfs3_trv_close(&lfs3, &trv) => 0;

        // make bad
        lfs3_emubd_mkbad(CFG, victim) => 0;
        // reset ck flags
        lfs3_fs_unck(&lfs3, LFS3_GC_CKDATA) => 0;

        // run gc to repair bad blocks
        lfs3_gc_t gc;
        lfs3_gc_open(&lfs3, &gc,
                LFS3_GC_WRONLY
                    | LFS3_GC_CKDATA
                    | LFS3_GC_REPAIRDATA) => 0;
        for (lfs3_block_t i = 0;; i++) {
            // a bit hacky, but this catches infinite loops
            assert(i < 2*BLOCK_COUNT);

            lfs3_sblock_t steps = lfs3_gc_write(&lfs3, &gc, 1);
            assert(steps >= 0);
            if (steps == 0) {
                break;
            }
        }
        lfs3_gc_close(&lfs3, &gc) => 0;

        // make the block good again
        lfs3_emubd_mkgood(CFG, victim) => 0;

        // evicted block should no longer be in use, anywhere in the
        // filesystem
        lfs3_trv_open(&lfs3, &trv, LFS3_T_RDONLY) => 0;
        for (lfs3_block_t i = 0;; i++) {
            assert(i < 2*BLOCK_COUNT);

            struct lfs3_tinfo tinfo;
            int err = lfs3_trv_read(&lfs3, &trv, &tinfo);
            assert(!err || err == LFS3_ERR_NOENT);
            if (err == LFS3_ERR_NOENT) {
                break;
            }

            // hmm, ok, one exception is mdir's spare blocks,
            // these don't actually store anything at rest
            if (!(tinfo.btype == LFS3_BTYPE_MDIR
                    && trv.blocks[1] == -1)) {
                assert(tinfo.block != victim);
            }
        }
        lfs3_trv_close(&lfs3, &trv) => 0;
    }

    // check that disk matches our simulation
    for (lfs3_size_t j = 0; j < sim_size; j++) {
        char name[256];
        sprintf(name, "batman%03x", sim[j]);
        struct lfs3_info info;
        lfs3_stat(&lfs3, name, &info) => 0;
        assert(strcmp(info.name, name) == 0);
        if (sim_isdirs[j]) {
            assert(info.type == LFS3_TYPE_DIR);
            assert(info.size == 0);
        } else if (sim_isstickys[j]) {
            assert(info.type == LFS3_TYPE_STICKYNOTE);
            assert(info.size == 0);
        } else {
            assert(info.type == LFS3_TYPE_REG);
            assert(info.size == SIZE);
        }
    }

    lfs3_dir_t dir;
    lfs3_dir_open(&lfs3, &dir, "/") => 0;
    struct lfs3_info info;
    lfs3_dir_read(&lfs3, &dir, &info) => 0;
    assert(strcmp(info.name, ".") == 0);
    assert(info.type == LFS3_TYPE_DIR);
    assert(info.size == 0);
    lfs3_dir_read(&lfs3, &dir, &info) => 0;
    assert(strcmp(info.name, "..") == 0);
    assert(info.type == LFS3_TYPE_DIR);
    assert(info.size == 0);
    for (lfs3_size_t j = 0; j < sim_size; j++) {
        char name[256];
        sprintf(name, "batman%03x", sim[j]);
        lfs3_dir_read(&lfs3, &dir, &info) => 0;
        assert(strcmp(info.name, name) == 0);
        if (sim_isdirs[j]) {
            assert(info.type == LFS3_TYPE_DIR);
            assert(info.size == 0);
        } else if (sim_isstickys[j]) {
            assert(info.type == LFS3_TYPE_STICKYNOTE);
            assert(info.size == 0);
        } else {
            assert(info.type == LFS3_TYPE_REG);
            assert(info.size == SIZE);
        }
    }
    lfs3_dir_read(&lfs3, &dir, &info) => LFS3_ERR_NOENT;
    lfs3_dir_close(&lfs3, &dir) => 0;

    for (lfs3_size_t j = 0; j < sim_size; j++) {
        if (sim_isdirs[j]) {
            char name[256];
            sprintf(name, "batman%03x", sim[j]);
            lfs3_file_t file;
            lfs3_file_open(&lfs3, &file, name, LFS3_O_RDONLY)
                    => LFS3_ERR_ISDIR;

        } else {
            char name[256];
            sprintf(name, "batman%03x", sim[j]);
            lfs3_file_t file;
            lfs3_file_open(&lfs3, &file, name, LFS3_O_RDONLY) => 0;

            uint32_t wprng = sim_prngs[j];
            uint8_t wbuf[SIZE];
            for (lfs3_size_t j = 0; j < SIZE; j++) {
                wbuf[j] = 'a' + (TEST_PRNG(&wprng) % 26);
            }

            uint8_t rbuf[SIZE];
            if (sim_isstickys[j]) {
                lfs3_file_read(&lfs3, &file, rbuf, SIZE) => 0;
            } else {
                lfs3_file_read(&lfs3, &file, rbuf, SIZE) => SIZE;
                assert(memcmp(rbuf, wbuf, SIZE) == 0);
            }
            lfs3_file_close(&lfs3, &file) => 0;
        }
    }

    // check that our file handles match our simulation
    for (lfs3_size_t j = 0; j < sim_file_count; j++) {
        uint32_t wprng = sim_files[j]->prng;
        uint8_t wbuf[SIZE];
        for (lfs3_size_t j = 0; j < SIZE; j++) {
            wbuf[j] = 'a' + (TEST_PRNG(&wprng) % 26);
        }

        lfs3_file_rewind(&lfs3, &sim_files[j]->file) => 0;
        uint8_t rbuf[SIZE];
        lfs3_file_read(&lfs3, &sim_files[j]->file, rbuf, SIZE) => SIZE;
        assert(memcmp(rbuf, wbuf, SIZE) == 0);
    }

    // clean up sim/lfs3
    free(sim);
    free(sim_prngs);
    free(sim_isstickys);
    free(sim_isdirs);
    for (lfs3_size_t j = 0; j < sim_file_count; j++) {
        lfs3_file_close(&lfs3, &sim_files[j]->file) => 0;
        free(sim_files[j]);
    }
    free(sim_files);
    lfs3_unmount(&lfs3) => 0;
'''
